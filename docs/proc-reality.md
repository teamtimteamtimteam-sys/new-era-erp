# 加工模块对照现实(PROC-0)—— 勘察,不建任何东西

**这份文件是一次访谈的记录,写在访谈进行当中,不是事后整理的。**
本仓库有过两次教训:看板的四支臂丢在一次对话里(OPS-18 → OPS-19),
高管视图的规划也只存在于对话里、后来只能重建成文件。**所以这里边问边写。**

> **本次会话【不实现任何东西】。** 产出只有两样:这份文件,以及一份只有 Tim
> 答得了的问题清单。不排刀、不动库、不动代码。

## 这份文件的三种事实,它们【不许被压平】

每一条都标着它的来源,因为它们的可信度与保质期完全不同:

| 标记 | 意思 | 它错了会怎样 |
|---|---|---|
| **【码】** | 从仓库里读出来的事实,带对象名 | 不会错;会过时(代码改了) |
| **【荐】** | Claude 按行业惯例给的建议,**Tim 已确认** | 可能整条不适用于这盘生意 |
| **【Tim】** | Tim 自己的话 | 这是唯一的权威 |
| **【未知】** | 还没有答案,**带返回条件** | 把它当成决定,就是本仓库最贵的那一种错 |

**「我们还不知道」是一个合法答案。** 粉料线还没到,有些事实此刻并不存在。
记成 **【未知】+ 返回条件**(什么事件发生了才答得了),**绝不让占位变成决定**。

---

# 第一部分 · 今天的代码到底建了什么(STEP 1)

**全部来自仓库,逐条带对象名。以下每一条都不需要问 Tim。**

## 1 · 一次「加工」是什么

**【码】** `processing_runs` = **一个离散事件**,状态只有 `committed` / `reversed`
两态 —— **没有"进行中"**;一经提交只能整体冲销(`rollback_processing_run`)。

* **投料:** `processing_inputs`,一行 = 从某个批次消耗了多少。
  **【多进多出本来就支持】** —— 不是"一进一出"。
* **产出:** `processing_outputs`,一行 = 产出了哪个批次多少。同样是 N 行。
* **再加工【已经支持】:** `processing_inputs.output_batch_id`(FIN-25)让一次加工
  消耗【上游的产出批】,与 `inbound_batch_id` 恰一非空(XOR);
  `guard_processing_input` 拒绝自吞(一张单不能消耗自己的产出)。
* **时间:** `process_date` 是一个 **date**。
  **【没有开始/结束/班次/工时】** —— EQP-2a 实测过整个加工族:唯一的世界侧日期
  就是这一个 date。所以**一次加工跨班次、或者持续几天,今天表示不出来**。
* **机器:** `processing_runs.equipment_id`(EQP-2a),可空,空 = 【未归属】。
* **工序/操作类型:** **一个都没有。** 加工单上没有任何一列说这一炉【干的是哪道工序】
  —— 破碎?筛分?放电?系统看不出区别。

## 2 · 「损耗」是什么

**【码】** `processing_runs.loss_qty`,**一个 numeric,没有类别、没有理由**。

`commit_processing_run` 里它是:

```sql
COALESCE(p_loss_qty, v_total_input - v_total_output)
```

* 唯一的守卫是 `LOSS_NEGATIVE`(不许为负)与
  `OUTPUT_EXCEEDS_INPUT`(产出不许大于投入);
* **`loss_qty` 与 `总投入 − 总产出` 【可以不相等】而无人过问** —— 调用方传什么就是什么;
* **粉尘、电解液挥发、水分 —— 一个都没有。**

> **【码】水分 / 干基 / 湿基:整个仓库【零处】。**
> `grep -rilE "moisture|dry_basis|wet_weight|水分|干基|湿基" db/ app/ docs/` 返回空。
> `inbound_batch_metals.content_pct` / `output_batch_metals.content_pct` /
> `assay_result_metals.content_pct` 三处都是 0–100 的百分数,
> **没有任何一列说这个百分数是【以什么为基】测的**。
> 回收率(`processing_metal_recovery_all`)因此是拿【过磅重量】直接乘含量算的。

## 3 · 「产出批」是什么

**【码】** `output_batches` = **可售库存**。
`state ∈ ('库存中','部分售出','已售罄')`,带 `remaining_qty`、`customer_id`(预售)、
`purity`。`allocate_processing_costs` 给**每一条**产出腿摊上成本
(`allocated_cost_base` / `unit_cost_base`)。

> **【码】没有"这条流是成本不是产品"这个概念。** 每一个产出都被当作带成本的存货。
> 一条要花钱处置的废流,今天只能被建成一个"有成本的可售批次" —— 而那是反的。

## 4 · 工单层规划什么

**【码】** `work_orders`(WO-1a),四态 draft/released/closed/cancelled。
**"在做中"与"完成度"不存,由挂上来的加工单推导。**

* `work_order_lines` —— 计划投料,**按【物料】不按批次**(排计划时批次常常还不存在),
  `UNIQUE (work_order_id, material_id)`;
* `work_order_expected_outputs` —— 预期产出,**表注明写着这是【人估的】不是一条标准**:

  > **【码】今天这个库里没有任何可以推出预期产出的东西** —— 没有配方/BOM
  > (Doc 2 把它留给多工序那次升级),投料侧 19 条含量行的 `content_source`
  > **全是 NULL**(一条化验来源都没有),而两侧都测过的(加工单, 金属)组合
  > **只有 3 个**。**三个观测不是一个回收率。**

* `scheduled_date` 可空且永不默认;`processing_runs.work_order_id` 可空 =【计划外】。

## 5 · 化验挂在什么上

**【码】** `assay_results`,父是 **进料批 或 产出批**,恰一非空(`num_nonnulls = 1`)。

* `is_final` 区分正式证书与初检/部分读数;
* `superseded_by` 记录复验取代(**复验链已经建好**,按父各自成链);
* `applied_at` / `applied_by` 是"已执行"标记 —— **记录与执行分开**
  (`record_assay_result` 只落表,`apply_assay_result` 才动批次);
* 进料化验执行 = 抄含量 **+ 重算应付**;产出化验执行 = **只抄含量**
  (产出批没有一张应付可以重述);
* `inbound_batch_metals.content_source ∈ ('assay','manual')` + `source_assay_id`
  —— **每一个含量数都说得出它是化验来的还是手填的**;
* `material_required_metals`(ASY-P1)—— 这种物料【应当验哪些金属】;
  **没有行 = 没有要求,而表注明写着那是一个假设不是一个事实**。

> **【码】金属清单是【写死的 CHECK】,四处重复:**
> `metal IN ('ni','co','li','mn','cu','al','fe')`
> —— 在 `inbound_batch_metals` / `output_batch_metals` / `assay_result_metals` /
> `material_required_metals` 上各写了一遍。**没有石墨、没有塑料、没有电解液。**

## 6 · 进料这条路记什么

**【码】** `inbound_batches` —— 库存与应付两条账的源头。

* `material_id` + `supplier_id` + `quantity` + `unit`(默认 kg)+ `arrival_date`;
* `unit_price` 是**应付之锚**,只能经 `set_inbound_unit_price()` 改,改价必须留痕;
* `stage ∈ ('待加工','加工中','已加工完')` —— **只有三态,而且是加工进度,
  不是这批货【是什么】或【什么状态】**;
* `pricing_status ∈ ('unpriced','provisional','final')` —— **暂定价 → 正式价这条路
  已经完整建好了**;
* `declared_qty`(GRN-1b)—— 申报量,**留空落 NULL 不是 0**;
* `guard_supplier_qualification` —— 收货时查供应商证书(处置为 block 的过期证拦货)。

## 7 · 「这批货是什么」今天怎么表达

**【码】`materials.category` 是 `text NOT NULL`,【没有 CHECK,没有字典表】。**
线上实测四个在册物料的 category 是:

| category | 条数 | 物料名 |
|---|---|---|
| `black_mass` | 1 | probe |
| `NiH` | 1 | Special Battery Material |
| `进料-电池` | 2 | NMC Cathode Foil / NTF-1 walk scratch |

**三种命名法混在一列里:英文小写下划线、化学体系缩写、中文短语。**
`chemistry` 同样是自由文本(线上两个值:`NMC`、`Special Chemistry Structure`)。

**【码】唯一一条【真的是分类】的轴是监管轴:** `materials.waste_classification_code`
→ `waste_classifications`(MAT-1),两行种子:`focused` / `non_focused`,
`is_controlled` 布尔。**既有物料全部 NULL 且刻意不回填** ——
NULL 的意思是"没有人分过类",不是"非受控"(`db/fixtures/53` A 臂钉住这三态可辨)。

> **也就是说:今天系统里【没有】"这批来的东西是电池 / 电子废料 / 包装 / 耗材"
> 这个问题的位置。** 监管轴回答的是"要不要受控处置",那是另一个问题;
> 两者同时成立而互不蕴含(`materials.waste_classification_code` 的列注释自己写着这句)。

---

# 第二部分 · 十条假设,先拿代码打一遍(STEP 2)

**规矩:仓库答得了的,【不许拿去问 Tim】。** 下面每一条先判生死,再决定问不问。

| # | 假设 | 判 | 杀死它/留下它的是什么 |
|---|---|---|---|
| 1 | 投料是连续/批次战役式的,不是一进一出 | **半死** | **【码】多进多出本来就支持**(`processing_inputs`/`processing_outputs` 各 N 行)。活下来的一半是**时间**:`process_date` 是一个 date,全族没有起止/班次 —— 跨班次的一炉表示不出来。并入 #6 |
| 2 | 筛上物回炉再喂,中间物从不过磅 | **半死** | **【码】回炉已经建好**(FIN-25:`processing_inputs.output_batch_id`,带自吞守卫)。活下来的一半是**"从不过磅"**:每条腿的数量都 NOT NULL,**一条不称重的循环流在今天【无法表示】** |
| 3 | 损耗不是一个数;粉尘 / 电解液挥发 / **水分**;湿重与化验基不是一回事 | **完全存活** | **【码】`loss_qty` 是一个 numeric,无类别无理由**,而且可以与"投入−产出"不相等而无人过问。**【码】水分/干基/湿基整个仓库零处**;含量百分数**不声明基准**;回收率拿过磅重量直接乘含量 |
| 4 | 产出不止黑粉(铜箔铝箔、隔膜、壳体、电解液),而且**有些流是处置成本不是产品** | **半死** | **【码】多产出支持**(N 条产出腿)。活下来的一半更硬:**`output_batches` 就是"可售库存"**,每条产出腿都被摊上成本。**"这条流要花钱处置"在今天是反的** |
| 5 | 正极料与负极料可能是两种物料,不是一种 | **基本已死** | **【码】`materials` 是自由文本主档,产出可以有 N 条腿** —— **schema 上什么都不拦**。剩下的不是系统问题,是主数据问题:那条线到底分不分 |
| 6 | 机器现实:停机、卡料、跨班次、**heel 料串批** | **部分存活** | **【码】停机已经建好**(EQP-2a `equipment_downtime` + EQP-2d 的屏幕,一台机器同时只能有一段开口)。**跨班次表示不出来**(见 #1)。**heel / 串批:一个字都没有** |
| 7 | 前处理:放电、分选、破损与带电电芯 | **完全存活** | **【码】加工单上没有任何"工序/操作类型"** —— 一炉就是投入→产出,系统看不出这一炉是放电还是破碎。放电只作为一台**固定资产**存在(FA-2026-0001),不是一道工序 |
| 8 | 化验滞后,有暂定值与最终值,计价挂在最终值上 | **死透了** | **【码】已经完整建好**:`inbound_batches.pricing_status ∈ (unpriced, provisional, final)`;`assay_results.is_final` / `applied_at` / `superseded_by`(复验链);`apply_assay_result` 重算应付,`apply_output_assay` 只抄含量。`assay_results` 的表注开头就是「货到在先,真实含量在后」 |
| 9 | 掺配到目标品位 | **半死** | **【码】N 进 1 出结构上【就是】掺配**。活下来的一半:**没有"目标品位"这个概念** —— 没有规格、没有公差,`materials.spec` 是自由文本 |
| 10 | **进料分类法**:先问【是什么】(电池/电子废料/包装/耗材),再对电池问【状态】 | **完全存活** | **【码】`materials.category` 是无 CHECK 的自由文本**,线上三种命名法并存。监管轴(`waste_classifications`)回答的是另一个问题。**"状态"这条轴一个字都没有** |

## 桶的计数(STEP 2 要求报的那个数)

| 桶 | 计数 | 处置 |
|---|---|---|
| **(a) 仓库自己答得了** | **15** | 已在第一部分逐条答完,**一条都不问 Tim** |
| **(b) 行业惯例,带建议去问** | **5** | 问,但附上推荐答案 —— Tim 一个字就能接受或推翻 |
| **(c) 只有 Tim 答得了** | **9** | 逐条问,一次一条 |

**(a) 的十五条**(逐条已在第一部分给出证据):
多进多出支持 ✓ · 回炉支持 ✓ · 暂定/最终计价已建 ✓ · 停机已建 ✓ ·
正负极可作两种物料 ✓ · 损耗无类别 ✗ · 水分/基准零处 ✗ · 无工序类型 ✗ ·
一炉无时长 ✗ · 金属清单是写死的七个 · 无 BOM/标准回收率 ✗ ·
进料无"是什么"分类 ✗ · 有监管分类(全 NULL,刻意不回填)✓ ·
产出批必是可售存货 · 无目标品位/公差 ✗

**(b) 的五条**(带推荐):化验含量要不要声明**基准**(推荐:要,干基)·
损耗要不要**分类**(推荐:要,但只分到"分得开、量得到"为止)·
处置流怎么建(推荐:产出批 + 处置成本,**不给存货价值**)·
金属清单要不要从 CHECK 变成字典表(推荐:要)·
"状态"轴是不是**电池专属**(推荐:是 —— 这是 Tim 自己的框法)

**(c) 的九条** 在第三部分逐条记录,每条带来源标记与返回条件。

> **(c) = 9 偏多,已经压过一轮。** 压不下去的理由:它们全部是关于
> **Evoltrya 那条线本身**的事实(它收什么、做哪几道工序、卖哪几条流),
> 而这类事实**不在仓库里,也不在行业常识里**。
> 压下去的做法是**给每一条都附上推荐答案**,让 Tim 一个字就能修正,
> 而不是从头讲一遍。

---

# 第三部分 · 访谈记录(边问边写)

## R1 · 进料分类法的第一条轴 —— **提的方案被推翻,四条修正**

**Claude 提的:** `materials` 上一个小的封闭枚举
`battery / ewaste / packaging / consumable / other`,只有 `battery` 能被加工消耗。
配的问题是"电子废料到底进不进加工"。

**【Tim】四条修正,四条都成立:**

### N1 ·「battery」这个名字覆盖不了它要覆盖的东西

> **【Tim】** 可加工的那一类必须包含**整颗电芯与模组、极片废料,以及【我们买进来的
> 黑粉】**(掺配到品位就在假设清单上)。看到 "battery" 的人会把它读成"整颗电池",
> **第一天就会把买进来的黑粉分错类**。
> **按【功能】给这个值起名 —— 它是"可以被一炉加工吃掉的东西" —— 不要拿它的
> 某一个实例来起名。**

### N2 · 那么【产出】算哪一类?—— 而这一条比它听起来更硬

> **【Tim】** `materials` 里同时装着这个厂**自己产出**的东西(黑粉、铜箔、隔膜、壳体)。
> 要么每一行都必须有 kind、枚举里得有一个值给它们;要么 kind 可空,
> 而**"没人决定过"就与"不适用"再也分不开** —— 这个三态坑本仓库已经在
> `supplier_types` 与"没测过的金属含量"上各踩过一次。
> **在这一列存在之前就把它定下来,并写下理由。**
> **注意一种物料可以【两者都是】:黑粉是这里产的,也可能是买进来的。**

> **【码】这一条在【线上已经是事实】,而且比 Tim 说的更强 ——
> 不是"可能",是【每一个真实物料都已经如此】:**
>
> | 物料 | category | 进料批 | 产出批 |
> |---|---|---|---|
> | `MAT-2026-0001` NMC Cathode Foil | `进料-电池` | **9** | **10** |
> | `MAT-2026-0002` Special Battery Material | `NiH` | **8** | **7** |
> | `ZZ-SMOKE-PROBE` probe | `black_mass` | 1 | 0 |
> | `ZZ-SMOKE-NTF` NTF-1 walk scratch | `进料-电池` | 1 | 0 |
>
> **两个真实物料【都】同时有进料批和产出批。**
> 所以「按功能切一刀的单值分区」**不是将来会撞上问题,是今天就已经不成立**。

### N3 · 备件

> **【Tim】** 机器的关键备件也是到货的实物,要管库存,而且**不按批次追溯**。
> 它算 `consumable`,还是自成一类?**问,不要假设。**

### N4 · 那 `materials.category` 怎么办

> **【Tim】** 它已经同时装着三种命名法。**两列互相重叠的分类、其中一列还是没有
> CHECK 的自由文本,正是这套系统长出一个没人信得过的字段的方式。**
> 报告 category 是【与 kind 分工并存】还是【退役】——
> 若并存,那条分工必须**写在列上**。

**处置:** 四条全部并入下一轮,**不写任何迁移**。
N2 的证据把提案的形状直接否掉了,所以下一轮问的是**形状本身**,不是取值。

## R2 · 形状定了(Option A),而 Tim 又加了五条 + 一个作业

**【Tim】Option A 接受:** `kind`(按【本性】分区,NOT NULL)
+ `may_be_processed`(NOT NULL 布尔,**加工闸门唯一读的那一列**)。
「这里产的」**不是一列** —— 它就是"这个物料有没有产出批"。

**【Tim】N5–N9,外加一个作业:在提最终取值清单之前,把清单【全列出来】,
并把线上【每一行】物料映射到恰好一个值;映射不了就【报成一条发现】,
不许为了让映射闭合而发明一个值。**

### 作业先做:线上四行物料,逐行映射

**【码】线上在册四行(另有四行已软删),`category` 的取值实测:**

| 物料 | name | category | chemistry | 进料批 | 产出批 | 映射得了吗 |
|---|---|---|---|---|---|---|
| `MAT-2026-0001` | NMC Cathode Foil | `进料-电池` | `NMC` | 9 | **10** | **映射得了 → 电池料**(极片材料)。**但它自己的 category 值【已经与它的数据矛盾】**:写着「进料」,却有 10 个产出批 |
| `MAT-2026-0002` | Special Battery Material | `NiH` | `Special Chemistry Structure` | 8 | 7 | **映射不了。** 名字是占位符("特殊电池材料"、化学体系"特殊化学结构")—— `NiH` 也许指镍氢,**但那是猜的**,而这一行的名字什么都没说 |
| `ZZ-SMOKE-PROBE` | probe | `black_mass` | — | 1 | 0 | **它根本不是一个业务物料** —— 冒烟脚本的残骸行。而且它正是 AGENTS.md 点名的那一个:一张【在册真批次】`IN-2026-0180`(100,000 kg,余 99,970)引用着它,所以 `check:scratch` 报它并说**不要删** |
| `ZZ-SMOKE-NTF` | NTF-1 walk scratch | `进料-电池` | — | 1 | 0 | **同上** —— 走查残骸行 |

> ### 发现 F1 ·「category」里【至少四种】命名法,而其中一种编码的是【方向】
>
> 在册四行里已有三种:`black_mass`(英文小写下划线)、`NiH`(化学体系缩写)、
> `进料-电池`(中文短语)。**已软删的四行里露出第四种:`产出-黑粉`。**
>
> **`进料-` / `产出-` 这个前缀编码的是【这个物料在流程的哪一侧】** ——
> 而 Option A 刚刚裁定那**不是物料的属性**(它是"有没有产出批")。
> **实测证据就在同一张表里:`MAT-2026-0001` 的 category 写着「进料」,
> 而它有 10 个产出批。这个值今天就是假的。**
>
> **这条发现独立地判了 N9:`category` 不该与 `kind` 并存。**
> 它同时装着三件事(本性 / 化学体系 / 流程方向),三件各有各的正确去处,
> 而其中一件在今天已经与数据矛盾。

> ### 发现 F2 · 四行里【只有一行】映射得了,而它的旧值是错的
>
> 1 行映射得了(且旧值已被自己的数据推翻)· 1 行是占位符,**映射需要猜** ·
> 2 行根本不是业务物料(冒烟/走查残骸)。
>
> **这条发现直接把 N7 答完了:没有什么可回填的,因为没有什么真东西可回填。**

### N5 · 两列可以互相矛盾 —— 约束放哪、为什么放那

**【荐,待 Tim 确认】** 放在**表上的 CHECK**,不放触发器、不放应用层:

```sql
CONSTRAINT materials_processable_only_where_possible CHECK (
    may_be_processed = false
    OR kind IN ('battery_material', 'ewaste')     -- 取值清单见下一轮
)
```

**为什么是 CHECK 而不是触发器:** 这条规矩**整个落在一行之内**,
不需要看第二张表 —— 而本仓库用触发器的理由恰恰是"CHECK 看不见另一张表"
(`payment_term_template_lines` 那条跨父子的规矩就是这样才用的守卫触发器)。
CHECK 是声明式的、进镜像、**对每一个写入者成立,包括直连 psql**,
而且没有任何 `SET LOCAL` 绕得过去 —— 这正是 Tim 说的"直插不许说出不可能的话"。
形状抄 `purchase_order_lines_one_source` 与 `equipment_maintenance_performer_shape`。

### N6 ·「other」是 NOT NULL 枚举里的一个陷阱

**【荐,待 Tim 确认】拿掉 `other`。**
Tim 的判断成立:它**看起来像一个决定,行为上是"我不知道"** ——
三态坑换从逃生门进来。

**代价说清楚:没有 `other`,加一种新的 kind 就是【一支迁移】。**
而这正是对的 —— **"这里可以进哪几类东西"是一个牌照与商业模式的问题,
不是一个录入决定。** 对照:`certificate_types` 是 RUNTIME CONFIG
(操作员加一行就多一种证书),因为那件事真的会变;而 kind 是**加工闸门读的那一列**,
多一种 kind 意味着多一条闸门规则 —— 那需要有人想清楚,不是填一行。

**这条与 N5 是同一个决定的两半:** 要让 N5 那条 CHECK 成立,
取值清单就必须写在 CHECK 里;写在字典表里就只能退回触发器,
而那正好丢掉 N5 要的那个性质。

### N7 · NOT NULL 与回填

**【荐,待 Tim 确认】不回填,一行都不。** 用 `CHECK (...) NOT VALID` 那条既有惯例:

* 新行**必须**说出 kind(约束对 INSERT 生效);
* 现存四行**留空不动** —— 它们**本来就是"没有人决定过"**,而那是真话;
* **本仓库对这一模一样的情形已经做过一模一样的决定**:MAT-1 的
  `materials.waste_classification_code` —— *「既有物料全部 NULL,而且不回填。
  NULL 的意思是"没有人分过类",不是"非受控"」*,`db/fixtures/53` A 臂钉住三态可辨。
* FIN-32 的 `business_date` 是同一条惯例的另一次:
  *「NOT VALID 在插入时生效,而把 15 行历史 NULL 留着 —— 它们是历史,不是 bug,
  回填它们等于发明一个没人记录过的事实」*。

**与 Tim 那条"绝不为保住测试数据设计迁移"的立场一致:**
这个做法**不是**为了保住那四行 —— 它们会在切换那天整批消失。
它只是拒绝**为了满足一条约束而伪造四个值**。两件事不冲突。

## R3 · 取值清单定了;N10 把 N9 也一并判了

**【Tim】** 包装与备件**各自成类**(包装可能随货出去、可能可回收;备件挂在机器上、
有关键度)。**后拆比现在多一个枚举值贵。**

**【Tim】`reagent` 不加。** 粉料线还没到、流程图还没定稿,今天确认不了要不要用工艺药剂。

> ### 【未知 U1】工艺药剂(reagent)算不算一类物料
> **返回条件:第一次真的采购一种工艺药剂的那一天。**
> **【Tim】现在为一个想象出来的情形加一个值,正是这个项目拒绝做的事;
> 而一个没人用的枚举值会教下一个读它的人"这一类在用"。**

### 定下来的取值清单

| kind(NOT NULL,写在 CHECK 里,**没有 `other`**) | `may_be_processed` |
|---|---|
| `battery_material` —— 整颗电芯、模组、极片废料、**买进来的黑粉** | **是一个选择** |
| `ewaste` | **是一个选择** |
| `packaging` | 强制 false |
| `consumable` | 强制 false |
| `spare_part` | 强制 false |

### N10 · 今天到底【谁】在读 `category` —— 查完了,答案是"没有人拿它做决定"

**【码】全仓库的读者,逐个点名:**

1. **`app/materials/materialQuery.ts`** —— 物料列表的**筛选下拉 + CSV 导出**。
   它自己的注释写着:*「category 不是 DB 枚举,而是 app 层用 options.ts
   (CATEGORY_OPTIONS)约束的值;且 CustomSelect 允许自由文本,
   **所以自由文本的 category 行无法被下拉选中**」*;
2. **`MATERIAL_SORTABLE` 明确把 category 排除在外**(*「按中文规范值排序无意义」*);
3. **视图:0 处。函数:0 处。**(`leave_accrual_rate` 里那个 `'category'` 是假期口径的字符串,不是这一列);
4. **fixture:只有 INSERT,没有一条断言。** 它们填一个值只是为了满足 `NOT NULL`
   (例如 fixture 06 的 `INSERT INTO materials (code, name, category)`)。

> **结论:没有任何一处拿 `category` 做决定。** 它是一列自由文本上的一个列表筛选器,
> 而它的合法取值住在**第三个地方**(app 的 `options.ts`),数据库对此一无所知。

> ### 发现 F3 · 第三份命名权威,而且它是【方向优先】的 —— 正是 Option A 否掉的那个形状
>
> **【码】`app/materials/options.ts` 的 `CATEGORY_OPTIONS` 是七个值:**
> `进料-电池` · `进料-黑粉` · `产出-黑粉` · `产出-金属` · `产出-其他` ·
> `耗材辅料` · `其他`
>
> 三件事同时成立,每一件都指向同一个判决:
>
> 1. **这份清单按【方向】切在最前面**(进料- / 产出-)—— 而 Option A 刚裁定
>    方向不是物料的属性。`MAT-2026-0001` 标着「进料」却有 10 个产出批,
>    **是这份清单的形状让那个矛盾成为必然,不是有人填错了**;
> 2. **它把黑粉切成了两个值**(`进料-黑粉` / `产出-黑粉`)——
>    也就是说既有设计对"黑粉两者都是"的答案是**把它建成两种物料**。
>    那会给买进来的和自己产的黑粉两个不同的 `material_id`,
>    **库存合计、批次血缘、安全库存阈值全部就地裂开**;
> 3. **它已经带着一个 `其他`** —— N6 刚拿掉的那个逃生门,在 app 侧一直开着。
>
> **而线上四行里,只有一行的值来自这份清单。** `black_mass` 与 `NiH` 是自由文本,
> **那个下拉根本选不中它们** —— 也就是说物料列表的分类筛选器**今天就够不到
> 四行里的两行**,而没有任何东西说过这件事。

### N9 的判决 · `category` **退役**,不与 `kind` 并存

**【荐,依据 N10 的实测】** 三条理由,每一条都是量出来的:

* **没有任何一处拿它做决定**(N10)—— 所以退役不会破坏任何判断;
* **它同时装着三件事**(本性 / 化学体系 / 流程方向),而三件各有正确去处:
  本性 → `kind`;化学体系 → `chemistry`(**已经存在,而且已经装着 `NMC`**);
  方向 → **不是一列**,它是"有没有产出批";
* **它的取值已经与数据矛盾**(F1),而**它的形状让那个矛盾成为必然**(F3)。

**留着它 + 写一条分工,行不通** —— 分工要写成"category 说本性,kind 也说本性",
那不是分工,是两份定义。**本仓库为"两份定义必然漂开"付过很多次账。**

**退役要连着做三件事,否则只是把烂摊子挪个地方:**
`materials.category` 与 `CATEGORY_OPTIONS` 与那七个 i18n 键
(`materials.category.*`)**一起走**,并把物料列表的筛选器改成按 `kind` 筛。

### N11 · 那一行占位符 —— **它是一个决定,不是一个映射难题**

**【Tim】** `MAT-2026-0002`「Special Battery Material / Special Chemistry Structure」
映射不了是对的,**所以不要猜**。按"整批清掉重建"的裁定,**它走**。
**明说出来,而不是把它留在一张表的"映射不了"那一栏里** ——
一行映射不了、之后再也没被提起的记录,看起来与一次疏忽一模一样。

**处置:【Tim 裁定】切换时随全部测试业务数据一起清掉。不为它发明 kind。**

### N12 · 那条 CHECK 【管不到】什么 —— 而这句话要写在约束上

**【Tim】** 它挡得住"一个 consumable 被标成可加工"。
**它挡不住"一个本该可加工的 `battery_material` 被建成 false"** ——
那是一个**业务错误,schema 看不见**。

**【荐,待确认】用【报表】,不用屏幕上的提示:**

* 屏幕上的提示只在**建的那一刻**说话,而这个错**是事后才显形的**
  (有人想投料,发现投不了);
* 所以要一处**持续成立、会自己消失**的读数:
  `kind = 'battery_material' AND may_be_processed = false` 的物料清单。
* **但它不该是看板的一支臂** —— 那不是"等着人处理的事",它可能完全正确
  (确实有不该被加工的电池料)。它是**物料列表上的一个筛选/一列**,
  与 EQP-2d 记下的「哪些机器没人在盯」同一个形状、同一条判据。

**并且把这句话写在约束上**(本仓库最常犯的缺陷是**守卫的名字比它的射程宽**):

```sql
COMMENT ON CONSTRAINT materials_processable_only_where_possible IS
'挡的是【不可能】:consumable / packaging / spare_part 永远不可加工。
【它挡不住的】一个本该可加工的 battery_material 被建成 may_be_processed = false ——
那是一个业务判断,schema 看不见。那一半由物料列表上的筛选负责让它显形。';
```

### N13 · 清库之后,谁来填这两列

**【Tim】** 两列都是 NOT NULL,所以**物料表单必须两个都问**,
而 `may_be_processed` **必须是一个明说的选择,不是一个默认偏向某一侧的勾选框** ——
**一个默认值就是一个没人做过的决定,只是它从表单进来而不是从 NULL 进来。**

**【荐,待确认】表单与库【同一刀】。** 理由是本仓库自己的账:
**「库建好了、没有门」已经付过四次代价**(EQP-1a→1c-a 三刀操作员够不到;
`dispose_fixed_asset` 从 FIN-22 起就在而 app 里一个调用点都没有,FA-0 才发现)。
物料表单已经存在(`app/materials/`),加两个字段不是一张新屏幕 ——
**把它拆到下一刀没有省下任何东西,只是造出一个够不到的库。**

## R4 · 水分与"钱挂在哪个重量上" —— 基准未定,而五条修正与基准无关

**【Tim】基准这个问题没有作答,而是加了五条:无论选哪个基准,这五件事要么在范围内、
要么按名 park 掉。「不要让进料那一问把这个话题关掉。」**

> ### 【未知 U2】进料结算的重量基准(湿重 / 干重 / 按合同分)
> **返回条件:第一份【写明结算基准的】粉料进料合同签下来的那一天。**
> 今天已经在买电池废料(`MAT-2026-0001` 一个物料就有 9 个进料批),
> 所以**现行做法是存在的** —— 但粉料线还没到,而真正要紧的那几份合同没签。
> **不选一个惯例填进去。**

### N15 · 【已从仓库答完】机制**已经在了**,缺的是【入口】—— 所以修法比看上去小

**【码】暂定 → 最终这条路的【价格那一半】是完整的:**

* `price_history` 逐次留痕:`old_unit_price` / `new_unit_price` / `original_price`
  / `fx_rate` / `rate_as_of`(FIN-21:牌价取自哪一天哪一侧,事后推不回来,所以当场记);
* `apply_assay_result` 明确算出并返回 **`price_delta_usd`** —— 结算差额**已经是一个数**;
* `inbound_batches.pricing_status` 走 `provisional → final`,只有 `is_final` 的化验被
  执行后才升 final。

> **所以 H8【确实死透了】,不需要被提前** —— 它要的东西一件不缺。
> **真正的缺口是【驱动它的输入】:今天能驱动重算的只有【金属含量】,没有【水分】。**
> `assay_results` 上没有水分列,`assay_result_metals` 只收那七个金属的百分数。
>
> **这是一条好消息式的发现:** 水分不需要一套新的暂定/最终机制 ——
> **它需要的是搭上那套【已经建好并且在用】的机制**(多一列、多一个 delta 来源)。
> 修法的大小因此是"给已有的重算路加一个输入",不是"再造一条重算路"。

### N14 · 【已从仓库答完】**销售侧有一模一样的洞,方向相反**

**【码】** `sales_records`:`amount_base = round(quantity × unit_price × fx_rate, 2)`,
而 `quantity` 取自产出批 —— **同样是过磅重量**。

> **两侧的暴露是对称的:** 买进来按干重结算而我们按湿重记应付 → **我们少付/多付**;
> 卖出去客户按干重付而我们按湿重开票 → **我们多收/少收**。
> **【Tim】只修一侧比两侧都错更坏 —— 两个数会打架,而没人说得清哪个对。**

**处置:两侧同刀,不 park。** 理由是 Tim 那句话本身,加上一条仓库自己的账:
`price_history` 那套暂定/最终机制**只建在进料侧**,销售侧没有对应物 ——
所以"只修进料"不但留下一个洞,还会造出**两侧机制不对称**的第二个问题。

### N16 · 【设计约束,记下来】蒸发掉的水**不是工艺损耗**

**【Tim】** 若进料按一个基准记、产出按另一个基准称,**干燥失重会以"没回收出来的料"
的样子出现在收率里**。而收率在这套系统里**本来就只是一个有用的估计,不是可审计的 KPI**
—— **一个水分形状的误差藏在里面,恰恰是最像真的那一种。**

**要求(Tim 明确):** 说清所选基准下 `loss_qty` 的行为,
并**用真实数字写进 fixture 断言,不许只写在散文里**。

> **【码】而今天 `loss_qty` 连一个类别都没有**(见第一部分第 2 节):
> 它是 `COALESCE(p_loss_qty, 总投入 − 总产出)`,一个数,没有理由,
> 而且**可以与"投入−产出"不相等而无人过问**。
> **也就是说今天的模型连"这部分损耗是水"都说不出口。**

### N17 · 【已从仓库答完】存货按【哪个重量】估值

**【码】** `allocate_processing_costs` 第 257 行:

```sql
unit_cost_base = round(f.allocated / f.quantity_produced, 4)
```

**除的是产出腿的【过磅重量】。** 而 `unit_cost_base` 往下喂:
分摊(再加工按上游 `unit_cost_base` 估值)、`batch_margin`、以及销售定价的参考。

> **【Tim】把应付改对而把单位成本留错,只是把缺陷挪了一张表。**
> **要求:说明估值用哪个重量,并把理由写在那一列上。**

### N18 · 【设计约束,记下来】缺失的水分**绝不许用 0 代替**

**【Tim】** 零水分是一个**测量结果**。而**乘数的身份是看不见的** ——
`1 − 0` 会静默地产出一个看起来很合理的数。

> **【码】本仓库对这个形状有过一次记录在案的实例:** 无价投料在分摊时**计 0**,
> 而它没有被静默 —— `processing_outputs.cost_incomplete` 逐层传染,
> 列注释写着*「零永不静默,层层传染」*。**水分要抄的就是这个形状。**

**要求:没测过就渲染成"没测过",在这个数出现的【每一处】,不是只在录入那一屏。**


---

# 第四部分 · 收口(STEP 4)

## 十条假设,访谈之后的判决

| # | 判决 | 杀死它 / 留下它的**证据** |
|---|---|---|
| 1 连续/战役式投料 | **半死** | **码**:多进多出本来就支持。**活的一半是时间** —— `process_date` 是一个 date,全族无起止(EQP-2a 实测)。并入 #6 |
| 2 回炉、中间物不过磅 | **半死** | **码**:回炉已建(FIN-25 + 自吞守卫)。**活的一半**:每条腿数量 NOT NULL,**不称重的循环流表示不出来** |
| 3 损耗不是一个数 / 水分 / 基准 | **完全存活,而且是最贵的一条** | **码**:`loss_qty` 一个数、无类别、**可以与"投入−产出"不相等而无人过问**;**水分整仓库零处**;含量不声明基准;**应付、单位成本、销售额三处都乘过磅重量** |
| 4 多产出;有些流是处置成本 | **半死** | **码**:多产出支持。**活的一半更硬** —— `output_batches` 就是可售存货,每条腿都摊成本,**"要花钱处置"今天是反的** |
| 5 正负极是两种物料 | **已死** | **码**:主档自由文本 + N 条产出腿,**schema 什么都不拦**。剩下的是主数据问题 |
| 6 停机 / 卡料 / 跨班次 / heel 串批 | **部分存活** | **码**:停机已建(EQP-2a/2d)。**跨班次表示不出来**;**heel 一个字都没有** |
| 7 前处理:放电、分选、破损电芯 | **完全存活** | **码**:加工单上**没有任何工序/操作类型**;放电只作为一台固定资产存在,不是一道工序 |
| 8 化验滞后 / 暂定与最终 | **死透了** | **码**:`pricing_status` 三态 + `is_final` + `superseded_by` + `price_history` 逐次留痕 + `price_delta_usd`。**机制一件不缺** |
| 9 掺配到目标品位 | **半死** | **码**:N 进 1 出结构上就是掺配。**活的一半**:没有目标品位、没有公差 |
| 10 进料分类法 | **完全存活;第一条轴已谈定,第二条轴未谈** | **码**:`category` 无 CHECK、**四种命名法**、**其中一种编码方向且已与数据矛盾**、**第三份权威在 app 的 options.ts 里**。见 F1/F2/F3 |

**存活 3 条完整(#3 #7 #10)、半存活 5 条(#1 #2 #4 #6 #9)、死 2 条(#5 #8)。**

## 仍然开着的问题 —— 每一条带返回条件

| 编号 | 问题 | 返回条件(什么事件发生了才答得了) |
|---|---|---|
| **U0** | **进料分类法的【第二条轴】:电池料的【状态】取值**(可分离 / 小型 / 进过水 / 破损)。**Tim 点名的第二问,本次访谈没走到** | **下一轮访谈就能答** —— 它不依赖粉料线,依赖 Tim。**这是下一次会话的第一个问题** |
| **U1** | 工艺药剂(reagent)算不算一类物料 | 第一次真的采购一种工艺药剂 |
| **U2** | 进料结算的重量基准(湿/干/按合同) | 第一份写明结算基准的粉料进料合同 |
| **U3** | 销售侧的重量基准 | 第一份写明基准的黑粉销售合同(多半与 U2 同时) |
| **U4** | 一炉 = 一次离散事件,还是一段连续战役 | 粉料线投产,第一周真实运行 |
| **U5** | 有没有不过磅的循环流 / heel | 流程图定稿,或产线调试 |
| **U6** | 哪几条产出流存在,其中哪几条是【处置成本】 | 流程图定稿 |
| **U7** | 哪几道前处理由 Evoltrya 自己做 | 产线投产(放电已有机器 FA-2026-0001,但它今天不是一道工序) |
| ~~**U8**~~ | ~~掺配到目标品位:有没有规格与公差~~ **已答(CONTRACT-1,2026-08-30)** | 触发条件写的是「第一份带规格的供货合同」—— **本刀建的就是那个**。答案:**用 min/max 上下界,不用 target±tolerance**(后者是前者的对称特例,两种都存就是两个写法);规格**住在合同上**(`contract_grade_specs`),不住在加工单上;**违反是【报告】不是【拒绝】**(化验回来时货已经在场上,而没有暂扣状态可以放它) |
| **U9** | 正极料与负极料是否分离 | 流程图定稿 |

## GAP LIST —— 要对上现实,模块需要动什么

**这是一张【清单】,不是刀。** 大小与排序等 Tim 读完再说(brief 明令)。

| # | 缺口 | 今天是什么 | 依赖 |
|---|---|---|---|
| G1 | **物料的 `kind` + `may_be_processed`** | `category` 自由文本、四种命名法、方向编码进值里、第三份权威在 app | 已谈定(R1–R3)。**含:表上 CHECK、`NOT VALID` 不回填、退役 category + `CATEGORY_OPTIONS` + 七个 i18n 键、列表筛选改按 kind、物料表单两列必答且不给默认** |
| G2 | **电池料的【状态】轴** | 一个字都没有 | **U0** |
| G3 | **水分与基准** —— 化验带水分、含量声明基准、干重推导 | 整仓库零处 | **U2/U3**。**搭已有的 `price_history` / `pricing_status` 机制,不另造**(N15) |
| G4 | **应付 / 单位成本 / 销售额三处的重量口径** | 三处都乘过磅重量 | 随 G3。**两侧同刀**(N14) |
| G5 | **损耗分类,以及"水不是损耗"** | 一个数,无类别,可与投入−产出不符 | 随 G3。**要 fixture 用真实数字断言**(N16) |
| G6 | **缺失水分不许当 0** | 无从谈起 | 随 G3。抄 `cost_incomplete` 的传染形状(N18) |
| G7 | **工序 / 操作类型** | 加工单上没有任何一列 | **U7**。它是 #7 与 #1 的共同前提 |
| G8 | **一炉的时长 / 跨班次** | `process_date` 是一个 date | **U4** |
| G9 | **heel / 串批** | 一个字都没有 | **U5** |
| G10 | **处置流(成本而非产品)** | 每条产出腿都被摊成本、都是可售存货 | **U6** |
| ~~G11~~ | ~~**目标品位与公差**~~ —— **CONTRACT-1(2026-08-30)划掉** | ~~没有~~ `contract_grade_specs`(min/max、至少一个界、下界不高于上界、同一元素不重复);`contract_grade_breaches` 按**单据当初抄下的那份副本**判 | ~~**U8**~~ 已答。**★ 它划掉的是「规格与公差这个概念一个字都没有」这件事,不是「掺配」那件事 ★** —— 规格今天是一条**合同条款**,加工单上仍然**没有**「配到这个目标」的动作;要那件事,先要 G7(工序类型)与一次关于「掺配算不算一道工序」的裁定 |
| G12 | **金属清单从写死的 CHECK 变字典表** | `IN ('ni','co','li','mn','cu','al','fe')` **四处各写一遍** | 无外部依赖 —— 但**加石墨/塑料/电解液之前必须先做** |
| G13 | **不过磅的循环流** | 每条腿数量 NOT NULL | **U5** |


---

# 第五部分 · PROC-0b —— 重量基准的答案、损耗的三件事、以及第二批十题

## U2 / U3 · 【Tim】重量基准,已答

> **【Tim】采购按【到货湿重】记账与结算 —— 也就是系统今天做的那件事。
> 所以进料应付【不需要改】,U2 关闭。**
> **销售必须能按湿重【或】干重报价,逐笔生意选一次。所以基准是一个【条款】。**
> 而这套系统对"条款"已经有形状:**FIN-26/27 在承诺那一刻把计价条款抄下来,
> 结算读那份副本、永不读活公式。照它做,不要另发明一套机制。**

**处置:** U2 **关闭**(无需改动)。U3 **改写**:不再是"基准是什么",
而是"基准作为条款挂在哪张承诺上"。

> **【码】那个形状确实在,而且正是 Tim 说的那一份:**
> `pricing_term_commitments` / `pricing_term_commitment_metals`(FIN-27)——
> *「一个被交易引用的公式不能在它脚下改变,因为条款在承诺那一刻被【抄】到
> 承诺记录上,结算读那份副本」*;`fixture 21` 钉住:承诺 → 改公式 → 结算,
> 断言的是**被承诺的那个数**,而**活公式算出来的数被同时算出并断言【不同】**
> —— 所以那一臂不可能靠两个答案恰好相等而通过。
> **也就是说 U3 不需要新机制,它需要在那份副本上多一个条款字段。**

## W1 · 化验含量的基准 —— **代码说不出口,而【那份沉默就是发现】**

**【码】逐处查过:**

* `inbound_batch_metals.content_pct` / `output_batch_metals.content_pct` /
  `assay_result_metals.content_pct` —— 三处都是 `numeric CHECK (0..100)`,
  **没有任何一列、任何一条注释说这个百分数是以什么为基测的**;
* `assay_results` 全部 18 列(实测)里**没有水分,也没有基准**:
  `id, code, inbound_batch_id, assay_date, lab_name, certificate_ref, sample_ref,
  is_final, notes, applied_at, applied_by, superseded_by, deleted_at, created_at,
  created_by, updated_at, updated_by, output_batch_id`;
* 全仓库 `moisture|dry_basis|wet_weight|水分|干基|湿基` **零命中**。

> ### 发现 F4 · 基准这件事**在系统里没有位置,所以它今天由【实验室的习惯】默默决定**
>
> 实验室**按惯例报干基**。而这套系统把那个数**乘在湿重上**:
> `processing_metal_recovery_all` 拿 `quantity_consumed × content_pct` 算投入金属量。
>
> **于是同一个错误一次性污染四个数,而每一个都自洽:**
> 含金属量 → 回收率 → 批次估值(经 `allocate_processing_costs` 的 `metal_value` 基准)
> → 销售定价参考。**四个数会互相印证,因为它们错得一模一样。**
>
> **Tim 已裁定采购按湿重结算,所以这【不是】应付的问题** —— 应付是
> `quantity × unit_price`,压根不经过 content_pct。
> **它是【金属账】的问题**:一个我们以为在跟踪的东西,可能从来没有被正确跟踪过。
> **所以水分仍然要进【进料化验】,为的是金属这本账,不是钱那本账。**

## W2 · 损耗是【三件事】,而它们塌进了一个数

**【Tim】把这一条钉死了:蒸发掉的水【本身就是一种损耗】,而且是"质量走了、金属没走"
的那一种。** 三件事:

| | 是什么 | 质量 | 金属 | 今天怎么表示 |
|---|---|---|---|---|
| (i) | **水与挥发物** | 走了 | **留着** | 只能算进 `loss_qty` → **回收率被冤枉扣分** |
| (ii) | **粉尘与洒漏** | 走了 | 走了 | 算进 `loss_qty` —— 这一种是**真损耗**,唯一表示对了的 |
| (iii) | **残渣与炉渣送去处置** | 走了 | 走了 | **根本不是损耗** —— 它是一条**带负价值的产出** |

**【码】今天:`processing_runs.loss_qty`,一个 numeric,
`COALESCE(p_loss_qty, 总投入 − 总产出)`,无类别、无理由,而且可以与"投入−产出"
不相等而无人过问。** 一个 numeric 表达不了三个事实 ——
**这就是为什么今天那个回收率不可能诚实。**

> **【Tim 指令】G5 与 G10 是【同一个主题】,不是两条。**
> 并且要回答:**损耗是不是应当从一个【列】变成一组【带类型的行】。**

**【荐,待确认】是,变成行 —— 而且理由不是"更灵活",是三条具体的:**

1. **一个数答不了三个问题。** 回收率要问的是"金属去哪了",而 (i) 与 (ii) 对这个问题
   的答案相反 —— 合在一个数里,回收率**永远**算不对,而且错的方向取决于当天湿度。
2. **(iii) 根本不属于这张表。** 送去处置的残渣有重量、有去向、有一张处置费单据,
   而且**有 Basel/牌照意义上的申报义务**(见下面 I 题)。把它记成"损耗"等于
   **让它从物料台账上消失**,而监管问的正是它。
3. **变成行之后,"加一种损耗类别"是加一行数据** —— 这正是 Tim 说的"预留位置
   而不撒谎"的那个形状(`certificate_types` / `waste_classifications` 的先例)。

## 第二批十题 —— 先拿代码打一遍

| 题 | 判 | 证据 |
|---|---|---|
| **A** 放电 / 荷电状态 | **半死 —— 而活下来的一半修法很小** | **【码】"运行前置条件"这个形状【已经在了】:** `commit_processing_run` 有 **18 条具名拒绝**,其中三条正是生命周期前置:`WO_NOT_RELEASED`、`EQUIPMENT_NOT_ACQUIRED`、`EQUIPMENT_DISPOSED`。**所以"未放电不许投料"是同一个形状,不是一个新机制。** 活着的一半:**没有任何地方记录一批料放没放过电** —— 放电只作为固定资产 FA-2026-0001 存在 |
| **B** 化学体系隔离 | **完全存活** | **【码】`materials.chemistry` 是自由文本**(app 侧 `CHEMISTRY_OPTIONS` 有 10 个值,但**数据库不认**;线上实测两个值,其中一个是 `Special Chemistry Structure` 这种占位符)。**`commit_processing_run` 里 `chemistry` 零命中** —— **一炉里混几种化学体系,没有任何东西拦、也没有任何东西记下来** |
| **C** 黑粉怎么定价 | **半死 —— 骨架在,惩罚项全缺** | 见下方专节 |
| **D** 取样、留样、仲裁化验 | **完全存活** | **【码】`umpire|counterparty_assay|split_sample|buyer_assay|仲裁` 全仓库零命中。** 有的是:`lab_name`(自由文本)、`certificate_ref`、`sample_ref`、`superseded_by`(复验链)。**没有的是:这份化验是【谁】的、留样、仲裁样、以及两家实验室不一致时算谁的** |
| **E** 销售侧的最终结算 | **完全存活,但机制可复用** | **【码】`apply_assay_result` 里 `sales_record` 零命中;没有 `reprice_output_sale`。** 而销售记录本身是**改不动的**(`guard_sales_record_movements_append_only`,`quantity`/`unit_price` 都在守卫里)。有的是 `price_source ∈ (computed, manual)` + `price_provenance`(**这笔价是怎么来的,已经记着**)。**采购侧那套 `pricing_status` + `price_history` + `price_delta_usd` 可以照搬,不必另造** |
| **F** 来料加工(tolling) | **完全存活** | **【码】`inbound_batches` 上没有任何"所有权"列**(`own\|toll\|consign` 零命中)。**收一批货就是把它记成我们的存货、并同时开出一笔应付** —— 应付之锚 `quantity × unit_price` 是无条件的。**"这批料不是我们的"在今天说不出口** |
| **G** 在制品(WIP) | **完全存活(而它是 G7 的后果)** | **【码】`processing_runs` 只有 `committed`/`reversed` 两态,没有"进行中"**;一次加工是原子的。**只要工序还没有类型(G7),"半路的料"就不存在也不需要存在** —— 两道工序之间的东西今天必须被记成一个产出批,或者干脆不记 |
| **H** 不合格与质量暂扣 | **完全存活** | **【码】`output_batches.state` 的 CHECK 实测就是三个【销售】状态:`库存中 / 部分售出 / 已售罄`。** 没有"暂扣"、没有"不合格"。**有 `stock_by_status` 里的"暂扣"概念(库存侧),但那是【库存动作】,不是【质量结论】** |
| **I** 监管口径的物料平衡 | **半死** | **【码】血缘已经建好**:`batch_lineage_all` 是一个 `WITH RECURSIVE`,上下游都能走(AUD-1 的可追溯报告读它)。**答得了的是"这个产出批的料从哪来"。** **答不了的是那道题的另一半:进来的这些料,有多少【处置掉了】** —— 因为处置那一半今天被记成 `loss_qty` 里的一个数(W2-iii),**它在物料台账上不存在** |
| **J** 粉尘、细粉与残渣 | **与 W2 同一题** | 有价值的那些 → 可以建成产出批(**支持**);要花钱处置的那些 → **今天只能塞进 `loss_qty`**(W2-iii)。**两者今天的表示方式不对称,而这正是 I 答不出来的原因** |

**死于代码:0 题完整死亡;A / C / I 半死;B / D / E / F / G / H / J 完整存活。**
(第一批是死 2 半死 5 存活 3;第二批更硬,因为这十题是**刻意挑的没人问过的角落**。)

### C 专节 · 定价机器到底表达得了什么

**【码】`calculate_metal_price_from_terms` 的算式,逐行读出来:**

```
gross      = Σ(每金属)  数量 × content_pct × payable_pct × 指数价
treatment  = 数量_kg / 1000 × treatment_charge_usd_per_tonne
discount   = gross × flat_discount_pct / 100
net        = gross − treatment − discount
```

| 行业结构里的一件 | 表达得了吗 | 依据 |
|---|---|---|
| **每金属的应付比例** | **✓ 正是这个结构** | `pricing_formula_metals.payable_pct`(0–100,**明确的 0% 是可表示的正当条款**) |
| **处理费(TC)** | **✓** | `treatment_charge_usd_per_tonne`,按吨 |
| **指数与口径** | **✓** | `price_index`(METAL-2)、`price_basis ∈ (spot, average)` + `average_days` |
| **条款在承诺时冻结** | **✓** | FIN-27 `pricing_term_commitments`,fixture 21 双向钉住 |
| **精炼费(RC)** | **✗** | 只有 `flat_discount_pct`,它**按 gross 的比例走**;而 RC 是**按含金属单位**收、与价格无关。**用折扣冒充 RC,价格一动数字就错** |
| **有害元素惩罚** | **✗ 而且是【双重】缺失** | 没有任何惩罚概念:无阈值表、无惩罚项。**更硬的一层:氟、氯根本【不在那七个金属的 CHECK 里】** —— 也就是说**连测都记不下来,遑论罚** |

> **所以 C 的答案是:骨架对了(应付比例 + TC + 指数 + 条款冻结是行业结构本身),
> 而【钱怎么被扣掉】那一侧只建了一半。**
> 而缺的那一半有一个**顺序**:惩罚要先有元素才谈得上 —— 即 **G12(金属清单从
> 写死的 CHECK 变字典表)是惩罚项的前置**,不是一件可以并行的事。

## R5 · C 的判决,以及 N19–N23

**【Tim】「还没有条款」不等于「不要建」。把【存在性】与【取值】分开:**
黑粉承购里精炼费与惩罚元素(**首推氟与氯**)近乎普遍,**需要它们的概率很高**;
未知的是 **Evoltrya 自己合同里的阈值与费率**。
**排序照 Claude 自己那条发现走:G12 落地之后,加一个惩罚元素就是加一【行】。**

> **【Tim 裁定】G12 现在做 —— 它本来就要做,而且是前置。
> RC 与惩罚等到【真的有一份条款】再说。
> 对着一份想象出来的合同建一张惩罚表,正是这个项目拒绝做的事。**

> ### 【未知 U11】RC 与惩罚的**阈值与费率**
> **返回条件:第一份写明扣减结构的承购/供货条款到手。**
> **存在性不是未知**(高概率需要);**取值是未知**。两者不许混为一谈。

### N19 · 【已查】`flat_discount_pct` **线上正带着一个 20%,挂在一个叫「123」的公式上**

**【码】线上只有一份计价公式,实测:**

| | |
|---|---|
| code / name | `PF-2026-0001` / **`123`** ← 占位符名字 |
| direction | **`both`** —— 采购与销售**都**适用 |
| price_basis / average_days | `average` / 30 |
| treatment_charge_usd_per_tonne | **800** |
| **flat_discount_pct** | **20** |
| supplier / customer | 都为空(通用公式) |
| 金属条款 | ni 70% · co 70% · li 70% |

> ### 发现 F5 · 那 20% 是活的,而没有任何东西说得出它【是什么】
>
> 数据答不了"有没有人把它当 RC 用" —— **而这正是问题所在**:
> `flat_discount_pct` 这个名字只说了"一个按比例的折扣",
> **一个按 gross 走的 20% 与一个按含金属单位走的 RC,在录入那天可以是同一个数,
> 之后每一天都不是。**
>
> 这一行是测试数据(名字叫「123」),**所以它不是一个要修的生产缺陷** ——
> 它是一个**证据**:这个字段**已经在被当作一个通用扣减项使用**,
> 而它没有任何东西阻止下一个人把 RC 塞进去。记在这里,免得切换之后重演。

### N20 · 【已查】C 对采购侧**同样成立**,而且**冻结机制只有买这一侧**

**【码】两件事,方向相反:**

1. **公式本身是共用的:** `pricing_formulas.direction ∈ ('purchase','sale','both')`,
   而线上那一份就是 `both`。**所以 RC 或惩罚一旦加进公式,两个方向自动都吃到** ——
   这一半是好消息,加在**同一个地方**。
2. **但"条款在承诺时冻结"这套【只建在买这一侧】:**
   `pricing_term_commitments` 的列实测是
   `purchase_order_line_id` / `inbound_batch_id` / `source_formula_*` /
   `price_basis` / `average_days` / `treatment_charge_usd_per_tonne` /
   `flat_discount_pct` / `price_index` —— **两个父都是采购侧的单据,
   没有任何一列指向销售。**

> **所以 Tim 那句"修一侧比两侧都错更坏"在这里有一个精确的形状:**
> 加一个 RC 到公式里,**买这一侧会被冻结、卖那一侧不会** ——
> 于是同一笔生意的两端会引用同一份公式的**不同版本**,而且**只有一端说得出
> 自己引用的是哪一版**。这与 E(销售侧没有最终结算)是**同一个缺口的两个面**。

### N21 · 水分是一个经典的惩罚元素 —— 于是 W1 与 C 连上了

**【荐,待确认】** 若水分超标要扣钱,那么水分**不只是金属算术的输入,它是一个计价条款**。
后果有两个,都要说明白:

* **它住在哪:不变。** 仍然是【进料化验】上的一个测量值 —— 一个数只该有一个来源;
* **它要被冻结成什么:变了。** 那个**阈值与费率**是条款,必须进
  `pricing_term_commitments`(与 TC / payable_pct 同列);
  **而测量值本身不冻结** —— 它是事实,事实会被复验取代(`superseded_by`),
  这正是 `price_history` 那条路已经在做的事。

> **判据一句话:【测出来的数】跟着化验走,【拿它换算钱的规则】跟着条款走。**
> 把两者放在同一张表上,就会得到一个"改了阈值,历史结算跟着变"的系统。

### N22 · 惩罚不总是钱 —— **超过某个点是【拒收权】**

**【Tim】** 那是一条**规格上限**,不是一条计价公式。
**一个假设"所有超标都能折算成钱"的设计,将没有地方表达【拒收】。**

**【荐,记下来,不建】两者是不同种类的东西,不该塞进同一张表:**

| | 惩罚 | 拒收 |
|---|---|---|
| 是什么 | 一条**计价条款**(阈值 + 费率) | 一条**规格上限**(阈值 + 权利) |
| 结果 | 金额变少 | **这批货不进来** |
| 落点 | 计价条款,承诺时冻结 | 更接近 `certificate_types.disposition` 那个形状:**一条会拦人的判据** |
| 今天有吗 | 没有 | 没有 —— 而且**收货侧唯一会拦的东西是供应商证书**(`guard_supplier_qualification`),**与货本身的质量无关** |

> **这一条同时把 H(质量暂扣)拉了进来**:拒收发生在**进门那一刻**,
> 而"验完发现不合格"发生在**进门之后** —— 后者需要一个
> "既不是可售、也不是没了"的状态,而 `output_batches.state` 的三个值
> (`库存中 / 部分售出 / 已售罄`)**全都是销售状态,没有一个是质量结论**。

### N23 · 【已查】远期计价期是**同一个函数里的第二个结构性缺口**

**【码】两个口径都是【严格向后看】的:**

```sql
-- spot:
WHERE mp.price_date <= v_ref  ORDER BY mp.price_date DESC LIMIT 1
-- average:窗口以 v_ref 收尾;price_from/price_to 报的是【实际参与均值的行】的范围
```

* **"按一个还没发生的月份的均价计价"表达不了** —— 那些行情行还不存在;
* **而它不会报错**:它会拿参考日之前存在的行算出一个数来。
  **又一次"自洽而错"** —— 与 W1 的基准、与 N19 的 20% 同一族;
* **冻结那一侧也缺一块**:`pricing_term_commitments` 冻结了
  `price_basis` / `average_days` / `price_index`,**但没有"计价期是哪一段"这个条款** ——
  M+1 需要冻结的正是那一段。

> **判决:是【第二个结构性缺口】,住在同一个函数里,不是同一个缺口的一部分。**
> 它已经在 `docs/forward-queue.md` 阶段 5 排着(「指数挂钩定价 + 远期计价期」,
> 那一段写着"第一份长约就会撞上它")。**本次不设计,只把它与 C 的关系钉住:
> RC / 惩罚是【扣减】那一侧的缺口,计价期是【取哪个价】那一侧的缺口,
> 两者共用一个函数、共用一张冻结表,所以动其中一个的那一刀应当同时看另一个。**

## R6 · D 的判决 —— **两条轴,一条现在建,一条 UNKNOWN**

**【Tim】选项 1/2/3 答的都是"谁的化验【说了算】",而那是一条【逐笔合同的条款】,
不是一个系统级设置。哪一个都不要建成政策。系统要能装下某一份合同说的那一个,
并在承诺时冻结 —— 就是它给计价条款用的那个 FIN-27 形状。**

**两条轴,文件里必须分开写:**

| 轴 | 是什么 | 现在怎么办 |
|---|---|---|
| **这份化验是【谁】的** | **化验行上的一个事实** | **无论合同怎么说都要建。** 即便我们完全接受对方的数,**我们自己的结果也必须并排放着** —— 否则永远学不到"某家供应商的实验室读数偏高"这件事。**记录 ≠ 说了算。** |
| **谁的化验【说了算】** | **交易上的一个条款** | **【未知 U12】**,返回条件:第一份写明化验条款的合同 |

> ### 发现 F6 · `superseded_by` **不能拿来冒充"对方的化验"**
>
> 它编码的是**「我们复验了,现在这个才是真相」**。而争议的形状**相反**:
> **两个数都有效、分属两方、由第三方裁决。**
> **把对方的化验记成一次 supersession,等于用他们的数【静默覆盖】我们的数**
> —— 而覆盖之后,"我们当时测的是多少"就再也拿不回来了。
> **所以 party 这条轴【模拟不了】,它必须是一个真的字段。**

### N24 · 【已判】分歧就是钱,而机器**可以复用**

**【码】** 采购侧那条路是:`apply_assay_result` → 重算应付 →
`price_history`(old/new/rate_as_of)+ **`price_delta_usd`**。
**一次争议裁决的形状与它逐字相同**:换一个含量数 → 重算 → 一个差额。

**差别只有一处,而且是加法不是改法:** 今天**一次只能有一份化验被 apply**,
且 apply 会替换批次含量。争议要的是**三份都记着(我方 / 对方 / 仲裁),
而 apply 的是【说了算的那一份】** —— 也就是多一个"哪一份说了算"的指针,
然后走原来那条路。**不必第二套实现。**

### N25 · 仲裁要花钱,**而谁付是一条规则**

**【记下来,不建】** 惯例是**离仲裁结果更远的那一方付**。
所以仲裁化验是一笔**带条件付款方的费用** —— 它同时是:
一笔 `expenses`(钱)、一条条款(谁付的规则,承诺时冻结)、
以及一个**要等结果出来才知道付款方**的判断。**三件事,今天一件都没有。**

### N26 · 留样**已经在队列里了** —— 这是【同一个主题】,不是第二件事

**【码】`docs/forward-queue.md:218`:「事件驱动,不排队」那一节里写着
**留样**,与**实验室工作流**并列。** 所以不提第二件事,只把它们连起来。

> **而这套系统【已经知道样品是实物、而且会用完】** ——
> `batch_required_assay_gaps`(ASY-P1)的谓词里就有「**还取得到样**」这一条,
> 它的迁移抬头写着:*「化验要取样,而【样没了】—— 料已经全部耗掉了」*(线上
> IN-2026-0011 / IN-2026-0153,`remaining_qty = 0`)。
> **留样模型需要的那个前提,系统里已经有一个成文的实例。**

### N27 / N28 · `lab_name` 与取样规程

**【码】** `lab_name` 是自由文本;`sample_ref` 是自由文本;
**取样规程(份数、缩分、水分单独取样)全仓库零处**。

> **【荐】`lab_name` 变字典,而且它属于"拖延很贵"那一类** —— 理由不是理论,
> 是**本次会话刚刚量到的**:`materials.category` 也是自由文本,
> 结果同时长出**四种命名法**,而**没有任何东西察觉**。
> 实验室将来要被比较、被审计、被指名为仲裁方 —— 那三件事都要求它是一个**行**。
> **成本见下面的清单。**

**取样规程属于哪一边:** 属于**留样**那一侧,不属于化验单。
理由:一张化验单是**一次测量的结果**,而规程是**那个样品怎么来的** ——
同一个样品可以被测多次(复验、仲裁),规程只有一份。
**放在化验单上会被抄 N 遍,而 N 份副本必然漂开。**

---

# 第六部分 · 拖延的代价(Tim 指定的那份清单)

> **【Tim 指令】"预留位置"【不许】变成"提前加可空列"** —— 一个没人填的列与
> "没人决定过"分不开,一个没人用的枚举值会教下一个人"这一类在用"。
> **不撒谎的预留方式:把那个东西做成【数据】而不是【列】**
> (`certificate_types` / `waste_classifications` 的先例),
> 于是**将来加一种损耗类别、一种金属、一道工序、一个惩罚元素,代价是【一行】。**

## 先说贯穿整份清单的那条规律

> ### 发现 F7 · **"拖延很贵"的那些,几乎全是【自由文本的分类列】**
>
> 本次会话量到了这个病的一个**已经发作**的实例:`materials.category` ——
> 自由文本、无 CHECK、**四种命名法并存**、其中一种编码方向且**已与数据矛盾**、
> 第三份权威在 app 里、**而没有任何一道闸门察觉**。
>
> **同一个病、尚未发作的还有两处:`materials.chemistry`、`assay_results.lab_name`。**
> 两者今天都是自由文本,而 app 侧 `CHEMISTRY_OPTIONS` 已经有一份十个值的清单
> —— **一份现成的种子,以及第三份权威的又一个实例。**
>
> **为什么自由文本的分类列拖延特别贵:** 值会**累积**。
> 今天转字典是"建表 + 外键 + 迁四行";一年后转字典是
> **"先把没人负责过的几百个拼法对账一遍"** —— 而那笔对账**没有真源可查**,
> 只能靠猜。**这不是一个渐渐变贵的成本,它是一个会变成【不可能】的成本。**

## 清单 —— 两批的每一个 parked 项

| 项 | 拖延的代价 | 依据 / 现在做什么小事能让它变便宜 |
|---|---|---|
| **G12 金属清单变字典** | **EXPENSIVE** | 写死的 CHECK **四张表各一遍**;加一个元素要动四处 + 每一个读者。**而它是惩罚项、石墨/塑料、以及氟氯的共同前置。【Tim 已裁定现在做】** |
| **RC + 惩罚(U11)** | **CHEAP** —— 前提是 G12 先落 | G12 之后,加一个惩罚元素 = **加一行**。**这正是"把东西做成数据"的回报,而且是可量的** |
| **G2 电池料的状态轴(U0)** | **看 G1 怎么建** | 若 `kind` 以 **CHECK** 落地 → **EXPENSIVE**(状态轴要新列 + 表单 + 每个读者);若以**字典表**落地 → **CHEAP**(状态挂在 kind 上,是行)。**小事:G1 用字典表,不用 CHECK** —— 见下方那条自我更正 |
| **G3/G4 水分与基准** | **EXPENSIVE,而且是【会变成不可能】的那一种** | **每过一天就多几行【没人记过基准】的化验**,而基准事后**推不回来**。**小事,现在就能做而且很小:在化验单上要求声明【基准】(干/湿),哪怕水分那一列还没建** —— 声明一个已知事实是记录,不是发明 |
| **G5/G10 损耗变带类型的行** | **CHEAP** | 现存加工单全是测试数据、切换即清(Tim 的裁定),所以没有数据要迁;代价只剩少数几个 `loss_qty` 读者。**小事:类别做成字典,于是"加一种损耗"是一行** |
| **G7 工序类型** | **EXPENSIVE** | 一炉今天是无类型的原子事件;工序类型一进来,`work_order_*`、差异、回收率、WIP 全部要重读。**小事:没有便宜的提前动作 —— 但它要等 U7,所以【别在此期间再写死"一炉只有一道工序"的假设】** |
| **G(WIP)** | **随 G7** | 它是 G7 的后果,不是独立一件 |
| **G8 一炉的时长 / 跨班次** | **CHEAP** | 加两列时刻是**加法**,现有读者读的是 `process_date`,不受影响 |
| **G9 heel / 串批** | **CHEAP** | 它是一条**记录**,不改任何既有推导 |
| ~~**G11 目标品位与公差**~~ | ~~**CHEAP**~~ **已做** | ~~新表,不动既有~~ —— CONTRACT-1 建成 `contract_grade_specs`,**确实是新表、确实没动既有**(那次估价是对的) |
| **G13 不过磅的循环流** | **EXPENSIVE** | 每条腿数量 NOT NULL,而**质量守恒那条断言**(`OUTPUT_EXCEEDS_INPUT`)建立在"每一克都被称过"之上。**小事:无** —— 要先知道那条流存不存在(U5) |
| **化验的 party 轴(D)** | **EXPENSIVE** | 【Tim 裁定无论如何都建】。拖延的代价很具体:**"这份化验当时是谁的"事后不可恢复** —— 而且 `superseded_by` 会诱使人用覆盖去凑合(F6),那会**销毁我们自己的数** |
| **谁的化验说了算(U12)** | **CHEAP** | 它是一个**条款**,抄进 `pricing_term_commitments` 那个已建的形状 |
| **`lab_name` 变字典(N27)** | **EXPENSIVE** | F7 那条规律的直接实例。**小事:现在就把它变成外键**(今天线上化验行极少,对账成本≈0) |
| **`chemistry` 变字典(B)** | **EXPENSIVE** | 同上,**而且 app 里已经有一份十个值的现成种子**。**小事:同上** |
| **化学体系隔离的判据(B)** | **CHEAP** —— 前提是 `chemistry` 先变字典 | 判据要读一个**可比的**值;自由文本上写不出可信的判据 |
| **销售侧最终结算(E)** | **CHEAP** | 机器齐全,照抄采购侧;**但要连着 N20 一起**(冻结表今天只有买这一侧) |
| **销售基准作为条款(U3)** | **CHEAP** | `pricing_term_commitments` 多一个条款字段 |
| **计价期 M+1/M+3(N23)** | **EXPENSIVE** | 与 RC/惩罚**共用一个函数、一张冻结表**;已在 forward-queue 阶段 5。**小事:动那个函数的那一刀顺手看一眼另一半** |
| **来料加工 tolling(F)** | **EXPENSIVE** | 收货**无条件**建存货 + 建应付;"这批不是我们的"要动**应付之锚**本身。**小事:无便宜动作 —— 但下一刀碰收货时【别把"收货即产生应付"这个假设再焊死一层】** |
| **质量暂扣 / 不合格(H)** | **EXPENSIVE** | `output_batches.state` 是三值 CHECK,加第四个值要动每一个按 state 分支的读者。**小事:state 变字典** |
| **拒收权(N22)** | **CHEAP** | 它是 `certificate_types.disposition` 那个形状的又一个实例 —— 一条**会拦人的判据**,做成数据 |
| **仲裁费与"谁付"(N25)** | **CHEAP** | 一笔 expenses + 一条冻结的条款,两样都有形状 |
| **留样与取样规程(N26/N28)** | **CHEAP** | **已经在 forward-queue 的事件驱动清单里**;与实验室工作流同一件事,别另起 |
| **监管口径的物料平衡(I)** | **随 G5/G10** | 血缘已建;缺的是**处置那一半**,而那正是 W2-(iii) |

## 一条对我自己先前建议的更正

> **N5 我建议把 `kind` 的取值写进【表上的 CHECK】**,理由是"直插不许说出不可能的话"。
> **Tim 这次的"把东西做成数据"指令与它拉反了**,而**仓库自己的先例站在 Tim 那一边**:
> `certificate_types` 是**字典表**,而收货闸门靠 **`guard_supplier_qualification`
> 这个触发器**读它 —— **触发器同样对每一个写入者成立,包括直连 psql。**
>
> **所以更正:`kind` 做成字典表 + 外键,处置规则(哪些 kind 允许可加工)
> 作为字典表上的一列,由触发器执行。** 失去的只有"CHECK 是声明式的、
> 在镜像里一眼看得见";换来的是 **G2 从 EXPENSIVE 变成 CHEAP**。
> **这笔交换是划算的,而我先前那条建议没有把它算进去。**


# 第七部分 · GAP LIST 续,G14 起(接第四部分,**不另起一张**)

| # | 缺口 | 今天是什么 | 依赖 / 代价 |
|---|---|---|---|
| **G14** | **化验含量的【基准】声明** | 三张表的 `content_pct` 都不说基准;实验室按惯例报干基,而系统把它乘在**湿重**上 | **W1/F4。EXPENSIVE 且会变成不可能** —— 每天都在多几行基准不可知的化验 |
| **G15** | **进料化验上的【水分】** | 全仓库零处 | 为**金属账**,不为钱(采购按湿重结算,U2 已关)。搭 `price_history` 已有的重算路(N15) |
| **G16** | **化验的 party 轴**(我方 / 对方 / 仲裁) | 只有 `lab_name` 自由文本;`superseded_by` 是"我们复验了" | **【Tim:无论合同怎么说都建】。** F6:拿 supersession 冒充会**销毁我们自己的数** |
| **G17** | **`assay_results.lab_name` 变字典** | 自由文本 | **F7 那条规律的实例。EXPENSIVE 拖延,今天成本≈0** |
| **G18** | **`materials.chemistry` 变字典** | 自由文本;app 侧已有十值现成种子 | 同上。**并且它是 G19 的前置** |
| **G19** | **化学体系隔离的判据** | `commit_processing_run` 里 `chemistry` 零命中 —— **混投没有任何东西拦或记** | 依赖 G18。**可能比目标品位(G11)更要紧** |
| **G20** | **精炼费(RC)** | 只有按 gross 走的 `flat_discount_pct` | **U11 取值未知;存在性高概率。依赖 G12** |
| **G21** | **有害元素惩罚(阈值 + 费率)** | 无 | **双重缺失:氟氯不在七金属 CHECK 里。依赖 G12** |
| **G22** | **拒收权(规格上限)** | 收货只拦供应商证书,**与货的质量无关** | N22。`certificate_types.disposition` 的形状 |
| **G23** | **销售侧的最终结算** | `apply_assay_result` 碰 sales 零次;销售记录不可改 | E。**机器齐全,照抄** |
| **G24** | **冻结机制扩到销售侧** | `pricing_term_commitments` 两个父都是采购单据 | **N20:不补它,同一笔生意两端会引用同一份公式的不同版本,而只有一端说得出自己引的是哪一版** |
| **G25** | **销售重量基准作为条款** | 无 | U3。与 G24 同一张表 |
| **G26** | **远期计价期(M+1/M+3)** | spot/average **严格向后看**,而且**不报错** | N23。已在 forward-queue 阶段 5;与 G20/G21 共用函数与冻结表 |
| **G27** | **仲裁费与条件付款方** | 无 | N25 |
| **G28** | **来料加工:料不是我们的** | 收货**无条件**建存货 + 应付 | F。**EXPENSIVE:要动应付之锚** |
| **G29** | **质量暂扣 / 不合格状态** | `output_batches.state` 三值,全是**销售**状态 | H。**小事:state 变字典** |
| **G30** | **放电状态作为投料前置** | 前置条件的**形状已经有**(18 条具名拒绝);缺的是**记录放没放过电的地方** | A。**依赖 G2**(状态轴) |
| **G31** | **`materials.kind` 用字典表而非 CHECK** | —— | **对 N5 的自我更正。它把 G2 从 EXPENSIVE 变成 CHEAP** |

**第一批 G1–G13,本批 G14–G31。合计 31 条,一张清单。**

## 新增 UNKNOWN(接 U0–U9)

| 编号 | 问题 | 返回条件 |
|---|---|---|
| **U11** | RC 与惩罚的**阈值与费率**(**存在性不是未知** —— 高概率需要) | 第一份写明扣减结构的承购/供货条款 |
| **U12** | **谁的化验说了算**(逐笔合同的条款) | 第一份写明化验条款的合同 |

**U2 关闭**(采购按到货湿重,系统已经如此)。**U3 改写**(不再问基准是什么,
而是问它作为条款挂在哪 → G25)。


---

# 第八部分 · PROC-1 落地(2026-08-21)—— G1 与 G31 关闭

**这一刀只动主数据:物料【是什么】,以及它【能不能被投料】。**
进料批、加工单、化验、计价一律没碰(唯一的例外见下面 ②)。

## 建了什么

| 对象 | 是什么 |
|---|---|
| `material_kinds` | 字典表,五行:`battery_material` / `ewaste` / `packaging` / `consumable` / `spare_part`。**RUNTIME CONFIG** —— 加一种是加一行。规则列 `may_ever_be_processed` 在行上(certificate_types 的形状) |
| `materials.kind_code` | 外键指向字典 |
| `materials.may_be_processed` | 加工闸【唯一读的那一列】 |
| `materials_kind_stated` | `CHECK (两列都不为空) NOT VALID` —— 新行必拦,八行历史留空 |
| `guard_material_kind_processable` | 触发器:`may_be_processed` 不许越过它那一类的规则 |
| `guard_processing_input`(改) | **多一条具名拒绝** `MATERIAL_NOT_PROCESSABLE` |
| `materials.category` | **已退役**,连同 `CATEGORY_OPTIONS` 与七个 i18n 键 |

## grill 改了三处

**① D2 的 NOT NULL 与 D7 的"清库重建"【互相矛盾】,而 D7 那一半今天做不到。**
实测:八行物料(四在册 + 四软删)**每一行都被批次引用着** ——
`MAT-2026-0001` 9 进 10 出、`MAT-2026-0002` 8 进 7 出,连两行冒烟残骸也各挂着一张
在册批次(`ZZ-SMOKE-PROBE` 上那张是 AGENTS.md 点名"不要删"的 `IN-2026-0180`)。
删物料要先删批次,而删批次要一路串进总账。**所以清库在切换那天,不是本刀。**
而只要那八行还在,真 `NOT NULL` 就【必须】编造八个值 —— 正是 D7 自己禁止的事。
**处置:`CHECK ... NOT VALID`**,本表自己的先例(MAT-1 的 `waste_classification_code`)。

**② F4 要的那道闸在 brief 自己的排除清单里("Anything about runs")。**
但一个没有任何地方兑现的 `may_be_processed` 就是 D8 亲口说的缺陷 —— 库建好了、没有门。
**处置:加,但只加一条,而且加在 `guard_processing_input` 上** ——
那个守卫本来就是"什么可以当投料"的那道门。`commit_processing_run` 一个字没动。

**③ D6 说"移除 app/materials/options.ts",那会顺手杀掉 `CHEMISTRY_OPTIONS`(十个值,
G18 的地盘)与 `UNIT_OPTIONS`。处置:只移除 `CATEGORY_OPTIONS`,文件留着。**

## 一条对我自己先前说法的更正

> **PROC-0 的 N10 说「fixture 只 INSERT 不断言」—— 那句话【错了】。**
> `db/fixtures/53` 的 A 臂**读它**:
> `SELECT count(DISTINCT category || '|' || COALESCE(chemistry,''))`,
> 用来证明"受控分类与 category/chemistry 互不蕴含"。
> **一处漏掉的读者,足以让"没有人读它"这个结论从『量过的』降级成『大致上』。**
> 已改读 `kind_code`,**而那条断言因此更强了**:category 是自由文本,三行相同只说明
> 三次敲了同一个字符串;`kind_code` 有外键,三行相同是【同一个种类】。

## D7 的答案:**一行都没走,一行都没重建**

八行**全部留着**(删不掉),两列**全部留空**。
**没有人为任何一行决定过值,所以没有人的名字挂在任何一个值上** —— 这正是想要的:
留空的意思就是"没有人决定过",而那是真话。清库在切换那天。

## GAP LIST 更新

* ~~**G1** 物料的 kind + may_be_processed~~ **已完成(PROC-1)**
* ~~**G31** kind 用字典表而非 CHECK~~ **已完成(PROC-1)** —— 它把 **G2 从 EXPENSIVE 变成 CHEAP**
* **G2(电池料的状态轴)现在是 CHEAP** —— 状态可以挂成字典上的行。仍然 blocked on **U0**


---

# 第九部分 · PROC-2 落地(2026-08-22)—— U0 关闭,G2 完成

**五条轴,五张字典。只动主数据与收货记录;加工单、化验、计价没碰。**

## 五条轴,以及每一条【自己说】它决定什么

| 轴 | 在哪 | 决定什么 | 规则列 |
|---|---|---|---|
| **形态** `material_forms` | 物料 | **货进哪一条链** | `implies_dismantling` |
| **来源** `material_sources` | 物料 | 废物代码;**"要不要放电"成不成立** | `implies_never_charged` |
| **规格尺寸** `material_size_formats` | 物料 | 拆解工作量与搬运方式 | **无 —— 刻意的** |
| **安全状态** `inbound_safety_states`(多值) | 进料批 | 能不能投料 | `may_be_fed` |
| **化学体系确定度** `inbound_chemistry_certainties` | 进料批 | 能不能投料(认不认得出) | `may_be_fed` |

**规格尺寸没有规则列是刻意的:** 它描述工作量,而工作量今天没有任何一处被系统读去
做判断。**给它加一个没人读的规则列,就是"一个没人用的值教下一个人这一类在用"。**

## grill 的结论:**没有任何一条轴与另一条重复,但【三条是有条件适用的】**

而条件全部做成了【数据】,不是散文:

* **形态**只对 `material_kinds.has_condition_axes` 为真的种类成立;
* **规格尺寸**只在 `material_forms.implies_dismantling` 为真时成立 ——
  黑粉与极片废料没有拆解可言;
* **来源的 `implies_never_charged` 与安全状态的"带电未放电"会互相矛盾** ——
  厂内边角料从来没充过电,它不可能是"未放电"。那一列把话说死:
  **"充没充电"这个问题对它【不成立】,而不是"答案是已放电"。**

**两个方向都拦:** 该填没填要拒,**不适用却填了也要拒** ——
只拦前者,那一列就会在不适用的行上长出值,而"空"再也不只有一种意思。

## 与既有东西的两处关系,都查过、都写下来了

**SOURCE 与 `suppliers.supplier_types` 【互相独立,不设外键】**,三条理由都是量过的:
① 那一列**今天没有字典**(text[],无 CHECK,无查找表 —— 它自己的文件抬头写着这句);
② 两者问的不是同一件事(一家贸易商可以卖厂内边角料);
③ **基数就不对** —— 实测 SUP-2026-0003 是 `[dismantler, recycler]`,多值,推不出单一来源。

**化学体系确定度与 `materials.chemistry` 【是两件事,不是一件事的两个名字】**(Tim 裁定):
`chemistry = 混合` 说的是**这一种物料就是混合料**(正当的主数据);
本轴的 `mixed` 说的是**这一批货结果是混的**。两者可同时成立、也可各自独立成立。
**所以【不要】把「混合」从化学体系字典里禁掉** —— 那会变成"不许把混合料登记成一种物料"。
分工写在两边,**一字不差**。

> **【G18 的一条发现,本刀不动】`不适用` 是个不同性质的问题。**
> 它是一个化学体系取值,意思却是"化学体系不适用" —— 而那正是
> `material_kinds` 已经回答的问题(包装与备件没有化学体系)。
> **它是一个塞进取值清单里的"不适用",因为当时没有别的地方放** ——
> 第四份命名权威的微缩版。留给 G18。

## 值:哪些发了,哪些是 UNKNOWN

* **规格尺寸发五个,`medical_aerospace` 被【考虑过并且划掉】**(Tim)——
  它断言的是这个厂今天不处理的一类货。**返回条件:第一次真的收到医疗或航空电池。**
* **安全状态发五个,热失控历史【不在里面,而那是一个决定】** ——
  **记在 `inbound_safety_states` 的表注上**,连同"看到少了一个就补会推翻一个已经
  做过的决定"这句话。fixture 113 的 F1 用行数正面钉住它。
* **化学体系确定度三个,而三者的界写在字典行上**:`mixed` = **我们知道它是混的**,
  一个确立的事实;分不出来的那一种叫 `unknown_pending`。
  **这与"测出来是零"对"从来没测过"是同一个形状。**

## 本刀【不建】的那道闸,以及它的排期

`may_be_fed`(安全状态与化学体系确定度各一)**只记事实,自己不拦任何人**。
读它的是 **PROC-3** —— 那道前置条件加在 `commit_processing_run` 上,
它已经有三条同形的生命周期前置(`WO_NOT_RELEASED` / `EQUIPMENT_NOT_ACQUIRED` /
`EQUIPMENT_DISPOSED`)。**fixture 113 的 F6 正面钉住"今天还没有闸"** ——
它会在 PROC-3 落地那天变红,而那正是它该红的时候(那一臂末尾写了怎么把它翻过来)。

## 屏幕不在本刀 —— **PROC-2b**

五条轴今天【库里齐了,界面上一个都填不了】。这与 PROC-1 不同:
那一刀 DROP 了一列,编译器逼着表单同刀改;**本刀纯加法,什么都没坏,
所以表单是一次独立的取舍,而不是一件被迫的事。**
**PROC-2b owns:** 物料表单的三条轴、收货页的安全状态(多值,要真的 UI)与化学体系确定度。
**这句话写在这里,是为了让它的缺席读起来是排期而不是遗漏** ——
而这个仓库为"库建好了没有门"付过四次账,所以它值得被点名。

## GAP LIST 更新

* ~~**G2** 电池料的状态轴~~ **已完成(PROC-2)** —— 而 PROC-1 的 G31(字典而非 CHECK)
  正是它变便宜的原因:三条轴的适用条件都挂成了字典上的一列。
* **U0 关闭。**


---

# 第十部分 · PROC-2c 落地(2026-08-22)—— 门口那一半,以及一扇被关掉的窗

## 建了什么

**两件事,而它们是同一件事的两半:**

1. **门口就问。** 建批次的两条路(`create_inbound_batch` /
   `receive_inbound_batch_against_po`)各多了两个尾参
   (`p_safety_states text[]`、`p_chemistry_certainty text`),两张表单也各多了
   同一块控件。**建批次与记状态从此是【一笔】。**
2. **整组写变成原子的。** 新函数 `set_inbound_safety_states(uuid, text[])`
   在一个函数体里删+插,取代 PROC-2b 那两条经 PostgREST 分别发出的语句。

## 为什么第 2 件不是"顺手整洁一下"

PROC-2b 的写法是 `delete(整组) → insert(新的一组)`,而 PostgREST 一次一条语句,
两条之间没有共同事务。delete 成功、insert 失败,库里留下的是一个**空集**。
而 `inbound_batch_safety_states` 的表注写着:

> **一条安全状态都没有 ≠ 安全 —— 它的意思是"没有人记过"。**

也就是说那一刻存着的**不是"写了一半",是一句意思完全不同的真话**,
并且没有任何东西会说它是残骸。**PROC-2b 把这扇窗写在代码注释里报了出来,
没有藏**;本刀关掉它。

**fixture 114 的 F3 臂【把那个缺陷造出来】,而不是用散文断言它:**
两个独立的 plpgsql 子块复刻"一次一条语句"的行为,断言它确实留下空集;
然后同一组数据走 RPC、给一个坏码,断言**上一组原封不动还在**。

## 故障注入矩阵:五格,以及【两格注不了、两格隔离不了】

| 目标臂 | 注入 | 结果 |
|---|---|---|
| F1 | 到货日的拒绝挪到写入之后 | 红在 F1 ✓ |
| F2 | 门口给的状态被丢掉(只落表头) | 红在 F2 ✓ |
| F2 | 整组写去重(把"记了两次"变成"记了一次") | 红在 F2 ✓ |
| F4 | 适用性守卫被架空(一律放行) | 红在 F4 ✓ |
| F4 | **守卫只在 UPDATE 上有效**(BEFORE INSERT 时按 `NEW.id` 回查批次) | 红在 F4 ✓ |

**去掉的那两格,理由写下来而不是补一格假的:**
* **"NULL 被当成空集"在建批次这条路上【不可观测】** —— 新批次本来就没有任何
  安全状态行,空集写进去与不写逐字同结果,DELETE 也无事可删。
* **F3 的原子性【注入不了】,而这个理由本身就是那条断言的证明** ——
  一个函数体对调用方永远是原子的,在里面加子块只改变错误传播。
  那个缺陷**住在应用代码里**,DB 层的矩阵够不到它;fixture 自己造洞的那一步比
  一次注入更强。

**隔离不了的那两格("适用就必填"、"种类不知道也拒"):它们会让【每一次建批次】
当场抛错**,于是先毒死 F1 而不是红在 F4 —— 那不是断言写错了,是这两种改动的
爆炸半径本来就覆盖全局。

## 矩阵抓出了一个【真的】bug,而它此前从未被任何东西看见

`guard_inbound_condition_applicable` 第一版在 `inbound_batches` 那一支写的是
`SELECT … FROM inbound_batches ib WHERE ib.id = NEW.id` —— **这是一个 BEFORE INSERT
触发器,那一行还不在表里**。于是 `NOT FOUND` 命中"查不到就放行"那一支:

> **守卫在【建批次】这条路上从来没有生效过,只在 UPDATE 上有效。**

它是被"那一格本该红在 F4、结果整块中止"逼出来的。fixture 因此补了一臂,
**正面钉住建批次那条路**——只测 UPDATE 那一头,这个洞会一直活着。

## D3 的答复:**不加 `not_checked` 这个取值**

**理由是这个仓库已经答过三次的同一件事**(`no_reference` / `price_index IS NULL` /
`waste_classification_code IS NULL`):**"没有人记过"是【一行都没有】,不是
【一行写着"没检查"】。** 把它做成一个取值,就等于让"没人看过"与"看过、
结论是没什么可记的"共用同一条记录 —— 而那两件事在 PROC-3 那道闸面前是相反的。

**同时记下这条答复【没有】覆盖的那一半(镜像发现,写进函数注释里):**

> **"检查过了,一条都不适用"与"没有人看过"今天在库里【分不开】** ——
> 两者都是零行。`material_sources.implies_never_charged` 是 PROC-3 手上唯一的
> 局部工具(它能替一部分料回答"这批货不可能带电")。
> **返回条件:PROC-3 那道闸如果需要区分这两者,它就是一张"谁检查过、什么时候"
> 的表,不是这本字典上的一个新取值。**

## 与 PROC-2b 那块屏幕的关系:**一份控件,两个宿主**

`IntakeConditionFields` 是唯一一份实现,`IntakeConditionPanel`(批次页,受控 +
保存按钮 + RPC)与 `IntakeConditionFormSection`(建批次两条路,带 `name`、跟着
整张表单提交)各自套着它。**抄一份是最快的写法,而屏幕上那两句话
——「一条都没勾 ≠ 安全」与「已知混合 ≠ 分不出来」——正是本刀真正在防的东西,
只要有一份没跟上,那块屏幕就在说另一件事。**

## 一条本刀记下来的规矩

> **一条新守卫,会让【别处】一个本来诚实的控件变成一个骗人的控件。**

PROC-2b 无条件摆出那一块是对的 —— 那时 `guard_inbound_condition_applicable`
还不存在,页面与服务端一致。本刀加了守卫,同一块界面立刻变成"一个服务端保证
会拒的控件"(AGENTS.md 明令禁的那件事)。**加守卫时要回头看一眼谁在摆这个动作。**

页面那一侧的判据与库那一侧**逐字同源**,包括那条最容易搞反的:
**查不到种类 = 没有人记过 ≠ 不适用** —— 库放行,页面也放行。

## 本刀【仍然不建】那道闸

`may_be_fed` 照旧只记事实。**fixture 113 的 F6 一个字没动**,它仍然正面钉着
"今天还没有闸",仍然会在 PROC-3 落地那天变红。

## GAP LIST 更新

* ~~**G32** 门口记不下已经知道的事~~ **已关闭(PROC-2c)** —— 这是 PROC-3 的前提:
  一道"没记过安全状态就不许投料"的闸,如果记它的唯一地方是事后才点得到的编辑页,
  那道闸就是不可过日子的。
* **新增 U13(返回条件已写明):** "检查过、无可记"与"没人看过"分不开 —— 见上。

> **【编号更正,PROC-3 读文档时发现】** 上面两条初写时占了 **G3** 与 **U1**,
> 而这两个编号【早就有主】:G3 是「水分与基准」(blocked on U2/U3,见第四部分),
> U1 是「工艺药剂算不算一类物料」(见第三部分)。改成 G32 / U13。
> **一张同一个编号指两件事的 GAP LIST,比没有编号更坏** —— 引用它的人会读到
> 另一件事,而两边都看起来是对的。记在这里而不是默默改掉:下一个人 grep
> 「G3」时会同时命中原主和这条更正,那正是他需要看见的。

---

# 第十一部分 · PROC-3 落地(2026-08-22)—— 什么东西可以投料

## 建了什么

**一条守卫,三条拒绝。** `guard_processing_input` 多了一段:进料批次投进加工之前,
读 PROC-2 记下的两条轴。它今天的拒绝顺序是

| | 码 | 问的是 |
|---|---|---|
| ① | `PROCESSING_INPUT_DIRECT_INSERT` | 你是不是绕过了 commit |
| ② | `PROCESSING_INPUT_SELF_CONSUME` | 这张单是不是在吃自己的产出 |
| ③ | `MATERIAL_NOT_PROCESSABLE`(PROC-1) | 这一**种**料许不许投 |
| ④ | `INPUT_SAFETY_STATE_NOT_RECORDED`(本刀) | 这一**批**有没有人看过 |
| ⑤ | `INPUT_SAFETY_STATE_NOT_FEEDABLE`(本刀) | 看过了,是坏的吗 |
| ⑥ | `INPUT_CHEMISTRY_NOT_FEEDABLE`(本刀) | 成分记成了一个不许投的值吗 |

③ 在 ④ 之前是**刻意的**:种类答不上来就不必问批次。fixture 115 的 F1 用一批
【两个理由都成立】的货正面钉住这个顺序。

## R2 —— 这道闸今天在线上拒掉几批:**0 批,而这句话需要下一句才完整**

线上 15 批在册货,**15 批【已经】被 ③ 拒着**,轮不到 ④⑤⑥。原因是 PROC-1 的
D7 决定不回填:线上 8 行物料的 `kind_code` 与 `may_be_processed` **全是空**,
而空的意思是"没有人决定过",守卫按 `IS NOT TRUE` 拦。

所以**操作上的事实是:线上的加工从 2026-08-21(PROC-1 落地)起就已经全关着了,
不是本刀关的。** 最近一张加工单是 2026-08-16,PROC-1 之后一张都没有。
**要开工,要做的事两刀是同一件**:有人得去说出每一种物料是什么种类、许不许投料。
做完那一步之后,本刀这三条才会第一次真的说话。

## R1 —— 摘掉一条旗是【无痕迹的编辑】,而合取让它变重了

合取意味着一批进过水的货**在那条旗摘掉之前永远投不进去**。而实测:那张表记着
谁把旗**挂上去**,**没有任何东西记谁把它摘下来、为什么摘**。
**一道安全闸,若它的解锁是一次查不到的编辑,那它比看起来弱。**
全文与处置方向记在 `docs/known-issues.md`(PROC-3 留下的三条 · 第 1 条)。

## 四个设计问题,Tim 都定了

* **D2 合取** —— 每一条安全状态都必须可投料。**一批已放电的货如果同时也进过水,
  那它就是进过水的:放电不能把水抵消掉。** 而且消息里**把所有坏的那几条一次点完**
  (`string_agg` 按 sort_order),否则人清掉一条再撞上下一条 —— 第二趟本来在第一趟
  就能知道。
* **D3 不对称,而理由写在守卫上** —— 安全状态**缺席要拒**,确定度**缺席放行**。
  安全状态防**起火**,一批没人看过的货与一批不安全的货后果相同;确定度防**算错**,
  而那个数字由后面的化验回答,不靠停线回答。**这句话必须写在现场**:下一个人看见
  不一致会想抹平,而抹平的方向选错,停的是产线(线上 23 批货一条确定度都没有,
  「待识别」本身又 `may_be_fed = false`)。
* **D4 停用 ≠ 撤规则** —— 守卫**只读 `may_be_fed`,不读 `is_active`**。
  两个动词写在两本字典自己的列注释上:`may_be_fed` 撤【规则】、留【事实】;
  `is_active` 停【选单】。谁也替不了谁 —— 说清楚这一点,才不会有人拿 `is_active`
  去撤一条规则,而那会是一条无痕迹、且一次性对所有批次生效的释放路径。
* **D5 适用性,而它不是假想的** —— 一个**可加工、但没有状态轴**的种类,
  绝不能因为"它没有安全状态"被拦住。**实测线上 `ewaste` 今天就是这一种**
  (`may_ever_be_processed = t`, `has_condition_axes = f`)——
  所以"可加工蕴含有状态轴"连**今天**都不成立,更不是一条定律。

## 一条【今天走不到】的分支,写下来而不是假装测过

D5 还有一半:**种类查不到 ≠ 不适用,两边都放行**。守卫里那一支在,
但它**今天到不了** —— `materials_kind_stated` 要求 `kind_code` 与
`may_be_processed` 同时非空,所以"没有种类"的行必然也"没表过态",
而没表过态的行在 ③ 就被拦下了。线上实测:种类为空的 8 行,
**种类为空却表了态的 0 行**。

**它没有 fixture 臂,因为造不出一个到得了它、又不先撞上 ③ 的场景 ——
而这句话本身就是那条断言:两道守卫的顺序保证了它到不了。**
留着那一支,是因为两个条件各自独立,而那条 CHECK 是 `NOT VALID`(只管新行)。

## fixture:113 的 F6 被【翻过来】,不是删掉

PROC-2 写 F6 时闸还不存在,它断言的是一个"还没建",并在文件里写明
**PROC-3 那天要把它翻过来、不要删掉**。照做了:现在它断言那一炉被按名拒,
并且**检查消息里有没有那一条状态的名字**。文件抬头从"这一条还没有闸"改写成
"这道闸保证了什么",同时留着它当初为什么这么写。
**一条被删掉的臂,与一条从来没写过的臂,长得一模一样。**

新的 fixture **115**,五臂:F1 前提+拒绝顺序 · F2 三条拒绝各一臂(含"确定度缺席
必须放行") · F3 合取两个方向 · F4 停用仍拒 + 改 `may_be_fed` 才解锁(而事实还在)
· F5 没有状态轴的可加工种类照样投得进。

**故障注入 10 格,10 格全部红在【预期的那一臂】上**,`injection_probe` 全程盯着
还原完整性。**其中 3 格第一轮红对了地方却"什么都没说"** —— 正方向的提交没有被
`BEGIN…EXCEPTION` 包住,守卫一拒就打断整块 DO,屏幕上只剩机器码。那是 PROC-2
栽过的同一处,矩阵又把它抓了出来;fixture 里 5 处正方向提交因此都改成按名报错。

**113 的 F6 没有属于它自己的注入格,而理由是它【本来就】与 115 的 F2b 测同一条机制**
(F6 就是照着"闸现在保证什么"翻过来的)。杀掉一个的注入必然杀掉另一个,
报出来的是先跑的那一个。F6 的价值不在于能被单独注入,在于它留下了
"这道闸不存在时,这里断言过什么"。

## 顺手修的一条,以及它的形状

`MATERIAL_NOT_PROCESSABLE` 是**同一个守卫**抛的,句子写在 `materials.errors.*`,
而 `localizeProcessingError` **从来没问过物料那一支** ——
于是**从 PROC-1 落地那天起,加工屏幕上那条拒绝一直是一串机器码**。实测确认。
补法是一条链(认不出时原样返回,所以安全),不是把码抄第二份:
抄一份就是第二处要跟着字典长的清单。

## 21 支 fixture 的连带改动 —— 那不是噪音,是这道闸的价格

线上真实数据一批都拒不到(见 R2),而 **fixture 那一侧 22 支全红** ——
因为它们造的都是电池料,而电池料吃状态轴。每一支因此在投料前补上一条
【可投料】的安全状态,条件由 `material_kinds` 现读(不写死批次),
且**每一次投料之前都补一次** —— 批次是各臂边跑边造的,开头补一次覆盖不到
后面才出生的那些。

**这件事值得写下来:一道闸的真实成本,不在它拦住了多少条线上数据,
而在有多少既有的路要为它重新说明自己。**

## GAP LIST 更新

* ~~**G2/G32 之后那一步:把记下来的事实变成一道闸**~~ **已完成(PROC-3)** ——
  PROC-1 记种类、PROC-2 记状态、PROC-2c 让门口就能记,本刀读它们。
* **新增 U14:** 摘掉一条安全状态没有任何痕迹(R1)。**返回条件:** 下一刀 ——
  要么给摘旗补一行 append-only 的记录,要么把"放电并核验"做成一个动作。
* **U13(PROC-2c 提的)不动**:"检查过、无可记"与"没人看过"仍然分不开。
  本刀**没有**碰它 —— 它今天的表现就是 `INPUT_SAFETY_STATE_NOT_RECORDED`,
  而那条拒绝对两者说的是同一句话。

---

# 第十二部分 · PROC-4 落地(2026-08-22)—— 那份金属清单变成一张字典

## S1 的第一个发现:**是八张表,不是四张**

切次简报说四张。线上目录逐条查过,是 **八**:
`assay_result_metals` · `inbound_batch_metals` · `output_batch_metals` ·
`metal_prices` · `pricing_formula_metals` · `pricing_term_commitment_metals` ·
`material_required_metals` · `pricing_formula_history`。

**七张的定义一字不差;第八张(history)多一个 `metal IS NULL OR`,而那是刻意的差别
不是漂移** —— 表头级的改动没有金属可言。外键对 NULL 天然放行,语义原样保留。

**这个差额本身就是这一刀的论据。** 少数出来的那四张会安安静静地留着 CHECK,
于是"加一行字典"之后,一半的表认新值、另一半照旧拒 —— 而没有任何东西会说这件事。
fixture 116 的 F2 因此**逐张**测八张,不抽查:抽查一张证明不了其余七张。

## S2:app 里【也】有一份 —— 第五类命名权,24 个文件在用

`app/metal-prices/options.ts` 的 `METAL_OPTIONS` 同时定着三件事:
**哪些值合法、它们怎么排、它们叫什么**。这正是 F7 那个形状,而且它每一次都成立。

**简报说"如果 STEP 1 发现 app 自己拿着一份清单,那就改变了答案"——它拿着,所以改了。**
本刀因此不只换库里的约束:24 个文件从"import 一份写死的数组"改成
"页面读字典、按 props 传进去"。**否则加一行字典也不会出现在任何一个下拉里** ——
那样它就不是字典,只是一条换了写法的 CHECK。

## S3:顺序有【两个互相矛盾的出处】,而这是实测不是推测

app 侧数组序 `ni, co, li, mn, cu, al, fe`(重要的排前面);
库侧视图与函数里一律 `ORDER BY metal`,那是**字母序**。
**同一批金属,下拉里镍第一,报表里铝第一。**

D4 把顺序变成字典上的一列。**下拉与选单改了;那 8 处 `ORDER BY metal` 的
报表与函数没动** —— 改它们会改既有报表与函数 JSON 的顺序,是一次看得见的行为
改动,不该混进这一刀。排期记在 `docs/known-issues.md`。

## S4 的预测,以及它成立了

24 支 fixture、**215 处**写着金属码。**预测:0 处需要改** ——
码一个字没变,CHECK 换成指向同一批码的外键,所以每一处照旧合法。
**实测:114/114 通过,一处没改。**

**这与 PROC-3 正好相反,而对比值得记下来:** PROC-3 收窄行为,22 支 fixture 全红;
PROC-4 只换清单的**出处**,零支需要动。
**一道闸的代价在"多少既有的路要重新说明自己";一次换出处的代价在"有多少地方
藏着副本"。** 两者都不是"改了多少行 SQL"。

## S5:没有任何【代码】认定就是七个

只有几句散文注释提到"7 个金属"(`metal_prices.sql` 的一条列注,以及几支迁移的
注释 —— 迁移只是变更日志,不参与重放)。**没有计数断言、没有七列的视图。**
所以清单变长不会撞上任何硬编码的数字。

## D3:这张字典叫 **`substances`**,不叫 `metals`

**排队要进来的东西里,氟、氯、石墨、塑料没有一个是金属。**
氟氯是惩罚元素,石墨是碳的一种形态,塑料连元素都不是。
一张叫 `metals` 的表在收下石墨那天就开始说假话,而那时它已经被八张表引用着。

本表的行是:**"一种物质,我们在物料里测它的含量、并在商务上核算它"** ——
这一句同时容得下今天的七个可付款金属、将来的惩罚元素、以及可回收的非金属流。

**名不副实的那一半也说清楚:指向它的八个列仍然叫 `metal`。**
改名实测要动 **623 处**(其中 15 处是函数吐出去的 JSON 键,改了就是改 API 形状),
而它**不挡路** —— 一个叫 `metal` 的列照样存得下 `graphite`。排期与代价都记在
`docs/known-issues.md`,不含糊过去。

## D2:只发七个,而排队的四个各自带着返回条件

* **氟 / 氯** —— 惩罚元素,今天**连记都记不下来**。返回条件:第一份写明惩罚结构的条款(U11)。
* **石墨** —— 返回条件:第一次真的回收出一条石墨流。
* **塑料** —— 返回条件:流程图定稿,确认它是产品流而不是处置流(U6)。

现在就加,等于宣称我们能记录、能定价它们,而今天两样都不能 ——
一个没人用得上的字典行会教下一个读它的人"这一类在用"(`material_kinds` 不加
`reagent` 是同一条)。

## 故障注入:7 格,7 格全部红在预期臂上 —— 但第一轮是 **0/7**

**第一轮七格全红在同一处,而且"还原后"也是红的。** 追下去是我自己的还原漏了:
第一格注入 `DELETE FROM substances WHERE code='fe'`,而 `restore()` 只装回了
八条外键,**没有把那一行装回来**。

**值得单独记下来的是:`injection_probe` 抓不到这一漏。**
探针比的是**对象定义**(表结构、约束、索引、RLS、函数体)——
而我改的是**数据**,定义一个字节都没变。于是它一路报"干净"。

> **一张 RUNTIME CONFIG 字典的【行】就是它的行为。**
> 注入矩阵还原一张字典时,必须连行一起还原;而这一点探针今天守不住。

修好之后 3/7 → 补了三处(F1/F3/F4 的正方向写入没有被 `BEGIN…EXCEPTION` 包住,
该红的臂**什么都没说**;PROC-3 栽过同一处)→ 6/7 →
最后一格 F4 的注入**不隔离**(它连未知码也拦,于是先撞 F2),
改成只拦"已存在但停用"的码 → **7/7**。

**还有一处是 fixture 自己的毛病,而它靠运气绿过:** F5 的"顺序照 sort_order"
在把七个 `sort_order` 全设成 0 时**照样通过** —— 并列时 `ORDER BY` 的结果是
未定义的,而它恰好排成了期望的样子。补了一条"七个 sort_order 必须两两不同"的前置
断言之后,那一格才红。

## GAP LIST 更新

* ~~**G12/G20 的前提:金属清单是写死的**~~ **已解除(PROC-4)** ——
  惩罚元素、石墨、塑料从此各差一行数据,不差一支迁移。
* **新增 U15:** 列名 `metal` 与字典名 `substances` 名不副实。
  **返回条件:** 与那 8 处 `ORDER BY metal` 的报表顺序一起改,单独一刀(代价 623 处)。

---

# 第十三部分 · PROC-5 落地(2026-08-23)—— 化学体系与实验室变成字典

## S1:这两列身上【一条约束都没有】

不是"CHECK 写死了一份清单"(PROC-4 的情形),是**彻底的自由文本**:
`materials.chemistry` 与 `assay_results.lab_name` 都是 `text`、可空、无默认、**无 CHECK**。
唯一的"清单"住在 app 里,而且**自带一个逃生门**:`其他` 是一个 sentinel,
选中它会打开一个自由文本框。

## 而那个门已经被走过了 —— 这一半的理由在【现在】

线上两行真物料,其中 **MAT-2026-0002(在册,8 个批次在用)的 chemistry 是
「Special Chemistry Structure」** —— 不在 app 自己的清单里,读起来像占位符。
`materials.category` 长出四种命名法走的就是这条路,只是那次没有人在半路看见。

## S5:实验室相反 —— 理由在【将来】,照直说

**线上只有一个非空 `lab_name`(FRL,1 行;3 行为空),没有任何重复拼法。**
所以这一半不是在治一个已经发作的病,是在**关门**:同一间实验室的两种拼法一旦
长出来,合并它们就是一个"这两个是不是同一家"的判断,而那个判断**没有人能事后
替当时的人做**。

## S2 / S3 / S4

* **S2**:化学体系有 **4 个** app 侧消费文件(清单 + 两张表单 + 列表页);
  **实验室 0 个** —— 它从来没有清单,只有两个自由文本框。
* **S3**:化学体系顺序**只有一个**出处(app 数组序),库里没有任何按它排序的地方
  —— 与 PROC-4 那种"两个互相矛盾的顺序"**不同**,这里只是把它从代码搬进数据。
* **S4 预测**:化学体系 **0 处**要改(fixture 只用 `NMC`,而它被发进字典);
  实验室 **2 支 6 处**(fixture 21 的 `Fixture Lab`、fixture 54 的 `fixture 54 lab`,
  都不会在只发 `FRL` 的字典里)。
  **实测:113 通过 / 2 失败,正是那两支,一处不多一处不少。** 改法是让它们
  **自带那一行字典** —— 而"加一行就能用"正是把它做成字典换来的东西。

## S6:`不适用` —— 退役

PROC-2 把它记成 G18:一个"不适用"被塞进取值列表,只因为当时没有别的地方放。
**现在有了**:`material_kinds.has_condition_axes` 回答"这一类物料有没有化学体系
可言"。留着它等于让同一个问题有两个答案,而两个答案会不一致。
**线上零行在用**,所以退役不花任何代价。留空的意思仍然是"没有人记过"。
`其他` 一并去掉(Tim 裁定)—— 它就是那个逃生门本身。

## 一次被实测推翻的判断,以及它换来的裁定

本刀原打算用 **NOT VALID 外键**把那个占位串原样留着,理由是
"外键只在外键列变动时才校验,所以那一行的别的字段照样改得动"。

**那句话是错的,而它是被测出来错的:**
```
SELECT tgattr FROM pg_trigger WHERE tgisinternal   →   空
```
RI 触发器**不带列清单**,于是带着字典外值的那一行**任何字段都改不动**。
(第一次的玩具实验给出过相反结果 —— **那一次是错的**;后来三次测量,
包括两次在真表 `materials` 上,一致。**一个与后续三次相悖的孤例不该被采信。**)

于是把这件事连同代价重新交给 Tim,裁定改成:**把那一行置为 NULL,外键正常生效。**
而这不是丢信息 —— NULL 在这一列上早有定义:**「没有人记过」**,那正是事实。
原值写在迁移文件里,复原只要一行 UPDATE,但复原之前得先有人说出它是什么。

## 迁移撞出来的另一件事:**线上八行物料现在一行都改不动**

那条 `UPDATE ... SET chemistry = NULL` 第一次跑就被 **`materials_kind_stated`** 拒了。
那是 PROC-1 的 `CHECK (kind_code IS NOT NULL AND may_be_processed IS NOT NULL) NOT VALID`,
而 **NOT VALID 的 CHECK 在【任何一次 UPDATE】上都重算整行**。PROC-1 刻意没有回填种类,
所以:

> **线上 8 行物料,8 行违反那条 CHECK,现在一行都改不动。实测 8/8。**
> 自 2026-08-21 起如此,而 **gate 与冒烟都看不见它** —— 那两样都不【改】既有业务行。

**这与 PROC-3 报告里"线上加工从 2026-08-21 起就全关着"是同一个根因。**
两件事一起解决:有人把八行物料的种类填了,加工的闸与物料的编辑同时活过来。
本刀不修它(那要有人做出 PROC-1 的 D7 拒绝替人做的判断),只是把那条 CHECK
卸下、改完、**原样 NOT VALID 装回去** —— 前后逐字相同,线上核对 `convalidated = f`。

## 故障注入:10 格,**10/10 一次到位**

前两刀的两条教训都带过来了,所以这次没有重犯:
* **还原连【行】一起还原**(PROC-4 第一轮 0/7 就栽在这里 —— `injection_probe`
  比的是对象定义,看不见一行被删掉的字典行);
* **正方向的写入全部包进 `BEGIN…EXCEPTION`**,该红的臂自己说得出抓到了什么;
* **F5 先断言 `sort_order` 两两不同**(PROC-4 有一臂在全并列时靠运气绿过)。

## 一处刻意的不一致,写下来免得被当成漏掉

**化学体系与实验室的【名字】在字典行上(`name_en`/`name_zh`);
而 PROC-4 的金属,名字仍在 i18n 里。**
不是随手选的,是按"要动多少处"决定的:金属有 **11 处**嵌在客户端组件深处的显示点,
把 label 一路传下去风险大于收益,所以那边用"check-i18n 现读字典"把 i18n 变成
一份**被检查的镜像**;而这里化学体系只有 3 处、实验室 2 处,全都喂得动 ——
于是名字回到字典自己身上,**加一种连翻译都不用配**。
理由写在 `app/components/dictionaries/dictionaryQuery.ts` 的抬头。

## GAP LIST 更新

* ~~**F7 点名的两处自由文本分类**~~ **已关闭(PROC-5)** ——
  `materials.category`(PROC-1)、金属清单(PROC-4)、化学体系与实验室(本刀)。
* **新增 U16:** 线上八行物料因 PROC-1 的 NOT VALID CHECK 而全部不可编辑。
  **返回条件:有人说出每一种物料的种类与可投料与否** —— 与 PROC-3 那道闸同一把钥匙。
* **U15(PROC-4 的列改名)扩容**:`assay_results.lab_name` 现在也存 code,
  与 `metal` → `substance_code` 一起改。

---

# 第十四部分 · PROC-6 落地(2026-08-23)—— 什么基准,谁的结果

## S1 先答,因为它决定了本刀能用什么机制

PROC-5 实测:**一条 NOT VALID 约束在【任何一次 UPDATE】上都重算整行**,
八行物料因此被冻住。所以本刀开工第一件事是问同一个问题:
**assay_results 的行写完之后会不会被 UPDATE?**

**会,而且是例行操作。** 逐条查出来的证据:

| 函数 | 语句 | 改的是谁 |
|---|---|---|
| `apply_assay_result` | `UPDATE assay_results SET superseded_by = … WHERE id = v_prior` | **上一份历史化验** |
| `apply_output_assay` | 同形 | 同上 |
| `unapply_assay_result` | `UPDATE … WHERE superseded_by = p_assay_result_id` | 同样是历史行 |

**只要有人对同一批货再化验一次,历史行就会被改。**

> **结论:NOT VALID 在这里是个陷阱。** 一条 NOT VALID 的 CHECK 会让【第一次复检】
> 把所有旧化验单冻死。基准的必填因此由**一条 BEFORE INSERT 触发器**执行 ——
> 它只看新行,对 UPDATE 一个字都不说。

**fixture 118 的 F2 正面钉住这条边界**:造一行没有基准的历史化验单,然后
`UPDATE … SET superseded_by = …`(apply 走的就是这一句),断言它**必须改得动**。
注入矩阵里也有一格专门把触发器换成 `BEFORE INSERT OR UPDATE` —— 它红在 F2。

## D3 的不对称,写出来免得被当成两套标准

| 列 | 机制 | 为什么 |
|---|---|---|
| `weight_basis` | 触发器,只管 INSERT;历史行留空 | **历史化验单按什么基准报的,没有人知道**,也无从考证。编一个值就是伪造一个可以拿去算钱的事实。 |
| `result_party` | 回填 `'ours'` → `NOT NULL` | **历史化验单全部是我们出的** —— 对手方与仲裁那两条路从来没存在过(零入口、零记录)。写 'ours' 是陈述一个已知事实。 |

**两件事看起来都是"给旧行填值",本质相反:一个是我们不知道,一个是我们知道。**
对照 PROC-1 的 `kind_code` 留空 —— 那是"从来没有人决定过",回填它就是替人做判断。

## D1:两个取值,而且是 CHECK 不是字典 —— 与前四刀【故意】不同

值:`as_received`(收到时/湿基)与 `dry`(烘干后/干基)。

**第三种不发。** 实验室行当里有 air-dried 一类口径,但**本仓库里没有任何证据说明
我们这一行在用它**(线上零条基准数据,合同也还没有)。返回条件:第一份写明按风干基
结算的合同。

**为什么是 CHECK 而不是字典** —— 前四刀把好几处清单做成了字典,而那些清单的共同点是
**要么自由文本(F7 的病),要么同一份清单复制在好几张表上**。这一条两样都不是:
封闭小集合,只在**一张表的一列**上。而最要紧的一条:

> **加一种重量基准【不该】便宜。** 加一种金属是加一行,因为那只是"我们又开始测一种
> 元素";而加一种重量基准是**改变我们报数字的口径**,它应当值一支迁移和一次对话。
> 做成字典等于把一个业务口径的变更降格成一次随手的数据录入。

## D2:水分可空,而空【永远不是零】

**一个乘数的单位元是看不见的**:把没测过的水分读成 0,`1 − 0` 会算出一个
**看起来完全合理的数**,没有任何东西会报错。这个仓库为一模一样的形状付过账 ——
金属含量:线上没有一行是真的零。

fixture 118 的 F3 因此断言的是**两者可辨**(一份测了得 0、一份没测,必须落进不同的桶),
而不是"某一列是 NULL" —— 后者一个把 NULL 读成 0 的实现也能过。
并且钉住:**没有水分就推不出干重,那时正确的行为是说"推不出来"。**

## S2–S5

* **S2**:`assay_results` 的写入口**只有一个**(`record_assay_result`);
  UPDATE 路径三条,全部是函数,人碰不到。
* **S3**:`content_pct` 流出到 **11 个函数/视图 + 23 个 app 文件**。列出来了,没动 ——
  每一处都是"基准不一致会变成一个错数字"的地方,而本刀只负责让基准**被记下来**。
* **S4**:**仓库对重量基准是真的沉默的。** 所有 "basis" 命中都是别的意思
  (`price_basis` / `allocation_basis` / 工资口径)。查了注释与文档,不只是 grep。
* **S5 预测**:**11 支 fixture、41 处**(4 支走函数 18 处,7 支直插 23 处)。
  **实测:11 支,一支不多一支不少,而且 4/7 的分法也对上了。**

## 撞出来的两件旧账(都不是本刀造成的)

### 一 · PROC-4 的"零残留"只对了一半

**函数体里还有四份写死的金属清单**,而 PROC-4 的 S1 只查了 `pg_constraint`。
本刀正在重建 `record_assay_result`,顺手把它改成读字典;**另外三支按名排期**
(`calculate_metal_price_from_terms` / `set_material_required_metals` /
`upsert_metal_prices`)。后果是具体的:往 `substances` 加一行之后,
外键放行而那三支函数会按 `METAL_INVALID` 拒掉。

**方法上的教训比那三支函数值钱:一份清单可能藏在四处 —— 约束、函数体、视图、app。
而且要在【线上目录】里查,不是在仓库里 grep(迁移文件全是历史,会把答案淹掉)。**

### 二 · fixture 54 的实验室行落在事务外面(PROC-5 的缺陷)

它**提交进了重建库**,泄漏给后面每一支 fixture。PROC-5 的 gate 没抓到,
因为每支只跑一次;PROC-6 连跑两次才让它现形。
**判据:一支 fixture 应当【连跑两次都绿】。**

## 故障注入:8 格,而第一轮是 5/8

三格错落到 F2,且"还原后"是红的 —— **`restore()` 没有还原【触发器】**。
探针比的是列/约束/索引/RLS/策略,**不含触发器**,所以 `assert_clean` 一路说干净。
**这与 PROC-4 那次"还原漏了字典行"是同一种病的另一个器官:**

> **探针守的是它捕获的那几类东西;它守不住的,还原脚本必须自己守。**
> 已经付过两次账:**行** 与 **触发器**。

补上之后 **8/8**。

## GAP LIST 更新

* ~~**U2/U3 的记录侧**~~ **已关闭(PROC-6)** —— 基准现在被记下来了。
  **注意这只关了"记录",没关"结算按哪个基准"** —— 后者是合同条款,仍是 U2/U3 的另一半。
* ~~**G16 化验的 party 轴**~~ **已完成(PROC-6)** —— 而 **U12(谁的结果算数)不动**:
  本刀只记录【是谁出的】,不裁决【谁说了算】,那是逐笔合同的条款。

  > ### 【编号更正,DOCS-RECONCILE-B,2026-08-23】这一行原本划掉的是【错的那个号】
  >
  > **本行此前逐字写的是:**
  >
  > > `* ~~**G22 化验的党派轴**~~ **已完成(PROC-6)**`
  >
  > **那句话有两处不对,而第二处是一个真的缺陷:**
  > ① 名字写成了「党派轴」,而本文件其余各处叫它「party 轴」;
  > ② **号码错了。** party 轴是 **G16**(定义在本文件 GAP LIST:
  > 「**G16** 化验的 party 轴(我方 / 对方 / 仲裁)」)。
  > **G22 是【拒收权(规格上限)】,而那件事【至今没有建】。**
  >
  > **所以这份文件此前把一个【没建的缺口】标成了【已完成】。**
  > 那不是一处笔误的代价:**一条被划掉的缺口,在后来每一个读者眼里都是一个已经
  > 建好的控制。** 而 G22 挡的是「收货只拦供应商证书,与货的质量无关」——
  > 一道**根本不存在**的质量拒收闸,被这份文件宣布为存在。
  > 这与本仓库反复点名的那一族是同一种病(`?? []` 把失败读成空集、
  > `! pgrep` 把"进程没了"读成"备份好了"):**一个声称覆盖了某块地面、
  > 实际没有的判词,比没有那个判词更坏,因为它制造信心。**
  >
  > **改法是最小的,而且【没有任何重新编号】:**
  > 把「已完成」的标记挪到它本来就该在的 **G16** 上;**G22 原样留在开着的
  > GAP LIST 里**(本文件上方那一行 `| **G22** | **拒收权(规格上限)** | …`
  > 从来没有被划掉,所以那一侧不需要动)。
  >
  > **记下这一条而不是悄悄改掉标记**,理由与本文件其余每一条更正相同:
  > **一份错过一次的文档应当自己说出来,否则下一个读者没有任何理由去核对它。**
* **新增 U17:** 三支函数仍写死七金属清单,PROC-4 的承诺在那三条路上不成立。
* **新增 U18:** `injection_probe` 的表捕获不含触发器。

---

# 第十五部分 · PROC-CLEANUP(2026-08-23)—— 让这一批的说法变成真的

三件"读起来像做完了、其实没有"的事。**没有新功能。**

## A · 那张物质字典此前只活了一半

**PROC-4 报的"残留的写死清单 0"只对【约束】成立** —— 它的 S1 只查了 `pg_constraint`。
本刀在**线上目录**里把四类对象一起查,并且**先剥掉 SQL 注释再匹配**:
否则 `record_assay_result` 会是假阳性,因为 PROC-6 在它的注释里【引用了】那份旧清单。
**这一点值得单独记:一个按文本找副本的检测器,分不出代码与注释 ——
而写下"我修好了它"的注释,恰恰会让那个对象看起来仍然有罪。**

三支函数,同一个形状,后果具体:往 `substances` 加一行之后
**外键放行,而它们按 `METAL_INVALID` 拒掉**。现在三支都读字典。

**证明是 fixture 119,不是 diff。** 这两者的区别是本刀最值得留下的一句:

> **"我把三处都改了"是一句关于【我做过什么】的话;
> "加一行之后每一条路都走得通"是一句关于【系统是什么样】的话。
> 前者会漏掉第四处,后者不会 —— 因为它不问副本在哪儿,它问结果。**

119 走六条路(行情 / 物料必测项 / 化验 / 计价 / 三张 metal 子表直插),
并有反面一臂(字典外的码每条路都要拒)—— 少了它,"把校验全删了"也能全绿。
**逐支把函数退回写死,119 逐支报红。**

## B · 探针的盲区,以及一条比补盲区更重要的话

它在 PROC-4 漏了【行】、在 PROC-6 漏了【触发器】,**两次都是 `assert_clean` 说干净
而库是脏的**。

> **一个对自己没走过的地面报"干净"的检查,比没有检查更坏:它制造信心。**
> 这是本仓库最常复发的那个形状(`?? []` 把失败读成空集、`! pgrep` 把"进程没了"
> 读成"备份好了"、下游登记的缺席读成事件的缺席),这次长在了一个守卫身上。

**补的:** 触发器 · 表注与列注 · 字典表的行。字典清单**直接读
`check_mirrors.RUNTIME_CONFIG_TABLES`** —— 不让调用方声明,因为"清单要靠记就会漏"
是这个仓库成文的处置。**而覆盖面(连同【看不见的那几类】)写进了探针自己的抬头**:
一个把自己的边界写在使用者站的地方的工具,就制造不了那种信心。

**自检注入 8 类,8 类全部抓到并点名。** 一个没有人看着它干过活的守卫,只是安静。

## C · fixture 泄漏:量过之后,选了更强的那条

| 方案 | 实测 | 抓得住什么 |
|---|---|---|
| 整套跑两遍 | 118 支 **3.07 秒** ×2 | 只抓**会撞车**的泄漏;而且**不说是谁** |
| **每支前后比指纹** | 一次 **32 毫秒**,合计约 **3.8 秒** | **抓任何泄漏,当场点名哪一支、多了几行** |

**简报担心的"加倍是否负担得起"没有成立** —— 因为 gate 的 270–436 秒**几乎全在重建
与线上比对上,不在 fixture 上**(118 支只要 3 秒)。所以两条都便宜,而后者严格更强,
于是只做后者。**没有悄悄把 gate 加倍。**

**C3:把 PROC-5 那个缺陷原样搬回去(fixture 54 的字典行挪到 `BEGIN` 之前),
泄漏检查点名它并报 `352 → 353`,其余 117 支零误报。**

## 本刀的类别:**加法且【可选】**

三支函数都是把判据从"写死七个"改成"读字典" —— **接受的集合只会变大,不会变小**,
没有新参数,旧调用方一个字都不用改。所以走标准顺序。
**对照 PROC-6:那一刀是"加法但【必填】"** —— 旧调用方不传新参数就会被拒,
于是新化验单在破窗里 22 分钟记不下来。**这条区别就是那 22 分钟的价钱。**

## GAP LIST 更新

* ~~**U17** 三支函数仍写死七金属~~ **已关闭(PROC-CLEANUP)**,并有 fixture 119 守着。
* ~~**U18** injection_probe 的表捕获不含触发器~~ **已关闭(PROC-CLEANUP)** ——
  连同【行】与【注释】一起补上,且覆盖面与盲区都写在它自己的抬头上。
* **新增 U19(方法):** 按文本找副本的检测器分不出代码与注释。
  今天靠"先剥注释"绕过;**真正的判据是行为**(fixture 119 那种)。
