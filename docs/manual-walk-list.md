# 手走清单 —— 只有人点得到的那些断言

`db/fixtures` 问"建出来的库跑起来对不对";`scripts/smoke-routes.mjs` 问"每条路由渲不渲得出来"。
**这份清单问第三个问题:操作员在屏幕上【读到的那句话】是不是人话。**

没有任何自动化跑得到它,原因是具体的而不是懒:

* 冒烟只发 **GET**,它断言 2xx —— 它从不提交任何表单;
* 表单用 `useActionState`(客户端组件),**没有渐进增强的隐藏字段**,所以
  无 JS 的 POST 根本到不了 action;走 Next 的 action 线格式需要客户端 bundle 里的
  action id,而那正是浏览器才有的东西;
* fixture 是 SQL,它够得到 RPC,**够不到 action 与 locale 文件**。

于是"数据库那侧完全正确、而屏幕上是一串机器码"这件事,**只有人点得出来**。
IOD-1b 与 IOD-2 各付过一次账,两次都是人点出来的。

## 每一节的状态 —— 三种,不是两种

**这份文件此前【没有任何状态词汇】。** §11 / §12 / §13 / §14 一个状态字都没有,
§8 / §9 只写着"这一条是那一刀的关闭条件"(那是一个**条件**,不是它满足了没有),
只有 §10 用一句散文说了自己没被走过。于是"走过了"与"还没人走"在这份文件里
**看起来完全一样** —— 而这正是这份文件本身要防的那种病,长在了它自己身上。

每一节的状态写在它标题下面【那一行】,不另立一张汇总表:
**一件事在两个地方陈述,两处必然漂开**(本仓库为这条付过很多次账)。

| 标记 | 意思 |
|---|---|
| **✅ 已走过** | 有人真的点完了。**没有第二层含义** —— 它不代表当时全对,只代表被走过 |
| **⏳ 欠着** | 还没有人走。**这不是"大概没问题"** —— 它的意思是【没人看过】 |
| **⛔ 卡在前置条件上** | 走不了,而且**不是因为没排上** —— 缺一件具体的东西,那件东西必须被点名 |

> **为什么第三种必须存在,而不是塞进前两种。**
> 【没人走】与【走不了】的处置**完全不同**:前者要排时间,后者要先造出那个前置条件,
> 排多少时间都没用。把后者写成"欠着",下一个人会一次次地想去走它、一次次地卡住;
> 写成"走过了"就更坏,那是一句假话。**这与本仓库反复说的那条是同一件事:
> "没测过"、"测了是零"、"你不能看" —— 三种不同的空,不许渲染成同一种。**

## 什么时候跑

改动了 **action 的错误分支、`*ErrorCodes.ts` 的集合、`messages/*` 里的错误/告警句子**,
或者新加了一个会抛到界面上的 RPC 错误码 —— 跑与之相关的那几条。不必每次全跑。

## 怎么起

```bash
npx next dev -p 3000
```
临时管理员账号自己建(参照 `scripts/smoke-routes.mjs` 的 `signIn`),或用一个真账号。
**被拒的走法不写任何流水**,可以放心在 live 上点;标着【会写库】的那几条会留下
**永久的 `inventory_movements` 行**(台账只进不出,`BEFORE DELETE` 抛 `MOVEMENT_IMMUTABLE`),
所以那几条要么别在 live 上点,要么接受它留下来。

---

## 1 · 库位分类落闸:明确排除 → 拒绝(IOD-2)

**前提**:一个在用库位配了 allowed_classes(例如只允许 `focused`);一个物料的分类
**不在**其中(例如 `non_focused`)。

**走**:`/inbound/new` → 选那个物料、那个库位 → 保存。
**期望**:红条是一句话,并且**说得出三条出路**(换库位 / 把这一类加进该库位 / 改物料分类):

> Location SG… is configured to hold specific material classes, and non_focused is not one of
> them — so nothing was saved. Pick another location, add non_focused to that location under
> Inventory → Storage Locations, or correct this material's classification in the material dictionary.

**不期望**:`IOD_CLASS_EXCLUDED|SG…|non_focused`(机器码原样)。

**为什么在这份清单里**:拒绝由 RPC 抛出(fixture 59 B3/E/F1/F2 已经钉住"抛得对"),
**但"抛完之后有没有被翻成人话"住在 action 的错误分支 + `messages/*` 里** —— fixture 是
SQL,够不到那两处。这一条 2026-08-13 手走时是红的:action 那时用一句手抄的正则挑
错误码,新码没被挑中,于是走了 `saveError` 分支把机器码端了出去。
现在判据改成 `isStockErrorCode()`(与 `STOCK_ERROR_CODES` 同一份),漂开的那条路没有了,
但**"句子长什么样"仍然只有人看得见**。

其余三个落地点同理,改了任何一处错误分支就顺带走一遍:
`/inbound/receive`、`/output/new`、以及批次页上的**转移**面板(它不重定向,告警直接回到面板)。

## 2 · 日期必填:说人话,不说约束名(IOD-2-fu1 / FIN-32)

**走**:`/output/new` → 填物料与数量,**产出日留空** → 保存钮应当是灰的、下面有一句
琥珀色说明;再想办法提交一次(绕过界面那道守卫)。
**期望**:

> Output date is required — the stock movement records the day the goods were actually produced

**不期望**:`new row for relation "inventory_movements" violates check constraint
"inventory_movements_business_date_required"`(约束原文)。

到货日在 `/inbound/new` 与 `/inbound/receive` 上同理。

**为什么在这份清单里**:这一条现在**有一半是自动的** —— fixture 25 H 臂断言三个 RPC
少了日期会**按名**拒绝(`ARRIVAL_DATE_REQUIRED` / `OUTPUT_DATE_REQUIRED`),那是
2026-08-13 手走之后补上的:当时 RPC 自己一道守卫都没有,只有 app 那一层有,
于是任何不经过表单的调用者都会撞到 FIN-32 的 CHECK 并拿到约束原文。
**剩下的一半仍然只有人点得到**:那个名字有没有在两个 locale 里接成句子、
以及界面那道守卫(禁用提交 + 说明)是不是真的在。

## 3 · 落地告警:琥珀、成句、一条一行(IOD-2)

**走**:把货收进一个**没配过 allowed_classes** 的库位,或收一个**未分类**的物料。
**期望**:落地页(`/inbound`、`/output`、收货完成页)顶部一条**琥珀色**横幅,
一条告警一行,并且**批次确实建出来了** —— 告警不是拒绝。

【**会写库**】这一条的期望结果是"保存成功",所以它会在 live 上留下永久流水。
2026-08-13 的做法是**只走渲染那一半**:直接请求
`/inbound?warn=%5B%22IOD_CLASS_UNCONFIGURED_LOCATION%7CSG…%22%5D`,
真组件、真本地化、不写库。RPC 会不会吐出这两个码,由 fixture 59 B1/C/E/F3 钉住。
两半合起来等于整条路径,而没有任何一行永久流水。

## 4 · 物料表单的【提交】那一半:安全库存阈值真的落库(SS-1)

**两条,新建与编辑各一条 —— 它们是两段不同的代码**:`app/materials/new/actions.ts`
走 `.insert()`,`app/materials/[id]/edit/actions.ts` 走 `.update()`,各有一份取值、
一份校验、一份写入。走通了一条不代表另一条。

**走 A(新建)**:`/materials/new` → 填必填项 + 安全库存填 `500` → 保存 →
回到列表,该物料那一列应当显示 `500 kg`,**而不是「未监控」**。
**走 B(编辑)**:`/materials/<id>/edit` → 把阈值**清空** → 保存 →
列表那一列应当变成**「未监控」**,而不是 `0`。
**走 C(两条表单各一次)**:阈值填 `0` 或 `-5` → 期望是那句话:
「安全库存必须是大于 0 的数字 —— 不想监控这个物料就留空」,
**不期望**约束原文 `materials_safety_stock_qty_positive`。

**为什么在这份清单里 —— 与前三条同一个理由,而且是同一堵墙**:表单用
`useActionState`(客户端组件),**没有渐进增强的隐藏字段**,所以无 JS 的 POST
到不了 action;走 Next 的 action 线格式又需要客户端 bundle 里的 action id。
冒烟只发 GET,fixture 是 SQL 够不到 action。**2026-08-13 的 SS-1 手走只走到了
渲染那一半**(两张表单的字段与提示句都确认渲染出来了,列表那一列也确认了),
**提交那一半一次都没有人点过** —— 那正是这一条存在的理由,别把它读成"已经走过"。

数据库那一侧不必在这里走:CHECK(NULL 或 > 0)由 fixture 60 F 臂两个方向各拒
一次,告警对 NULL 不响由 D 臂钉死。这一条只问【表单填的值有没有原样落进去】,
以及【拒绝有没有说人话】。

## 5 · 申报量真的落库,而【留空落的是 NULL 不是 0】(GRN-1b)

**两条,两张收货表单各一条 —— 它们是两段不同的代码**:
`app/inbound/new/actions.ts` 走 `create_inbound_batch`,
`app/inbound/receive/actions.ts` 走 `receive_inbound_batch_against_po`,
各有一份取值、一份校验、一份 RPC 调用。走通了一条不代表另一条。

**走 A(填了)**:`/inbound/new` 或 `/inbound/receive` → 申报量填 `1000`、
过磅数量填 `800` → 保存 → 打开该批次详情,「与采购单的对照」一段里应当出现
**「申报与实收不符」**,并写出 `申报 1000 / 磅秤 800 / 差 −200`,以及判它的那两个阈值。

**走 B(留空 —— 这一条才是要害)**:同样两张表单,申报量**什么都不填** →
保存 → 详情页**不许**出现「申报与实收不符」,而且不许出现任何"申报 0"的字样。
库里那一列应当是 `NULL`。**填 0 与留空必须是两种结果**:留空 = 没记录过,
0 = 供应商申报了零(一句没人说过的话)。

**走 C(选了采购行之后)**:在 `/inbound/receive` 选一个供应商 → 选一张采购单 →
选一条采购行。**数量框会被预填成剩余量(那是对的,便利)**;
**申报量框必须【仍然是空的】**。这一条是这三条里最重要的:
一个被预填的申报量,是系统替供应商说了话 —— 它会让"申报与实收一致"在没有任何
供应商文件的情况下成立,而那正是这一列存在要回答的问题。

**为什么在这份清单里**:与前四条同一堵墙 —— 表单是 `useActionState`,
冒烟只发 GET,fixture 是 SQL 够不到 action。数据库那一侧由 fixture 87 E 臂
三个方向钉死(没记 = NULL、相符 = 数字 0、超差 = 报),**这一条只问
【表单填的值有没有原样落进去,以及留空有没有真的留空】**。

## 6 · 收错料的那句话说的是【收下了】,不是【被拦住了】(GRN-1b)

**前提**:一张 confirmed/receiving 的采购单,某一行订料 A;现场收到的是料 B。

**走**:`/inbound/receive` → 选那张单、那一行 → **把物料改成 B** → 保存。
**期望两件事,缺一不可**:
1. **保存成功**,批次建出来了 —— 收错料【不拒绝】(换料是可以谈成的正当安排);
2. 批次详情的「与采购单的对照」里出现**「收到的不是订的料」**,写出两个料号,
   并且**明说这批货已经收下、已经入账**。

**为什么第 2 点的措辞是这一条的全部**:告警在屏幕上极容易被读成"这批货被卡住了"。
读错的操作员会去找一个并不存在的放行按钮,或者**更坏:再收一次**。
所以这一条要走的不是"有没有提示",而是**"提示有没有说清楚货已经进来了"**。

【**会写库**】走这一条会留下永久的 `inventory_movements` 行 —— 台账只进不出。
要么别在 live 上点,要么接受它留下来。

## 7 · 供应商模式面板的两个【只有人看得见】的状态(GRN-2)

**为什么这一条必须在这份清单里,而不是做成 fixture**:fixture 88 钉住的是
**视图**那一半 —— 无权读者拿到【0 行】(H 臂)、没有可比对收货的供应商仍然
拿到【一行全 0】(G/J 臂)。但"这一行 0 在屏幕上被印成哪一句话",是
`ReceiptPatternPanel` 里的一个 React 分支,**fixture 是 SQL,够不到它**
(本文件开头那三条理由,第三条)。而这两句话恰恰是这一刀的全部意义。

**走 A(具名空状态)**:`供应商` → **Staff Reimbursements** → 「收货记录」面板。
**期望那一句话是**:
> 最近 180 天里,没有任何一条【有订量可比】的收货 —— 所以这里没有可供评判的东西。
> 这【不是】一份干净的记录:它的意思是,还没有人能够检验这家供应商。

**不期望**:任何一行"短交 0 次 / 超收 0 次"之类的零。**看到零就是这一条失败了** ——
五种差异的计数在这个分支里【整块不渲染】,因为它们的分母是 0,而印出来的零会被
读成"全都合规"。同一块面板下方仍应印出「没法评判:1 条」——
那 1 条正是它唯一的收货。

**走 B(受限)**:用一个持 `module.suppliers.view`、【不持】`module.purchasing.view`
的账号打开任意一家供应商。**期望**:
> 这份记录在采购模块那道门后面,而你没有那个权限。这是一句【权限答复】——
> 它不是在说这家供应商记录干净。

**不期望**:空白、零、或者「这不该发生」。
**这一条今天在线上走不通**:实测五个持 suppliers.view 的角色(admin / auditor /
finance / gm / procurement)【全部】也持 purchasing.view,所以要走它得先临时建一个
只给 suppliers.view 的角色。**记下来而不是省掉**:角色表会变,而那一天没有人会
记得回来补这一条 —— GRN-1b 在批次详情上栽的正是这个分支(当时它渲染的是
「这不该发生」,对着 warehouse 与 operations 两个真实角色天天说)。

---

## 8 · 设备采购的整条链(EQP-1c-b)—— **这一条是那一刀的关闭条件**

> **状态:✅ 已走过。**(走查日期未记录 —— 本文件当时还没有状态词汇,所以这里【不补一个日期】:一个编出来的日期比没有日期更坏。)

**为什么它在这里:** 前面几刀(EQP-1a → 1c-a)全是操作员够不到的管道。
闸门绿、fixture 全过,都【不能】说明这个模块能用 —— 那正是本仓库记过的结构盲点
(月结走查在八步里点出七个页面级缺陷,而当时每一道闸都是绿的)。
所以这一刀的关闭条件是这条走查,不是一次绿闸门。

**在生产上走。** 它【会写库】:一张资产卡、一张采购单、一笔付款、一笔开支、
一条冲抵分录。这些都是真凭证,走完留下的东西见本节末尾「残渣」。

### 走之前

用一个同时持 `module.finance.edit` 与 `module.purchasing.edit` 的账号(admin 即可)。
**若你想顺带验一验权限那一半**,再用一个只有采购、没有财务的账号走第 2 步 ——
期望看到的那句话写在第 2 步里。

### 步骤

1. **登记机器。** `财务 → 固定资产` → 右上角「登记一台机器」。
   * **应当看见**:表单顶上一块蓝底说明,把【两扇门】分开讲 ——
     这一张是"已经决定要买、钱还没付";另一张(开支表单的资本勾选项)是
     "当场买断、发票在手"。**读不出区别就是这一条失败了** ——
     选错门的代价是一张成本对不上的卡。
   * **应当看见**:一句说明"这张表单上没有价格,而这是刻意的"。
     **不应当看见**:任何金额/单价字段。
   * 填:名称(例如 `熔炼炉 A`)、取得日、使用年限(月)、类别=设备。
   * 提交后**应当跳到这台机器的卡片页**,编号形如 `FA-2026-xxxx`。
   * **卡片页上应当写着**「还没有记过成本」与「尚未投用」——
     **两句话不一样,而且都不是空白。** 看到空白格就是这一条失败了。

1b. **(若这家机器供应商还没建)登记它。** `供应商 → 新建`。
   * 「供应商类型」多选里**应当有【设备供应商】这一项**(EQP-1c-b-fu 加的 ——
     Tim 第一次走查时四个选项全是电池价值链上的角色,卖机器的一个都不沾)。
   * 「这一家是什么」选**供货商**(goods_supplier)——那句话对机器供应商是真的,
     而且它与上面那个多选回答的是两个不同的问题:一个问能力,一个问他们做哪一行。
   * **已知的一处不完美,看到不必当成缺陷**:这个多选留空是允许的,而
     留空与「一种都不是」在列表页是同一个空格子 ——
     记在 `docs/known-issues.md`,等主数据那一刀处理。

2. **开一张设备采购单。** `采购 → 采购单 → 新建`。
   * 选供应商、币种、下单日。
   * **应当看见**:明细上方一个「材料 / 设备」的**模式开关**,
     旁边一句话说清"一张单要么全是材料、要么全是设备,不能混装,
     请在动手【之前】选好"。
     **这句话必须在你打字之前就在那里** —— 这一条的全部意义就是它:
     等你填完两种行、按下提交才被拒,那次拒绝浪费的正是你刚打的字。
   * 选「设备」。**应当看见**:
     - 「机器」下拉,列出第 1 步那台(格式 `FA-… — 熔炼炉 A`);
     - 数量与单位变成一个**读不了也改不了**的 `1 unit`,旁边一句
       「一行就是一台机器…四台机器是四条行」;
     - 一句「设备行永远不收货」。
   * **不应当看见**(EQP-1c-b-fu 修的就是这三样,Tim 第一次走查时它们还在):
     - 一个可以输入数量或单位的框;
     - **「计价公式」下拉** —— 一台机器有一个谈定的价,不按行情公式结算;
     - **「预计化验」那一段** —— 一台机器没有金属含量。
     三者都是【隐藏】,不是变灰:一个禁用的控件邀请人问「我为什么不能用它」,
     而这里的答案是【这个问题不适用】。
   * 「删除本行」在只有一行时是灰的,**旁边应当有一句话说明为什么**
     (「一张单至少要有一行」)。灰着却不说理由,读起来像是坏了。
   * 填一个估价(例如 100,000)。
     **应当看见**:同一行的「金额」变成 **100,000.00**。
     **不应当看见 0.00** —— 那是 EQP-1c-b-fu 修掉的一个缺陷:此前设备行的
     数量只在提交负载里被换成 1,屏幕上那个「1 unit」只是一段写死的文字,
     于是「金额 = 数量 × 单价」里的数量是空的,**填了价金额也一直是 0.00**。
     它还会往下传:估算总额是各行金额之和,而【按百分比的付款计划】乘的就是它 ——
     一台机器订单上的「30% 定金」会算成 0。看到 0.00 就是这一条回归了。
   * 提交。
   * **权限那一半(可选)**:用只有采购、没有财务的账号打开这张表单并选「设备」。
     **应当看见**:「你看不到资产卡 —— 那需要财务模块的权限。这与"一台都没登记"
     不是一回事。」**不应当看见**:一句"还没有登记过机器"——
     那句话会把一个权限答复说成一个事实陈述。

3. **看采购单页,以及供应商手里那一份。**
   * 采购单详情页的明细行,**机器那一格应当是它的名字**,不是 `—`。
     (EQP-1a 刻意把它留成破折号,理由是"命名它是界面那一刀的事" —— 就是这一刀。)
   * **列头应当写「机器」,不是「物料」**(EQP-1c-b-fu2 修的);
     **不应当看见「计价公式」那一列** —— 它在设备单上永远只能是一个破折号,
     所以整列不出现,而不是留着画横杠。
   * **右上角不应当有「按此单收货」这个按钮**(EQP-1c-b-fu2:隐藏,不是变灰 ——
     机器到厂不是一次收货,那不是一个可以问的问题)。取而代之,页面顶上
     **应当有一句话告诉你机器到了该去哪**(记在它的资产卡上)。
   * **页面下方不应当有「收货差异」与「收货批次」两整节** —— 同样的理由:
     设备行永远没有收货行,那两节会渲染成一排"暂无收货",而那读起来像
     "还没收到货",不像"这件事不适用"。
   * 点「打印/PDF」。**供应商那份上的行名也应当是机器的名字**,而且
     **那张纸上的列头也应当是 Machine**(EQP-1c-b-fu2)。
     行名这一处 EQP-1a 早就修好了(`po_document_data` 里
     `COALESCE(物料名, 资产描述)`),屏幕与纸取的是同一份实现 ——
     **两份不可能各说各话**;若它们仍然不一样,那本身就是缺陷。
   * **付款计划那几行:「on on shipment」这种重复的介词应当没有了**
     (EQP-1c-b-fu2 修的,只改了 PDF 模板那一处;屏幕那侧一直是对的)。
     **注意:你在这一刀之前已经签发过的 v1,里面那个重复的词【原样留着】** ——
     一次签发是"当时发出去了什么"的记录,不因为后来修好了模板就改写。
     那是设计,不是遗漏;**不要重新签发只为了让它好看**。
   * **抬头地址应当只出现一次「Singapore」**,不是
     「25 Haji Lane, Singapore, 189218, Singapore」(EQP-1c-b-fu2)。
     发票那份同理(它此前是"Singapore 189218"下面再来一行"Singapore")。

4. **付定金。** `财务 → 收付款 → 新建`,方向"付出",对象=该供应商,
   核销行挂到这张**采购单**上(不是挂到某张发票上),金额例如 30,000。
   * 回到采购单页,**应当看见**预付合计变成 30,000。

5. **把发票记成一笔开支,挂在那条设备行上。** `财务 → 开支 → 新建`。
   * 勾上「资本支出」。**应当立刻看见两个单选,各带一句话**(EQP-1c-c 加的 ——
     在这之前这张表单【只会新建一张卡】,对一台已登记的机器无路可走):
     - 「这笔支出买的是一台【新】机器」——由这笔支出生出卡并带上成本;
     - 「这笔支出是给一台【已登记】的机器加成本」——**本次选这个**。
     **模式必须是你选的,不是系统猜的** —— 选错的代价不对称:把"加成本"
     错走成"买新机器",会多出一张【撤不回来】的资产卡。
   * 选「已登记」之后:
     - **描述 / 类别 / 投用日 / 年限 / 残值 这几栏应当【消失】**(不是变灰)——
       它们属于【那张卡】,不属于这笔支出;
     - 出现「哪一台机器」下拉,**里面应当有第 1 步那台**
       (只列【还加得上成本】的:尚未投用、未处置);
     - 出现「采购单行(可选)」下拉,**里面应当有第 2 步那条行**。
   * 供应商=同一家、状态=挂账(unpaid)、金额=发票金额。
   * **看一眼下方的过账预览**:借方应当写着 **1500 固定资产-设备**,
     **不是一个省略号**(EQP-1c-c 修的 —— 资本模式下科目下拉是隐藏的,
     此前预览就落到了「…」)。
   * **故意先犯两个错,各看一句话**:
     - 供应商挑成**另一家** → 应当读到
       「采购单 … 属于另一家供应商,与这笔支出上的(…)不是同一家」;
     - 状态选**已付**、且不填供应商 → 应当读到
       「挂在采购单 … 上的支出,必须说出开这张票的供应商」。
       **注意它【不是】"供应商不一致"** —— 拿 NULL 去比不是不一致,是没人说过。
       读到"不一致"就是这一条失败了。
   * **第三个故意的错(可选,但值得看一次)—— 改写于 2026-08-29(CAPEX-1)。**
     原文让人去确认服务端会说「成本就冻住了」。**那句话已经不成立了,
     所以这一步现在要检查的是【它不再出现】,以及新的那句指路话出现了。**
     如果那台机器【已经投用】,「哪一台机器」的下拉里仍然不会有它 ——
     但下拉下面那句提示现在会说明【为什么】,并点名该去哪儿
     (那台机器的维修记录)。硬要绕过界面去试的话,服务端会说
     「这台机器已经投用了。追加成本要经过它的【维修记录】……」——
     **不应当读到 `ASSET_IN_SERVICE_NEEDS_MAINTENANCE` 这串字母,
     也不应当再读到任何一句"成本冻住了"。**
   * **★ 顺带走一次那条【新开的】路(CAPEX-1)★** 在一台已投用机器的
     【保养与维修】面板上记一条,勾「资本化」并写理由 → 那一行上出现
     「资本化」按钮 → 填日期金额提交 → **机器卡片页的成本应当变大**,
     而**「折旧」那一节里【已经过账的每一期金额一分钱都不许变】**。
     那才是这一刀要看的那件事:成本变了,历史没变。
   * 改对,提交。**回到机器卡片页:成本应当变成发票金额,并且
     「成本从哪来」里有一行指着这张开支单;「由哪张单买的」一节应当指着
     第 2 步那张采购单与那一行。**

6. **再记一笔试试。** 同样走「已登记」模式、同一台机器。
   * **「采购单行」下拉里那条行应当还在,但【是灰的】,后面跟着一句
     「已由 EXP-… 报销」**(EQP-1c-c:问题【适用】但被挡住 → 禁用并说明理由;
     只有【不适用】的东西才隐藏。藏起来会让人以为那条行不存在,
     而它就在采购单上明摆着)。
   * 所以这一步**挑不动它** —— 那正是这条推导在界面上该有的样子。
     若你想亲眼看那句服务端拒绝,把这一步留给下面的"故意犯错"清单。
   * **应当读到**:「第 N 行已经由 EXP-… 报销过了 —— 一条设备行只报销一次。
     若订单上的估价与发票对不上,请【改订单行】,不要再记一笔。若是发票本身
     开错了,冲销 EXP-… 再录一笔即可 —— 但这只在机器【尚未投用】时做得到。」
   * **不应当读到**:`PO_LINE_ALREADY_EXPENSED` 这串字母本身。**看到裸码就是缺陷。**

7. **冲抵定金。** 打开第 5 步那张开支单(`财务 → 开支 → 点开它`)。
   * **应当看见**一块「把定金冲抵到这张发票上」的面板,里面两个数:
     「这张发票还欠」(以发票币种计)与「还可冲抵的定金」(以本位币计),
     外加一句**说明这两个数币种不同、不能直接相减**。
   * **先故意把冲抵日留空** → 应当读到「冲抵日必填 —— 它决定这笔分录落在
     哪个期间,系统不会替你填。」**这一条是本刀的 X1,它此前默默用"今天"。**
   * 填日期与金额(例如 30,000),提交。
   * 回到机器卡片页:「那张单上的定金」一行**应当显示 已冲抵 30,000、还可冲抵 0**。

8. **试着投用它。** 卡片页底部「投用」。
   * 若你想看那句拒绝:先**新登记一台**机器(第 1 步),对**那台**点投用 →
     应当读到「资产 … 还没有任何成本。资产卡是在【决定要买】这台机器时建的,
     成本随发票到来 —— 请先把发票记到这张卡上。」
   * 对**第 1 步这台**(已经有成本的):卡片页「投用」一节
     **应当写着「可以投用了」** —— 并且(**改写于 2026-08-29,CAPEX-1**)
     **不应当再说投用会把成本冻住**。它现在说的是:投用【定住的是折旧算过的那个基数】,
     而成本以后仍然加得上去(经维修记录资本化,往后摊,不回溯)。
     ~~并提醒投用会把成本冻住~~
     填一个投用日提交,**应当成功**,卡片页顶上「投用日」出现那个日期。

### 一路上不应当出现的东西(看到就是缺陷)

* 任何一串**全大写下划线的机器码**(`PO_LINE_*` / `ASSET_*` / `EXPENSE_*` / `PREPAY_*`);
* 任何 `violates check constraint "…"` 之类的**裸约束违例**;
* 一个**按不下去又不说为什么**的按钮;
* 一个**空白格**,而它本该说「尚未投用」或「还没有记过成本」——
  这两种缺席不一样,空白把它们混成一种。

### 这条走查会留下的【残渣】,走完请记下来

在生产上会永久留下:**一张资产卡**(若走了第 8 步,它还会是"已投用"的,
而**投用不可撤销** —— 改投用日是一次人工分录更正)、**一张采购单**、
**一笔付款 + 它的核销行**、**一到两笔开支**(第 6 步那笔被拒的不落库)、
**一条冲抵分录**、以及第 8 步为看拒绝而多登记的**那张零成本卡**
—— 而**零成本卡今天退不掉**(处置按名拒,它也没有可冲的支出;
见 `docs/known-issues.md` 那一条)。

走完之后要做的两件登记:
1. 把 `/finance/assets/[id]` 从 `scripts/smoke-routes.mjs` 的 `EXPECTED_SKIPS`
   里**删掉** —— 有数据了,那条断言会自己报「预期会 SKIP 的路由跑起来了」;
2. 把这次留下的凭证编号记进 `docs/known-wrong-until-cutover.md`
   或作为真实业务保留,**由你决定**,但要写下来。

---

## 9 · 设备的一生:保养、停机、保养间隔(EQP-2d)—— **这一条是那一刀【和整个设备模块】的关闭条件**

> **状态:✅ 已走过。**(**Tim 于 2026-08-24 确认**。)
> **这是一次【确认】,不是一次复核** —— 记下来的是 Tim 说他走过了,
> 而不是有人对着屏幕把这九步重新验了一遍。两者在文件里必须分得开。
> **它是 EQP-2d【和整个设备模块】的关闭条件,所以阶段 1 至此闭合。**
>
> **顺带记下一个实测,免得下一个人以为脚本还照着能跑**(2026-08-24 只读勘察):
> 线上 `FA-2026-0001` 今天的状态与本节脚本的前提**已经对不上**了 ——
> 间隔行已存在(第 1 步的「未监控」只有一次机会,已用掉)、机器有 2 条保养记录
> (第 3 步那句琥珀诚实话的分支要求 `never_serviced`)、`in_service_date` 已被
> FIX-1 挪成 `planned_in_service_date`(第 6 步「已投用」的前提没了)、
> 而且**有一段停机仍然开着**(第 7 步以一次拒绝开场)。
> **这与「走过了」并不矛盾** —— 它说的是**再走一遍需要另一台机器,或者一次关于
> 这些遗留行的决定**。

**为什么它在这里:** EQP-2a(机器做过什么)、2b(保养记录)、2c(间隔与两支臂)
**三刀全部落在库里,一张屏幕都没有** —— 也就是说这个模块此前的每一次"绿闸门"
证明的都是【管道通了】,不是【有人用得了】。这一刀给它们装上门,而门好不好用
只有人点得出来。**所以这一条走完,设备模块才算关闭。**

**在生产上走。** 它【会写库】:一条保养间隔、两条保养记录、一段停机。
都是真行,走完留下的东西见本节末尾「残渣」。

### 走之前

* 用一个同时持 **`module.finance.view`**(机器的卡片在财务)与
  **`module.processing.edit`**(保养/停机/间隔写在加工侧)的账号 —— **admin 即可**。
  这两个码分属两个模块,**这不是配错了**,是那三张表刻意的分工:
  机器卡在财务,干活的人在加工。
* 打开 `财务 → 固定资产 → FA-2026-0001`(Bosch Deep Discharging Machine),
  滚到页面**最下面**那条分隔线之后 —— 新的三节全在那里。

> **先记下一个数,后面每一步都要用到它。**
> 这台机器的取得日是 **2026-08-21**。设 **k = 今天 − 2026-08-21**(天数)。
> **若你是在 2026-08-21 当天走这条,k = 0** —— 那么第 3、7 两步会告诉你
> 【什么都不该亮】,而那正是对的。**k ≥ 1 时整条走查才走得完整**,
> 所以如果今天就是 8-21,把这条留到明天再走。

---

### 步骤

1. **先看【未监控】—— 这一步必须排在最前面,而且只有一次机会。**
   `FA-2026-0001` 是全库唯一一台机器;**你一旦在第 2 步设了间隔,这个状态就没了**。
   * **应当看见**「保养间隔」一节里一块灰底,写着 **「未监控」**,下面一句话说明:
     *还没有人给这台机器设过保养间隔,所以没有任何东西在按它衡量。这与「未到期」
     不是一回事 —— 是还没有人决定要盯它。*
   * **不应当看见**:一张空表、一个「未到期」、一个 0、或者一片什么都没有的空白。
     **看到「未到期」就是缺陷** —— 那是把"没人看过"说成"看过了没问题"。
   * 顺手看一眼**首页**:「保养已到期」与「保养将到期」两块牌子都是 **0**。
     那个 0 是【真的零】(你有权限,只是确实没有到期的),不是「受限」。

2. **设一条保养间隔。**「保养间隔」→「加一条间隔」。
   * 种类 **例行保养**;到期之后 **上看板**;
   * **公斤间隔填 1000,提前量填 0**;
   * **天数间隔填 `k + 2`,提前天数填 2**(k 见上面那个数);
   * 保存。
   * **应当看见**表单上两句话:「公斤、天数,至少写一个」与「提前量必须小于间隔本身,
     0 是允许的」。

3. **看基线读数,以及【它的诚实那一句】—— 这是本刀最要紧的一屏。**
   * **应当看见**两行读数:
     * `距基线已处理 **0** 公斤 · 每 1000 一轮`
     * `距基线已过 **k** 天 · 每 k+2 一轮`
   * **应当看见**基线那一句:*从未保养过 —— 所以基线是这台机器的【取得日】
     2026-08-21,上面两个数说的是它到手之后做过的一切。*
   * **应当看见【琥珀色】的诚实那一句,而且它必须是这一条:**

     > **这个读数从零起步,而这里的零【不】等于磨损得少。在册的 10 炉加工全部
     > 发生在这台机器取得(2026-08-21)之前,而一炉加工只能在【提交那一刻】
     > 归属给机器,事后没有任何一条路能补 —— 所以那些加工带来的磨损,
     > 是我们看不见的磨损。**

     > **这一段是【实测过的】,不是推想**(2026-08-21,一次回滚掉的探针):
     > 设上间隔之后,`equipment_service_status` 对这台机器读出
     > `kg_since = 0`、`days_since = 0`、`never_serviced = true`,
     > 而 **`unattributed_runs_in_window` = 0**。
     > **请注意那个 0** —— EQP-2c 做进视图的那一列(窗口【之内】没人归属的炉数)
     > 在这台机器上是 **0**,而它的盲区恰恰是最大的:十炉全在窗口【左边】。
     > **也就是说,只靠那一列出提示的实现,对着这台机器会一句话都不说** ——
     > 而这台机器正是第一个会有人来看的。屏幕上那一句因此由【另一个数】驱动。

     **这一句不出现,就是本刀最重要的一条失败。** 屏幕上那个 `0 公斤` 有两种
     读法(磨损得少 / 磨损我们看不见),而这台机器恰恰是后一种 ——
     **一个没有注解的 0,读起来就是"查过了,没磨损"。**
     * 数字 **10** 是"在册(未冲销)且发生在取得日之前、谁都没归属"的加工炉数。
       它若显示成别的数,先别当缺陷:去 `加工` 列表数一下再说。
   * **状态标签应当是「将到期」(琥珀)**,后面跟着「按天数」——
     因为 k ≥ k+2−2,而 k < k+2。
   * **k = 0 时**:状态是「未到期」,而上面那句诚实提示【照样要在】。
     **诚实那一句与到不到期无关** —— 它说的是这个读数可不可信,不是这个数大不大。

4. **让它变成【已到期】,并看两块牌子分开动。**
   把那条间隔的**天数间隔改成 k**(提前天数保持 2 → 若 k ≤ 2,改成 0)。保存。
   * **应当看见**状态标签从琥珀的「将到期」变成**红色的「已到期」**,后面跟「按天数」。
   * **回首页**:「保养已到期」**1**,「保养将到期」**0**。
     **两块牌子,两个数字，永远不合并** —— 看到一块合起来的"保养告警 1"就是缺陷。
   * 点「保养已到期」牌子里那一行 → **应当落到 `FA-2026-0001` 这一页**
     (门牌指向【补救在哪里】,而补救就是在这一页记一次保养)。

5. **记一次例行保养,看基线【真的】重置。**
   「保养与维修」→「记一次活」。
   * **应当看见**:日期框是**空的**。**不应当看见**任何预填的今天 ——
     一个被悄悄填成今天的日期会把整条排程往后推,而没有任何东西看得出来。
   * 日期填**今天**;种类 **例行保养**;干了什么随便写(例如 `换油、清滤芯`);
   * **「谁做的」应当是一个三选一的开关**(我直接写名字 / 我们的员工 / 一家供应商),
     **不应当是三个并排的输入框** —— 那等于把一条数据库规矩("从不两个,至少一个")
     当成你的功课。选「我直接写名字」,写你自己;
   * 支出单**留空**;不勾资本化;保存。
   * **保存后应当看见**:
     * 保养列表多一行;
     * 「保养间隔」那一节的状态回到**灰色的「未到期」**;
     * `距基线已过` 变成 **0 天**;
     * 基线那一句从「从未保养过…取得日」**变成**「从上一次同一种保养算起,
       那一天是 <今天>」;
     * **诚实那一句也换了** —— 变成灰色的:*2026-08-21(或你记的那天)之后的
       每一炉加工都归属到了机器,所以这个读数在这段时间里是完整的。
       它对那之前的事情一个字都没说。*
   * **注意公斤那一行:它【前后都是 0】。** 这不是没生效 ——
     **重置看的是天数、基线日与那句诚实提示这三样**,公斤本来就没动过。
     (这正是第 3 步那句话的意思:0 不是"磨损得少"。)
   * **回首页**:两块牌子都回到 **0**。

6. **记一次维修,并【当场撞上资本化那条走不通的路】。**
   再点「记一次活」:日期今天,种类 **维修**,写 `更换主轴承`,谁做的随便,
   **勾上「把这次活资本化」**。
   * **勾上之后应当看见**:
     * 一个「为什么要资本化」的输入框,**旁边一句话解释为什么必须写**
       (*资本化是一个判断,而一个没有理由的判断在后来读它的人眼里与一次失误
       长得一模一样*)。**只是拒绝、不解释,就是这一条失败了。**
     * **一句琥珀色的话,把那条走不通的路说清楚:**

       > **判断记得下来,钱走不了** —— 这台机器已于 **2027-01-01** 投用,
       > 而给一台已投用的机器追加成本会被按名拒绝:那样做会让已经过账的每一期
       > 折旧无声地全错。「给在跑的机器资本化后续支出」是队列里单独的一刀;
       > 在它落地之前,这一行记下的是判断与理由,钱留在那笔支出原来的地方。

     * **注意那个日期是 2027-01-01 —— 一个【未来】的日子,而它今天就已经在拦。**
       这是对的:`record_expense` 的判据是"投用日填了没有",不是"投用日到了没有"。
       (**那个未来的投用日本身**是走查留下的一个占位值,已记在
       `docs/known-wrong-until-cutover.md`;要不要拒绝一个未来的投用日,
       是另一个问题,已经问过 Tim,**不在这一刀里**。)
   * **【故意触发一条拒绝】把理由框清空**,直接保存。
     **应当看见**这一句,一字不差:

     > **资本化必须给理由。一个没有理由的判断,在后来读它的人眼里与一次失误
     > 没有区别 —— 请写清楚延长了什么、改善了什么。**

     **不应当看见** `violates check constraint "equipment_maintenance_capitalisation_reason"`。
   * 把理由填上(例如 `更换主轴承,预计延长可用年限两年`),保存。
   * **应当看见**那一行的「是否资本化」栏里两段话分开:上面是**人的判断**
     (「是 —— 判断为延长了寿命或提高了产能」+ 你写的理由),下面一行小字是
     **系统的建议**,而它这次应当写着 *阈值建议:算不出来 —— 这次活没有挂已过账的
     支出单*。**「算不出来」不是「不达标」** —— 看到「不达标」就是缺陷。

7. **开一段停机,试着开第二段,再把第一段关上。**
   「停机」→「开一段停机」:停机时间填今天某个时刻,原因写 `主轴承异响`,保存。
   * **应当看见**一块**琥珀色、带粗边框**的区块,写着
     **「这台机器现在停着 —— 从 <时刻> 起,还没有人记下它什么时候恢复」**,
     下面一行小字「还在停」。
   * **不应当看见**一段结束时间是空白的普通流水行 ——
     **「开着的一段」与「少填了一栏」在屏幕上必须长得不一样。**
   * **应当看见**那块区块里一句话解释为什么没有「再开一段」的按钮
     (*一台机器同时只能停一次*)。**不应当看见**一个按不下去又不说为什么的按钮。
   * **【故意触发第二条拒绝 —— 要两个标签页】** 这条拒绝在正常操作里
     【碰不到】,因为屏幕不会给你一个服务端保证会拒的按钮。它真实的出现方式是
     **两个人同时开**,所以这样复现:
     1. 在**标签页 A** 打开这一页,**先不要开停机**;
     2. 在**标签页 B** 打开同一页,开一段停机(上面那一步);
     3. 回到**标签页 A**(它还不知道 B 干了什么),点「开一段停机」并保存。
     * **应当看见**这一句,一字不差:

       > **这台机器已经有一段开着的停机了。** 一台机器同时只能停一次 ——
       > 先把开着的那一段结束掉,再开新的一段。如果你并不觉得有开着的停机,
       > 那多半是刚刚有别人开了一段;刷新一下页面就看得见。

     * **不应当看见** `duplicate key value violates unique constraint "uq_equipment_downtime_open"`。
   * **【故意触发第三条拒绝】** 在那块琥珀区块里,把「恢复时间」填成一个
     **早于停机时间**的时刻,点「结束这一段」。
     * **应当看见**:**机器不可能在停机之前就恢复。请检查这两个时间。**
     * **不应当看见** `violates check constraint "equipment_downtime_period_order"`。
   * 把恢复时间改成一个**晚于**停机时间的时刻,结束它。
   * **应当看见**琥珀区块消失,流水表里那一行的「恢复时间」有了值、
     「停了多久」有了时长,而**整行不再是琥珀底色**。

8. **再故意撞两条间隔上的拒绝。**
   * 「保养间隔」→ 那条间隔 →「修改」→ **把公斤间隔填 1000、提前量填 1000**,保存。
     * **应当看见**:*公斤的提前量必须不小于 0 且【小于间隔本身】…
       提前量等于间隔,等于一盏从第一天起就亮着的灯。*
     * **不应当看见** `…lead_kg_shape`。
   * 「加一条间隔」→ 种类仍选 **例行保养** → 随便填个天数 → 保存。
     * **应当看见**:*这台机器的这一种活已经有一条间隔了。请改那一条,
       不要再加一条 —— 同一种活的两条间隔会互相把对方的钟拨回去。*
     * **不应当看见** `…one_per_kind`。
   * 「加一条间隔」→ 种类选 **维修** → **公斤与天数都留空** → 保存。
     * **应当看见**:*公斤、天数,至少写一个。*
     * 然后**取消**,不要真的加这一条(它会给残渣多一行)。

9. **最后看一眼「只记录,不上看板」与「不再监控」是【两件事】。**
   把那条间隔的「到期之后」改成 **只记录,不上看板**,保存。
   * **应当看见**那一行多一个灰色标签「只记录,不上看板」,而**读数还在**;
   * **回首页**:两块牌子都是 0(它不上看板了)。
   * 改回 **上看板**。
   * **点一下「不再监控」但在确认框里选取消** —— **应当看见**确认文案说清楚
     这两者的区别(*…如果你只是不想让它上看板,请把这条间隔改成「只记录,
     不上看板」—— 那样读数还在*)。**不要真的删**,留着它。

---

### 一路上不应当出现的东西(看到就是缺陷)

* 任何一串 **`violates check constraint "…"`** 或 **`duplicate key value violates …"`**
  的裸约束违例 —— 本刀把设备三张表的 **16 条约束**全部接上了句子,
  出现裸约束名就说明漏了一条;
* 任何**全大写下划线的机器码**(`MAINT_*` / `DOWNTIME_*` / `INTERVAL_*` / `ASSET_*`);
* 「保养间隔」一节里出现 **「未到期」而不是「未监控」**(第 1 步),
  或者一个**没有诚实提示的 `0 公斤`**(第 3 步)—— 这两条是本刀的核心;
* 一块**把「已到期」和「将到期」合起来数**的牌子;
* 一段**开着的停机**看起来像「结束时间忘了填」;
* 一个**按不下去又不说为什么**的按钮;
* 「阈值建议」把 **「算不出来」显示成「不达标」**(空不是零);
* 首页某块牌子显示 **「受限」**,而你点进那一页却看得见数据
  —— 那是把"你看得见"说成"你看不见"(EQP-2d 修的正是这一处)。

### 这条走查会留下的【残渣】,走完请记下来

在生产上会永久留下,全部挂在 **FA-2026-0001** 上:

1. **一条保养间隔**(`equipment_service_intervals`,例行保养,公斤 1000 / 天数 k,
   `disposition = 'warn'`)—— **它会让首页那两块牌子长期亮着或熄着**,取决于日历。
   这是这条走查唯一一件【会持续改变屏幕】的残渣。
2. **两条保养记录**(`equipment_maintenance`):一条例行保养,一条已资本化的维修
   (带理由,**没有** `capitalised_expense_id` —— 那条路今天走不通,见第 6 步)。
3. **一段已结束的停机**(`equipment_downtime`,带开始、结束与原因)。
   第 7 步里那次被拒的第二段**不落库**,那次被拒的"提前恢复"也**不落库**。
4. **第 8 步三条被拒的间隔一条都不落库。**

**这些都不过账 —— 三张表一分钱都不动总账**(`equipment_maintenance` 的表注
把这句话写成了它最重要的一条)。所以残渣的代价只在【屏幕上】,不在账上。

走完之后要做的三件登记:

1. 把这次留下的行记进 **`docs/known-wrong-until-cutover.md`** ——
   与 EQP-1c-b 那条走查残渣并排。**理由与那一条相同**:一次【考虑过的不删除】
   与一次【疏忽】在库里长得一模一样,除非有人把它写下来。
2. 若第 2 步那条间隔让首页长期亮着一盏你不想要的灯,**不要删那一行** ——
   把它改成 **「只记录,不上看板」**(第 9 步验过的那条路)。
   删行 = 回到「未监控」,那是另一个意思。
3. **确认设备模块闭合。** `docs/forward-queue.md` 里 EQP-2d 那一行在代码落地的
   那次提交里就已经划掉了(与 EQP-1c-b 同一条规矩:队列记的是【代码交付】),
   但那一行明写着**关闭条件是这条走查,不是绿闸门** ——
   **走完这一节,阶段 1(EQP-1a → 2d)才真的闭合。**
   走不通的那一步请直接说,那一条就是下一刀。

---

## 10 · 进料的五条状态轴,以及它们的门(PROC-2b)

> **状态:✅ 已走过。**(**Tim 于 2026-08-24 确认**。)
> **这是一次【确认】,不是一次复核** —— 与 §9 同一句话。
> 下面那段「这一节是加进来的,不是走过的」是 PROC-2b 当时写的**原话,保留**:
> 它记录的是**那一刀交付时**的状态,不是今天的状态。

> **【这一节是【加进来的】,不是走过的】** PROC-2b 只写了这些步骤,一步都没有走。
> 不要把它读成"验证过了"。
> **【这半句由 DOCS-RECONCILE 更正,2026-08-23】** 原文写的是"它与 §8 / §9 一起,
> 等 Tim 的同一次坐下"。**§8 后来单独走完了**,所以那句话现在只剩一半是真的:
> **还在等的是 §9,不是 §8。**
> 排在 §9 之后:§9 关的是设备模块,这一节开的是加工重建的第一扇门。

**为什么需要人点:** 五条轴 PROC-2 就建好了,而在 PROC-2b 之前【一个都填不了】。
更要紧的是 **PROC-3 那道闸**(拒绝未放电的料进炉)**在有人能记下"这批货已经放过电"
之前是建不出来的** —— 这一节走的正是那条路。

**在生产上走。** 它会写库:一种物料的三条轴,以及一批进料货的安全状态与化学体系确定度。

### 走之前
用一个同时持 `module.materials.edit` 与 `module.inbound.edit` 的账号(admin 即可)。

### 步骤

1. **看"不适用"是怎么说话的 —— 这一步是本刀最要紧的一屏。**
   `物料 → 登记一种物料`。**先什么都不选。**
   * **应当看见**形态、来源、规格尺寸三块各是一行灰底的话:
     *先选种类 —— 这一条适不适用要看它。**这【不是】"不适用"**,是还不知道。*
   * **不应当看见**三个空下拉,也不应当看见【什么都没有】——
     一个没有解释就消失的控件,与一个加载失败的控件长得一模一样。
2. **选「包装材料」。**
   * 三块应当变成:*不适用:包装材料 不回答状态轴。只有电池料才有形态、来源与规格尺寸。*
   * **注意那句话里的"包装材料"是从字典里读出来的** —— 换一种种类,那个词跟着变。
   * 「可以被投进加工吗」那两个钮**应当整个消失**,换成一句"这个问题由种类回答"
     —— **不应当**看见一个禁用的、预选在"不可以"上的钮(那还是一个默认值)。
3. **改选「电池料」。** 形态与来源变成真的下拉;**规格尺寸【还是】一行灰底的话**,
   而且换了一句:*先选形态 —— 这一条适不适用要看它。*
   * **这两句话不一样,而它们必须不一样** —— 一句说"要看种类",一句说"要看形态"。
     写一句话会有一半时候是错的。
4. **形态选「黑粉」。** 规格尺寸那一行应当变成:
   *不适用:黑粉 不需要拆解,所以没有"来自哪一类应用"可记。*
5. **形态改选「整包」。** 规格尺寸这才变成一个真的下拉,而且**以「请选择」开局** ——
   **不应当**预选任何一项。
6. **【故意触发两条拒绝】**
   * 种类选电池料、形态留「请选择」→ 保存。**应当看见**:
     *这一类物料必须说出【形态】与【来源】。两者都永远不会替你填。*
   * 形态选「黑粉」之后,若你有办法让规格尺寸带上值(例如先选整包+某个规格,
     再把形态改回黑粉),保存 → **应当看见**:
     *黑粉 不需要拆解,所以没有"来自哪一类应用"可言。规格尺寸请留空。*
   * **两条都不应当出现** `violates check constraint` 或 `MATERIAL_...` 这样的机器码。
7. **把这种物料建出来**(电池料 / 黑粉 / 退役料 / 可以投料),记下它的编号。
8. **到一批进料货上记状态。** `进料 → 任意一批 → 编辑`,滚到「到货状态」那一块。
   * **应当看见**五个复选框**一个都没勾**,下面一句琥珀色:
     *这批货【没有记过任何安全状态】。那的意思是**没有人记过**,不是"这批货是安全的"。*
   * **勾上「进过水」与「破损或变形」两个** —— 一批货可以同时是两者。
   * 「带电未放电」「进过水」等旁边**应当看见**一句小字「记录为不可投料」;
     **而它今天不拦任何人**(闸在 PROC-3)。
9. **化学体系确定度:那条界必须摆在你面前。**
   * 下拉以「— 没有记过 —」开局。**应当在下拉【下面】看见**:
     *「已知混合」的意思是你【知道】它是混的 —— 一个确立的事实。
      如果你分不出来它是什么,那是「待识别」,不是「已知混合」。*
   * **这句话不出现就是缺陷** —— 站在一个乱糟糟的集装箱前面的人,
     最容易把"我分不出来"选成"已知混合",而那两个的后果完全不同。
   * 选「待识别」,保存。
10. **再回来把它改掉 —— 这一步是 PROC-3 的前提。**
    把安全状态改成只勾「已放电并核验」,保存。
    * **应当看见**保存之后只剩那一个 —— 保存是【整组替换】,不是往上加。
    * **这一步证明的是那个【转变】记得下来**:一批货到的时候带电,后来才放电并核验。
      **只在收货时记一次,PROC-3 那道闸就永远无法被满足。**

### 一路上不应当出现的东西(看到就是缺陷)

* 任何 `violates check constraint` / `violates foreign key constraint` 的裸约束名;
* 任何 `MATERIAL_*` 的全大写机器码;
* 一个**没有解释就消失**的控件(第 1 步);
* 「不适用」与「还不知道」**说同一句话**(第 3 步);
* 任何一个**预选好的**下拉或勾选框;
* 「没有记过安全状态」被显示成一片空白,而不是那句话(第 8 步)。

### 残渣

一种新物料(带三条轴)、一批货上的安全状态行与确定度。**都不过账。**
走完请记进 `docs/known-wrong-until-cutover.md`,与 §8/§9 的残渣并排。


## §11 · FIX-2 —— 七处只能由人眼确认的地方(2026-08-23)

> **状态:✅ 已走过。**(走查日期未记录,理由同 §8。)

**为什么在这里而不是在 fixture 里**:整刀都在渲染层,而 fixture 到不了渲染层。
唯一能被 SQL 钉住的那半个已经钉了(fixture 111 的 F6:没有间隔行的机器,
`equipment_service_status` 没有 monitored 行,而 `equipment_usage` 仍给得出公斤数
—— 那正是新写法的取数处)。**剩下的是"那两句话画出来了没有"**,只能看。

| # | 去哪儿 | 看什么 |
|---|---|---|
| A | `/finance/assets/[id]`,一台【没有设过保养间隔】的机器 | 「未监控」那一块里**必须同时有**:自取得日以来的公斤数,**和**那句"这个读数从零开始,而这里的零【不代表】磨损少"。此前两样只在设了间隔之后才出现。 |
| B | `/purchasing/orders/[id]`,PO-2026-0007 | 「取消」按钮**在**(变灰),旁边一句话说的是**真正挡住的那一条**。设备单上**绝不该**出现"已收货"字样。另:已结束的单上「修改」也是变灰+一句话,不是消失。 |
| C | `/purchasing/orders/[id]`(审批关着时)· 资产卡投用块 | 两处**都不再出现 `docs/…md`**;取而代之的是【设置 → 财务】与【财务 → 日记账】。 |
| D | 资产卡投用块 | "为什么投用不了"**只说一遍**,在按钮旁边;上方那份清单已经没有了。 |
| D1 | 同上,三种资产各看一次 | 标签是中性的「投用状态」;取值分别是「尚未投用」/「计划 2027-01-01 投用」/「2026-03-16 起在役」。**一个未来的日期绝不能出现在过去时的标签下面。** |
| E | 一条【只设了天数、没设公斤】的保养间隔 | 那一行读的是"…没有设公斤间隔,所以这一项不作衡量",**不是** "every not stated"。 |
| F | 一段开着的停机,结束时刻填一个**早于开始**的时刻 | 按钮变灰,旁边说"这个结束时刻早于本段的开始(…)"。此前按钮是【能点】的,点下去才换来一次数据库拒绝。 |
| G | 化验页的「化验机构」下拉 · 物料页的「化学体系」下拉 | 下面各有一句:今天要加一个新值得有数据库访问权,**还没有这个页面**。 |

## §12 · DICT-ADMIN —— 字典维护那扇门(2026-08-23)

> **状态:✅ 已走过 —— 但第 1 步 ⛔ 卡在前置条件上。**
> **缺的那件东西,点名:一个【持有五张字典中任一编辑权、而【不】持有 `action.manage_permissions`】的账号。今天不存在。**
> 第 1 步要证明的正是这两个判据【不是同一个】—— 导航里「字典维护」那一项判前者,「设置」判后者。用一个两者都持有的账号(例如 admin)去看,两个判据都为真,
> **那一步会绿,而它什么都没有证明。** 所以它既不是"走过了",也不是"还没走":
> **它是【走不了】的,直到那个账号存在。**
> 造出那个账号本身是一个动作,归 `docs/forward-queue.md` 阶段 3 的【内部验收】那一节 —— 同一批账号发出去的时候顺手就有了。

**`/settings/dictionaries`。一个通用屏幕 + 按字典的五个小节(Tim 的裁定)。**

| # | 看什么 |
|---|---|
| 1 ⛔ | **⛔ 卡在前置条件上 —— 这一步今天走不了,见本节抬头。** **入口在**:顶部导航里多了「字典维护」。**它与「设置」是两项** —— 那一项判 `action.manage_permissions`,这一项判"这五张字典的权限任持其一"。用一个只有物料权限、没有 manage_permissions 的账号看一次:**那一项必须在**。**而那个账号今天不存在** —— 用 admin 走这一步会绿,但它证明不了这两个判据不是同一个。 |
| 2 | **第二条路**:化验页的「化验机构」下拉、物料页的「化学体系」下拉,底下各有一条直链过来。**人撞到墙的那一刻就在那儿。** |
| 3 | **加一个值**:code 只在新建时可填;填一个只差大小写的近重复(比如已有 `NMC` 时填 `nmc`),必须**按名拒**并把已有那个拼法说出来。 |
| 4 | **改一个值**:code 那一格是灰的,旁边写着为什么(改 code 等于改外键指向,那是数据迁移)。 |
| 5 | **规则布尔**:物料种类与安全状态那两节,规则列是**三态下拉**(请选择/是/否),不是裸勾选框;每个下拉底下有一句说明它到底管什么。不选就保存,要按名拒。 |
| 6 | **停用之前**:确认框里必须说出**有多少行带着这个值**,并说清它们不受影响、什么都不会被删。 |
| 7 | **停用之后**:那一行仍然在表里(灰底),按钮变成「重新启用」。 |
| 8 | **顺序**:改一个值的「顺序」再回到读它的那个下拉,位置要跟着变。 |
| 9 | **没有删除按钮** —— 这是结论不是遗漏:12 条外键全是 NO ACTION,在用的值数据库本来就删不掉,而没用过的值停用已经覆盖。 |
| 10 | **权限拒绝**:用一个两种权限都没有的账号打开这一页 —— 看见的必须是**一句"你不能编辑这里的任何一张字典"**,而不是一张空表。 |

## §13 · FIX-3 —— 三处只有人眼能确认的地方(2026-08-23)

> **状态:⏳ 欠着。** 没有人走过。

**为什么这三条进不了 fixture:** 两条是 CSS 排版(按钮宽度),一条是一行文字**摆在
屏幕的哪个位置**。fixture 是 SQL,冒烟只断言 HTTP 2xx 与服务端错误 —— 两者都看不见
"这个按钮被撑得像个输入框"或"这一行在网格的第四格所以没人看见"。
(`--reach` 也不行:它读 DOM,而这三条差别在**渲染出来的样子**上。)

### 13.1 采购单:两个禁用按钮保持自然宽度

去一张**已关闭**的采购单(`/purchasing/orders/<id>`):

* **「作废订单」** 被挡住时,按钮应当是**按钮的宽度**,下面另起一行是琥珀色的理由。
  *此前:列方向 flex 默认 `align-items: stretch`,按钮被那句长理由撑到与它同宽,
  读起来像一个文本框。*
* **「修订」** 在订单已关闭时同样被挡住 —— **这一个此前没有人报过**,而它是同一个
  毛病(B2 要找的就是它)。同样检查:按钮自然宽度,理由自己一行。

**两个都要看** —— 只看第一个的话,第二个会以同样的形状活下去。

### 13.2 资产卡片:投用状态在标题旁边,而且【只有一处】

去任意一张资产卡片(`/finance/assets/<id>`):

* 标题(资产编号 + 描述)**右边**应当有一个小框,写着三者之一:
  「尚未投用」/「计划 {日期} 投用」/「{日期} 起在役」。
* **下面那排数字应当只有三格**:成本 → 累计折旧 → 净值。
  *此前状态是那一排的第四格 —— 那一排是【钱】的刻意序列,状态不是钱,归错了类。*
* **确认屏幕上只有一处在说投用状态。** 这条是本刀最要紧的自检:FIX-2 刚在这块
  屏幕上拔掉过一份"同一句话说两遍、措辞还不一样"。搬家不该变成第二份。

三种状态最好各看一台机器:没有计划的、有计划的(未来日期)、已在役的。
**未来的日期绝不能配一个过去时的标签。**

### 13.3 保养间隔:一台【没有在监控】的机器说的是哪一句

去一张**没有任何保养间隔行**的资产卡片,看「保养间隔」那一块下面的诚实提示:

* 应当读到:**没有在监控任何保养间隔,所以从来没有量过任何一段时间里有几炉没归属**,
  因此这个数不是一句关于完整性的话。
* **绝不该**读到「自 {日期} 起每一炉都归属到了机器,所以这个读数对那段时间是完整的」。
  *此前 `?? 0` 把"没测过"(NULL)读成了"测过、是零",于是掉到了那句完整性宣告上。*

对照:一台**有**间隔行、且窗口内确实有未归属炉次的机器,应当读到"可能不完整:
这段时间里有 N 炉没有归属到任何机器"。**这两句必须是不同的两句。**

## §14 · SESSION-1c —— 认证这条路上只有人眼看得见的部分(2026-08-23)

> **状态:⏳ 欠着。** 没有人走过。

**为什么这一节几乎【整节】都在这里,而 fixture 里一条都没有:**
认证发生在数据库【之外】。`db/fixtures` 是 SQL,它够得到 RPC,够不到中间件、
够不到 cookie、够不到 `/login`。冒烟只发 GET 且断言 2xx,它看不出一张登录表单
有没有在说话。**所以这一刀的机器证据只有两样** —— `npm run build` 里那道棘轮
(`scripts/check-auth-error-swallowing.mjs`,四个分支都做过故障注入),
以及一次完整冒烟证明这些页面照常渲染。**其余全在这一节。**

> **说清楚而不是含糊过去:本刀没有 fixture,而这不是偷懒。**
> 一条断言不了任何东西的 SQL 臂,比没有那一臂更坏 —— 它会让下一个人以为
> 这块地面有人看过(AGENTS.md 那条"一个报告了却不拦的判词不是闸")。

### 走之前

`npx next dev -p 3000`,用任意一个真账号登录。**这一节【一行库都不写】** ——
它走的全是拒绝与渲染,可以放心在 live 上点。

### 14.1 · 会话【确立地】结束 → 登录页要说话

**怎么造:** 浏览器开发者工具 → Application → Cookies → 把
`sb-<ref>-auth-token` **删掉**(不是改坏,是删掉 —— 那是"真的没登录"那一支,
实测 `AuthSessionMissingError`)。然后点任意一个导航项。

* **应当看见**:登录页上一块**琥珀色**的说明,写着
  **「你的登录已经结束」**,下面一行「重新登录即可继续。上一页里打过的内容没有被保存。」,
  再下面一行小字告诉你登录后会回到哪一页。
* **不应当看见**:一张**什么都不说的登录表单**。那正是这一刀之前的样子 ——
  而一张沉默的登录表单,与一次崩溃在屏幕上长得一模一样。
* **接着登录。应当直接回到你刚才要去的那一页**,不是 `/suppliers`。
* **再故意打错一次密码**:两块提示应当**同时在**(「你的登录已经结束」在上,
  「邮箱或密码错误」在下),而且**回到哪一页那件事不能丢**。

### 14.2 · 【判断不出】要说判断不出,而且要一直这么说 —— **本节最要紧的一屏**

**怎么造:** 这一条要让认证服务够不着,而你的会话【完好无损】。
最干净的做法是在 `/etc/hosts` 里把 `<ref>.supabase.co` 指到 `127.0.0.1`
(走完记得删掉),或者用一个能拦截该域名的代理。**不要靠登出来模拟** ——
登出走的是另一支,这一节要的恰恰是"人没有被登出"。

* **应当看见**:一页写着 **「暂时确认不了你的登录状态」**,琥珀色区块里两句话 ——
  *系统联系不上认证服务,所以它【不知道】你是不是还登录着 —— 这【不是】说你被登出了*,
  以及 *你的会话很可能好好的。这一页什么都没有保存,也什么都没有丢。*
* **应当看见**一行小字写出**你本来要去的那条路径**,以及一个「重试」按钮指向它。
* **不应当看见登录表单。** 看到登录页就是这一条失败了 —— 那是把"我不知道"
  说成"你没登录",而这一刀的全部意义就是这一句。
* **看响应头**(开发者工具 Network):应当是 **503**,带 **`Retry-After`**。
  **200 就是缺陷** —— 一个 200 等于断言这次请求成功了,而它根本没能被判断。
* **【反复点重试,故障还在】** —— **每一次都应当是同一页、同一句话。**
  **它永远不许"熬"成登录页。** 系统确实不知道,它就应当一直这么讲。
  这一条要真的点五六次才算走过。
* **中英各看一次**(右上角语言开关会写进 `NEXT_LOCALE` cookie;这一页直接读那个
  cookie,不走 i18n 文件 —— 理由写在 `lib/supabase/middleware.ts` 里)。

### 14.3 · 导航条:空着 ≠ 你没有权限

**怎么造:** 与 14.2 同一个手法,但**故障要恰好只落在页面渲染那一次**,
而不是中间件那一次 —— 这条缝很窄,造不出来是正常的。
**造不出来就照实记「未能触发」,不要写成"看过了,没问题"。**

* **若触发了**:应当看见一条**只剩标题 + 一块琥珀提示**的导航条,写着
  「导航暂时显示不出来 / 刚才没能确认你的登录状态……这【不是】说你没有权限」。
* **不应当看见**:导航条**整条消失**。一条空着的导航条读起来是
  "这个系统没有你能用的东西" —— 而那是一句假话。

### 14.4 · 顺带确认两件本刀改过的小事

* **登录页现在是双语的**(此前是全站唯一一处硬编码中文的页面)。
  切换语言,四处都要跟着变:标题、副标题、两个字段标签、按钮、底部那句话。
* **标题写的是 `EVoltrya OS`,不再是 `SWM-OS`。**
  (实测全仓库 `SWM-OS` 只剩 3 处,全部是一次性测试账号地址 `admin@swm-os.test`
  ——那是一个真实标识,**不要改它**。)

### 一路上不应当出现的东西(看到就是缺陷)

* 任何一次**中途跳到登录页而没有那句「你的登录已经结束」**;
* 【判断不出】那一页**退化成登录页**(哪怕是在第十次重试之后);
* 那一页返回 **200**;
* **整条消失**的导航条;
* 登录页上**任何一句写死的中文**(把语言切成英文,那一页应当没有中文)。

### 残渣

**零。** 这一节不写任何一行库 —— 它走的全是拒绝与渲染。
`/etc/hosts` 改过的话记得改回来。

### 14.5 · 【从来没登录过】的人不该被告知"你的登录已经结束"

**怎么造:** 用一个**无痕窗口**(确保一个认证 cookie 都没有),直接敲一条受保护的
URL,例如 `http://localhost:3000/suppliers`。

* **应当看见**:登录页,**没有那块琥珀色的「你的登录已经结束」**。
  他从来没有过一个会话,所以那句话对他是假的 —— 与 14.1 那一条是同一条规矩的
  两个方向:**只说确立得了的原因**。
* **应当看见**:登录之后仍然回到 `/suppliers`(`next` 照常带,那一半与有没有过
  会话无关)。
* **对照 14.1**:同一条 URL,带着一个失效的 cookie 进来,那句话**必须在**。
  两次的差别只有 cookie,而屏幕上必须真的不一样。

## §15 · IMPORT-1 —— 批量导入,以及那个【我没能替你确认】的入口(2026-08-24)

> **状态:✅ 【全部走过】(FULLY WALKED,2026-08-24)—— 唯一的例外是第 1 步的
> 【反方向】那一半,它 ⛔【卡在一个前置条件上】(BLOCKED ON A PREREQUISITE),
> 而那个前置条件与 §12 第 1 步等的是【同一个第二个账号】。**
>
> **走过并通过的(Tim,2026-08-24 两次):**
> * ✅ 入口 · 导航项 · 模板下载链接 · 「还没有导入过任何东西」那句空状态;
> * ✅ **第 3 步** —— 登记号必填那句拒绝渲染正确,而且读起来是人话;
> * ✅ **第 4 步** —— **一次导入真的成功了**(IMPORT-2 之前它做不到);
> * ✅ **第 5 步** —— 重跑同一份文件,**整份被拒,撞上的编号被一个不落地点名**;
> * ✅ **第 6 步** —— 近重复**只警告**,引出了既有的拼法与它的编号,而且**要勾了才让提交**。
>
> **⛔ 第 1 步的反方向【仍然】走不了 —— BLOCKED ON A PREREQUISITE,缺的东西有名有姓:**
> **一个持某种编辑权、但【不】持 `action.bulk_import` 的账号。**
> **这是本节唯一一件没有走过的事**,其余全部走过(含 FIX-4 的再走查)。
> 今天线上有 `chef1949@126.com`(finance,不持该权限),所以"那一项不出现"这一半
> 其实验得了;**真正卡住的是与 §12 第 1 步【同一个】前置条件** ——
> 一个只持字典编辑权、不持 `action.manage_permissions` 的账号。
> 两节等的是同一批账号,而那批账号在
> `docs/forward-queue.md` 的【内部验收】那一节发。
>
> ### FIX-4 的再走查:**已走过,通过(2026-08-24)**
>
> **第 4/5/6 步在 FIX-4 之后由人重走了一遍,四条都成立:**
> * ✅ **提交钮留在原地** —— 提交成功之后它**没有消失**,而是**可见且禁用**,
>   并且**说出它禁用的理由:这个文件已经导入过了**;
> * ✅ **那句成功的话【活下来了】** —— 「已导入 N 行」不会被后续的状态变化抹掉;
> * ✅ **那个明确的「再导一个文件」控件管用** —— 它是一次明确的点击,不是猜;
> * ✅ **换一张表之后,上一份预览【没有诈尸】** —— 旧预览不会在新表下复活。
>
> **连带确认:** FIX-4 自己带的那个"只置上、不放下"的标志位不再复现 ——
> 第一次选表之后提交钮**没有**被永久禁用。
>
> **下面这段是走查【之前】写的提醒,留下来作为记录,它已经被上面那四条答掉了。**
>
> **⚠ FIX-4(2026-08-24)之后,第 4/5/6 步【要再走一遍】—— 这一节的 ✅ 是对着
> IMPORT-2 那一版界面盖的。** FIX-4 动的正是提交之后那一段:提交钮不再被换掉
> (留在原地、禁用、说出为什么)、多了一个「再导一个文件」、而"重来"是把整个
> 向导重新挂载。**走查发现的最后一条正是"提交成功之后那个按钮不见了"** ——
> 那一条修在这里,而修完的东西没有被人走过。
> **顺带,FIX-4 自己带了一个缺陷并在同一刀里修掉了**:一个只会被置上、
> 从来不被放下的标志位,会让提交钮在第一次选表之后【永久】禁用 ——
> 交付即是死的。它没有出现在任何一次走查里,因为它在走查之后才被写进去。
> **再走一遍的时候,第 4 步之后请多按一下「再导一个文件」,并确认换一张表之后
> 上一份预览【不会诈尸】。**
>
> **原本第 1 步那句「我确认不了入口」已经不适用了**,留在下面是因为它记着
> 这个仓库为"没有入口的页"付过四次账,而这一次是【人】把它确认掉的。
> 简报要求「**用手确认这一页的入口**」,而我确认不了 —— 我读得到代码,点不到屏幕。
> 我能说的只有:那一项是 `app/components/NavLinks.tsx` 里一条**静态**链接
> (判据 `canBulkImport`,即 `action.bulk_import`),所以它**不是** `[id]` 那种
> 走查结构性看不见的动态路由。**但"代码里有一条链接"与"人点得到它"是两句话**,
> 而这个仓库为这个区别付过四次账。

### 走之前 —— **文件的内容写在这里,不要靠口头**

**账号两个,都已经存在:**
* **admin**(`admin@swm-os.test`)—— 持 `action.bulk_import`;
* **一个不持它的**(`chef1949@126.com`,finance)—— 第 1 步的反方向要用它。

**先下载模板**(选「供应商」→ 下载)。它现在有**三行**:表头 · 必填标记 ·
**取值说明**。第三行以 `#` 开头,导入会跳过它 —— **原样传回来也走得通**
(六张表都验过)。

> **两件此前只在口头上的事,现在写在文件里与屏幕上:**
> ① `counterparty_type` 只接受 **`goods_supplier` / `forwarder` / `service_vendor`**;
> ② `supplies_goods` **不再出现在模板里** —— 它由数据库自己算,填了会被拒。

**四个文件,照这个填**(编号用 `ZZWALK15-` 开头,一眼认得出):

| 文件 | 给哪一步 | 内容 |
|---|---|---|
| **A** | 第 3 步(会被拒) | 2 行。`code`=`ZZWALK15-SUP-1`/`-2`;`legal_name` 随便;`country`=`SG`;`counterparty_type`=`goods_supplier`;**`tax_id` 留空**。其余列**全部留空** |
| **B** | 第 4、5 步 | 就是 A,把 `tax_id` 填上(两个不同的字符串,例如 `ZZWALK15TAX1`/`2`) |
| **C** | 第 6 步(近重复) | 1 行。**新**编号 `ZZWALK15-SUP-3`、**新** `tax_id`;`legal_name` 填 **`ACME BATTERY RECYCLING PTE LTD`** —— 与线上 `SUP-2026-0002` 的 `Acme Battery Recycling Pte Ltd` **只差大小写** |
| **D** | 第 7 步② | 只留表头(以及模板那两行),没有数据行 |

> **可选列请【真的留空】,不要填任何占位符。** 这一步本身就是在验 IMPORT-2 修的那件事:
> **空格子 = 没有填**,于是列的默认值生效(`supplier_types` 会落成 `{}`,
> `status` 落成 `draft`)。此前那个空格子被当成 NULL,于是整份文件被拒。

> **第 7 步③(「还没有导入过任何东西」)必须【最先】看** —— 第 4 步一提交它就不成立了。
> 与 §9 第 1 步的「未监控」同一种一次性。

### 步骤

1. **入口 —— 先看这一条,它是欠着的那一件。**
   * 顶部导航里**应当有「批量导入」一项**,与「字典维护」「设置」并列。
   * **不应当**只能靠敲 `/settings/import` 进去。
   * 用一个**没有** `action.bulk_import` 的账号(例如只有物料权限的)看一眼:
     那一项**应当整个不出现**,而直接敲 URL 应当看见一句**权限答复**
     (「你不能批量导入主数据」),**不是一张空页**。

2. **模板。** 选「供应商」→ 点「下载这张表的模板」。
   * 表头**应当**含 `code, legal_name, country, counterparty_type, tax_id …`;
   * 第二行**应当**在必填列下面写着 `required`;
   * **不应当**出现 `id` / `created_at` / `status` / `user_id` 这些列。

3. **一次会被拒的预览。** 把模板里 `tax_id` 留空,填两行,上传。
   * **应当看见**逐行的话:*第 N 行:导入一个往来户时【登记号必填】……*
   * **应当看见**那句全或全无的说明,**而且库里什么都没有变**。
   * **不应当看见** `IMPORT_TAX_ID_REQUIRED` 这串字母本身,
     也不应当看见任何 `violates check constraint`。

4. **一次成功的导入。** 补上登记号,预览 → 应当看见「N 行全部可以装进供应商」
   与那句「预览……然后整支回滚,现在还什么都没有写入」。点提交。
   * **应当看见**「已导入 N 行」,以及那句**序列已推进**的说明;
   * 下面「最近的导入」表里**应当多一行**,写着文件名、行数与编号范围。

5. **重跑同一个文件。** 再传一次同一份。
   * **应当看见**:整份被拒,并且**把撞上的编号一个不落地列出来**;
   * **不应当**看见"跳过了 N 行"之类的说法 —— 跳过与"我特意没动它"不许长得一样。

6. **近重复只警告。** 拿一个与既有供应商**只差大小写**的名字导一行(登记号用新的)。
   * **应当看见**琥珀色的一段,引出**既有的拼法与它的编号**,以及一个要勾的确认;
   * **不勾就点提交:提交钮应当是灰的**,旁边写着为什么;
   * 勾上之后**应当导得进去** —— 它是警告,不是拒绝。

7. **三种空,三句话。** 分别造出来看一眼,它们必须**互不相同**:
   ① 一个没有权限的账号打开这一页;② 传一个只有表头的文件;
   ③ 一个还没有导入过任何东西的库(「最近的导入」那一段)。

### 一路上不应当出现的东西(看到就是缺陷)

* 任何 `IMPORT_*` 的机器码、任何 `violates check constraint`、任何 `duplicate key`;
* 一个**按不下去又不说为什么**的提交钮;
* 导入之后**在界面上新建**一条同类记录时报编号冲突
  (那意味着序列没有被推进 —— fixture 124 钉的正是这一条);
* 「最近的导入」被读成一份**血缘**:它只回答"那个文件进去了没有"。

### 残渣

**你导进去的那些行是真的。** 走查建议用一两行、并用一眼认得出的名字
(例如 `ZZWALK15-…`),走完记进 `docs/known-wrong-until-cutover.md`。
**注意导入的行不会自己消失**,而这一刀**没有**建"撤回一次导入"的路
(见 `docs/import-scoping.md` 第七节)。

## §16 · IDLE-DRAFT —— 空闲超时与草稿留存,只有人眼看得见的那几处(2026-08-24)

> **状态:✅ 【已走过】(WALKED,Tim,2026-08-24)—— 唯一的例外是 16.6,
> 它 ⛔【卡在一个前置条件上】(BLOCKED ON A PREREQUISITE)。**
>
> **16.1 现在是【看见的】,不再是【推出来的】。** 这一节原本写着:关于"预警不会
> 毁掉正在填的表单",我能给出的只有一个结构性论证(IdleWatcher 是 `{children}` 的
> 兄弟、按钮不导航),而"这一步要的是看见"。**现在它被看见了** ——
> 半张填好的表单在预警出现、并按下「继续在这里」之后原样还在,页面没有重新加载。
> **那正是这一节说它需要的东西**,所以这一句从"论证"改成"观察"。
>
> **⛔ 16.6(换一个人登录,不该看见别人的草稿)走不了** —— 它需要
> **第二个真人账号**,而那正是 §12 第 1 步与 §15 第 1 步等的【同一个】账号。
> 三节等的是同一件事,而它在 `docs/forward-queue.md` 的【内部验收】(e)① 里。
> **草稿按 user id 隔离这件事本身在代码里是确定的**(键名含 user id,
> 取不到 user 就整个不存),但那与"看见它不出现"是两句话 —— 与本仓库为
> 「代码里有一条链接」与「人点得到它」付过四次账是同一条。
>
> **服务端那一半在建的时候就实测过了**(见下面「已经测过的,不必再走」)。

### 已经测过的,不必再走(2026-08-24,真的发了请求,不是读代码)

七个探针,对着真的 dev server + 真的会话:

| 探针 | 结果 |
|---|---|
| 没有活动标记(刚上线的人) | 200 —— **不会把所有人踢下线** |
| 活动 = 现在 | 200 |
| 活动 = 29 分钟前 | 200(没到 30 分钟) |
| 活动 = 31 分钟前 | **307 → `/login?reason=ended`** |
| **RSC 预取**,活动 = 31 分钟前 | **307** —— 预取【逃不掉】超时 |
| **RSC 预取**,活动 = 现在 | 200,而且**没有刷新活动 cookie** |
| 完全没有 cookie(从没登录过) | 307 → `/login`,**没有 `reason=ended`** |

**最要紧的那一条:七个响应里没有任何一个【刷新】过活动 cookie。**
中间件只读不写 —— 那正是"预取把会话永远续下去"这件事不会发生的原因。

### 16.1 · 预警真的会出来,而且【不毁掉正在填的表单】—— **本节最要紧的一屏**

**走之前**:打开一张长表单(建议 `/suppliers/new`),**把它填掉一半,不要提交**。
然后**什么都别碰**,等二十八分钟。

* **应当看见**:屏幕底部浮出一条琥珀色的提示,写着还剩多久(分:秒 倒数),
  以及一句「这一页什么都没有丢」。
* **它应当浮在当前这一页上**,不是把你跳到别处 —— 你填的那些字应当还在。
* 点「继续在这里」:提示消失,**而你填的字一个都没有变**,页面**没有重新加载**。
* **不应当**看见:整页刷新、表单被清空、跳到别的地址。

> **为什么这一屏是最要紧的:整件事就是为它存在的。** 预警如果把表单卸载掉,
> 那这个"预警"本身就是它要防止的那种工作损失。
> **我能给出的只有一个结构性的论证,不是一次观察**:`IdleWatcher` 在根布局里是
> `{children}` 的【兄弟】而不是包裹它的父节点,而「继续在这里」那个按钮只做两件事
> (记一次活动、把提示收起来),没有任何导航、没有 `router.refresh()`。
> **所以它【不可能】重新挂载表单** —— 但那是推理,这一步要的是看见。

### 16.2 · 三十分钟真的会到,而且落在那句对的话上

接着 16.1,这一次**不要点**「继续在这里」,再等两分钟。

* **应当**被带到登录页,并且看见 **「你的登录已经结束」**(SESSION-1c 那一屏);
* **不应当**看见一张没有任何说明的登录表单。

**另一半,换一个干净的浏览器(或无痕窗口)走**:直接敲一条受保护的地址,
例如 `/suppliers`。

* **应当**被带到登录页,而且**不说**"你的登录已经结束" ——
  一个从来没登录过的人没有会话可以结束。
  (这一条服务端已经实测过,列在上表最后一行;这里再用眼睛确认一次那句文案。)

### 16.3 · 草稿:被发现、被套用、被丢弃

在 `/suppliers/new` 填几个格子,**不要提交**,直接把标签页关掉。重新打开这一页。

* **应当看见**一条蓝色的提示:「发现一份 …… 存下的草稿」,**而格子应当还是空的** ——
  **草稿不会自己填进去**,那是一次预填穿着救援的外衣;
* 提示里**应当**写着草稿存在哪(这台设备的这个浏览器,72 小时);
* 点「套用这份草稿」:格子填上,**而那条提示不消失** —— 它改成
  「这些格子来自一份草稿,不是来自记录本身」,一直显示到提交为止;
* 点「丢弃」:提示消失,再刷新也不该再出现。

### 16.4 · 受限表单【在人动手之前】就说清楚它不留草稿

打开 `/hr/employees`(新建或编辑一名员工)。

* **在开始打字之前**就应当看见一句灰色的话:这张表单带着受限数据,
  草稿只保留到登出为止;
* **不应当**是"填完了才发现没留住"。

### 16.5 · 换一台设备,草稿不跟过去

在 A 设备上留一份草稿,到 B 设备上打开同一张表单。

* **不应当**看见任何草稿(它存在 A 的浏览器里);
* 那一句「只存在这台设备的这个浏览器里」应当已经把这件事说在前面了。

### 16.6 · 换一个人登录,不该看见别人的草稿

A 在这台设备上留一份草稿并登出;B 在同一台设备上登录,打开同一张表单。

* **不应当**看见 A 的草稿。草稿按 user id 隔离 ——
  把 A 打的字递给 B,B 一保存就等于把 A 的工作记在了 B 名下。

### 一路上不应当出现的东西(看到就是缺陷)

* 预警出现时页面重新加载、或表单被清空;
* 草稿【自己】填进格子,而没有一条提示说它是草稿;
* 一份套用过的草稿在提交之前那条提示就消失了;
* 受限表单上出现"草稿保留 72 小时"这种话(它不该保留那么久);
* 平白无故被登出(那说明活动没有被记下来 —— 请立刻报,并说明当时在做什么)。

### 残渣

16.3 会在浏览器存储里留下草稿键(`evoltrya:draft:v1:<user>:<form>`)。
走完点一次「丢弃」即可;或者清掉浏览器存储。**库里不留任何东西** ——
草稿从来不进数据库。

## §17 · GST-2 —— 单据开始携带税,只有人点得到的那几处(2026-08-25)

> **状态:🚶【Tim 走过一遍(2026-08-26),并且走出了三件事】—— 见下面的
> 「走出来的」。这一节因此【不再是 NOT WALKED】,但也还没有走完。**
>
> **线上的 GST 开关现在是【开着的】**(`gst_registered = true`,登记号在册)——
> 是 Tim 在这次走查里用 GST-3 的面板打开的,那一步本身就是 17.0 的验证。
> 下面每一格只在开关打开时存在,所以现在它们都在。
> **走完之后要按顺序关回去**(先清理单据,再关),理由见「前置与收尾」。
>
> ### 走出来的三件(2026-08-26)
> 1. ~~**双语字符串是【拼起来】的,不是按界面语言选的**~~ —— **已修(GST-FIX-3,2026-08-26)**。
>    六处全部改成按 locale 选一个(与仓库里另外一百多处同一个写法),
>    **并加了一条棘轮**:`scripts/check-bilingual-concat.mjs` 进构建、基线 0,
>    第七处会让【构建】变红,而不是等下一次走查。
>    **F5 的格名也跟随语言 —— 这是一次裁定**:格【号】才是标识(它自己一列),
>    标签是确认用的说明;需要 IRAS 逐字措辞的是【把数字敲进 myTax Portal】那一步,
>    而那用的是 CSV 导出 —— 那里 Label (EN) 与 Label (ZH) 是【两列】,逐字措辞一直都在。
> 2. **F5 的钻取"点了没反应"** —— 见 17.10。**第二次走查时它是好的,但原因【从未查明】** —— GST-FIX-1 给过一个解释、被 GST-FIX-2 证伪,而唯一能定案的那次观察(加锚点【之前】的客户端点击)没有人做过,现在也做不成了。记作【症状消失】,不是【已修复】。
> 3. **贷项凭证在这套单据上根本走不到** —— 见 17.14。
>
> ### ★【这一节此前【不是一份走查脚本】—— 2026-08-25 更正】★
>
> 它原本写着「先在数据库上把 `finance_settings.gst_registered` 打开」。
> **那句话把一份手走清单变成了一份需要 psql 提示符的清单** —— 而这份文件
> 存在的全部理由,是问"操作员在屏幕上读到的那句话是不是人话"。
> 一个要求走查者先去开数据库连接的步骤,已经不是这个问题了。
>
> 更坏的一层:**当时 app 里根本没有那个控件。** GST-1 建了机器、GST-2 接了
> 单据,而 `gst_registered` 在 `app/` 里只被读过、一处也没有被写过 ——
> 于是这一节**整节 100% 走不通**,两刀的成果一个人也碰不到。
>
> **而这件事不是任何一道检查发现的,是 Tim 发现的。** 构建绿、闸绿、
> 冒烟 188 条路由全过 —— 因为每一条都只问"渲染得出来吗",
> 没有一条问"这条路走得通吗"。判据记在 `docs/known-issues.md` 的
> **GST-NO-DOOR** 条(本仓库第五次"库建全了、门没有")。
>
> **GST-3(2026-08-25)建了那扇门。下面的步骤据此重写。**
>
> ### 前置与收尾 —— 都在屏幕上做,不碰数据库
>
> **开:** `/finance/settings` → GST 面板 → 填【GST 登记号】(任意一串,
> 例如 `M9-TEST-1`)→ 按「打开 GST」。**登记号是硬前置**:留空时按钮是禁用的,
> 而且数据库那一侧也会按名拒(IRAS 要求税务发票印它,而发票 PDF 在号码为空时
> 那一行整条消失 —— 不是拒绝出票,是安静地不印)。
>
> **关(走完之后):** 同一个面板按「关闭 GST」。
> ★**顺序只有一个方向成立**★ —— 先把这一节里造出来的单据【作废 / 冲销掉】,
> **趁开关还开着**,再关开关。反过来做不到:关掉之后,带税码的费用单
> 【再也冲销不了】(`GST_NOT_REGISTERED`),而那正是关闭那两条拒绝挡住的事。
> 拒绝会**点名**是哪几张单据挡着路。

### 为什么这几处只有人眼能确认

fixture 129 已经从数据库那一侧钉住了全部规则(十二臂,六次故障注入各自变红)。
它够不到的恰恰是这一节要问的:**那句拒绝到了屏幕上是不是人话,那个默认值
是不是【看得出它从哪儿来】。** 一个悄悄默认的税码在 SQL 里与一个明说的默认
长得一模一样 —— 分得开它们的只有屏幕。

| # | 走哪儿 | 要看见什么 |
|---|---|---|
| 17.0 | `/finance/settings` | GST 面板本身。**关着的时候**:一句"尚未注册"的说明,三条【后果】印在按钮**上面**(单据开始带税码 / F5 开始推导 / 没设默认税码的往来对象会按名拒),登记号栏为空时按钮**禁用**且紧邻一行字说明为什么。这一格是 GST-3 的全部内容 —— 在它之前,这一页只读一列 `locked_before`。 |
| 17.0b | 同上,登记号留空点按钮 | 点不下去(禁用)。若绕过前端直接提交,数据库按名拒 `GST_REGISTRATION_NO_REQUIRED` —— 而屏幕上要出现那句**人话**,不是一串管道分隔的码。 |
| 17.1 | `/customers/<某个客户>/edit` | 多出一格**默认税码**,初值是「—— 未设 ——」。下面那句话要说清楚**留空不等于没有税**。选一个 SR 存下来。 |
| 17.2 | `/suppliers/<某个供应商>/edit` | 同上,但只列**进项**码(TX/ZP/EP/BL/OP)。销项码根本不出现在下拉里 —— 一个能选到拒绝的下拉是在把人骗去撞墙。 |
| 17.3 | `/finance/invoices/new`,选 17.1 那个客户 | 税码那一格**自动填成 SR**,并且底下有一句「这个客户的默认:SR」。**那句话是这一格的全部意义** —— 没有它,这个 SR 看起来就是凭空出现的。 |
| 17.4 | 同上,把客户换成一个**没设默认**的 | 税码回到空,底下变成琥珀色的一句「这个客户没有默认税码,必须在这里选一个」,**提交按钮是禁用的**。这一格要证明的是:它宁可拦住你,也不替你猜。 |
| 17.5 | 同上,把开票日改成 **2022 年的某一天** | 合计里的税率变成 **7%**,不是 9%。这一条是"税率按【单据自己那一天】解析"在屏幕上的样子 —— 改日期,税跟着变。 |
| 17.6 | 开出那张票,再打开它 | 税额进了总额;`/finance/invoices/<id>` 上看得到。 |
| 17.7 | `/finance/expenses/new` | 税码那一格,**并且金额栏下面那句「请填不含税净额」要在**。这一句是这一节最容易被漏掉、也最贵的一句:没有它,人会把供应商账单上的总额填进去,而系统会在它上面再加一次税。 |
| 17.8 | 同上,选一个 **BL** 码 | 下拉里那一项后面写着「税不可抵」。选它、存下来,再看 F5:采购额进了 **box5**,而 box7 **没有**动。 |
| 17.9 | `/finance/gst` | 顶上那句"还没有任何一张单据带税码"的横幅,在 17.6 之后**应当消失**。 |
| 17.10 | `/finance/gst/<期间>`,点任意一格的「打开」 | ★**这一格的结论改过两次,三个版本都留着 —— 这一节里第二处这种形状**★ **① GST-FIX-1 说**:钻取没坏,只是反馈不明显(它看到的是一段【正确的空】)。**② GST-FIX-2 证明①是错的** —— 那次观察发生在发票作废【之后】,而 Tim 点的时候 box1 是 1143.00;重建等额发票后实测:`f5_box_detail` 各返回 1 行,服务端渲染在【当前版本】与【他当时那一版】上都正确。**③ Tim 第二次走查(2026-08-26,INV-2026-0009 在册)**:地址栏变了、页面跳到了钻取那一段、那一段列出了这张发票 —— **它现在是好的**。★但这【不是】「查明了」★:中间隔了两刀,而【加锚点之前】的那次客户端点击**从来没有人观察过**。记作**症状消失、原因未查明**(`docs/known-issues.md` 的 GST-DRILL-UNEXPLAINED)。**今后走这一步要记的四件事**:① 地址栏变了吗 ② 有没有发出 RSC 请求 ③ 那一段出现了吗 ④ 视口动了吗。 |
| 17.11 | 作废一张带税发票,再回到 F5 | 作废时**要出现「冲销日」那一栏**(带税的发票有一张只过税的分录要冲)。**GST-3 之前那一栏不出现** —— 判据当时写的是「是不是订单流发票」,而 GST-2 让带税的 sale 发票也过分录了,于是这种发票在屏幕上【根本作废不了】,连带把关闭开关那条路也堵死。作废之后它从 box1 里消失,而勾稽仍然是 ✓✓ —— 两边一起走。 |

> **17.12(可以走,但属于另一条路):** "先收款、后开票"那条路
> 在已注册时被按名拒绝。屏幕上要看到的是那一整句话(它解释了为什么拒绝、
> 以及正确的顺序是先开票再收款),**不是一串 `GST_UNALLOCATED_RECEIPT_UNSUPPORTED`**。
> 走它需要一个客户与一笔收款,而这两样在测试库里都有 —— 所以它**可以走**,
> 只是它属于 `/finance/payments` 那条路,不属于上面这张表。
>
> **17.13(关闭那一侧的两条拒绝 —— 值得专门撞一次):** 在还没有清理单据时
> 按「关闭 GST」。应当看到**点了名**的一句人话:带税码的费用单那一条
> (它说的是"关掉之后就冲销不了了,先冲销再关"),或者在册带税发票那一条
> (它说的是"账上会报着供应额,而公司声称那一季未注册")。
> **两条是不同的码、不同的句子** —— 一条是机械事实,一条是判断,不该合并。
>
> ### ★【17.14 · 贷项凭证 —— 这一节此前【根本没有这一步】,而 Tim 撞上了它】★
>
> **走不到,而且这不是缺陷。** 贷项凭证的门是齐的:`/finance/credit-notes`
> 在财务导航里,有列表页、详情页、PDF,开票页上有「开贷项凭证」的控件。
> **但 `create_credit_note` 只收 `kind='order'` 的发票**(`CN_INVOICE_NOT_ORDER_KIND`),
> 而这一节 17.3–17.6 造出来的是 `kind='sale'` 的发票 —— 于是那个控件不出现。
> 一张 sale 发票的更正走的是【作废】(17.11),不是贷项凭证。
>
> ⚠ **后果要写下来:GST-2 给贷项凭证接的那套税(税码与税率【从被冲的那一行抄来】、
> 税额借 2100、在 F5 上是一笔【负的供应】)—— 没有任何一次人手走查碰过它。**
> fixture 129 的 K2 臂从数据库那一侧钉住了它,并且做过故障注入;
> 但"那句拒绝到了屏幕上是不是人话""那个负数在钻取里印得对不对",
> 仍然**一个人也没有看过**。
>
> **要验它需要先有一张订单流发票**:建销售订单 → 确认 → `/sales/orders/<id>`
> 开票(那张票是 `kind='order'`)→ 回到 `/finance/invoices/<id>` 开贷项凭证 →
> 再看 F5 的 box1/box6 是否【减下去】、钻取里那一行是否是【负数】。
> 那是一条比本节其余步骤都长的路,所以它单独列出来,而不是塞进上面那张表
> 假装它和别的步骤一样便宜。

---

## §18 · EQP-PAY-1 —— 设备里程碑与质保金,只有人点得到的那几处(2026-09-01)

**状态:未走过。** 下面每一条都写清了【为什么自动化够不到】,
以及其中哪几条**今天根本走不了、以及为什么不该硬走**。

### 18.1 设备单的里程碑下拉里【没有】after assay(R5 的屏幕那一半)

**走法:** `/purchasing/orders/new` → 上方种类选 **设备** → 付款计划加一期 →
展开里程碑下拉。

**要读到的:** 下拉里是 **下单时 / 装运时 / 到货时 / 安装完成 / 验收合格 /
培训完成 / 固定日期** —— **没有「化验后」**。切回 **材料**,「化验后」回来,
而安装/验收/培训三项消失。

**为什么冒烟够不到:** 下拉的选项由 `orderKind` 这个**客户端 state** 过滤,
而冒烟只发 GET、读服务端渲染的 HTML —— **点一下单选钮之后的样子它看不见**
(与 CAPEX-1 记下的那条"藏在客户端开关后面的文案冒烟看不见"是同一条)。

**注意它【不是】控制:** 服务端另有两道独立的拒绝(`create_purchase_order` 按名拒 +
表上的触发器),`db/fixtures/176` 的 A / C 两臂钉着它们。这一条走的是**体贴**那一层。

### 18.2 切换种类时,已选的里程碑被换掉了 —— 而屏幕【说出来了】

**走法:** 种类留在 **材料** → 加一期、里程碑选「化验后」→ 把种类切成 **设备**。

**要读到的:** 付款计划上方出现一句琥珀色的提示:
**「有 1 期的付款里程碑在这一类采购单上用不上,已经改掉 —— 请复核。」**
而那一期的里程碑已经变成「下单时」。

**为什么这一条值得单列:** 悄悄改掉一个人已经选好的东西,与在提交时才拒绝他,
**是同一个毛病的两面**。这一句话是那个毛病的解药,而**没有任何检查看得见一句话在不在**。

### 18.3 ★【有质保金】与【没有质保金】在屏幕上分得开 ★(验收标准的一条)

**这一半已经【做成机制】了 —— 不必用人走。** `scripts/smoke-routes.mjs` 新增了一条
质保金栏探针:它自己按条件挑出那张【带设备行】的采购单,再按那张单**今天有没有
质保金**选针 —— 没有就必须印出那句明说的话,有就必须印出标题。
针从 `messages/en.ts` 里现读(与现金预测那条同一个惯用法),所以改了文案它跟着走;
线上一张设备单都没有时它**跳过并说出来**,而不是悄悄变绿。

**它替掉的那次人走是:** `/purchasing/orders/PO-2026-0007`(线上唯一一张设备单,
**没有**质保金)→ 滚到付款计划下面 → 读到
「这张单没有质保金条款 —— 这里的机器一分钱都不扣留。」**不是一片空白,不是一个 0%。**
**人仍然值得看一眼的是【它读起来像不像人话】**,而那正是这份文件存在的理由;
探针只保证那句话**在**。

**今天走不了的那一半,以及为什么不硬走:** "带质保金"那一侧要新建一张设备采购单,
而**线上是共享库、厂子没开工,已有的测试残留正是在册的问题之一** ——
为了走一遍清单往里塞一张假单,是拿一个更贵的问题换一个便宜的确认。
它由 `db/fixtures/177` 的 **H 臂**盯着:同一次调用里建一张带、一张不带,
断言前者在状态视图里有 1 行、后者**一行都没有**。
**★ Tim 下第一张真的带质保金的设备单时,这一条要顺手走一遍 ★** —— 那一天它是免费的。

### 18.4 到期【提示】而不【付款】,以及部分扣留写得下理由

**今天走不了。** 要让一条质保金进入「已到期 · 待确认」,得有一台机器的**验收日在
13 个月前**,而线上两台机器**都还没有验收日**(`in_service_date` 也都是 NULL —— 厂子没开工)。

**它由 fixture 钉着:** `db/fixtures/177` 的 B 臂(到期只改状态、
`released_at` 仍是 NULL——**没有任何应付因此成立**)、D 臂(放 30,000 / 扣 10,000 +
理由记得下来;不说理由被拒;加起来对不上被拒;重复放款被拒)。四条都做过故障注入。

**那一天要走的:** 卡片上出现的是**一个问题**(「请确认这台机器在质保期内没出过毛病,
并写明放多少、扣多少」)+ 两个空的金额框 + 一个理由框,
**不是**一个「放款」按钮。两个金额框**都不许有预填值** ——
预填一个"全额"等于替人做了那个判断。

### 18.5 单据 PDF 上的介词短语

**走法:** `/purchasing/orders/PO-2026-0007/pdf`。

**要读到的:** 第 3 期印的是 `— after assay`(**不是** `on post assay`,更不是 `on on ...`)。
这一句现在来自字典的 `phrase_en`,不再来自 PDF 里那张手写映射。
**冒烟看不见 PDF 的正文**(它只断言 2xx),而这句话正是 EQP-1c-b-fu2 那次
「on on shipment」修复所在的地方 —— 同一处又改了一次,所以要再看一眼。

### 18.6 CFO 的两个新权限(第一项)

**今天走不了,而这是一条【记录在案的】阻塞,不是遗漏。** 唯一持 `cfo` 的是 Tim,
而他同时持 `admin` —— **`admin` 把它整个盖住了**,所以以他的身份登录看不出任何区别
(`docs/approvals.md` §0 早就写着这条,并且写着它的失效条件:"第二个人拿到 cfo 那天")。

**已经做过的替代:** 一次**只持 cfo** 的会话探针(回滚掉),实测:
`module.finance.view` ✓ / `data.view_pay` ✓ / `payroll_lines_masked` 读到 1 行且
`gross_pay` 可见(不是遮蔽的 NULL)/ `gl_control_reconciliation` 与
`management_pack_data` 都 OK;而 `module.finance.edit`、`module.hr.view`、
`action.manage_permissions` **都不在**。
**★ Tim 把第二位管理员授出去之后,这一条要以那个人的身份走一遍 ★。**

---

## §19 · CHART-1 —— 图表与组织架构,只有人眼能确认的那几处(2026-09-03)

**为什么这一节比平常长:** 本刀的产出【全部是屏幕上的形状】,而这套系统的
自动检查**一条都不看形状**。冒烟断言 2xx、构建断言键与类型、fixture 断言库的行为 ——
**没有任何一条能回答"这张图看起来对不对"**。所以下面每一条都点名它为什么只能由人走。

### 19.1 组织架构图:那句「还没有记录汇报关系」

**走法:** `/hr/org`(人力 › 组织架构)。

**要读到的:** 一句明说「**还没有记录任何汇报关系**」的话,后面跟着一份人名列表,
**外加一句「这不是在说他们都直接向公司汇报」**。

**要确认【没有】的东西:一棵把 N 个人平铺成一排的树。** 那正是 CHART-0 §三-C
点名禁止的画法 —— 屏幕会因此说出一句这套数据没有资格说的话。
**代码上它由 `hasStructure` 那个分支拦着**,而"这个分支渲染出来好不好看"
只有人看得见。

**顺带确认一件会让人吓一跳的事(不是缺陷):** 名单里会出现
**4 个 `ZZ-2BL-*`** 与(冒烟正在跑时)**3 个 `ZZ-SMOKE-*`**。
它们是走查账号与冒烟临时行,`docs/known-wrong-until-cutover.md` 记着,
**本页刻意不过滤** —— 一个按命名规则悄悄藏行的页面比多几行坏得多。
出处那一栏已经写着「测试数据」。

### 19.1b 【入口】人力菜单里真的多了「组织架构」这一条 —— **只有人点得到**

**走法:** 顶栏点开【人力】,看菜单里有没有「组织架构 / Organisation chart」,点它。

**★ 为什么这一条【必须】由人走,而不是"顺手看一眼" ★**
`/hr/org` 与它的 9 个同级页一样,**唯一入口是顶栏那个二级菜单**,
而那个菜单是**点开才渲染的**(`ModuleBar` 的 `{isOpen && …}`)——
`--reach` 结构上看不见它(AGENTS.md 记着这条盲区,NAV-CLEANUP-1 ② 还把它扩大了)。

**实测确认过这不是本刀引入的形状:** 全仓搜索 `/hr/kpi`,
除了 `lib/modules.ts` 那一行注册表条目之外**一个入站链接都没有** ——
也就是说人力那一族的每一页本来就是这样进的。
`scripts/check-nav-routes.mjs` 已经验过「注册表有这条 · 路由存在 · 判据放行」,
**它答不了的是"这一条画在菜单上、而且点得动"**。

> **顺带一条给下一个跑 `--reach=admin` 的人:** 那一跑的
> `EXPECTED_UNREACHABLE.admin` 只列了 `/login` `/set-password` `/welcome` 三条,
> 而 admin 那一跑最后一次实测是 **2026-08-11** —— 早于 KPI-1(08-29)与本刀。
> 所以它很可能把 `/hr/kpi`、`/hr/org` 等**二级菜单专属**的页报成"走不到"。
> **那是爬虫看不见二级菜单,不是这些页没有入口。** 真跑之前先读这一段,
> 否则会去修一个不存在的缺陷。**本刀没有跑 `--reach`**(admin 单角色实测 63 分钟,
> 而本刀没有动导航判据、没有动权限码,只加了一条注册表条目)。

### 19.2 手机上的那一份(390px)—— **两种渲染只有人能同时看见**

**走法:** 手机开 `/hr/org`;再在桌面浏览器把窗口拖窄过 `md`(768px)断点。

**要读到的:** 窄屏是**缩进列表**,宽屏是 **SVG 树**。

**★ 为什么只能人走:两块【都在 DOM 里】,靠 CSS 显隐 ★**
`fetch` 一次就把两份都拿到了,所以**任何基于 HTML 文本的断言都分不出
"手机上看得见哪一份"** —— 它对两份都为真。AGENTS.md 记过这个形状
(`sm:hidden` 的链接对爬虫可见、对人不可见),这里是它的良性版本,
但**判据同样够不着**。要看它得有一个会改视口的浏览器,本仓库没有。

### 19.3 汇报环的告示 —— **今天【造不出来】,而这本身是结论**

**走法:今天走不了,而且这是一条【记录在案的阻塞】,不是遗漏。**

`employees` 上有 `trg_employees_no_manager_cycle`(`guard_manager_cycle`),
**它在写入那一刻就拒了 A→B→A 与自环**(`MANAGER_CYCLE`)。
所以**通过界面造不出一条汇报环**,那块红色告示在真实操作下渲染不出来。

**已经做过的替代,两侧各一:**
* **库那一侧**:`db/fixtures/186` A 臂 —— 环与自环各拒一次,按名断言 `MANAGER_CYCLE`;
  故障注入(`DISABLE TRIGGER`)当场变红。
* **推导那一侧**:`scripts/check-org-tree.mjs` ⑥/⑥bis/⑥ter/⑦ ——
  环被报出来、不进 roots、一个人都不丢、挂在环下面的人仍然渲染、两条环分别报。

**那一天要走的(有人直连库写坏一行之后):** 页面顶部一块**红框**,
写着「发现汇报环 —— 这是一处需要有人去改的数据错误」,底下按汇报方向列出环上的人,
**并且环上的人一个都没有消失**。

### 19.4 账龄横条:90+ 那根的颜色,以及它与上面那条汇总条对不对得上

**走法:** `/finance/receivables`。

**要读到的:** 汇总条(四个数字)下面多了一张横条图,**四根条**,
**90+ 那根是【红的】,其余三根是【蓝的】**,每根右边的金额
**与上面汇总条里那个同名的数字逐字相同**。

**为什么人走:** "两处显示同一个数"是这个仓库反复付账的形状,而
**这一次两处读的确实是同一个 `report.buckets`**(代码上是同一个对象)——
所以要人确认的不是数值,是**它们看起来是不是一致**(格式、货币符号、千分位)。

### 19.5 流水构成:零的那几类【还在】

**走法:** `/inventory/reports`,页面底部。

**要读到的:** 12 根条,按条数**倒序**,**条数为零的类型仍然列着**(条长为 0)。
**要确认【没有】的:把零的类型滤掉。** 滤掉之后,读者就再也看不出
"某一类至今一条都没有" —— 而那恰恰是这张图能说的最有用的一句话之一。

### 19.6 两条既有进度条换了颜色 —— 只有人能确认"没变难看"

**走法:** `/purchasing/orders`(收货进度)与
`/finance/bank/statements/<id>/reconcile`(对账进度)。

**要读到的:** 进度条从 Tailwind 的绿/蓝/灰换成了品牌色 ——
**满格是森林绿、进行中是海洋蓝、槽是品牌灰**。
**要确认的是它在真实数据上仍然读得出来**(对比度、在表格行里不喧宾夺主)。
**没有任何自动检查看颜色**;`check-css-declarations` 之类看的是写法不是观感。

### 19.7 `/settings/dictionaries` 的**速度**(这一条 Tim 最该自己走)

**走法:** 部署之后打开 设置 › 字典,**掐表**。

**要读到的:** 它**几秒之内**打开,而不是 30–40 秒。

**为什么必须人走:** 本刀量的是 **dev server 上的墙上时间**(24,905 ms → 见报告),
而 **Tim 报的 30–40 秒是【线上】的**。两者是两台机器、两条链路。
**改动前后的比值**在 dev 上是可比的,**绝对值不是** ——
所以"线上到底变成几秒"这个问题,只有在线上掐表才答得出来。
**页面内容应当逐字不变**(五张字典、每行的用量数字都和以前一样)。

---

## §20 · TOOLS-1 —— 只有人走得了的那几处(2026-09-03)

### 20.1 ★ 最要紧的一条:一个【零权限】的账号,第一次登录看得见什么 ★

**走法:** 发一个只有 `employee` 角色的账号,用它登录,**什么都别点,先看 dock**。

**要读到的:** dock 上**至少有一条**,而且那一条是「单位换算」。

**为什么只有人走得了:** `npm run check:dock` 算得出"每个角色至少一条"
(而且它会为 0 变红),但它**算不出那一条在屏幕上长什么样**——
图标有没有、标签会不会被截断、点下去是不是真的到得了。
而这恰恰是本刀最该被人看一眼的地方:**它是六位同事第一眼看见的东西。**

> **本刀实测的数(算出来的):** employee 的默认 dock = 1 条(`/tools/converter`)。
> 收窄金属行情之前是 1 条(`/metal-prices`),**如果不补换算器就是 0 条**。

### 20.2 金属行情:那五个角色现在会读到一句【具名的】受限

**走法:** 以 `operations` 或 `warehouse` 身份打开 `/pricing/metal-prices`。

**要读到的:** 一块琥珀色的受限说明(`data-access-denied`),**不是空白页,也不是 404**。
**要确认【没有】的:** 一张画得出来但一行数据都没有的表 —— 那是 OPS-15 点名的那种谎。

**为什么只有人走:** 冒烟以 admin 身份跑,它永远走的是【通过】那一支;
`--reach` 结构上看不见二级菜单。逐角色的谓词已由 `db/fixtures/188` C 臂钉住
(admin true / employee false,并做过故障注入),但**"拒绝那一屏长什么样"要人看**。

### 20.3 定价的第三级:菜单里"定价"出现两次,读起来别扭吗

**走法:** 顶栏点开【工具】。

**要读到的:** 二级四条(任务 · 日历 · 单位换算 · 定价),而**下面还有一个
标着「定价」的分组标题**,里面是公式 / 计算器 / 金属行情。

**这是一处【我拿不准】的地方,所以交给人看。** 财务不会这样:它的落地页标签是
「概览」,所以没有重字。这里 Tim 的委托点名二级第四条就叫「定价」,
而分组标题也叫「定价」。**两者都对,合起来可能读着重复。**
要改的话有两个方向(把落地页标签改成「概览」,或者把分组标题去掉),
**本刀不替它选。**

### 20.4 手机上的日历(390px)与换算器

**走法:** 手机打开 `/tools/calendar`;再把桌面窗口拖窄过 `md`(768px)。

**要读到的:** 窄屏是**按日期分组的列表,只列有事的那几天**;宽屏是七列网格。
**两块都在 DOM 里**(靠 CSS 显隐),所以**任何基于 HTML 文本的断言都分不出
手机上看得见哪一份** —— 与组织架构图同一条限制。

**顺带确认一件本刀顺手修好的旧账:** 宽屏那张网格的**列现在与真实星期对齐了**。
旧的 `/hr/leave/calendar` 把 1..N 号直接铺进 7 列、**没有前导空格**,
于是 1 号永远落在第一列,不管它其实星期几 —— **那张月历的列从来没对过**,
而没有任何东西会说。搬到共享组件上之后它自己好了。
**请在 `/hr/leave/calendar` 上也看一眼**,确认请假与假期还在原来的日子上。

### 20.5 换算器的三档,尤其是湿转干

**走法:** `/tools/converter`。

**要读到的:** 三块卡片,每一块下面都印着**算式**与**这些数字的出处**。
湿转干那一档按【换算】按钮之后给出重量与含量两个数。

**要确认的两件:**
1. **它与结算对得上。** 湿转干调的是结算调的同一支数据库函数,取整 4 位也来自结算。
   真要核对,拿一份有水分的化验单按同样的数走一遍。
   **注意线上今天没有素材**:`sales_settlements` 0 行、带水分的化验 0 行(实测)。
2. **水分填 100 会被按名拒**,而不是给出一个无穷大或者一个 0。

### 20.6 执照页的标题

**走法:** 采购 ▸ 执照(菜单条目与页面抬头现在都是「Licences / 执照」)。

**要读到的:** 描述行现在开头就说 **"THIS COMPANY'S OWN licences — not a supplier's"**
(中文:「【本公司自己】的执照 —— 不是供应商的」)。
**长标题扛着的那个区别搬到了这里**,而这一行本来就在屏幕上(LicencePanel.tsx:86)。
要确认的是:**只读标题的人会不会仍然误会**。那是一个只有人能下的判断。

---

## §21 · CONV-0 —— 拒绝态收敛与六件走查,只有人眼能确认的那几处(2026-09-03)

**状态:⏳ 欠着** —— 一处都没有人走过。下面每一条都**不是**「大概没问题」,
它的意思是**没人看过**。

这一刀改的东西有一个共同点:**闸门、构建、类型检查全都答不了它们。**
「一枚药丸在右对齐的金额列里会不会把列撑歪」「一张变矮的条图还读不读得出构成」
「一句中文在供应商眼里是不是人话」—— 这三类都只有人能答。

### 21.1 ★ 那枚药丸放进金额列里,列还对不对得齐 ★

**这是本节最要紧的一条,也是最可能出事的一条。**

`MaskedValue` 此前是一行灰斜体「受限」,现在是一枚**带边框、带内边距、
`whitespace-nowrap` 的药丸**。它出现的位置**绝大多数是右对齐的等宽金额列**。
一枚药丸比两个字宽出 `px-2` 两侧 + 1px 边 + 圆角,而且它是 `inline-flex`。

**走法:**用一个**看不到金额**的角色(auditor 看得到大部分,**建议用
`operations` 或 `warehouse`**)逐个打开:

* 采购 ▸ 采购单**列表**(`/purchasing/orders`)—— **列表页风险最高**,一列里
  几十行,只要有几行是药丸、其余是数字,列宽会被最宽的那个撑开;
* 采购单**详情**与它的**质保金**面板;
* 收货 ▸ 某一张进料 ▸ 编辑 —— **到岸成本**与**计价**两个面板;
* 收货 ▸ 化验详情;
* 运营 ▸ 加工单详情与它的**成本**面板;
* 首页看板。

**要判断的:**
1. 行高有没有跳 —— 一行有药丸、下一行是纯数字,两行等不等高;
2. 金额列的**右边缘还对不对齐**;
3. **390px 手机上**药丸会不会把表格撑到横向溢出(`whitespace-nowrap` 不换行);
4. 最要紧的一条主观判断:**它现在读起来像「一句答复」,还是像一个装饰性的标签?**
   这一刀的全部理由是「受限不是零、也不是没填」。**如果它读起来像个标签,
   那这一刀没有达到目的** —— 那是一个只有人能下的判断。

### 21.2 `ActorName` 三种状态并排看

**走法:** 设置 ▸ 已删除记录(`/settings/deleted`)—— 线上 16 行里 14 行属于
「未记录」那一类,是同屏看到多种状态的最好位置。再看 任务 ▸ 某个任务 ▸ 变更历史。

**要读到的:**
1. 「未记录」现在是**药丸**,而它后面那段小字来由 **在药丸外面**(不是药丸的一部分);
2. 「受限」是**同一枚药丸**(用没有人事权限的角色看);
3. ★「该账号未关联员工档案」/「这份档案已经不在了」**仍然是普通灰字,不是药丸** ★
   —— 这是**刻意的**:它说的是数据的状态,不是你的权限。
   **要判断的是:并排放在一起时,读者分不分得出这两类是两回事。**
   如果分不出来,那说明这个区别需要比「有没有药丸」更强的表达。

### 21.3 三页整页拒绝,以及那条新出现的「回首页」

**走法:** 用一个**没有**对应权限的角色直接敲 URL:
`/settings/accounts`(要 `action.manage_permissions`)· `/settings/import`
(要 `action.bulk_import`)· 任意一个模块页(例如 `employee` 角色开 `/finance`)。

**要读到的:** 三页长得**一模一样**,并且**三页现在都有「回首页」**
—— 后两页此前没有。**要判断的:**那条链接在设置页上读起来是不是多余
(它是合并时收到「有链接」那一边的结果,而那是一处**可见的**选择)。

### 21.4 流水构成图变矮了 —— 还读得出构成吗

**走法:** 库存 ▸ 报表(`/inventory/reports`),拉到底。

**要读到的:** 最长的一根条现在大约占轨道的 **四分之一**(此前它是满格的)。
每根条旁边仍然印着「条数 · 百分比」,而那个百分比与条长**现在同一个分母**。
图下那句话现在写着「条长是全部 N 条流水里的占比」。

**要判断的(这是接受这次改动的关键):**
**一张矮了的图,还读不读得出「哪几类占大头」?** 如果十二根条都短到分不出彼此,
那说明合计当分母虽然诚实、但这张图需要换一种画法(例如只画前几类 + 「其余」)。
**这个判断只有看着真图的人下得了。**

### 21.5 账龄 90+:数字现在也是红的

**走法:** 财务 ▸ 应收(`/finance/receivables`)。

**要读到的:** 图里 90+ 那一行,**条与它右边的数字现在是同一个颜色**
(`#B75B53`)。上面汇总条里的 90+ 仍然是 Tailwind 的 `text-red-600`。

**要判断的:** ★ 两个红**不是同一个红**(4.53:1 的品牌砖红 vs 4.62:1 的鲜红)。
量出来的理由写在 `docs/refusal-convergence.md` ②d:把汇总条也换成品牌红会跌破 AA。
**但那是对比度的答案,不是「看起来协不协调」的答案。**
要判断的是:同一页上两个不同的红指着同一个数,读者会不会觉得它们在说两件事。
**如果会,那这一条没修完。**

### 21.6 三张图的「这张图画的是」

**走法:** `/finance/receivables` · `/inventory/reports` · `/hr/org`,读每张图
标题下面那一行小字。

**要读到的:** 字段名是**「这张图画的是」**(不再是「来源」),后面是一句人话,
**不是等宽字体**,不再有任何表名或函数名。

**要判断的:** 那三句话**中文读起来自不自然**。它们是各自写的、不是从英文直译的,
但「自不自然」只有母语者读得出。**六位新同事就要到岗,他们是这三句话的真实读者。**

### 21.7 ★★ 八份 PDF 各自打开,读脚下那一句 ★★

**这是本节工作量最大的一条,而且没有任何自动化替得了它** ——
`render-pdf-samples.mjs` 只把纸落到磁盘上,它**刻意不断言**。

**走法:** 每一份都真的下载一次 PDF:

| # | 从哪里 |
|---|---|
| 1 | 采购 ▸ 某张采购单 ▸ 下载 PDF |
| 2 | 销售 ▸ 某张销售订单 ▸ PDF |
| 3 | 销售 ▸ 某张报价单 ▸ PDF |
| 4 | 销售 ▸ 发运 ▸ 某张送货单 ▸ PDF |
| 5 | 财务 ▸ 发票 ▸ 某张 ▸ PDF |
| 6 | 财务 ▸ 贷项凭证 ▸ 某张 ▸ PDF |
| 7 | 财务 ▸ 对账单 ▸ 某张 ▸ PDF |
| 8 | 产出 ▸ 某个批次 ▸ 可追溯报告 ▸ PDF |

**每一份要确认的:**
1. 那一句在**总计与条款【之下】**,在正文的末尾;
2. 它**只出现一次** —— ★ 拿一份**多页**的单据(对账单或采购单最容易多页)
   确认它**不在每一页上重复** ★;
3. 它**没有挤掉**这份单据原有的页脚说明(贷项凭证那句「这不是退款」、
   报价单那句「这是要约不是发票」、送货单那段分类说明);
4. 它是**小号灰字正体** —— **不是斜体**。斜体在这套字体栈里会让路由 500,
   理由与实测见 `docs/refusal-convergence.md` ②f。
   **要判断的:小灰字够不够「像一条附注而不是一条条款」。**
   如果它读起来仍然像一条条款,那需要别的办法(缩进、分隔线),不是斜体。

**★ 第 7 与第 8 份要各看两遍 ★** —— 它们**跟随界面语言**。
把界面切成中文再下载一次,确认中文那一句印出来了、而且**不是豆腐块或乱码**
(PDF-1 栽过一次:`上海金属回收有限公司` 印成 `wÑ^Þ6 Plø`,HTTP 200,没有一道门变红)。
**另外六份把界面切成中文之后仍然应该是英文的** —— 那是既定政策,不是缺陷。

### 21.8 定价:三个孩子离开菜单之后,还找得到吗

**走法:** 用 `procurement` 或 `sales` 登录 → 顶栏「工具」。

**要读到的:**
1. 二级只有四条:任务 · 日历 · 单位换算 · **定价** —— 「定价」**底下什么都没有**,
   也没有那个重复的组标题;
2. 点「定价」→ **四张卡**:公式 · 计价器 · **金属行情** · 每日行情录入。
   卡片下面**没有**那条灰色的「查看行情历史 →」小链接了;
3. 进 `/pricing/metal-prices/bulk` 之类的深页,**面包屑读的是「金属行情」**,
   不是字面量 `breadcrumb.metal-prices`(这一条差点漏掉,闸门没有抓住它)。

**要判断的:** ★ **一个每天要录行情的人,现在多点一次还是少点一次?**
这一刀把三个入口从菜单里撤了,理由是「同一个词出现两次」。
**代价是每天最高频的那条路多了一跳**(此前:工具 → 每日行情录入;
现在:工具 → 定价 → 每日行情录入)。**只有真的每天用它的人说得出这值不值。**

### 21.9 银行搬进「期末」

**走法:** 财务 ▸ 展开菜单。

**要读到的:** 「银行」现在在**期末**那一组里(在「凭证包」之后),不在「配置」。

**要判断的:** 做月结的人翻到「期末」时,**期望不期望在那里看到银行对账**。
判定它是对账而不是账户主数据是从代码读出来的(见 `docs/refusal-convergence.md` ②e);
**但「它该不该和月结、GST、预提放在一起」是一个流程判断,不是一个代码判断。**

---

## §22 · CONV-1 —— 列表页模板的头四页,只有人眼能确认的那几处(2026-09-03)

**状态:⏳ 欠着** —— 一处都没有人走过。

`survey-phone` 答的是**够不够得到**(U1/U2),它答不了**读不读得懂**。
这一节全部是后者,而模板要被抄 190 次,所以这几条比平常更值得走。

### 22.1 ★★ `/inbound` 在 390px 上:三列 + 展开,到底读不读得了 ★★

**这是本节最要紧的一条。** 13 列的表在手机上只留了三列:
**批次号 · 物料 · 余量**,其余 10 列进「›」展开区。

**走法:** 手机(或浏览器 390px)打开 `/inbound`。

**要判断的:**
1. 三列在 390px 上**挤不挤**?批次号是 `IN-2026-0180` 这种等宽长串,物料名可能是
   「镍钴铝正极边角料」。`table-fixed` 会把宽度平分 —— **有没有哪一列被压成每行一个字?**
2. 点「›」展开一行,**10 个标签/值对**读起来是清单还是一堵墙?
3. ★ **留【余量】而不留【数量】是本刀的判断** ★ —— 理由是「人来这一页最常问还剩多少」。
   **一个真的每天用它的人同意吗?** 如果他要的是「当初收了多少」,这个选择就错了,
   而它会被抄到别的台账上。
4. ★ **没留供应商** ★ —— 按供应商找批次现在要展开。这对不对?

### 22.2 `/sales/quotes`:没留「客户」那一列

留的是**报价号 + 有效期至**(过期琥珀片挂在有效期上)。
**客户没留** —— 理由是「过期必须一眼看见,而一张过了期却看起来正常的报价会被拿去报价」。

**要判断的:** 手机上找报价,人是先想起**客户**还是先想起**报价号**?
如果是客户,这个取舍反了。

### 22.3 空态现在是一个琥珀块,不再是一行灰字

**走法:** `/commissions`(线上 0 行,一定会看到空态)。

**要读到的:** 「还没有人写下来」那句话现在装在一个**琥珀色的块**里,
与「你进不来这一页」是同一种形状。

**要判断的:** ★ 这**会不会看起来像出错了** ★。琥珀在这套系统里一直是「注意」,
而「这张登记簿还是空的」是一件**正常**的事。
本刀认为读者要读的是同一类东西(这里为什么没有东西),**但那是一个判断,不是一个机制**。
如果它读起来像警告,那么空态需要自己的第三种颜色。

### 22.4 `/commissions` 的两块无条件提示还在不在

**走法:** 同上,`/commissions` 空态那一屏。

**要读到的:** 「它不过账」与「计提那一半没建」两块提示**仍然画着** ——
即使一行数据都没有。

**这一条是一次真实的回归修回来的**(第一版把它们放进了 `children`,而 `children`
只在有数据时画)。**走查要确认的是它真的回来了。**

### 22.5 `/inbound` 的骨架屏:它比真页面好还是坏

**走法:** 从别的页面点进 `/inbound`(不要直接刷新)。

**要读到的:** 先出现一块灰色骨架(标题条 + 12 行),然后换成真表。

**要判断的:**
1. 骨架与真页面**对不对得上**?换过去的那一刻**整页跳不跳**?
2. `/inbound` 冒烟量到 ~3 秒(含编译)。**3 秒的骨架比 3 秒的旧页面好吗?**
   —— 旧的行为是停在上一页一动不动。有人会觉得那更安静。**这只有人能判断。**

### 22.6 服务端排序的表头:它现在长得不一样了

**走法:** 桌面打开 `/inbound`,点几个可排序的表头。

**要读到的:** 可排序的表头现在带一个 `↕` / `▲` / `▼`,不可排序的没有。
排序**行为**已经用 14 个 URL 证过没变(`docs/list-page-template.md` ⑤)。

**要判断的:** 五个可排序、八个不可排序 —— **看得出哪些能点吗**?
`↕` 够不够明显?

### 22.7 那条「这一屏 15 行,全体 45 行」的提示

**走法:** `/inbound`,看表格上方。

**要读到的:** 一句「这一屏 N 行,全体 M 行 —— 排序在数据库里对全体做,
所以第一页就是全体的前 N 名」。

**要判断的:** 这句话**帮到人了还是只是噪音**?它在每一个分页的列表页上都会出现,
所以它会被读 17 次以上。

### 22.8 `/finance/claims` 的已决登记簿(**今天看不到**)

**线上 `decided` 是 0 行**,所以那张表今天渲染不出来 ——
**本刀的转换在这一页上没有被真的看过。**
有数据那天要走一遍:上半的决定队列**一个字都没改**,下半才是新的。

---

## §23 · CONV-2 —— 可编辑网格模板的三页,只有人眼能确认的那几处(2026-09-03)

**转了三页:`/hr/leave/types` · `/hr/reviews/scale` · `/me`。**
机器证据见 `docs/editable-grid-template.md` §⑦。**下面这些机器一条都答不了,
而其中 23.1–23.4 是【本刀最要紧的那一半】** —— 手机探针只测首屏,
**它看不见任何编辑态**(那一节的方框写了为什么)。

### 23.1 390px 上,真的改一行(**最要紧的一条**)

`/hr/leave/types`,390px:点某一行左边的 `›` 展开 → 点「编辑」→ 改「标准天数」→「保存」。

* 展开区里每个字段是不是**整宽、带标签**,而不是挤在格子里?
* 上面那一行留下的**编号与名称**,够不够让你认出「我在改哪一行」?
* 「保存」「取消」两枚按钮**够不够得着**(它们是 `min-h-11`)?
* **格子里【不应该】出现任何输入框** —— 若在手机上看见了,那是缺陷。

### 23.2 390px 上,「多点一下」到底烦不烦

桌面是「点编辑 → 改 → 保存」,手机是「点展开 → 点编辑 → 改 → 保存」。
**这是为了让每个字段整宽带标签而付的代价,而值不值是一个判断,不是一次测量。**
`/me` 的自评表在 `all-rows` 模式下**每一行都要单独展开**才能填 —— 那一页尤其要看。

### 23.3 `beforeunload` 那个对话框(★ 记录在案的那条例外)

任一页改一格、**不保存**、直接关标签页(或按刷新)。

* 它**拦不拦**?
* **浏览器自己那句话读起来像不像"出错了"?** —— 这是本系统里
  **唯一一次说不出理由的拒绝**(措辞我们拥有不了),裁定是照收。
  **要确认的是它读起来能不能接受**,不是它对不对。
* 顺带确认那条**声明过的限制**:改一格、不保存、点侧栏跳到别的页 ——
  **它不会拦,输入会丢。** 确认这件事发生时"不至于让人措手不及"。

### 23.4 保存失败时,打好的字有没有留住

`/hr/leave/types`:把某一行的「标准天数」改成一个库会拒的值(例如一个负数),点保存。

* 那一行**下面**是不是出现一条红框?(不是页顶)
* **打好的字还在吗?行还在编辑态吗?**
* 页面**没有刷新**吧?(刷新会把你正要改的输入毁掉 —— 那正是这条规矩的理由)
* ★ 顺带:**那句话是不是一串 Postgres 原文?** 今天**就是** ——
  `app/hr/leave/types/actions.ts:26` 直接回 `error.message`。
  **本刀没修**(理由见模板文档 §②f),**确认它长什么样**,好让错误码那一刀知道要处置什么。

### 23.5 「未保存」那枚小片,以及禁用的「保存」

改一格之前:「保存」应当是**灰的、按不下去**(脏是算出来的)。
改一格之后:那一行第一格右边多出一枚琥珀色的**「未保存」**。
**改回原值呢?** —— 它应当**自己消失**,「保存」重新变灰
(因为脏是一次比较,不是一个存下来的开关)。**这一条是 Q5 那条裁定的现场。**

### 23.6 空态没有把出口一起藏掉(★ 差一点发出去的那个缺陷)

* `/hr/reviews/scale`:把**所有**档位停用/删到空。
  **底下那张「新增一档」的卡还在吗?** ——
  第一版写法会把它连同空态一起藏掉,**人会被留在一个自己走不出去的空页上**。
* `/hr/leave/types`:假别表为空时,**上面那条子导航还在吗?**
  (它走 notices 槽,正是为了不被空态吃掉。)

### 23.7 进不去的时候

拿一个**没有** `module.hr.view` 的角色打开 `/hr/leave/types` ——
应当是 CONV-0 那张**整页拒绝**,不是一张空表。

### 23.8 `/me` 的自评面板(**今天机器没看到过**)

★ **这一页的行为对照【是空的】** —— 探针用的临时管理员没有处于 `self_review`
的评估,所以两棵树都渲染 0 行。**两边都是零不构成"相同"的证据。**
**所以 `/me` 这一页整个只有人能确认:**

* 有一份自评在进行时,那张目标表**每一行开局就能填**(没有「编辑」钮);
* 表**外面**那段自评正文还在、还能填;
* 「保存」与「定稿」两枚按钮都在,**定稿之后整张表变只读、两枚按钮都不见了**,
  并且出现那句「已提交」;
* ★ 存一次草稿之后**再改一格**,「未保存」应当重新出现 —— 这条走的是
  `footer` 槽把草稿递出去那条路,是本刀唯一一处组件**不拥有**保存的地方。

## §24 · CONV-3 —— Kind-E/C/D 七页,只有人眼能确认的那几处(2026-09-03)

七页:`/hr/leave/holidays` · `/settings/dictionaries` · `/purchasing/licences` ·
`/finance/cash-forecast` · `/finance/payroll-payments` · `/finance/processing-costs` ·
`/finance/claims`。**一处都还没有人走过。**

### 24.1 每一页的「新增」出口在空态下还在(★ 与 CONV-2 §23.6 同一条判据,七次)

对下面每一页,把它的表清到零行(或用一个天然为空的角色/时段打开),确认
【新增那张表单】仍然画在屏幕上,不是被空态一起藏掉了:

* `/hr/leave/holidays`:换一个还没录任何假期的年份,「新增假期」的表单还在;
* `/settings/dictionaries`:五个小节各自的「+ 新增值」按钮,与行数无关地一直在;
* `/purchasing/licences`:0 张执照时,「新增执照」按钮(在表**外面**,标题行)还在;
* `/finance/cash-forecast`:经常性成本一行都没有时,「+ 新增一行」按钮还在;
* 以上四页的空态文字本身应该改由 **DataTable 自己的 `empty`** 画出来
  (表格区域里一句灰字),而不是整块内容消失。

### 24.2 `/finance/cash-forecast`:透视表【没有】换成 DataTable,横向滚动还顺手吗

13 周 × 币种那张表刻意留成原样(见 `ForecastGrid.tsx` 抬头的 CONV-3 说明)。
在 390px 上打开这一页:

* 那张透视表能不能横向拖动看到全部 13 周?拖动手感如何(它没有 DataTable
  的三道手机网,只靠外层 `overflow-x-auto`)?
* 表下面那 3 张换成 DataTable 的表(明细行、未定日款项、OPEX 覆盖)是否
  正常展开、能看清哪几列留在了表里?
* 未定日款项那张表原本整行琥珀底,现在只有两个格子各自带底色 ——
  两格之间会不会露出一条不是琥珀色的缝?能不能接受(§25 模板缺口 3)?

### 24.3 `/finance/cash-forecast`:经常性成本里「停用」的行是否仍然一眼可辨

`RecurringLines.tsx` 把整行变灰(text-gray-400)改成了逐格套 `<span>` ——
停掉一条经常性成本,确认那一行在视觉上仍然**整行**发灰,不是几格灰几格不灰
的花斑(§25 模板缺口 3 的现场)。

### 24.4 `/finance/payroll-payments`:已付的行【没有】勾选框

原表里已付的行本来就没有勾选框,转换后这一点必须原样保留 ——
打开一期有部分人已付、部分未付的薪资,确认:

* 已付的行那一格是**空的**,不是一个禁用态的灰色勾选框;
* 未付的行才有勾选框,勾上之后金额与「付选中的 N 人」按钮上的数字联动;
* 这一页**没有用** DataTable 的 `selection` prop(它假设整表可选,这页部分行
  不可选)——确认没有全选框出现在表头。

### 24.5 `/finance/processing-costs`:两张表的选中集互不干扰(★ 本刀最要紧的一次人工验证)

这一页是 Kind-C 选择设计的证明现场。打开这一页,「实际额」与「估算」各自
是一张独立的 DataTable,各带各的表头全选框:

* 在「实际额」表里点全选,「估算」表的选中数量与按钮**完全不受影响**;
* 反过来,在「估算」表逐条勾选,「实际额」的「汇付 N 条」按钮上的数字不动;
* 两张表头的全选框在部分选中时,应显示【半选】(indeterminate)状态,不是
  实心勾或空框二选一;
* 手机上(390px)勾选框是否仍然可点、够不够大。

### 24.6 `/finance/claims`:决定队列(卡片)与已决登记簿的两个空态各说各话

* 把所有报销都决定完(或用一个没有任何报销记录的场景),确认「待决」区块
  显示 `expenseClaims.noPending`,「已决」区块显示 `expenseClaims.noneForEmployee`,
  两句话**不会因为对方是空的而被一起吞掉**;
* 上半卡片队列一个字没动(本刀只套了外壳)——确认审批/驳回的下拉、日期、
  备注仍然照常工作;
* 备用金那句提示(`expenseClaims.pettyCashRuledOut`)在两个区块都为空时
  仍然显示(notices 槽,无条件渲染)。

### 24.7 `/settings/dictionaries`:受限时走的是 CONV-0 整页拒绝,不是旧的琥珀框

用一个五张字典权限一个都没有的角色打开这一页,确认看到的是 CONV-0 那张
**整页拒绝**(标题 + 拒绝块,可能带「回首页」),而不是转换前那种页面内嵌的
琥珀色提示块 —— 这是本刀把这一支从内嵌提示改成 `state:'restricted'` 的地方。

### 24.8 `/purchasing/licences`:两层拒绝分得开

* 没有 `module.purchasing.view` 的角色:整页打不开,CONV-0 的模块级拒绝;
* 有 `module.purchasing.view` 但没有 `module.suppliers.view` 的角色
  (例如只挂了采购权限、没挂供应商权限的角色):应看到**这一页自己**的
  `ListPage` 整页拒绝(标题「公司执照登记」+ `company.licence.restricted`),
  与上面那种模块级拒绝文案不同,确认两者不会混淆。

## §25 · CONV-4 —— finance 23 张,只有人眼能确认的那几处(2026-09-04)

### 25.1 六张带筛选工具栏的页面:筛到零行时,工具栏仍然在(★ 本刀最要紧的一次人工验证)

`expenses` · `bank/statements` · `invoices` · `journal` · `payments` · `fx`
六张页面把 `state` 定成恒为 `'ok'`,理由是筛选工具栏是真实的【出口】,
不能被空态分支吞掉(见 `docs/list-page-template.md` §⑨-4)。这是一个
自动化检查看不出来的行为——打开每一页,填一个必然筛不出任何行的条件
(例如未来的日期区间),确认:

* 工具栏(日期输入框/下拉/搜索框)仍然完整可见、可点、可改;
* `DataTable` 自己的 `empty` 提示画在表格原本的位置,不是整页变成一个
  孤零零的提示块;
* 清空筛选条件能立刻看到行重新出现,不需要刷新页面。

### 25.2 五处新的 `rowClassName` 整行样式,视觉上是否真的连成一整行

`freight`(冲销单据发灰)· `close`(已重开的年结行发灰)· `invoices`
(已作废发票发灰)· `assets`(折旧预览合计行加粗)· `revaluation`
(重估预览合计行加粗)—— 这是 `DataTable` 第一次用 `rowClassName`,
确认每一处手机上展开区(点开箭头之后露出的那段 `<dl>`)背景色跟主行
是不是同一个颜色,不要出现主行发灰、展开区却是白底的花斑(与 CONV-3
§⑧-8 记过的"整行样式格与格之间露缝"是同一类风险,这次是主行与展开区
之间,不是格与格之间)。

### 25.3 `assets` / `close`:一页两张表,一张手写一张 DataTable,拼接处是否突兀

这两页的主表(带 `AssetActions`/`ReopenForm` 行内表单)按兵不动,紧接着
下方是套了模板的新表(折旧预览 / 年结历史)。确认:

* 两张表之间的间距、边框风格看起来是【同一页的两个部分】,不是两个
  拼错位置的组件;
* 手机上先看主表(横向滚动或原样宽表?)、再看新表(留几列 + 展开区),
  两种手机适配方式并存在同一页上,读者切换时是否别扭。

### 25.4 `gst` / `wht` / `cashflow` / `packs`:多表页面,手机上逐张表能不能分清楚是哪一张

这四页各自有 2-3 张表叠在同一页(`gst` 税码参照 + 申报期间;`wht` 欠缴
月度 + 汇缴登记簿 + 法定税率;`cashflow` 汇总 + 明细;`packs` 报告体
(未转)+ 存档登记簿(已转))。手机上打开,确认:

* 每张表上方的小标题(`<h2>`)清楚分开了表与表的边界;
* 展开区(如果那张表有)展开的是【正确的那一行】,不会因为页面上有
  好几张表而错位;
* `packs` 的报告体(PackBody,本刀未转,可能自带横向滚动或其它手机
  处置)与紧接着的存档登记簿(已转 DataTable)两种手机行为并存,是否
  读起来像两个不同的产品。

### 25.5 `price-exposure`:唯一一张头寸表【今天没有被真实数据渲染过】

见 `docs/list-page-template.md` §⑨-9 第 2 条:今天的测试数据里卖方向
一份合同都没有,`SellPositionsTable` 从未被真实渲染。第一份真实合同
数据到场时,需要有人打开这一页确认:

* 表格正常出现(不是继续显示"没有合同"那句话);
* 手机上留下的两列(合同、数量)确实是最该留的两列;
* 页面上其余那些"叙述性缺席"提示(买方向未建模等)在有数据之后
  仍然正确地保持无条件渲染,不会被新出现的表格意外挤掉或吞掉。

### 25.6 `balance-sheet` / `pnl` / `trial-balance` / `payables` / `receivables`:
套上 `ListPage` 外壳之后,页面顶部的观感与其余已转页面是否一致

这五页的表格本身一个字没动,只是外层从手写 `<div className="p-8">`
换成了 `<ListPage>`。确认:

* 标题、（如果有）右上角动作按钮的位置与间距与本刀转换的其它页面
  一致(不会因为外壳换了而突然跳位);
* `balance-sheet`/`pnl` 页顶的筛选工具栏(`Suspense` 包裹)在新外壳下
  仍然正常工作;
* `payables`/`receivables` 的 `AgingAsOfControl`(日期回溯控件)与
  `AgingAsOfNotice` 提示条,在新外壳下位置与此前一致。
