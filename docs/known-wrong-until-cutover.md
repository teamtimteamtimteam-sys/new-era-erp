# Known wrong until cutover

测试库里【看着不对、其实已知、生产全新重建即消失】的账。新发现的这一类问题
加一行到这里,不要每次被人看到就重新解释一遍(AGENTS.md 有约)。

| 哪里看着不对 | 为什么 | 处置 |
|---|---|---|
| 17 张 FIN-0 之前的日记账凭证(37 行)按 USD 本位计的 debit/credit | 换基准货币时点名不重算、不冲销(FIN-0) | 生产全新重建,不带旧凭证 |
| 1100 的 USD 余额:按分录行合计 = 26,550,按未结单据合计 = 0 | FIN-2 之前的结算在本位币空间跑,旧行对不上新口径(FIN-3 Part A2 的发现) | 同上;新单据两算法精确一致(fixture 断言过) |
| ~~6600 'FX Gain/Loss' 停用后仍带历史余额~~ | 【划掉,2026-08-04 复核】6600 从未被过过账 —— 零分录、零余额。它只是被建出来又停用了,没有任何看着不对的东西 | 已不成立;留此行只为不让人再去找那笔不存在的历史 |
| 七月测试薪资期间挂着未汇的 CPF 444.00,8 月 7 日起 HR 看板出现 cpf_due 警报 | FIN-5 的临期警报对着测试数据在正常工作 | 生产全新重建,不带测试期间 |
| 结掉 7 月那两条旧加工成本行会让 2200 出现无对应贷方的借方 | 那两行录入时自动应计触发器还没上线(cut2a 迟到,镜像头有记),应计从未过账 | 生产全新重建,触发器从第一天就在 |
| PROC-2026-0003:成本条目合计 500,批次 OUT-2026-0007 仍按 400 摊(单位成本 2.6667) | 该单 7-02 分摊过,之后一条估算从 100 改成 200 —— 总账跟着动了(JE-2026-0021 冲旧记新),批次不会自己重算。FIN-8 的【分摊已过期】标记就是为这个建的,它当时还不存在 | 生产全新重建;这一单可安全重跑(其 55 单位销售尚无 COGS 分录,重跑会一并补上) |
| 那条 100→200 的电费条目在 processing_cost_entry_history 里没有记录 | 留痕触发器是 FIN-8(2026-08-04)才建的,那次改动发生在它之前。旧值 100 只能从 JE-2026-0021 的冲销行读回 —— 【不补造历史行】,补出来的痕迹是假的 | 生产全新重建,触发器从第一天就在 |
| PROC-2026-0106:电费 200 已在总账(JE-2026-0022 借 5110 贷 2200),批次成本里一分没有 | 该单从未分摊过(allocated_at 为空)—— 条目一录入就进账,进批次要等人点分摊 | 生产全新重建;同样由【分摊已过期/从未分摊】标记暴露 |
| JE-2026-0007(冲销镜像)、JE-2026-0036(IN-2026-0154 定价)、JE-2026-0038(IN-2026-0154 核销)三张分录的 entry_date 比真实(新加坡)业务日早一天;0036 还按 8/5 的牌价 1.24 计价 | FIN-20 之前库跑 UTC:SG 00:00–08:00 之间 CURRENT_DATE 是昨天,服务端盖章的分录日期与 CURRENT_DATE 取的牌价都跟着早一天(FIN-19/20 的走查发现)。同窗口的 JE-2026-0017 / 0024 日期【没错】—— 那两张的日期是显式传的期间日 | 生产全新重建,不带测试分录;时区自 FIN-20 起为 Asia/Singapore,这个窗口不再存在 |
| 现金流量表里六对冲销分录各占【两行】明细(原分录 + 等额反向的冲销分录),净额为 0;其中 JE-2026-0006(7-30)的冲销 JE-2026-0007 日期【早一天】(7-29),所以按 7-29/7-30 切开的区间会只看见其中一条腿 | OPS-17 去掉了 cash_flow_statement 的 status='posted' 过滤 —— 两条腿都算才是对的(只留 posted 会丢原分录、留冲销分录,错成 -原分录,live 上差 1,166.98)。日期早一天那件事是 FIN-20 之前的 UTC 窗口,本表上一行已有记 | 生产全新重建,不带这些测试冲销对;**注意合计数字本身不是"看着不对"—— 改动之后的现金口径(如 2026-08-31 的 -45,648.37)是【对的】,与资产负债表一致,重建后依然如此** |
| PO-2026-0001(USD 120,000,`fx_rate = 1`)与 2026-08-04 之前的五笔 USD 收付款(RCPT-0001/0002/0003、PMT-0001/0002)按 1:1 折本位币 | **不是默认值泄漏,是换基准货币的旧账**:FIN-0 在 2026-08-04 把本位币从 USD 换成 SGD,在那之前 USD 单据的 1:1【是正确的记录】。之后建的 PO-2026-0002/0003 拿的是 1.26,8-05 之后的收付款拿的是真汇率 —— 分界线干净得可以当证据用。本表首行“17 张 FIN-0 之前的凭证”说的是同一件事 | 生产全新重建,不带这些旧单据。**刻意不修**:改一行 PO 的汇率不会连带改 PMT-2026-0002、JE-2026-0014/0016、预付核销与那张进料批 —— 修一半比不修更难读,而 FIN-0 当初对全部 17 张凭证下的正是同一个判断 |
| 两条采购行(PO-2026-0001 / 0002)与批次 IN-2026-0154 引着计价公式 PF-2026-0001,却结算不了 —— 应用化验会 PRICING_TERMS_NOT_COMMITTED | FIN-27 起结算只读【成交时抄下的条款副本】,而这几行成交在触发器之前,当时没抄。**刻意不回填**:照公式今天的样子补一份,记下的是它现在说的话,未必是当时谈的 —— 编造的承诺比缺失的承诺更坏(同 FIN-26 的灰色"出处未知") | 生产全新重建,不带这些旧行;这几批货仍可走手工定价(set_inbound_unit_price),界面在批次页与采购单行上都画着"条款未承诺" |
| 九张既有加工单的 `allocation_basis` 全是 `metal_value`,而【没有任何人选过它】 | FIN-36 之前这一列带着 schema 默认值,`commit_processing_run` 从不设它。**值本身是真的** —— 算术当时用的就是 metal_value;假的只是"有人做过这个选择"这层含义 | 生产全新重建,不带这九张单;新单必须在表单上显式选。**刻意不加 `is_defaulted` 标记**:那是给九行注定消失的数据造一列,而它们要表达的那件事这一行已经说清楚了(APR-1 用 `is_reconstructed` 是因为那些行会长期活着,这里不会) |
| `cu` 2026-07-30 的 80,000 USD/t 已【软删】,该金属只剩 06-25 的 24,000 与 08-10 的 20,000 | 那一行与两侧邻居相差 3.3 倍,判定为键入错误(METAL-1 建这项检查的起因就是它)。**刻意不改成某个数字**:没人知道 7-30 的铜是多少,编一个"看起来合理"的值就是伪造一条记录(与 FIN-26 的灰色"出处未知"同一条)。**为什么现在删而不是等重建**:那一行落在每一个 30 天均价窗口里【直到 2026-08-29】,删之前铜的 30 天均价是 50,000 —— 现货的 2.5 倍,而 `average` 口径的公式定价会照着它算 | 生产全新重建,不带这条测试行。已核实它【没有】进过任何存下来的估值:sales_records / purchase_order_lines 的 price_provenance 与 processing_runs 的 allocation_snapshot 全表扫过,零命中 |
| 11 条既有行情的 `anomaly_check` 为空,列表上显示「无检查记录」 | 它们录入时 METAL-1 的检查还不存在,当时没有比对过。**刻意不回填**:事后重算用的是【今天】的序列,"上一条"已经不是当时那一条,算出来的判词是一句没人说过的话(与上面几行"不补造历史"同一条) | 生产全新重建,检查从第一天就在。徽标特意与「未比对」(no_reference)分开:一个是"当时没有这项检查",一个是"当时没有可比的对象",两者都不等于"查过、没问题" |
| 11 条既有行情的 `price_index` 为空,列表上显示「未声明」;LME 与 SMM 两条序列【一条报价都没有】 | 录入时还没有指数这个轴,没有人选过。**刻意不指派**:替它们宣称"这条来自 LME"与给那条 80,000 编个数字是同一种伪造,而且更坏 —— 结算会照着它算钱(METAL-2)。声明了指数的公式看不见这些行,所以在 LME/SMM 的报价录进来之前,那样的公式会【点名拒绝】结算,而不是悄悄用一条不知出处的数字 | 生产全新重建,行情从第一天就带着指数录入。既有公式(唯一那张 PF-2026-0001)同样未声明指数,与这条序列对得上,所以今天的计价一切照旧 —— fixture 49 的 C 臂钉住了这个回归 |
| PO-2026-0004 收了 **100,000**,而订的是 20,000(五倍);任何"订量 vs 已收"的显示都会把这一行画得很难看 | 测试数据 —— 收货从来没有对着订量设过上限,这一行是那段时期留下的。**PUR-2 刻意不围着它设计**:已收下限约束的是【这一次修改】(订量不能砍到已收之下),它【不断言】现有数据的任何事。把守卫写成"订量必须 ≥ 已收"的不变量,会让这张单从此改不动,而它需要的恰恰是能被改 | 生产全新重建,不带这张单。**画得难看是对的** —— 那正是这类数据该有的样子,不该被显示层抹平 |
| `IN-2026-0180`(Acme,100,000 kg,2026-08-10)的物料是 `ZZ-SMOKE-PROBE` —— 一个命名像临时行的物料 | 一次收货测试用了 2026-08-06 留下的临时物料。**这条不能靠"按命名删临时行"清掉**:删了它,这张在册的真批次就指向一个已删的物料,比留着残骸坏得多(`npm run check:scratch` 的第三列就是为这种情形加的) | 生产全新重建,不带任何一方。**刻意不删**:处置这一条要么改这张批次的物料、要么连批次一起作废,两者都是业务动作而不是清扫动作 |
| `ZZ-SMOKE-M25` / `ZZ-SMOKE-S25` / `ZZ-SMOKE-IB25` 与 `OUT-2026-0159/0160`、`PROC-2026-0142` —— 2026-08-06 一整套 fixture-25 形状的行 | 那一次 fixture/探针**没有回滚,而是逐行软删**。整簇【彼此自洽且全部已软删】:没有任何在册的行引用它们,屏幕上一个都看不见 | 生产全新重建。**刻意不硬删**:硬删会打断它与那张已冲销加工单之间的外键,而它们此刻不可见、不参与任何计算 —— 清理它换不来任何东西,只换来一次动手的风险 |
