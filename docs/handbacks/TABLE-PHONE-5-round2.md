# TABLE-PHONE-5 交回报告 —— 最后两张无列头的表,以及 `RAW-TABLE-PHONE` 结案

**两张都做完了,`RAW-TABLE-PHONE` 关了。** 卡住这两张的从来只有一件事 ——
**折叠任何一列都要现造文案** —— 而 Tim 批下那 9 个 key 之后,那件事没有了。

★ **本刀是一次【桌面改动】**,是本族的第二次(第一次是 `/inventory` 的单位合并)。
**九个新 key 是列头,列头要上就是两档都上。** 单独报告见 §3。

---

## 1 · HEAD、与 `ad39bb3` 的差别、树

| | |
|---|---|
| HEAD(开工前) | `7f3694e` |
| origin/main(开工前) | `7f3694e` —— **相等** |
| 树(开工前) | 干净(`git status` 0 行) |
| HEAD(收工后) | 见文末 §12 |

**与 `ad39bb3` 的差别:恰恰是委托书 §0 预测的那一种,一条,而且只有一条。**

```
7f3694e TABLE-PHONE-4 交回报告 §12:补齐推送与部署的实测记录 —— 部署 6346862808
        success,两个问题分开问过,窗口 173s(第四个数据点,而它把「方差不到 10%」推成了 15%)
 docs/handbacks/TABLE-PHONE-4-round2.md | 55 ++++++++++++++++++++++++++++++++++
 1 file changed, 55 insertions(+)
```

**纯文档,只动上一刀自己的交回报告。** 按 §0 的授权 PROCEED。
★ **这是连续第六刀** —— 委托书里的哈希稳定地落后一格,原因已经查明并写在 §0 里,
**它是流程的形状,不是谁犯的错**,不必再查。

---

## 2 · 开工前的确认 —— 对着文件量,两张全对

| | 委托书说的 | 文件里读到的 | 判定 |
|---|---|---|---|
| `ContainerPanels.tsx` | `:119`,5 列,无表头 | `:119` `<table>`;**全文件 `<thead>` 0 处、`<th` 0 处**;5 列 | ✓ 全对 |
| `NewOrderForm.tsx` | 批次表记 `:786`,TABLE-PHONE-3 之后 `:804` | 今天 `:804`;`git show e516194^` 逐字为 `:786`;**该表无 `<thead>`**(全文件唯一的 `<thead>` 在 `:877`,是另一张表);5 列 | ✓ 全对 |

**「还没动过」也量过:** `ContainerPanels.tsx` 全文件 `sm:table-cell` / `sm:hidden` 命中 **0**。

---

## 3 · ★ 九个新 key —— 逐字,两种语言;以及它们【桌面档也上】

### 3.1 · 九条,逐字

| key | English | 中文 |
|---|---|---|
| `logistics.colShipmentCode` | `Shipment` | `发货单号` |
| `logistics.colOrderCode` | `Order` | `订单号` |
| `logistics.colCustomer` | `Customer` | `客户` |
| `logistics.colShipDate` | `Shipped` | `发货日` |
| `purchasing.calcColMetal` | `Metal` | `金属` |
| `purchasing.calcColContentPct` | `Content %` | `含量%` |
| `purchasing.calcColPayablePct` | `Payable %` | `计价%` |
| `purchasing.calcColUnitPrice` | `Price` | `单价` |
| `purchasing.calcColMetalValue` | `Metal value` | `金属价值` |

**第五列(拆离钮)没有 key** —— 它自己带着字(`logistics.detach`),
照 `/finance/close` 那条先例,列头留空,不另造文案。

### 3.2 · ★★ 最后两个的措辞被一道闸改过,而那道闸是对的 ★★

**我原本写的是 `Price (USD/t)` / `Metal value (USD)`(中文 `单价(USD/t)` / `金属价值(USD)`),
照抄了 `pricing` 那边已经上线的说法。`db/gate.py` 当场红了,退出码 1**:

```
✗ messages/ 里【新增】了写死的币种:
     messages/en.ts :: calcColUnitPrice   (在册 0 处,现在 1 处)
       第 5170 行 [unit]: calcColUnitPrice: 'Price (USD/t)',
       计量单位(USD/t、USD/kg)—— 金属行情按市场惯例就是美元报价,**这些是真的**。
     messages/en.ts :: calcColMetalValue   (在册 0 处,现在 1 处)
       第 5171 行 [label]: calcColMetalValue: 'Metal value (USD)',
       把币种烤进列头/标签 —— **FIN-0 那个病**。本位币一改,这些字就成了谎话。
```

**它把两条分成了两类,而这个分类是对的:**
* `Price (USD/t)` 是 **[unit]** —— 计量单位,「这些是真的」,但**要过一遍眼才准进基线**;
* `Metal value (USD)` 是 **[label]** —— **FIN-0 那个病**,把币种烤进列头。

☞ **处置:两条都不带币种。** 理由不是为了让门变绿,是**屏幕上一个字都没少**:
* 单价那一格自己带着 `/t`(`… + '/t'`,源码里本来就有);
* **整块的口径由块末那一行说出来** —— `… − … − … = … USD →`;
* 那两处 `formatMoneyBare` 的文档参数原文就写着「**计价明细整块是行情口径 USD**」。

★ **`scripts/currency-messages-baseline.json` 一个字都没动**(`git status` 对它 0 行)。
在册数从跑第一次时的 78 回到 **74**(label 36 · prose 16 · unit 18 · account-name 4)——
那 4 处正是我自己加进去又拿掉的。**基线没有被抬高。**

☞ **登记一条给 Tim 的文案裁定(不是本刀的活):**
`pricing.colPrice` = `Price (USD/t)` 与 `pricing.colValue` = `Value (USD)` **在册**,
也就是说**同一个概念在计价器那一页带着币种、在这一页不带**。
两种都说得通,但它们**不一致**,而统一它是一次文案裁定。

### 3.3 · key 放在哪个命名空间 —— 一个都没有借

* `ContainerPanels` 的文案走 `labels: Record<string,string>` 这个 props,真源在
  `app/logistics/containers/[id]/page.tsx`,namespace 是 **`logistics`**。
  ★ 逐条比过 `page.tsx:134-164` 那整个 `labels` 字面量:**没有任何一条对得上这四列**
  —— TABLE-PHONE-3 说的这件事,今天仍然成立。
* `NewOrderForm` 里 **58 处 `t()` 走 `purchasing`**(另有 suppliers 3 · inbound 2 ·
  finance 2 · common 2 · metals 1),所以五个计价列头进 **`purchasing`**。

★ **`Customer` 这个词在另外四个 namespace 里都有 `colCustomer`**
(`sales` · `finance` 等,逐字都是 `Customer` / `客户`)。**一个都没有借** ——
借一个别的模块的键只因为那个词碰巧一样,**就是 TABLE-PHONE-3 点过名的「绕个弯的现造」**。
同理没有借 `pricing.colContent` / `colPrice` / `colValue`,尽管那三个几乎就是这三列。

### 3.4 · ★ 它们在桌面上也出现了 —— 单独报告

**九个都是 `<th>`,写在 `<thead>` 里,没有 `hidden sm:table-cell` 的那几个在每一种屏幕上都印。**
这两张表**此前从来没有过列头**,所以这不是「手机档顺手带上的」,是**一次桌面改动**:

> **五列裸的数字没有主语,那不是手机上才有的毛病,是每一种屏幕上都有的。**

**本族的第一次桌面改动**是 TABLE-PHONE-1 的 `/inventory` 单位合并(单独批、单独报)。
**这是第二次,同样单独报**,好让一次实走能把看见的东西归到正确的那一刀上。

---

## 4 · 两张表:留什么、折什么、为什么,以及带 `name` 的输入框

### 4.1 · `ContainerPanels:119` —— 装着的发货单(读侧)

| 桌面 5 列 | 手机 |
|---|---|
| 发货单号(Link) | **留 —— 身份** |
| 订单号 | 折叠 |
| 客户 | **留** |
| 发货日 | 折叠 |
| 拆离钮 | **留 —— 控件** |

**一句话的判断:** 身份是单号,控件是拆离钮(**够不着 = 不存在**,DBLOCK-1 用在版式上),
中间那一格给**客户** —— 因为**它是这四列里唯一一列说的是话、不是另一个编号**。
☞ 旁证不是我想的:**同一个 `<section>` 里那个「装入发货单」的下拉,它的 option 文本是
`{s.code} · {s.order_code} · {s.customer}`** —— 写这个组件的人认为一张发货单由这三样认出来。
三样里塞得下两样,**留下的那一样应该是不用查就读得懂的那一样。**

### 4.2 · `NewOrderForm:804`(加了 thead 之后 `:808`)—— 计价明细(只读)

| 桌面 5 列 | 手机 |
|---|---|
| 金属 | **留 —— 主语** |
| 含量% | 折叠 |
| 计价% | 折叠 |
| 单价(`…/t`,取不到时是 `—`) | **留** |
| 金属价值 | **留 —— 这一行算出来的数** |

**一句话的判断:** 委托书要的是「**价钱出来是多少,以及为什么**」——
「多少」是**金属价值**,「为什么」在三个乘数里挑一个,**挑了单价**,理由是:
☞ **只有它会是 `—`。** 源码 `cl.price_usd_per_tonne !== null ? … : '—'` ——
**那一天那个金属没有行情**,于是这一行的价值是 0 而没有任何别的地方说得出为什么。
含量% 与计价% 永远是两个数,**它们不会解释一个意外的结果;那个破折号会。**

### 4.3 · ★ 带 `name` 的输入框(TABLE-PHONE-4 §4.2 那条规矩)—— 两张都量过

判据:剥注释后按 `<tr>` / `<td>` 的出现次序数列,再在**格子里**找 `name=`;
同时数表【之前】未闭合的 `<form>`,判断它有没有 `<form>` 祖先。

| 表 | `<form>` 祖先 | 表内带 `name` 的控件 |
|---|---|---|
| `ContainerPanels:119` | **无** —— 整张不在任何 `<form>` 里 | **第 5 列:`name="reason"`** |
| `NewOrderForm:808` | **有(1 层未闭合)** | **0 个** |

**两条结论,都要说清:**
1. `ContainerPanels` 那个 `name="reason"` 在**拆离列**,而**那一列本来就留在明面上**
   —— 规矩自动满足。**另外它整张不在任何 `<form>` 祖先里**,那个输入框的表单就是
   它自己那一个(`onSubmit` 里 `new FormData(e.currentTarget)`)。**两条独立的理由。**
2. `NewOrderForm` 的计价明细**表内 0 个带 name 的控件**(委托书说「相信是只读的」——
   **量出来确实是**)。☞ **但它【确实住在一个 `<form>` 里】**,
   也就是说 **§4.2 那条规矩在这里是【真的带电的】,只是这张表恰好没有踩到它。**
   把它写下来,免得下一个人以为「只读面板」天然安全。

### 4.4 · 两档都出现的东西,提出来写一次

`orderCodeText` / `shipDateText`(ContainerPanels)· `contentPctText` / `payablePctText`(NewOrderForm)
—— 两处**引用同一个描述**,没有抄第二份。
★ **那个 `×` 原样带着**:`payablePctText` 是 `` `× ${cl.payable_pct}%` ``,
桌面格子里本来就有那个 `×`,**叠进去的时候去掉它就是【改了那一列装的东西】**(§4 明令禁止)。
代价是手机上读作「计价%: × 90%」,略嫌别扭 —— **但它是同一个字符串,不会漂移。**

---

## 5 · 一个字段都没丢 —— 校准、两张表、两次咬人

量具:`nofieldlost.mjs`,**不进仓库**(裁定 R-Q8)。判据五条,全部会红:

1. `thead` 里的 `<th>` 数 = 桌面列数;带 `hidden sm:table-cell` 的是被折叠的。
2. **折叠块的【直接子项】数 = 被折叠的列数** —— 数**项**不数标签。
3. **每一项都要带标签**,除非在这张表自己的 self-labelling 白名单里。
4. **覆盖率是一条断言**:找不到 `<thead>`、读出 0 个 `<th>`、找不到折叠块、折叠块 0 项
   —— **一律红,绝不当成 0**。
5. **scope 必须显式声明并印在结果行里。**

**注释先剥掉**(带字符串/模板串识别,长度与行号都不移)。
**标签用【括号配平】读,不用正则** —— 对 `t('finance.colAmount', { ccy: baseCurrency })`
这种带参数的、以及 `labels.colOrderCode` 这种走 props 的,都成立。

### 5.1 · 校准行(未改动的 `/finance/payables`)先跑

```
  绿  CALIBRATION /finance/payables  桌面 8 列 · 手机留 3 · 折叠 5 · 叠加项 5 · 无标签 0/白名单 0
      [scope: 行内 —— 行渲染在同一个组件里(page.tsx 的 groups.map)]
CALIB_OWN_EXIT=0
```
**8 = 3 + 5,五项全带标签 —— 与那张表已知的事实、以及 TABLE-PHONE-4 报的数逐字相符。**

### 5.2 · 全表

```
  绿  CALIBRATION payables          桌面 8 列 · 手机留 3 · 折叠 5 · 叠加项 5 · 无标签 0/白名单 0  [scope: 行内(groups.map)]
  绿  ContainerPanels(装着的发货单)  桌面 5 列 · 手机留 3 · 折叠 2 · 叠加项 2 · 无标签 0/白名单 0  [scope: 行内(attached.map);第 5 列是拆离钮,列头刻意为空(自带文案),它没有被折叠]
  绿  NewOrderForm(计价明细)        桌面 5 列 · 手机留 3 · 折叠 2 · 叠加项 2 · 无标签 0/白名单 0  [scope: 行内(l.calc.lines.map);整张只读,一个输入框都没有]

全绿:3 张表,一个字段都没丢。
NOFIELD_OWN_EXIT=0
```

**两张都是 5 = 3 + 2。白名单一条都不用开** —— 被折叠的四列每一列都有自己的列头 key。
(拆离钮那一列**没有被折叠**,所以它不需要白名单;白名单管的是**叠加块里的**无标签项。)

### 5.3 · ★ 那条覆盖率断言【没有被这一刀放松】

对**同样两张表结案前的样子**(`git show HEAD:…`)再跑一次:

```
★ 红  BEFORE ContainerPanels:119  ★ 整张表没有 <thead> —— 拒绝判定  [scope: 行内(attached.map)]
★ 红  BEFORE NewOrderForm:804     ★ 整张表没有 <thead> —— 拒绝判定  [scope: 行内(l.calc.lines.map)]
NOTHEAD_OWN_EXIT=1
```

**它红了,而且说出了自己为什么答不了** —— **不是一句静默的「叠加项 0」。**
**这道闸还会拒绝下一张无列头的表。**

### 5.4 · 咬人两次,两种咬法,各带自己的退出码

**① 删掉【一整条叠加项】**(ContainerPanels 的「发货日」那一块)—— 咬的是判据 ②:
```
★ 红  ContainerPanels(装着的发货单)  ★ 叠加项 1 ≠ 折叠列 2
BITE_A_OWN_EXIT=1
```

**② 只删【标签】、把值留着**(NewOrderForm 的「含量%」那一项)—— 咬的是判据 ③:
```
★ 红  NewOrderForm(计价明细)  ★ 1 个叠加项没有标签:<div className="font-mono"> {contentPctText} </div>
BITE_B_OWN_EXIT=1
```

★ **两次咬的是【两条不同的判据】** —— 第一次数量对不上,第二次数量是对的而**主语没了**。
一条只数数量的闸会放过第二种,而第二种正是 §2C 那句话:**一个没有主语的数字,比那一列原来的样子更糟。**

**复原并复跑:**
```
RESTORE_IDENTICAL=yes   (sha256 逐字相同:两个文件咬人前后各自一致)
  绿 × 3
RESTORED_OWN_EXIT=0
```

---

## 6 · 队列与记录

**`docs/forward-queue.md`:**
* **TABLE-PHONE-6 与 TABLE-PHONE-7 两行都划掉了**(`~~…~~`),并写明
  **卡住它们的从来只有那一个裁定**,批下来之后各是一次判断、半刀的工 —— 与那条自己的预估一致。
* 触发条件从「要一个裁定」改成 **「没有了 —— `RAW-TABLE-PHONE` 已关」**。
* ★ 记下 **`purchasing/orders/new/NewOrderForm` 到此不再是半张脸**(`:858` 分期是
  TABLE-PHONE-3 做的,`:786` 是这一刀)。

**`docs/known-issues.md`:**
* 条目标题改成 **`~~RAW-TABLE-PHONE-SWEEP~~ —— ★ CLOSED(2026-09-09,TABLE-PHONE-5)★`**,
  并在标题底下**先立一句**:结案不等于解决了,边界写在 §七 / §八。
* 新增 **§五批之后**(本刀的两张表、九个 key、带 name 的量法)。
* 新增 **§七 · 结案 —— 这一族的整条弧线**:交下来的 **54**、它**三个方向**的错
  (4 个假阳性 / 每个列数偏高一格 / 12 个漏网)、真数 **72 张表 / 62 个文件**、
  **29 张免修**、**43 张工作单**、**9 张有外壳(WRAPPED)**、
  **33 张本族做掉**,以及 `43 = 9 + 33 + 1`(那个 1 是 payables 自己,FIX-2b 早做过,
  它在每一批里以**参照**身份出现)。五批 **33 张新表 / 30 次判断**,逐批列出。
  并点名这一族**被自己的量具纠正过五次**。
* 新增 **§八 · 这一族【从来没有验证过】的东西**(见下 §9)。

**`RAW-TABLE-PHONE-WRAPPED` 留着,而且给了它自己的一条记录:**
★ 它此前**只活在 `docs/forward-queue.md` 里**。母条一关,它就会连一份记录都没有 ——
所以 `known-issues.md` 里**新立了一条 `## RAW-TABLE-PHONE-WRAPPED …(OPEN · UNMEASURED)`**,
9 张 / 8 个文件逐个点名,并写死触发条件:

> **先量,再决定。** 今天没有任何人量过它们在 390px 上读不读得懂 ——
> 那需要 `scripts/survey-phone.mjs`(真浏览器读 `scrollWidth`)或者一次手走。
> **不要在没量之前改它们**(裁定 R-Q6)。

---

## 7 · 我自己定的、属于细节的那些

1. **中间那一列给客户,不给订单号**(§4.1)—— 判断,不是委托书写死的。
2. **「为什么」那一列给单价,不给两个百分比**(§4.2)—— 同上,理由是那个 `—`。
3. **拆离列的列头留空**,照 `/finance/close` 的先例;**不为它造一个「操作」**。
4. **`×` 原样带进叠加区**(§4.4),宁可读起来略别扭,也不改那一列装的东西。
5. **两张表的可见格 padding 从 `px-3` 改成 `px-2 sm:px-3`**,照 payables
   (`px-2 sm:px-4`)的先例 —— 手机上省一点横向。**桌面档逐字不变。**
6. **计价明细的列头用 `font-medium text-gray-500`**,不用 `bg-gray-100` 的表头条 ——
   那张表整个是 `text-xs` 的灰底小块,加一条深色表头会把它变成另一种东西。
7. **`Price` / `Metal value` 不带币种**(§3.2)—— 这一条**不只是细节**,它是被一道闸改的,
   所以单独写在 §3.2。

---

## 8 · Tim 该去哪里走、每一页要什么状态

**两页都用设备模拟,390px。**

### 8.1 · `/logistics/containers/[id]` —— 装着的发货单
**要什么状态:一个【至少装了一张发货单】的箱子。**
☞ ★ **我没能替你确认线上有没有** —— 本次会话里连库的动作被挡下了,我没有绕。
**一眼就能知道的办法:先开 `/logistics/containers` 列表页,看「发货单 / Shipments」那一列** ——
**任何一个数不是 0 的箱子,点进去就有行。** 全是 0 的话这张表在线上渲染不出来,别白走。
* 看什么:**表头出现了**(此前一个都没有);手机上三列 **发货单号 · 客户 · 拆离钮**;
  单号底下叠着 **订单号** 与 **发货日**,各自带标签。
* ★ **顺手看一眼拆离钮按得动** —— 按下去之后那一格会长出一个 `w-64` 的理由输入框
  加一个钮,**那是本页在 390px 上最挤的一处**(见 §9)。

### 8.2 · `/purchasing/orders/new` —— 计价明细
★ **这一页【不需要线上有任何单据】** —— 状态是在表单上现敲出来的。**顺序是死的:**
1. 单据种类**选材料**(不能是设备 —— `isEquipment` 为真时这一整块不画);
2. 加一行,选材料;
3. 展开 **「预计化验 / Expected assay」**,**至少敲一个金属的含量**
   (`assayCount(l) > 0` 是三个前提之一);
4. 在那一行的**计价公式**下拉里选一张公式(`l.formula_id` 非空 —— 第二个前提);
5. 点 **「按化验估算 / Compute from assay」** —— 这一步才生出 `l.calc`;
6. 点它右边那个 **`▸ …… USD/kg`** 把面板展开(`l.calcOpen`)。**表在这里面。**
* **前提里有一条要线上有东西:得存在一张可选的计价公式。** 下拉是空的就走不到第 5 步。
* 看什么:五个列头出现了;手机上三列 **金属 · 单价 · 金属价值**;
  金属底下叠着 **含量%** 与 **计价%**。
* ★ **如果哪个金属那天没有行情,单价那一格是 `—`** —— **那正是我把这一列留在手机上的理由**,
  值得特意找一个这样的日期看一眼。

---

## 9 · 没能验证的,以及会包行的列头

### 9.1 · 没能验证的
* **一个像素都没量过,一次浏览器都没跑过。** 本刀与前四刀一样,**读的是标记**。
* **「一个字段都没丢」的口径是标记层面的**:那个字段**在 DOM 里、而且带着一个标签**。
  它**不**等于 390px 上读得到,更不等于读得懂。
* **线上有没有数据没能确认**(§8.1)—— 连库被挡,没有绕。
* **`RAW-TABLE-PHONE-WRAPPED` 那 9 张,一如既往,没量。**

### 9.2 · ★ 会包行的列头 —— 新的那九个【从来没有上过屏幕】

**英文是风险,中文不是** —— 中文列头短得多(`客户` 2 字 vs `Customer` 8 字符),
而**本刀所有的判断都是对着两份文案里较短的那一份在心里排的,两份都没上过屏幕**。

| 位置 | 最长的英文 | 我担心什么 |
|---|---|---|
| `NewOrderForm` 计价明细 | **`Metal value`(11)** | ★ **最危险的一个**:那张表 `<table className="border-collapse">` —— **没有 `w-full`,没有任何宽度约束**,列宽全靠内容。手机上三列里有两列是新列头(`Price` / `Metal value`),而格子里是 `font-mono` 的数字。**它到底会不会把那个 `bg-gray-50` 的小块顶宽,没量过。** |
| `ContainerPanels` 发货单表 | `Shipment`(8)· `Customer`(8) | 表是 `w-full` 的,三列;但**第三列装的是拆离钮**,而按下之后那一格里是 `w-64`(256px)的输入框 + 一个钮。**390px 的屏幕上,一列就要 256px。** |

☞ **那个 `w-64` 是既有的、本刀没有动的**(§7 明令:输入框的高、内边距、边框、字号
都归样式普查)。**登记给样式普查,不在这一刀里修。**
★ 顺带登记:`ContainerPanels` 里 `field` 那个 class 与本族别处的输入框宽度
(`w-20 / w-24 / w-28 / w-32 / w-40`)是同一个不一致 —— TABLE-PHONE-4 已经登记过,这里再点一次名。

---

## 10 · `db/gate.py`

★ **它第一次跑是【红的】,而红的原因是我**(§3.2 那两个带币种的列头)。
改掉之后复跑:

```
GATE_OWN_EXIT=0
GATE_WALL_SECONDS=235      (脚本自己印的 == 三个判词(wall-clock 155s))
```

**四条判词,逐字:**
```
判词【可重建性】:✓ 仓库能从零建出库(prelude 足够,B1/B2 双侧断言见上)
判词【镜像 vs 线上】:✓ 一致(结构、种子、引导、自洽、definer)
判词【行为断言】:✓ db/fixtures 全部通过(建出来的库跑起来是对的)
   判词【匿名面】:✓ 线上是基线的子集(基线 327 条)
```

**235s 落在 180–700s 里,不用改这个范围。**
☞ 说清那两个数为什么不一样:脚本抬头那句 `三个判词(wall-clock 155s)` 是**它自己算到那一行为止**的,
**【匿名面】那一段跑在它后面**;235s 是从进程起到进程止,包含它。
(第一次那趟红的只有 192s —— 因为它**在币种那一步就停了,根本没跑到匿名面**。)

---

## 11 · `npm run build`

```
BUILD_OWN_EXIT=0
```

**eslint 冻结闸:**
```
基线  error 42 · warning 88
现在  error 42 · warning 88
✓ 没有新增的 eslint 问题。
```

**元检查(`check-instrument-selfproof`):**
```
✓ check-instrument-selfproof:32 支量具都写了瞄准线;其中 22 支(构建链里的,含本支)都带着覆盖断言。
```

**`check-datatable-phone` —— 还是 123:**
```
✓ 手机声明:123 个调用点(DataTable 119 · EditableTable 4) —— columns 模式 123(各自至少一列 priority)· scroll 模式 0(各自带 why) · 静态读不出 1(由渲染期那道网兜着)
```
**本刀一个 `DataTable` / `EditableTable` 都没有新增或转换,所以这个数不该动,而它没动。**

---

## 12 · 推送与部署

### 推送 —— 实测,不是读推送命令的输出

| | |
|---|---|
| HEAD(收工后) | **`a2c7af1`** |
| origin/main | **`a2c7af1`** |
| **复核方式** | ★ `git fetch origin` 之后**比对两个哈希**,**不是**读 `git push` 的输出 |
| 树 | 干净(`git status --porcelain` 0 行) |
| `messages/` diff | **+9 个 key**(两种语言各 9 条)—— 见 §3,本族唯一一次新增 key |

### 部署

| | |
|---|---|
| deployment id | **6347944719** |
| sha | `a2c7af16bc16dacbed86abb1756b61166aed6720` —— **与本地 HEAD 逐字相同** |
| state | **success** |
| success 时刻 | **2026-09-09 10:46:17Z**(18:46:17 CST) |

**两个问题分开问过:**
1. **push 后约 101s 问「这个 sha 有部署记录吗」** —— **没有**(空数组 `[]`)。
   **又是那一幕:窗口里还没有本刀的记录。**
2. **继续等,记录在 `10:46:31Z` 那一轮出现;再问「它的 sha 是不是 `a2c7af1`」** ——
   `id=6347944719 · sha=a2c7af16… · state=success`。**等到 sha 对上才收工。**

### ★ 窗口长度 —— 第五个数据点,而【我这一刀的量法退步了,要说出来】

★★ **我没有像 TABLE-PHONE-4 那样记下推送那一刻的钟点,所以这个数我只能给出一个【区间】。**
可查的两个锚点:
* 提交时刻 `2026-09-09T10:43:26Z`(`git log -1 --format=%cI`,推送紧随其后);
* 推送**之后**的第一次读表 `10:43:59Z`。
* success `10:46:17Z`。

```
推送 ∈ [10:43:26Z, 10:43:59Z]  →  success 10:46:17Z  =  【138s – 171s】
```

| 刀 | 窗口 |
|---|---|
| TABLE-PHONE-1 | 165s |
| TABLE-PHONE-2 | 155s |
| TABLE-PHONE-3 | 150s |
| TABLE-PHONE-4 | 173s |
| **TABLE-PHONE-5** | **138–171s(区间,不是点)** |

☞ **这个区间整个落在既有描述里,所以那句话不用再改:仍然是「150–173s,方差约 15%」**
(TABLE-PHONE-4 把它从「不到 10%」诚实地放宽成 15%,本刀没有推翻它)。
☞ ★ **但要记下【本刀的量法比上一刀差】:** 上一刀报的是 `173s` 一个点,
我报的是一个 33 秒宽的区间,**原因只是我没在推送那一刻读一次表**。
**这不是那条常数变模糊了,是我的记录变粗了。**
**下一刀:`git push` 前后各 `date -u` 一次,把那个点钉死。**
☞ 与本仓库那条「写下来的成本必须是量过的成本」一致:**量得粗就报得粗,不去假装那是个点。**

### ★ 本刀会不会让下一份委托书的哈希又落后一格

**会 —— 原因还是那一个:这一节是部署【之后】才写得出来的,
所以它必然是一条【docs-only 的后续提交】,而下一份委托书多半写在它落地之前。**
☞ **这是第六次了。它不是失误,是这个流程的形状**(TABLE-PHONE-4 预测了第六次,
而它就是本刀开工时读到的那一条)。
   下一刀读到「HEAD 比委托书多一条只动交回报告的提交」时,照 §0 的授权 PROCEED 即可。
