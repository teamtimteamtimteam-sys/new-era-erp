# FONT-2 —— 交回报告(2026-09-11)

> ### ★ 一句话:屏幕上变了什么
> **没有写字号类的文字从浏览器默认 16px 落到了 14 / 20;全站写死的灰统一成两档品牌色
> (正文 `#182B4B` · 次级 `#62738C`);勾选框与单选框旁边那些【选项文字】从半粗回到了正常字重。**

> **这一刀完成【字体 / 排版】那一族。** FONT-1 定了「用哪一种字、标题多大、链接什么色」;
> **FONT-2 定「正文长什么样」**。★ **`v1.4.18` 在这一刀上线之后发。**

---

## 1 · 开工闸(§1.1–1.2)—— 每一项都重查过

| 检查 | 读数 | 判词 |
|---|---|---|
| `HEAD` | `6a7284934a32c3bd2bd08e17a3a3e7f367167f4b` | ✅ 与委托书逐字相同 |
| `origin/main` | 同一个 SHA | ✅ 相等 |
| 工作区 | `git status --porcelain` **无输出** | ✅ 干净 |
| `git diff --stat 1eda268 HEAD -- app/ scripts/` | **空**(0 字节) | ✅ **于是 round 1 的改前读数仍然有效** |
| round 1 的输出(§1.2 点名的 12 份) | **全部在** | ✅ `/tmp/FONT-2-stopgate.md`(1334 行)· `/tmp/font2/` 下的 11 份 · `before/after-routes.jsonl` **282 行 = 141 × 2** |

---

## 2 · §1.4 —— 委托书里当作阈值用的每一个数,逐条重量

> **判词三档:confirmed(重量之后逐字相同)· wrong · not re-measured。**

| # | 委托书说 | 本刀量到 | 判词 |
|--:|---|---|---|
| 1 | HEAD = `6a72849…` | 逐字相同,= `origin/main`,树干净 | ✅ **confirmed** |
| 2 | 次级 **12 / 400 / 16**(Q2) | spec §4.7.3 逐字就是这一档;取样页 `#62738C` 共 **137** 个元素,desktop **12px×64 / 14px×60 / 15px×13**、phone **14px×65 / 12px×64 / 15px×8** | ✅ **confirmed**(两个数都逐字重量过) |
| 3 | 正文 **14 / 400 / 20**,色 `#182B4B` | 取样页整页最大的一组:**14/400/20 `rgb(24,43,75)` ×108** | ✅ **confirmed** |
| 4 | `text-foreground`:**21 个** JSX className 站点 | ★ **21**(分母:原始 grep **34** · 注释里 **1** · 遮掉注释 **33** / 24 个文件;其中 12 处住在 cva 串里) | ✅ **confirmed** |
| 5 | `app/globals.css` 那句注释写着 **23** | ★ **33** —— 已就地更正 | ❌ **wrong**(委托书是对的,注释是错的) |
| 6 | 选项文字 **34 个**,今天全部 500 | 见 §6 的 Q6 读数 | ✅ **confirmed** |
| 7 | Q13① **13 个元素**坐在四种浅底上 | **13**(`#E5E7EB` 7 · `#E5EEF4` 4 · `#DDE7EF` 1 · `#E1F5FF` 1) | ✅ **confirmed** |
| 8 | Q13② 日历小片 **13 个元素**,3.13 / 3.12 | **13**(绿 8 · 红 5),比 **3.13 / 3.12** | ✅ **confirmed** |
| 9 | Q15 探针卡死 **约 1/141,路由不固定** | ★ **这一刀又撞了一次:`--mode=drift` 在 `/purchasing @ phone` 上卡死** —— **又是 phone,又是一条新路由** | ✅ **confirmed**(第四次独立命中) |
| 10 | 「998 个要逐处判」 | ★ **725**(分母 3423) | ❌ **wrong** —— 见 §3 |
| 11 | `text-[13px]` · `text-[15px]` 要转成 14/20 | ★ **范围内各 0 处** —— `text-[13px]` **只写在 `app/login/`**;`text-[15px]` 只写在 `table-style.ts` / `data-table.tsx`(关掉)与 `app/login/` / `app/brand-sampler/`(不许编辑) | ❌ **wrong**(那两个数是**全树**口径,不是**范围内**口径) |
| 12 | round 1 预测两层的停止条件都是 **零条** | 见 §7 的改后全量读数 | (见 §7) |

---

## 3 · §5.1 —— 分类:每一个站点的桶、判词与落点

**分母:FONT-2 范围内 3423 个代码点 / 481 个文件。**
全表:`/tmp/font2/classification-final.json`(逐站点:桶 · 判词 · 动作 · 那一条裁定 · 具体的 class 编辑)。

| 动作 | 站点 | 占 3423 |
|---|--:|--:|
| ★ **真的改了** | **1199** | **35.0%** |
| **按裁定不动** | **1726** | **50.4%** |
| **不用改**(已经是目标 / 零效果) | **478** | **14.0%** |
| ★ **判不出来,留着并列出** | ★ **20** | **0.6%** |

### 3.1 按桶 × 动作(每一格都带分母 3423)

| 桶 | 改 | 不动 | 不用改 | 列出 | 合计 |
|---|--:|--:|--:|--:|--:|
| SECONDARY | 1078 | 246 | 22 | 16 | **1362** |
| STATE | 8(只改字号)| 1161 | 0 | 0 | **1169** |
| OTHER | 10 | 108 | 250 | 1 | **369** |
| BODY | 101 | 50 | 206 | 2 | **359** |
| 第三档(缺席与分隔) | 2(只改字号)| 106 | 0 | 0 | **108** |
| OPTION TEXT | 0 | 55 | 0 | 0 | **55** |
| HEADING-LIKE | 0 | 0 | 0 | 1 | **1** |
| **合计** | **1199** | **1726** | **478** | **20** | **3423** |

> ★ **STATE 与第三档那两格里的「改」,改的【只有字号】,一个字色都没有动。**
> §3 的 SIZE CLASSES 那一条管的是「不在标准上的字号类」,它**不看颜色** ——
> 而这正是 round 1 实测过的那一层(P2)的字面:一条按 class 选元素的规则,
> **照样打中一个琥珀色的 11px**。把颜色的否决顺手套到字号上,
> 会让这一刀与那次实测**不是同一件事**。

### 3.2 按裁定【不动】的,逐条(合计 1726)

| 站点 | 那一条裁定 |
|--:|---|
| **862** | **Q16 / S4 状态色** —— 红 · 琥珀 · 绿 · destructive 四族 |
| **420** | **Q4 表格单元格** —— 源码里就在 `<td>/<th>` 里的 **146** · 写在列定义回调里、最终渲染进一个格子的 **274** |
| **175** | **R0(扩展版)** —— 自己没有字色类,颜色从一个带状态色的祖先继承(red-700 129 · amber-900 31 · blue-900 6 · green-900 4 · red-800 3 · red-600 2) |
| **86** | **颜色住在这个元素【自己的】内联 style 里** —— `--brand-muted-text` 51 · `--brand-text` 32 · `--brand-destructive` 3。★ **它们今天就已经在目标上,给它加一个类是零效果外加第二个主人** |
| **60** | **Q3 第三档:缺席与分隔**(`text-gray-300/400`) |
| **55** | **Q6 选项文字** —— 值由**一条 base 规则**给,一个调用点都不改 |
| **35** | ★ **`<ConfirmButton>` 是一颗按钮** —— 按钮那一族整族关闭。**round 1 的角色表只认 `<Button>` / `<button>`,漏了这一个组件名** |
| **14** | ★ **`app/**/pdf/*Document.tsx` 是 `@react-pdf/renderer` 的文档定义** —— 它渲染的是一份 PDF,不是 DOM,**Tailwind 的 class 在那里一个字节都不生效** |
| **11** | **Q10 `--brand-muted-glass`** 保留自己的 token |
| **10** | 颜色从祖先的**内联 style** 继承(`--brand-text` 6 · `--brand-muted-text` 4) |
| **1** | `app/components/ui/select.tsx` 的箭头 —— **控件那一族已关闭**,而且它给的是一个图标的颜色,不是文字 |
| **1** | S4:白字坐在实心底上 |

### 3.3 ★ 判不出来、留着并列出的 20 个(分母 3423)

| 形状 | 个数 | 为什么留着 |
|---|--:|---|
| className 是**算出来的**,颜色由一个兄弟表达式给(`'… text-xs ' + statusPillClass(x)`) | **5** | ★ 给它加一个字色类 = **给同一个颜色找第二个主人**,而**谁赢取决于生成的 CSS 里谁排在后面**,不取决于这一串里谁写在后面 |
| 颜色住在一条**算出来的**内联 style 里 | **2** | 同上 |
| className 是一个**三元 / `cn()` 表达式**,而要动的是它里面的**字色** | **5** | ★ **这一刀的一条通则:算出来的 className 里,【字号 token】可以换,【字色】不换。**<br>理由不对称,而这一点要说清楚:换一个字号 token(`text-[11px]` → `text-xs`)**不会**引入第二个颜色主人;而在一个三元里加 / 换字色,要先答「另一支怎么办」「它和外面那个 `cn()` 参数谁赢」。<br>· `alert.tsx:55` —— 另有一条:队列把它**点名**给了**开着的 ALERT-2b**;<br>· `card.tsx:11 / :51` —— `CardDescription` 的 `text-muted-foreground` **已经解析成 `--brand-muted-text`**(spec §4.4 的 MEASURED 值),换写法是**零效果**;<br>· `RequiredMetalsPanel.tsx:67` 与 `notifications/page.tsx:80` —— 两处都是 `cond ? 'text-gray-600 …' : '…'`,**改得动,但它要的是一次逐处判,不是一条机械规则**。☞ 它们是这 20 个里**最容易在下一刀结清**的两个。 |
| 自己没有字色类,却住在一条**横幅 / 徽章**里 | **6** | **它的颜色今天归那个容器** —— 与 R0 逐字同一条理由 |
| `card.tsx` 的 `CardTitle`(`text-base`) | **1** | ★ **它不是漂移,是 spec §4.4 记着的 MEASURED 值(16 / 500 / 22)** |
| 那唯一一处「画成标题、却不是标题标签」(`app/inbound/receive/done/[id]/page.tsx:82`) | **1** | Q7:**不换标签**(换标签是一次行为改动);只改 class 是把问题搬走,不是修 |

### 3.4 ★★ 手核改掉了规则表的哪几处 —— 每一处都是一次【会改错颜色】的判断

> §5.1 要求:**手核至少 30 个转换站点,外加每一个要改的 B / C 站点。**
> 实测:**手核 53 个分层抽样(层 = 编辑种类 × 模块,53 个层各抽 1 个)+ 13 个 B 组要改的
> + 131 个 C 组要改的(按形状归并成 3 类逐类看)+ 全部 33 个 colour-add。**
> ★ **抓到 5 类错,合计改掉 60 个站点的判词。**

| # | 规则表本来要做什么 | 手核为什么否了它 | 改掉几个 |
|--:|---|---|--:|
| ① | 把 `text-[color:var(--brand-destructive-text)]` 转成**正文色** | ★ round 1 的桶函数**不认任意值写法的品牌 token 字色类** —— 它只认 `text-destructive` 这种语义名。于是 2 处 destructive、4 处已经戴着次级 token 的、1 处已经戴着正文 token 的,**被【反着】改** | **7** |
| ② | 给一个 `<ConfirmButton>` 加字色类 | ★ **它是一颗按钮**,而按钮那一族整族关闭。**角色表只认标签名,不认组件名** | **35** |
| ③ | 给 `app/**/pdf/*Document.tsx` 里的 `<Text>` 加 Tailwind 类 | ★ **那是 react-pdf,不是 DOM** —— 加了也不生效 | **14** |
| ④ | 把一个 `<ConfirmButton details={<p className="… text-foreground">}>` 当成表格单元格文字 | ★ 它**写在**一个 `render:` 回调里,却**渲染进确认对话框** —— Q4 的判据是「渲染到哪里」,**两个方向都会错** | **4**(它们因此**回到了**范围里) |
| ⑤ | 给 6 个住在横幅里、自己没有字色类的站点加次级色 / 给 7 个颜色另有主人的站点加字色类 / 把 `CardTitle` 的 16px 降到 14px | 见 §3.3 | **14** |

> ### ☞ 而其中三类【不是】14 个例外,是【三条新规则】
> 手核的产出不是一张例外表,是三条可以机械执行的判据,写进了这一刀的分类器:
> ① **任意值写法的品牌 token 字色类**(`text-[color:var(--brand-*)]`)按 token 归桶;
> ② **祖先链要读第三层** —— 祖先与自己的**内联 style**、**四种浅底**、以及
>    写在 `cn(…)` 表达式里的状态色 token(round 1 的正则要求 token 两边是空白,
>    而在 `cn('… text-amber-900', className)` 里它右边是一个 `'`);
> ③ **一个站点【写在哪里】不等于【渲染到哪里】**,两个方向都要判。

---

## 4 · §5.2–§5.5 —— 落地的形状

### 4.1 一个目标一种写法(§5.2)

| 目标 | 采用的写法 | 为什么是这一个 |
|---|---|---|
| 正文色 | **`text-[color:var(--brand-text)]`** | 改前树里就有 **44 处**在用它 |
| 次级色 | **`text-[color:var(--brand-muted-text)]`** | 改前树里就有 **58 处**在用它;而语义名 `text-muted-foreground` 只有 **19 处** |
| 14 / 20 | **`text-sm`** | Tailwind v4 在这棵树上把它编译成 `.875rem / calc(1.25 / .875)` = **逐字 14px / 20px**(从构建产物读出来的) |
| 12 / 16 | **`text-xs`** | 同理,`.75rem / calc(1 / .75)` = **逐字 12px / 16px** |

★ **每一个 class 名都是一个完整的字符串字面量** —— 没有一处是拼出来的。

### 4.2 base 层那两条(§5.3)

```css
/* ① Q12 —— body 的默认字号与行高(与 FONT-1 的字色 / 字族同一条规则) */
body { font-size: 14px; line-height: var(--text-sm--line-height, calc(20 / 14)); }

/* ② Q6 —— 选项文字的字重,紧挨着 FONT-1 那条控件字重规则 */
@layer base {
  label:has(input[type='checkbox']),
  label:has(input[type='radio']) { font-weight: 400; }
}
```

### 4.3 §5.5 —— 剥的时候只拿走被替掉的那一个

★ **KEEP WINS 逐条兑现:** 编辑器在 `ownClassName` 给出的**源码区间**里做**整词替换**,
**只动那一个 token**,同一串里别的类一个字节不碰。

**自证(改后重扫,与分类器同一支扫描器):**
- ★ 计划落点 **1228** · 落地 **1228** · 跳过 **0**;
- ★ 改后重扫 **4056** 个站点(与改前逐字相同 —— **一个标签都没有多、也没有少**);
- ★ 1199 个改过的站点里,**1199 个**戴上了目标 class **且**不再戴着被替掉的那一个;
- ★ **判成"不动 / 不用改 / 列出"的 2224 个站点,className 逐字未变(0 个动了)。**

### 4.4 两处点名的单点改动

| 改了什么 | 为什么 | 读数 |
|---|---|---|
| `app/home.module.css` 的 `.badge`:`--brand-muted-text` → `--brand-text` | **Q13①** —— 这颗「not built」徽章的底是 `--brand-muted` `#E5EEF4`,次级色对它只有 **4.11:1**,过不了 AA | **4.11 → 12.02** |
| `app/finance/assets/new/NewAssetForm.tsx` 的那个 `<Link>`:加 `app-link` | **Q8** —— 它是全系统**唯一**一个没有拿到链接色的链接(它的蓝是从 `text-blue-900` 的横幅**继承**来的,className 就是 `underline` 两个字) | 见 §6 的 Q8 读数 |

★ **横幅本身一个字节都没有动**(它的蓝是一条没有人裁过的状态色 —— 见队列新加的第 ② 条)。

### 4.5 §5.6 —— `npx tsc --noEmit`

```
TSC_OWN_EXIT=0
```
(跑了两次:落点写完之后一次、`badge.tsx` 那一处之后一次,两次都是 0。)

---

## 5 · §6.1–6.2 —— 控件普查(改后)

| 跑法 | 覆盖 | 它自己的退出码 |
|---|---|---|
| `SURVEY_OUT=.survey-out/font2-after … --mode=drift` | **141 条路由 × 2 视口**;★ **phone 上 `/purchasing` 撞到那个已知的渲染器卡死**,按规矩单独补量(`--only=/purchasing,/tools/pricing/calculator`,**伴随路由的读数丢掉**)→ 合并后 **not-ok 0 条** | `DRIFT_OWN_EXIT=0`(覆盖断言 8 条全过)· 补量 `DRIFT_REPAIR_OWN_EXIT=0` |
| `--mode=edit` | 4 条路由 × 2 视口,not-ok 0 | `EDIT_OWN_EXIT=0`(覆盖断言 7 条全过) |

### ★ `--mode=edit` 在 FONT-2 【之后】仍然活着 —— 这一条是委托书点名要证的

| 路由 | 视口 | 候选 | 表内控件 | 与 FONT-1 / round 1 比 |
|---|---|--:|---|---|
| `/hr/leave/types` | desktop | **13** | 0 → **12** | ★ **逐字相同** |
| `/hr/leave/types` | phone | **13** | 0 → **12** | ★ **逐字相同** |
| `/hr/reviews/scale` | desktop | **4** | 0 → **14** | ★ **逐字相同** |
| `/hr/reviews/scale` | phone | **4** | 0 → **14** | ★ **逐字相同** |

☞ **这正是 FONT-1 那次量具改动要证的唯一一件事:判据从【颜色】换成【内容】之后,
它不会因为颜色变了而瞎掉。★ 而这一刀改的【就是颜色】—— 它仍然给得出候选。**

### 5.1 `--mode=compare`(改前 vs 改后)

```
成员:A=3550  B=3552
  多出来的成员 : 18      少掉的成员 : 16      值变了的成员 : 97
```

**97 个值变了的成员,按【哪一个字段变了】归类 —— 一条都没有落在意料之外:**

| 个数 | 变了什么 | 判词 |
|--:|---|---|
| ★ **68** | `label.native` 的 **font-weight 500 → 400** | ★ **Q6,一个不多一个不少** —— 34 个选项标签 × 2 个视口 = 68 |
| **27** | `TABLE` 的行高(63.84↔42.42 · 128.42↔112.42 · 144.42↔112.42 …) | ★ **两次跑之间【数据行变了】** —— 改前那一份是 FONT-1 round 3 于 **10:27 UTC** 量的,这一份是 **14:23 UTC**;中间 7 张表各多了 1–2 行真数据 |
| **2** | `TABLE` 表头高 **41 → 37**(`/purchasing/payment-terms/new`) | ★ **这一刀** —— 变小 4px,竖向,报告不停手 |
| ★ **2** | `select.native` **40px → 38px**,字号 **16px → 14px**(`/tools/pricing/formulas/new` #1) | ★ **这一刀,而它是一颗【已经在案的】控件** —— 见下 |

**多出来 18 / 少掉 16 个成员** —— ★ **全部是数据:** 多出来的 18 个都是 `TABLE` 的行;
少掉的 16 个是 `/finance/claims` 上的 4 个输入框 + 4 个标签(两个视口各 8 个)——
**那一页的表单随数据出现/消失**,不是这一刀拿走的。

### ★★ 那一颗原生 `<select>` —— 它是这一刀在【控件】上唯一动到的东西

| | |
|---|---|
| 它是谁 | `/tools/pricing/formulas/new` 的第 2 颗原生 `<select>` |
| 读数 | 高 **40px → 38px** · 字号 **16px → 14px**(两个视口逐字相同) |
| ★ **为什么它会动** | ★ **它【没有】字号类** —— 它是全树**唯一**一颗**从来没有采用共享控件样式**的原生 `<select>`(`docs/forward-queue.md` 的「合并的小件 ④」早就单独登记过它)。它的字号一直**从 `body` 继承**,于是 `body` 一改它就跟着动。 |
| ★ **它是不是一次停手** | ★ **不是。** ① §4(d) 管的是「S5 关掉的输出」,而这一颗**按记录就是那个【没有采用】共享样式的例外**;② 它**变小**,而变小不是停止条件;③ 它离 §4.1 的标准(**32px**)**更近**了;④ `selectFit` 逐颗重量:**改前装得下、改后装不下的 0 颗**。 |
| ★ **别的原生 `<select>` 为什么没动** | 它们都带着自己的字号类(`control-style.ts` 的 `text-base md:text-sm`),**`body` 的继承按构造够不着它们** —— 而这正是 round 1 那条「代码点 ≠ 屏幕上的元素」的又一次实测。 |

### 5.2 行高闸

```
node scripts/check-row-height-baseline.mjs \
  --now=.survey-out/font2-after/controls-drift-merged.json \
  --edit=.survey-out/font2-after/controls-edit-baseline.json \
  --row-height=report --baseline-heading='FONT-1 之后的读数'
ROWHEIGHT_OWN_EXIT=0
```
> ✓「12 张含控件的表:横向四项(滚动壳内容宽 · 表在不在)与基线相同;
> 竖向 **1 处**变化【报告,不停手】;390px 整页溢出**没有新增也没有长大**;
> 12 张已裁定横滚的表**一张都没有多出滚动范围**。」

★ 它还打了 **5 条**「表头签名重名」警告(`/settings/dictionaries` 6 张 · `/settings/reference` 3 张 ·
`/finance/cash-forecast` · `/finance/processing-costs` · `/inventory/reports/snapshot` 各 2 张)——
☞ **本刀的比对器按 `(签名, 序号)` 认表,没有踩这一条。**

★ **写完 `docs/row-height-baseline.md` §9 之后又自证了两次:**
① 对着 **§9** 跑 → `EXIT 0`,逐项相同(证明那一节**解析得出来、形状对**);
② 再对着 **§8** 跑 → ★ **它打出了那条【被取代】警告并点名 §9**(第 2012 行)——
**FONT-1 加的那个机制在这一刀上真的响了。**

---

## 6 · §6.3–6.4 —— 全量读数层(141 × 2)与【停止条件】

**跑法:** round 1 那支探针的读数层,每条路由读:每一张表的**逐列宽度 · 行高 · 滚动范围** ·
**整页溢出** · 每一颗原生 `<select>` 的 `fits`(保留区 20.3px)· **平台字体**
(`CSS.getPlatformFontsForNode`,在 `document.fonts.ready` 之后 —— **声明的字族不是渲染的字体**)。
★ **每走完一条路由就写一次结果,出错不覆盖已有结果。**

| | |
|---|---|
| 覆盖 | ★ **282 / 282**(141 条路由 × 2 视口),`READVERIFY_OWN_EXIT=0` |
| ⚠ 卡死 | ★ **1 条:`/purchasing/licences @ phone`** —— 又是 `phone`,又是一条**新**路由。按规矩单独补量,**伴随路由 `/tools/pricing/calculator` 的读数丢掉**,`REPAIR_OWN_EXIT=0` |
| 合并后 | ★ **282 行,ok 282,not-ok 0** |

### 6.1 ★★ §4 的停止条件,逐条 —— **四条全部是零**

| 条 | round 1 的预测 | ★ 实测 |
|---|---|---|
| **(a) 390px:改前 0 → 现在 >0(CARVE-OUT)** | 0 条 | ★ **0 条** |
| **(a) 390px:本来 >0 又长大** | 0 条 | ★ **0 条** |
| **(b) 不滚的表【开始滚】** | 0 张 | ★ **0 张** |
| **(c) 已滚的表【多出滚动范围】** | 0 张 | ★ **0 张** |
| 原生 `<select>`:改前装得下、改后装不下 | 0 颗 | ★ **0 颗** |
| **(d) S5 关掉的输出** | 没动 | ★ **没动**(`<Input>`/`<Textarea>` 在取样页上逐字不变;`control-style.ts` / `table-style.ts` / `button.tsx` / `table.tsx` **一个字节都没有被编辑**) |
| **(e) 取样页的 h2 / 字段标签 / 表 / `<Input>` / `<Textarea>`** | 一条都没响 | ★ **一条都没响** —— 见 §8 |
| 平台字体变了 / 不是产品字体 | — | ★ **0 条** |

> ### ☞ 于是【例外】与【修复】这两栏都是空的,而这个空是有名字的
> ★ **§3 的 Q1 例外一次都没有用上 —— 因为没有任何东西长大。**
> **零处例外证明 · 零处 `flex-wrap` 修复 · 零处列宽被动过。**
> ☞ 这与 round 1 在**同一棵 DOM 上真的注入两层**量出来的预测**逐条相同**
> (注入生效自证 281/281 · 撤销逐字回到改前 562/562 · 同机制正例跑 281 次响 149 次)。
> ★ **一次预测与一次实测对上了,而两次用的是【不同的机制】**(一次是注入样式表,
> 一次是真的改了 1199 个落点再跑一遍)—— **这比同一支量具跑两遍硬。**

### 6.2 NOT-stop,逐条报告

**① 整页溢出:全树 282 个路由×视口里,变了的只有 1 个 —— 而且是变【小】**

| 路由 | 视口 | 改前 | 改后 |
|---|---|--:|--:|
| `/purchasing/payment-terms/new` | phone | **149** | ★ **143**(−6) |

★ **round 1 预测的就是这一条,同一条路由、同一个数(149 → 143)。**

**② 表:18 张有差别(分母 —— desktop 与 phone 合计 408 张表)**

| 差在哪 | 张数 | 是什么 |
|---|--:|---|
| `bodyRows` + `rowH` | **14** | ★ **两次跑之间数据行变了**(balance-sheet 20→21 · claims 2→3 · expenses 6→7 · payables 17→19 · pnl 9→10 · trial-balance 28→30 · stocktakes 7→8,每条两个视口)。**改前那一份是 4 小时前量的。** |
| `colW` | **8** | ★ **列宽重新分配,而每一张表的【列宽合计】一个像素都没有变**(它们都是 `w-full`)。<br>逐张:`/finance/expenses` 1376→1376 · `/finance/pnl` 831.99→832.00 · `/hr/claims` 1088.01→1088.01 · `/hr/leave` 1087.99→1088.01 · `/stocktakes` 1375.99→1376.00 · `/purchasing/payment-terms/new` desktop 767→767、**phone 505.58→500.44(−5.14)** |
| `headH` | **2** | `/purchasing/payment-terms/new` **41 → 37**(两个视口),**变小** |

★ **没有一张表变宽。没有一张表开始滚。没有一张滚动范围变大。**

**③ 平台字体:282 / 282 条路由×视口的拉丁文字仍然是产品字体(`Google Sans`),0 条变化。**

---

## 7 · §6.5 —— 屏幕上的文字,逐项核对(每个数都带分母)

> **量法:** 141 条路由 × **两个**视口,首屏逐元素读计算值 + 逐元素往上走到第一个不透明的底。

### 7.1 正文(S2:14 / 400 / 20 · `#182B4B`)

| | desktop | phone |
|---|--:|--:|
| 首屏渲染成 `#182B4B`、**不在格子里**的元素 | **2032** | **992** |
| − 标题档(18 / 20 / 24px,FONT-1 的字号表) | 250 | 229 |
| − **字重不是 400**(`<label>` 的 500 · `font-medium` / `semibold` / `bold`) | 507 | 423 |
| − SVG 组织图的 `<text fontSize>`(`app/components/charts/OrgChart.tsx`,**不是一个 class 站点**) | 14 | 0 |
| − 日历事件小片(**Q13② 一个字节不碰**,11px / `leading-tight`) | 16 | 13 |
| − 字号不是 14px 的其余(**Q9:颜色说了算,字号留着**) | 176 | 19 |
| ★ **剩下的【正经的正文文字】** | **1069** | **308** |
| ★★ **其中逐字等于 14 / 400 / 20 的** | ★ **1069 / 1069 = 100%** | ★ **308 / 308 = 100%** |
| **不合的** | ★ **0** | ★ **0** |

### 7.2 次级(S3:12 / 400 / 16 · `#62738C`)

| | desktop | phone |
|---|--:|--:|
| 首屏渲染成 `#62738C`、不在格子里的元素 | **520** | **387** |
| − 字重不是 400 | 8 | 6 |
| − 字号不是 12px(**Q9 的镜像:一个 `text-sm text-gray-500` 转色之后仍然是 14px**) | 220 | 181 |
| ★ **剩下的【正经的次级文字】** | **292** | **200** |
| ★ **其中逐字等于 12 / 400 / 16 的** | ★ **280** | ★ **189** |
| ⚠ **不合的** | **12** | **11** |

⚠ ★ **那 12 / 11 个,逐条说清楚:** 它们全部是 `12 / 400 / **20**` —— 12px 配一个
**显式的 20px 行高**,出自 `app/components/overview/Figure.tsx:125` 的
`className="text-xs leading-5"`(路由:`/finance` `/hr` `/logistics` `/operation` `/purchasing` `/sales` 六个模块首页)。
★ **它改前就是这样** —— 这一刀**不剥行高类**(§5.5 的 KEEP WINS:只拿走被替掉的那一个 class)。
**不是这一刀造成的,登记在案。**

### 7.3 单独数的那几族(它们**不进**上面的分母)

| | desktop | phone | 归哪一条裁定 |
|---|--:|--:|---|
| 住在 `<td>` / `<th>` 里的文字 | **3218** | **1114** | Q4 |
| `--brand-muted-glass` | **395** | **257** | Q10 |
| 仍然指着 `--foreground` `#171717` 的 | **66** | **40** | Q5 的并存例外(树里剩 **11 处**代码点 / 4 个文件,全在关掉的组件里) |
| 白字坐在实心底上 | **335** | **210** | S4 |
| 状态色(红 / 琥珀 / 绿 / 蓝 / 紫 / 天蓝 / 翠绿 / destructive) | 首屏逐色直方图见 `/tmp/font2/r2/verify-text.json` | | Q16 / S4 |

### 7.4 ★★ Q12 —— 行高是一个【比例】,三个证明都过了

| 要证的 | desktop | phone |
|---|---|---|
| `body` 逐字 **14 / 20** | ★ **141 / 141 条路由** | ★ **141 / 141** |
| 一个 **15px** 的后代(自己不写行高) | ★ **21.4286px** | ★ **21.4286px** |
| 一个 **12px** 的后代 | ★ **17.1429px** | ★ **17.1429px** |

> ★ **写成【长度】的话,那两个后代都会是 20px。** 量到的是 15 × (20/14) 与 12 × (20/14) ——
> **这就是「它是一个比例」的全部证明**,而它是**在真的元素上读计算值**读出来的,
> 不是从样式表里看出来的。
>
> ★ **改前的读数(round 1,同一支探针):`body` 是 16 / 24。**
> ★ **`--text-sm--line-height` 这个 theme 变量实测 = `calc(1.25 / .875)` —— 它解得开**,
> 所以生效的是 `var(--text-sm--line-height)` 那一支,**不是 `calc(20 / 14)` 那个回退**。
> ☞ 两条路算出来的值逐字相同(1.428571…),回退留着是因为 Tailwind v4
> **只为真的用到的工具类发出 theme 变量** —— 一条解不开的 `line-height` 会**静悄悄地退回 `normal`**。

### 7.5 ★★ Q6 —— 选项标签 400,字段标签 500

| | desktop | phone |
|---|--:|--:|
| ★ **选项标签**(`<label>` 里有 checkbox / radio) | **34** | **34** |
| ★ **其中渲染成 400 的** | ★ **34 / 34** | ★ **34 / 34** |
| ★ **字段标签** | **390** | **390** |
| ★ **其中渲染成 500 的** | ★ **390 / 390** | ★ **390 / 390** |
| **两边都没有第三种值** | ✓ | ✓ |

**选择器的两个候选,各自命中几个(直接问 CSS,不用 JS 重新实现一遍判据):**

| 选择器 | 命中 |
|---|--:|
| ★ **采用的:`label:has(input[type='checkbox']), label:has(input[type='radio'])`** | ★ **34** |
| 对照:`label:has(> input[…])`(只认直接子元素) | **32** |

☞ ★ **后代版多抓到 2 个** —— 那两个把 `<input>` 又包了一层。**取的是覆盖得住的那一个,
不是字面最短的那一个。** 而它**一个字段标签都没有误伤**(命中 34 = 选项标签总数)。

### 7.6 FONT-1 的字号表纹丝不动

| 角色 | desktop | phone |
|---|---|---|
| `h1` | **24 / 700 / 32** `#182B4B` ×139 | ×138 |
| `h2` | **20 / 700 / 28** ×120 | ×119 |
| `h3` | **20 / 600 / 28** ×19 | ×19 |
| `<label>` 字段标签 | **14 / 500 / 20** ×390 | ×390 |
| `<label>` 选项标签 | ★ **14 / 400 / 20** ×34(Q6,**这一刀要的**) | ×34 |

### 7.7 Q8 —— `/finance/assets/new` 那个从横幅继承蓝色的链接

| | 改前 | ★ 改后 |
|---|---|---|
| 渲染色 | `lab(26.1542 15.7545 -51.5504)` = **`#1C398E`**(Tailwind `blue-900`,从横幅**继承**来的) | ★ **`rgb(0, 112, 157)` = `#00709D`**(`--brand-ocean-text`,FONT-1 裁过的链接色) |
| `className` | `underline` | `underline app-link` |
| 静息态装饰 | `underline` | `underline`(没有变) |

★ **Q8 要求先量一次:`#00709D` 对着【横幅真正的底】过不过 4.5:1。**

| 量到的 | |
|---|---|
| 横幅的底(逐层读 `background-color` 的**原文**,不猜) | `<div class="… border-blue-200 bg-blue-50 …">` 的 **`lab(96.492 -1.14644 -5.11479)`** |
| 按 CSS Color 4 (D50) 换算 | ★ **`#EFF6FF`** |
| ★ **换算器自证(同一支器,两个已知值)** | `lab(44.0605 29.0279 -86.0352)` → **`#155DFC`** = Tailwind v4 `blue-600` ✓<br>`lab(26.1542 15.7545 -51.5504)` → **`#1C398E`** = `blue-900` ✓ |
| ★ **`#00709D` on `#EFF6FF`** | ★ **5.07:1 ✓ 过 AA** → **裁定成立,`app-link` 留着** |
| 对照:它**原来**继承的 `#1C398E` on 同一个底 | **9.53:1** |

⚠ ★ **照直说这一步的代价:对比度从 9.53 掉到 5.07** —— 仍然远在 AA 之上,
但它**确实变低了**。换来的是:**全系统最后一个没有拿到链接色的链接,现在拿到了**。
**这是 Tim 的裁定要的那一件事,不是一次副作用 —— 但那个数要摆出来。**

> ### ⚠ 量的时候撞见一件量具的事,记在这里
> ★ **同一支探针里有【两支】背景色走法,而其中一支认不出 `lab()`。**
> FONT-1 那支 `links()` 报这个链接坐在 **`rgb(241, 249, 254)`**(= 页面底色)上 ——
> 它**一路走过了那条横幅**,因为横幅的 `bg-blue-50` 计算出来是 `lab(...)`,而它只认 `rgb()`。
> FONT-2 那支 `opaqueBgOf` 认得 `lab()`,报的是横幅本身。
> ☞ **两支走法、两个答案,而报告只会印其中一个。**
> 本刀的处置:**Q8 这一条不信任何一支走法,直接把祖先链上每一层的 `background-color` 原文打出来再算。**

### 7.8 ★★ §6.6 —— 次级色的对比度:**首屏不过 4.5:1 的 = 0**

★ **desktop 0 · phone 0。**(日历事件小片按裁定不碰,而它们今天渲染的是**正文色**,不是次级色,
所以它们不在这个分母里;它们自己的 3.13 / 3.12 已经单独立案。)

⚠ ★★ **而这个零是【修出来的】—— 两处,一处是我造成的,一处不是:**

| 在哪 | 读数 | 谁造成的 | 怎么修的 |
|---|---|---|---|
| `/inbound/receive` 两颗输入框旁边的**单位后缀**(`kg`) | `#62738C` on `bg-gray-100` **`#F3F4F6`** = ★ **4.39** ✗ | ★ **这一刀。** 它把 `text-gray-600` 转成了次级色,而 `#F3F4F6` **不在** Q13① 点名的那四种浅底里 | 字色改判为**正文色** `--brand-text` = ★ **11.94** ✓。同一个形状在 `/stocktakes/[id]` 的 `CountList.tsx` 上还有 1 处,一起改(**UNMEASURED** —— 动态路由) |
| `/hr/leave/calendar` · `/tools/calendar` 的**星期表头**(`Mon`…`Sun`,每页 7 个,共 14 个元素) | `#62738C` on `--brand-muted` **`#E5EEF4`** = ★ **4.11** ✗ | ★ **不是。它改前就是这个颜色** —— 这一刀只把它的 `text-[11px]` 换成了 `text-xs` | 字色改判为**正文色** = ★ **12.02** ✓ |

> ### ☞ 第二条要单独说两句
> ① **修它【不是】一次范围扩张** —— Tim 的 Q13① 裁的是**那四种底**
> (`#E5E7EB` · **`#E5EEF4`** · `#DDE7EF` · `#E1F5FF`),不是一份 13 个元素的名单。
> 这一格坐的正是其中一种。**照裁定办,不是替他扩范围。**
>
> ② ★★ **round 1 为什么没有看见它:那一份对比度普查【只量了 phone】。**
> 而这一格写着 `hidden md:block` —— **它是宽屏视图,按构造不可能出现在那份读数里。**
> ☞ **一份「0 个不合规」的读数,它的分母是【它量过的那些视口】。** 已记进 `AGENTS.md`。

---

## 8 · §6.7–6.10 —— 中文 · 取样页 · 截图 · UNMEASURED

### 8.1 §6.7 中文(`NEXT_LOCALE=zh`,6 条路由 @ 390px)

| 路由 | 中文渲染成什么字 | 中文字号 / 行高 | 整页溢出 | 截图 |
|---|---|---|--:|---|
| `/` | **PingFang SC** | **14 / 20** | **0** | `zh_root@390.png` |
| `/finance/invoices` | PingFang SC | 14 / 20 | 0 | `zh_finance_invoices@390.png` |
| `/purchasing/orders` | PingFang SC | 14 / 20 | 0 | `zh_purchasing_orders@390.png` |
| `/hr/employees` | PingFang SC | 14 / 20 | 0 | `zh_hr_employees@390.png` |
| `/inventory` | PingFang SC | 14 / 20 | 0 | `zh_inventory@390.png` |
| `/settings/dictionaries` | PingFang SC | 14 / 20 | 0 | `zh_settings_dictionaries@390.png` |

★ **6 / 6 条:平台字体与字号【与 round 1 的读数逐字相同】,整页溢出全部 0。**
★ 平台字体用 `CSS.getPlatformFontsForNode` 读(**不是**声明的字族串),在 `document.fonts.ready` 之后。
⚠ **仍然成立的那一半(FONT-1 记过):这条只在 macOS 上量过。Windows 上那条链落到微软雅黑,一次都没有验证。**

### 8.2 §6.8 `/brand-sampler`(§4(e)),两个视口

| 值 | round 1(改前) | ★ round 2(改后) | 判词 |
|---|---|---|---|
| `h2` | 24/700/32 ×4 · 18/700/28 ×1 | ★ **逐字相同** | ✅ 没响 |
| **`<td>`** | 14/400/20 ×162 · 15/400/21.43 ×46 · (次级色)14/400/20 ×8 · 15/400/21.43 ×4 | ★ **逐字相同** | ✅ 没响 |
| **`<th>`** | 14/500/20 ×14 · 15/500/21.43 ×20 · 14/700/20 ×1 | ★ **逐字相同** | ✅ 没响 |
| **`<Input>`** desktop | 14/400/20 ×5 | ★ **逐字相同** | ✅ 没响 |
| **`<Input>`** phone | ★ **16/400/24** ×5 | ★ **逐字相同** | ✅ **手机上仍然是 16px** |
| **`<Textarea>`** phone | 16/400/24 ×3 | ★ **逐字相同** | ✅ 没响 |
| **字段标签** | 14/500/20 | ★ **14/500/20 ×9**(全部) | ✅ **没响** |
| `<label>` 合计 22 个 | 22 个全部 500 | ★ **9 个 500 + 13 个 400** | ★ **预期之内** —— 那 13 个是列显隐面板的**选项标签**(§4(e) 明写「Q6 的选项标签变成 400 是预期的」)。q6 逐个数:**可见的选项标签 12 个,全部 400;字段标签 9 个,全部 500**(第 13 个是一个不可见的选项标签) |
| **`body` 自己** | **16 / 24** | ★ **14 / 20** | ★ **预期之内**(§4(e) 明写),而且**这一刀要的就是它** |

> ### ★★ §4(e) 一条都没有响,而这个「没响」是【被证明过的】,不是一个假的零
> ① 同一次读数里 `body` 确实从 **16/24 变成了 14/20** —— ★ **证明这一刀真的到了这一页**;
> ② 手机上 `<Input>` / `<Textarea>` **仍然是 16px** —— ★ **证明 base 层那两条没有走进关掉的控件**;
> ③ 取样页上的 Q12 三证也过了:`body` 14/20 · 15px 后代 **21.4286** · 12px 后代 **17.1429**。
>
> ⚠ **而这条闸对这一刀本来就很松,必须说出来:** `@layer base` 压不过工具类,
> 而取样页写着 class 的元素因此**按构造**够不着 —— **唯一够得着它的是 `body` 那一条**
> (字号与行高是**继承**下去的,不是工具类)与 **Q6 那条 `:has()` 规则**(特指度赢过 `label`)。
> ☞ **本刀两个成分都报:一个是【真的没动】,一个是【这条闸拦不住它】。**

★ **另外报一件 §4(e) 没有点名、但也动了的:** `app/components/ui/card.tsx` 的 `CardTitle`
带着 `text-base`,而它**唯一的消费者就是取样页**。★ **这一刀【没有改它】** ——
`docs/variant-c-spec.md` §4.4 把 `CardTitle` 记成 **MEASURED 16 / 500 / 22**,改它是改一条实测标准。
**列进"判不出来、留着没动"的 20 个里。**

### 8.3 §6.9 截图(390px,`/tmp/font2/shots-after/`,共 **25** 张)

| 组 | 张数 |
|---|--:|
| ★ round 1 那 **7** 张,**同一个取景**重拍 | 7 |
| 读数层顺手拍的另外 12 条路由(与 FONT-1 同一张单子) | 12 |
| 中文 6 条 | 6 |
| ★ **例外证明的截图** | ★ **0 张 —— 因为这一刀一处例外都没有用上(没有任何元素长大)** |

### 8.4 §6.10 ★ UNMEASURED —— 改了、但这一趟【没有在屏幕上看见】的

| | 个数 | 为什么 |
|---|--:|---|
| ★ **住在【动态路由段】(`[id]` 一族)下的落点** | ★ **613 / 1199**(122 个文件) | ★ **探针只走 141 条静态路由** —— 58 条带 `[id]` 的详情 / 编辑页**一条都不走**(取 id 那套机制住在 `smoke-routes.mjs` 里,复制它就是仓库里第二份会漂的定义)。<br>最多的:`app/inbound/[id]` 63 · `app/sales/orders` 60 · `app/output/[id]` 58 · `app/purchasing/orders` 52 · `app/finance/assets` 51 |
| ★ **`/login`** | — | ★ **`app/login/` 下一个文件都没有被编辑。** 而 `/login` 对**已登录会话重定向**,所以探针从来没有真的渲染过它。<br>★ 它上面会变的是两样,而两样都来自**共享**的东西:① `body` 的默认字号与行高;② `app/home.module.css` 的 `.badge` 字色(Q13①)。★ **两样都没有在 `/login` 上被量过。** |
| ★ `app/components/ui/badge.tsx` 那一处 `text-foreground` | 1 | ★ **这个组件今天【零个产品使用者】**(`check-base-isolation.mjs` 的 `GUARDED` 里就有它)。**它不渲染在任何一条路由上。** |
| `/stocktakes/[id]` 的 `CountList` 单位后缀 | 1 | 动态路由 |
| 首屏之外的一切 | — | ★ 这一趟量的是**首屏**:对话框、展开的下拉、Tab 面板、折叠行**从不打开**。★ **一个"点开才出现"的元素在这里读成不存在,而那个零的准确含义是「首屏上一次都没出现」,不是「系统里没有」。** |

---

## 9 · ★★ Tim 的 Q1–Q16,逐条 DONE / NOT DONE ★★

> **判据(委托书 §9):「代码里有」不算做到,「操作员在屏幕上看得见」才算。**

| # | 裁定 | 状态 | 证据 |
|--:|---|---|---|
| **Q1** | 例外放宽到「只由这一刀裁过的四个文字值引起的横向增长 = 报告」;**CARVE-OUT 原样保留**;**写进 spec §4.7.7** | ★ **DONE**(写进去了)<br>★ **一次都没有用上** | spec §4.7.7 新增一小节:谁 / 哪一天 / 为什么,**FONT-1 那句原样留着并划掉、标明被取代**。<br>★ **实测:没有任何元素长大 —— (a)(b)(c) 全部 0 条**,于是**零处例外证明**。见 §6.1 |
| **Q2** | 次级 = 12 / 400 / 16;确认它的出处是 Tim 在 FONT-1 闸上的批语,并把取样页那三种字号的读数记在旁边 | ★ **DONE** | spec §4.7.3 新增一格:出处 + 「desktop 12px×64 / 14px×60 / 15px×13、phone 14px×65 / 12px×64 / 15px×8 —— **多数派在两个视口上是反的**」+ 「§S3 那句作废」 |
| **Q12** | 行高写成**比例**;改前先量 `body` 继承的行高;改后证 14/20、15px 后代 ≈21.43、12px 后代 ≈17.14 | ★ **DONE** | ★ **改前 `body` 是 16 / 24**(round 1 探针,取样页与全树一致)。<br>★ **改后:141/141 条路由两个视口 `body` = 14 / 20;15px 后代 = 21.4286;12px 后代 = 17.1429。** 见 §7.4 |
| **Q3** | 第三档(`text-gray-300/400`)**不动**,并在 spec 里给它名字「第三档:缺席与分隔」 | ★ **DONE** | spec §4.7.10.3。**106 个站点不动**(另外 2 个只改了字号,一个字色都没动)。★ 顺带就地更正了那个桶名 —— **它里面一个失效控件都没有** |
| **Q4** | 表格单元格文字**整族不动**,判据是「它最终渲染进哪里」 | ★ **DONE** | **420 个站点不动**(源码里就在 `<td>/<th>` 里的 146 · 写在列定义回调里的 274)。<br>★ **而判据的【反向】也兑现了:4 个 `<ConfirmButton details={…}>` 写在 `render:` 回调里、却渲染进对话框 —— 它们【回到了】范围里,照样转了。** spec §4.7.10.4 |
| **Q5** | 21 个 JSX className 的 `text-foreground` 转成正文色;`alert.tsx` / `badge.tsx` 按「有没有归给一条开着的刀」判;更正 globals.css 那句「23」 | ★ **DONE** | ★ **21 / 21 全部转了。**<br>★ `alert.tsx` —— 队列**点名**给了开着的 **ALERT-2b**(「115 处页级红盒子的去处」)→ **不碰**。<br>★ `badge.tsx` —— 队列里 `badge` **0 次** → **转了**(⚠ 它零个使用者,**UNMEASURED**)。<br>★ 树里剩 **11 处 / 4 个文件**,全在关掉的组件里 → spec §4.7.10.5 记成一条**并存例外**。<br>★ globals.css 那句「23」→ **33**,并写明它为什么会错 |
| **Q6** | 一条 base 规则把选项文字压回 400;不改任何调用点;报选择器与它命中的数 | ★ **DONE** | 选择器 **`label:has(input[type='checkbox']), label:has(input[type='radio'])`**;命中 **34**(直接子元素版只命中 32)。<br>★ **实测:34 个选项标签全部 400;390 个字段标签全部 500;一个调用点都没改。** 见 §7.5。spec §4.7.2 |
| **Q7** | 「画成标题却不是标题标签」那一处:**报,不改** | ★ **DONE** | 代码点 **1 个**:`app/inbound/receive/done/[id]/page.tsx:82`。**没有改**,并立成一条队列条目(新加的五条 ⑤) |
| **Q8** | `/finance/assets/new` 那个 `<a>` 加 `app-link`,**先量 `#00709D` 对着横幅真正的底** | ★ **DONE** | ★ **先量了:横幅底 `lab(96.492 …)` = `#EFF6FF`,`#00709D` on 它 = 5.07:1 ✓ 过 AA** → 加了。<br>★ 改后实测渲染色 `rgb(0, 112, 157)`,两个视口各 1 个。**横幅一个字节没动。** 见 §7.7 |
| **Q9** | 次级字号 + 正文字色:**颜色说了算,字号留着**;这一族单列报 | ★ **DONE** | **26 个站点**,其中这一刀动了 **18** 个(**只动颜色**)。屏幕上的对应读数:desktop **176** 个正文色元素字号不是 14px、**220** 个次级色元素字号不是 12px —— **全部是这条裁定的直接结果**。spec §4.7.10.6 |
| **Q10** | `--brand-muted-glass` 那一族不动 | ★ **DONE** | **11 个站点不动**;屏幕上 desktop **395** · phone **257** 个元素仍然渲染 `#55657D`。spec §4.7.10.7 |
| **Q11** | 不拆刀 | ★ **DONE** | 一刀做完 |
| **Q13①** | 四种浅底上的 13 个元素取**正文色** | ★ **DONE**,而且**多修了 14 个** | `app/home.module.css` 的 `.badge`(**4.11 → 12.02**);其余 11 个**本来就已经是正文色**(逐个确认过:`/stocktakes` 的徽章没有字色类、`/tools/converter` 与 `/hr/org` 的两处是内联 `--brand-text`、`/finance/assets` 那颗是禁用钮的 `--color-disabled-text`)。<br>★ **另外 14 个是 round 1 按构造看不见的**(只量了 phone,而那是宽屏视图)—— 见 §7.8 |
| **Q13②** | 日历事件小片**一个字节不碰**,并登记 | ★ **DONE** | 一个字节没碰(它们的 `const cls` 是一个**算出来的**字符串,连扫描器都没有把它算成站点)。**队列新加的五条 ①** |
| **Q14** | round 1 预测两层零停手;§4 照常 | ★ **DONE** | ★ **实测四条停止条件全部 0**,与预测逐条相同。见 §6.1 |
| **Q15** | 补量的单条读数**算数**;把探针卡死那件事登记进 `known-issues.md` | ★ **DONE** | ★ **这一刀自己又撞了两次**(`--mode=drift` 的 `/purchasing @ phone`;读数层的 `/purchasing/licences @ phone`)——**又是 phone,又是新路由**。两条都单独补量、伴随路由读数丢掉。`docs/known-issues.md` 的 `FONT2-PROBE-WEDGE-390` |
| **Q16** | 状态色逐族点头;蓝 / 紫 / 天蓝 / 翠绿**不动并登记** | ★ **DONE** | **862 个状态色站点 + 175 个 R0 继承的**一个字节没动;**蓝 22 · 紫 4 · 天蓝 1 · 翠绿 1** 不动,立成**队列新加的五条 ②**。spec §4.7.10.2 |

### ★ SIZE CLASSES(§3 最后那一条)

| 类 | 范围内 | 转了 | 没转的去处 |
|---|--:|--:|---|
| `text-[11px]` → `text-xs` | 44 | ★ **23** | 21 个在表格单元格里(Q4) |
| `text-[10px]` → `text-xs` | 19 | ★ **8** | 11 个在表格单元格里 |
| `text-base` → `text-sm` | 23 | ★ **19** | 3 个是 **E6 触控档**的单位后缀(spec §2A.1:收货 / 盘点两页保留 `text-base`)· 1 个是 `card.tsx` 的 `CardTitle`(spec §4.4 的 MEASURED 值) |
| `text-[13px]` | ★ **0** | 0 | ★ **它只写在 `app/login/`** —— 委托书那个数是**全树**口径 |
| `text-[15px]` | ★ **0** | 0 | ★ 只写在 `table-style.ts` / `data-table.tsx`(关掉)与 `app/login/` / `app/brand-sampler/`(不许编辑) |
| ★ **列出来、没有标准覆盖的展示字号** | | | `text-lg` **11** · `text-[0.95em]` **1** —— **队列新加的五条 ③** |

---

## 10 · §2–§8 的每一条,逐项 DONE / NOT DONE

### 10.1 §2 —— 在案的裁定(S1–S8)

| # | 它说什么 | 状态 | 证据 |
|--:|---|---|---|
| **S1** | 标准是**整张** `/brand-sampler`,spec 是它的成文形式 | ★ **DONE** | §6.8 两个视口整页读;spec §4.7.3 / §4.7.10 新增的每一条都标了出处 |
| **S2** | 正文 14 / 400 / 20,色 `#182B4B`(`body` 已经是这个色) | ★ **DONE** | §7.1:**1069 / 1069 · 308 / 308 逐字等于 14/400/20** |
| **S3** | 次级 12 / 400 / 16,色 `#62738C` | ★ **DONE** | §7.2:**280 / 292 · 189 / 200**;不合的 12 / 11 个是 `text-xs leading-5`,**改前就是那样** |
| **S4** | **KEEP WINS**;状态色与含义色留 | ★ **DONE** | 编辑器在**源码区间**里做整词替换,**只动被替掉的那一个 token**;2224 个"不动 / 不用改 / 列出"的站点 className **逐字未变(0 个动了)** |
| **S5** | 八族关闭(control-style / table-style / `<Input>`/`<Textarea>` 输出 · button.tsx · table.tsx · FONT-1 的 base 规则 · 控件字重 · 链接 token 与类 · 字体面与字族 · `--foreground` / `--background`) | ★ **DONE** | `git diff --name-only` 里**这几个文件一个都没有** ★;`--foreground` / `--background` 两个 token **一个字节没动**;`app-link` / `app-link-inline` 的定义没动(只在一个调用点**加了**一个 `app-link`,那是 Q8 点名的) |
| **S6** | 永不编辑 `app/login/` 与 `app/brand-sampler/` | ★ **DONE** | `git diff --name-only` 里这两个目录**一个文件都没有** ★。它们**渲染出来的字**确实变了(`body` 默认 · Q6 的 base 规则 · `home.module.css` 的徽章)—— **那按 S6 不算编辑,也不是范围泄漏** |
| **S7** | 不改文案 / i18n 键 / 行为 / 提交形状 / 标签;不迁移 | ★ **DONE** | `check-i18n`:缺键 **0** · 新增 **0** · 删除 **0**;`git diff --name-only` 里**没有** `messages/` / `i18n/` / 任何 `.sql`;**一个标签都没换**(Q7 那一处只报不改)—— 改后重扫 **4056** 个站点,与改前**逐字相同** |
| **S8** | `min-w-0` / `max-w-full` 禁令仍在 | ★ **DONE** | 这一刀**一个宽度类都没有加**。1228 个落点全部是字色 / 字号 token |

### 10.2 §5 · §6 · §7 · §8 的每一条

| 条 | 状态 | 在哪 |
|---|---|---|
| §5.1 分类表(逐站点:桶 / 动作 / 裁定 / 具体编辑) | ★ DONE | `/tmp/font2/classification-final.json` · 本文 §3 |
| §5.1 手核 ≥30 个转换站点 + 每一个要改的 B / C 站点 | ★ DONE | **53 个分层抽样 + 13 个 B + 131 个 C + 全部 33 个 colour-add**;★ **抓到 5 类错,改掉 60 个判词** —— 本文 §3.4 |
| §5.1 判不出来的列出来 | ★ DONE | **20 个**,本文 §3.3 + 队列新加的五条 ④ |
| §5.2 一个目标一种写法 | ★ DONE | 本文 §4.1 |
| §5.3 base 层三件(body 字号行高 · 选项字重 · 注释更正) | ★ DONE | 本文 §4.2;globals.css 那句「23」→ **33** |
| §5.4 落点转换 | ★ DONE | **1199 个站点 / 1228 个 class 落点 / 297 个文件** |
| §5.5 剥的时候只拿走被替掉的那一个 | ★ DONE | 本文 §4.3 的自证 |
| §5.6 `tsc --noEmit` | ★ DONE | `TSC_OWN_EXIT=0`(两次) |
| §6.1 drift + edit,改前跑,not-ok 补量 | ★ DONE | 本文 §5 |
| §6.1 `--mode=edit` 仍在两条点名路由上给得出候选 | ★ DONE | 13 与 4,**逐字相同** |
| §6.2 `--mode=compare` + 行高闸 | ★ DONE | 本文 §5.1 / §5.2 |
| §6.3 全量读数层 141 × 2(列宽 · 行高 · 滚动范围 · 溢出 · select fit · 平台字体) | ★ DONE | 本文 §6 |
| §6.4 §4 逐条 + 例外证明 + 修复 | ★ DONE | ★ **四条全 0;零处例外;零处 `flex-wrap` 修复;一个列宽都没有被人为动过** |
| §6.5 文字逐项核对 | ★ DONE | 本文 §7 |
| §6.6 次级色对比度 | ★ DONE | ★ **0 / 0**,而它是**修出来的** —— 本文 §7.8 |
| §6.7 中文 6 条 | ★ DONE | 本文 §8.1 |
| §6.8 取样页 §4(e) | ★ DONE | 本文 §8.2 |
| §6.9 重拍 round 1 那 7 张 + 例外截图 | ★ DONE | 7 张重拍;**例外截图 0 张,因为没有例外** |
| §6.10 UNMEASURED | ★ DONE | 本文 §8.4 |
| §7 五份文档 | ★ DONE | 本文 §11 |
| §8.1–8.4 四道检查 | ★ DONE | 本文 §12 |

---

## 11 · §7 —— 文档(与工作【同一个提交】)

| 文档 | 改了什么 |
|---|---|
| **`docs/variant-c-spec.md`** | ★ **§4.7.3**:正文与次级从「等 FONT-2」改成**已落地**,每一个值标了出处;新增「次级那 12px 的出处」一格(Q2)与「落地的形状」一格;★ **就地更正「998 个要逐处判」**(998 是**全角色**的 B+C,含 FONT-1 已改完的 287 个链接;范围内要逐处判的是 **725**,分母 **3423**)。<br>★ **§4.7.2**:新增**选项文字字重 = 400** 的例外(Q6),`<label>` 那一行加了指针。<br>★ **§4.7.7**:新增 Q1 的放宽 —— ★ **FONT-1 那句「字号 / 行高 / 字重造成的增长仍然是停手」原样留着、划掉、标明【已被取代】**,并写了谁 / 哪一天 / 为什么;**CARVE-OUT 一个字没改**。<br>★ **§4.7.10(新)**:最终规则表(class → 桶 → 目标)· 状态色名单(Q16)· 第三档(Q3)· 表格单元格(Q4,含它的**反向**坑)· `text-foreground` 并存例外(Q5)· 成对规则(Q9)· muted-glass(Q10)· 对比度与 Q13 · ★ **§4.7.10.9 落地之后的全部读数** |
| **`docs/row-height-baseline.md`** | ★ 新增 **§9「FONT-2 之后的读数」**(§6 / §7 / §8 **一个字节没动**):12 张含控件的表的四项 · ★ **逐列宽度** · 390px 整页溢出 · 横滚表,外加**三个机读块**。<br>★ **自证过两次**:对着 §9 跑 `EXIT 0`;再对着 §8 跑 → **它打出了那条【被取代】警告并点名 §9(第 2012 行)** |
| **`docs/forward-queue.md`** | ★ FONT-2 **标成已完成**,【字体 / 排版】那一族 **整族关闭**;`v1.4.18` 由它发。<br>★ **就地更正**那条「`body{font-size:14px}` 会给 `/settings/dictionaries` 加 +12px」的预测 —— **实测一个像素都没动**,并写清楚 round 1 的 **L3 是一个【上界】层**、而这一刀真的下的那一条只动 `body`。<br>★ 外观索引刷新:**新加 6 行**。<br>★ **新立五条**:日历小片对比度 · info 横幅的蓝(+ 紫 / 天蓝 / 翠绿)· 三种没有标准的展示字号 · 判不出来的 20 个站点 · 那一处「画成标题却不是标题标签」。<br>★ 「合并的小件 ④」那颗原生 `<select>` **就地更正读数**(40/16 → 38/14,并写清楚它为什么会动、为什么不是回归) |
| **`docs/known-issues.md`** | ★ 新增 **`FONT2-PROBE-WEDGE-390`**:390px 那一趟探针每跑一趟约卡死 1 条路由(**1 / 141**),★ **而卡的不是同一条** —— 四次独立命中,共同点只有「都在 phone」。补量的办法与「补量算数」的先例一起写进去 |
| **`AGENTS.md`** | ★ **CONFIRM-1 命中表**新增 **FONT-2** 一行(六个数 / 断言,逐条实测)。<br>★ **新增四段失效模式**:①「一支扫描器的**分桶**那一层,可以和它的**读取**那一层不一样瞎」;②「一条按 class 选元素的注入 / 规则,会顺着 class 走进这一刀根本不许碰的组件里」(含**两个方向**的「写在哪里 ≠ 渲染到哪里」);③「一个**上界**层的读数,被当成了一次窄得多的改动的预测」;④ ★ 「一份『0 个不合规』的读数,它的分母是**它量过的那些视口**」 |

---

## 12 · §8 —— 四道检查,每一条都带它【自己】的退出码与实测时长

| # | 命令 | 起(UTC) | 止 | 秒 | ★ 它自己的退出码 | 读数 |
|---|---|---|---|--:|---|---|
| 8.1 | `node scripts/check-i18n.mjs` | 14:54:38 | 14:54:38 | **<1** | ★ `I18N_OWN_EXIT=0` | ★ **缺键 0 · 新增 0 · 删除 0** —— `git diff --name-only` 里**没有任何** `messages/` / `i18n/` 文件 |
| 8.2 | `npm run build` | **15:45:54** | **15:46:36** | ★ **42** | ★ `BUILD_OWN_EXIT=0` | **24 条静态检查全过**;eslint **基线 error 42 / warning 88 → 实跑 error 42 / warning 87**(★ **没有超过基线**;少掉的那 1 条 warning 在 `EmployeeForm.tsx`,★ **INPUT-2 那一刀就已经还掉了** —— INPUT-2b / INPUT-3 / FONT-1 三份交回报告记的都是 87,**不是本刀造成的**);★ **`check-instrument-selfproof`:36 支量具都写了瞄准线,其中 23 支带覆盖断言** |
| 8.3 | `node scripts/smoke-routes.mjs`(**不带** `--reach`) | **15:46:36** | **16:03:23** | ★ **1007** | ★ `SMOKE_OWN_EXIT=0` | ★ **248 ok · 6 skipped(no data)· 0 FAILED** |
| 8.4 | `python3 db/gate.py`(经 `db/run_detached.sh --timeout 2700`) | 见 §12.1 | | | 见 §12.1 | 四条判词逐字引在 §12.1 |

### ⚠ 冒烟顺手报了 6 条【滞留的临时行】—— 照直说它们不是这一刀的

```
· materials       ZZ-SMOKE-PROBE  (866.9h)  ← 仍被引用
· materials       ZZ-SMOKE-M25    (866.9h)  ← 仍被引用
· materials       ZZ-SMOKE-NTF    (699.6h)  ← 仍被引用
· suppliers       ZZ-SMOKE-S25    (866.9h)  ← 仍被引用
· customers       ZZ-SMOKE-CJK    (224.5h)  (无人引用)
· inbound_batches ZZ-SMOKE-IB25   (866.9h)  ← 仍被引用
```

★ **最年轻的那一条也有 224.5 小时(≈9 天),而这一刀从开工到现在不到 3 小时。**
☞ **它们按年龄就不可能是这一刀留下的。** 那道检查自己写着「**只报告,不删除**」——
理由是**归属与年龄正是一次清扫无法安全知道的东西**,而其中 5 条**仍被真单据引用**,
删掉会让一张真单据指向一个已删的行。★ **本刀照规矩:报出来,不顺手删。**

---

### 12.1 §8.4 —— `db/gate.py`,四条判词逐字

| | |
|---|---|
| 命令 | `db/run_detached.sh --log /tmp/font2/r2/logs/gate.log --label "db/gate.py" --timeout 2700 -- python3 db/gate.py` |
| 起 → 止(UTC) | **16:04:11 → 16:15:03** |
| 包装器等到那一行用了 | **652 秒** |
| 闸自己报的 wall-clock | **522 秒** |
| ★ **它自己的退出码** | ★ **`RUN_EXIT=0`** —— **这是脚本自己打进日志的那一行**,不是包装器的状态 |

**四条判词,逐字引用:**

```
判词【可重建性】:✓ 仓库能从零建出库(prelude 足够,B1/B2 双侧断言见上)
判词【镜像 vs 线上】:✓ 一致(结构、种子、引导、自洽、definer)
判词【行为断言】:✓ db/fixtures 全部通过(建出来的库跑起来是对的)
判词【匿名面】:✓ 线上是基线的子集(基线 327 条)
```

★ **这一刀一行 SQL 都没有改** —— 闸绿是预期之内的;跑它是因为**推送之前必须绿**,
而「预期之内」不是一条读数。

---


## 13 · ★ Tim 该去哪儿看

> **每一条都写清楚:哪条路由 · 哪个视口 · 那一页要什么权限。**
> ★ 本刀所有读数用的都是一次**用完即删的 admin 会话**,所以下面每一页
> **用 admin 打开**都看得见;标着「要某个权限」的那几条,换一个没有那个权限的人看到的是拒绝态。

| 看什么 | 路由 | 视口 | 权限 | 你会看见的那一件事 |
|---|---|---|---|---|
| ★★ **选项文字回到正常字重**(这一刀屏幕上最好认的一件) | `/purchasing/payment-terms/new` | **390px** | 采购编辑 | 「Active」「%」「Fixed」三处勾选 / 单选旁边的字,**不再比输入框里的字粗一档** |
| 同上,更多的 | `/tools/pricing/formulas/new`(6 处)· `/hr/leave/new`(5)· `/suppliers/new`(5)· `/sales/customers/new`(4) | 两个都行 | 各模块编辑 | 同上 |
| ★ **没有写字号类的文字从 16px 落到 14px** | `/` 首页 · `/finance` 等模块首页 | desktop | 任何 | 说明性的那几行字**小了一档**,与旁边的正文对齐了 |
| ★★ **全站的灰归成两档** | `/me` · `/settings/accounts` · 任何一张新建表单 | **390px** | 自己 / 管理员 | 输入框下面那些**提示语**统一成同一种灰(`#62738C`);而「没有关联」「—」那一档**刻意更淡**,没有被拉平 |
| ★ **首页那颗「not built」徽章** | `/` | 两个都行 | 任何 | 它的字**从灰变成了深蓝** —— 那是因为它原来在自己那块浅底上**过不了对比度**(4.11:1 → 12.02:1) |
| ★ **日历的星期表头** | `/tools/calendar` · `/hr/leave/calendar` | ★ **desktop**(手机上是另一种视图) | 任何 | `Mon`…`Sun` 那一行的字**从灰变成了深蓝**,同样是对比度 |
| ★ **那个一直不是链接色的链接** | `/finance/assets/new` | 两个都行 | 财务编辑 | 蓝色提示框里那句「record an expense…」的链接,**从横幅的深蓝变成了系统的链接色** |
| ★ 溢出**变小**的那一条 | `/purchasing/payment-terms/new` | **390px** | 采购编辑 | 右边少滚 6px;表头也矮了 4px |
| ★ 中文 | 任意路由 + 切中文 | **390px** | 任何 | ★ **中文一个字都没有变小** —— 它本来就是 14px,`body` 那条规则够不着它 |
| ⚠ **这一刀改了、但你在首屏看不到的** | 详情 / 编辑页(`/finance/invoices/<id>` 一类) | — | — | ★ **1199 个落点里有 613 个住在动态路由下,这一趟一次都没有被量过** —— 要看就得真的打开一张单据 |

---
## 14 · 这一刀每一步的实测时长

| 步 | 起(UTC) | 止 | 秒 | 它自己的退出码 |
|---|---|---|--:|---|
| §1 开工闸(HEAD · origin/main · 树 · `diff 1eda268` · round 1 的 12 份输出) | 13:52 | 13:53 | ~60 | 0 |
| §1.3 通读(停止闸 1334 行 · spec §2A/§4.6/§4.7.x/§9 · brand-tokens · 队列四节 · 行高基线 §8 · known-issues · AGENTS · 9 个源码文件 · 3 支量具的抬头) | 13:53 | 14:00 | ~420 | — |
| ★ §5.1 分类器(6 次迭代,每一次都是一处手核抓到的错) | 14:00 | 14:12 | **~720** | `DECIDE_OWN_EXIT=0` |
| ★ 祖先链第三层扫描器(`ancestry.mjs`,3 次迭代 —— 第一版踩了【开标签里套着别的标签】那个坑) | 14:12 | 14:17 | **~300** | `ANC_OWN_EXIT=0` |
| §5.3–5.5 落地(base 层 3 处 + 1228 个 class 落点 + 2 处点名改动) | 14:19 | 14:21 | **~120** | `APPLY_OWN_EXIT=0`(计划 1228 · 落地 1228 · 跳过 0) |
| §5.6 `tsc --noEmit` | 14:21:42 | 14:21:50 | **8** | `TSC_OWN_EXIT=0` |
| ★ §6.1 `--mode=drift`(141 × 2) | **14:23:42** | **14:52:35** | ★ **1733** | `DRIFT_OWN_EXIT=0` |
| §6.1 `--mode=edit` | 14:52:35 | 14:53:49 | **74** | `EDIT_OWN_EXIT=0` |
| ★ §6.3 全量读数层(141 × 2,含 19 张截图) | **14:54:45** | **15:22:33** | ★ **1668** | `READVERIFY_OWN_EXIT=0` |
| §6.1 补量 `/purchasing` | 15:22:34 | 15:24:39 | **125** | `DRIFT_REPAIR_OWN_EXIT=0` |
| §6.7–6.9 取样页 + 7 张截图 + 中文 6 条 | 15:24:39 | 15:26:31 | **112** | `AFTER2TASK_OWN_EXIT=0` |
| ★ §6.4 的两处对比度修复 + 6 条路由重量 | 15:27:30 | 15:29:02 | **92** | `REPAIR_OWN_EXIT=0` |
| Q8 横幅底色实测 | 15:38 | 15:39 | **~50** | `Q8_OWN_EXIT=0` |
| §8.1 `check-i18n` | 14:54:38 | 14:54:38 | **<1** | `I18N_OWN_EXIT=0` |
| ★ §8.2 `npm run build` | **15:45:54** | **15:46:36** | ★ **42** | `BUILD_OWN_EXIT=0` |
| ★ §8.3 `smoke-routes`(不带 `--reach`) | **15:46:36** | **16:03:23** | ★ **1007** | `SMOKE_OWN_EXIT=0` |
| ★ §8.4 `db/gate.py` | **16:04:11** | **16:15:03** | ★ **652**(闸自报 wall-clock 522) | ★ `RUN_EXIT=0` |

★ **与 round 1 的估价比:** 流程那一半估 **≈5301 秒**,实测
(drift 1733 + 读数层 1668 + edit 74 + build 42 + smoke 1007 + gate 652 + 补量 217)= ★ **5393 秒**,
**差 1.7%**。☞ **那份估价是从实测拼出来的,而它站住了。**

---

## 15 · ★ 我【没能】验证的

| # | 没验证什么 | 为什么 |
|--:|---|---|
| 1 | ★ **613 / 1199 个落点**(住在动态路由下的) | 探针只走 **141 条静态路由** —— 取 id 那套机制住在 `smoke-routes.mjs` 里,复制它就是仓库里第二份会漂的定义。☞ **它们是【未测量】,不是【不存在】** |
| 2 | ★ **首屏之外的一切** | 对话框、展开的下拉、Tab 面板、折叠行**从不打开**。★ 一个"点开才出现"的元素在这里读成不存在 |
| 3 | ★ **`/login` 本身** | 它对已登录会话**重定向**,所以探针从来没有渲染过它。★ 它上面会变的两样(`body` 默认字号 · 那颗徽章的字色)**都没有在 `/login` 上被量过** |
| 4 | ★ **`badge.tsx` 那一处 `text-foreground`** | 那个组件**零个产品使用者**,今天不渲染在任何一条路由上 |
| 5 | ★ **Windows 上的中文回退链** | 与 FONT-1 逐字同一条:只在 **macOS + `chrome-headless-shell`** 上量过。Windows 上那条链落到微软雅黑,**一次都没有验证** |
| 6 | ★ **非首屏的次级色对比度** | §6.6 的「0 个不合规」,它的分母是 **141 条静态路由 × 2 视口 × 首屏**。☞ **这一次还多了一个限定词:视口** —— 而 round 1 正是在这一条上漏掉了 14 个元素 |
| 7 | **深色模式** | 本仓库没有深色模式(`dark:` 变体 0 处),不适用 |

---

## 16 · 部署与残留

> ★ **这一节在紧随其后的一次【纯文档】提交里填。**
> 理由与 FONT-1 逐字同一条:**部署与残留的读数要等这一次推送真的上线之后才存在**,
> 而把它们写进工作那一个提交,等于在读数出现之前就先写下判词。

| 项 | 状态 |
|---|---|
| §8.6 推送后三方 SHA 相等(`HEAD` · `origin/main` · `git ls-remote`) | ⬜ 待填 |
| §8.7 Production 部署:先绑 deployment id → sha,再问状态;要求 `success`;数它的状态记录条数 | ⬜ 待填 |
| §8.8 残留(用完即删账号 0 · 幽灵授权 0 · `reap-ephemeral` 干净 · `.ephemeral/` 空 · 自己的进程与端口 0 · 孤儿 chrome) | ⬜ 待填 |

---

## 17 · ★★ v1.4.18 的发布说明 —— 给测试的人看的那一句 ★★

> **We've finished the type work: every screen now uses one product typeface with one set of heading sizes, one body size, and just two text colours — so please look on purpose at the small print under form fields, the labels next to tick-boxes and radio buttons, and any page you read a lot of numbers on, and tell us if anything now looks too faint, too small, or out of step with the line above it.**

---

pbcopy < docs/handbacks/FONT-2.md
