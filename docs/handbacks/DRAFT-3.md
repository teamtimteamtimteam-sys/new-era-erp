# DRAFT-3 —— 草稿模型表搬家,**第 3 刀**(2026-09-21)

> ### ★ 一句话:**四张表搬完了,而这一刀最值钱的产出是【两次测量】** ——
> ★ `#9` 的 `hasFixed` 第一次被证(此前是一次外推);
> ★★ `IDLE-DRAFT-GRID-HALF-RESTORE` 第一次被**复现**(此前整条标着 `NOT MEASURED`)。

**开工闸:** 工作树干净;`HEAD` = `origin/main` = `git ls-remote origin main` =
`190d9f1b2d7acfaf2d8c247ba9651a4adf30d972`(DRAFT-2 收尾,仅文档)。

**零迁移** —— `db/` 一个字节没动。于是**没有备份、没有 gate、没有破窗**。

**搬了四张:** `#9 TemplateForm` · `#8 NewOrderForm`(付款计划)· `#6` / `#7 NewWorkOrderForm`。
**`<EditableTable>` 调用点:8 → 12。**

---

## 0 · grilling 改了什么 —— 九个问题,Tim 九条全收

★ **三条改变了这一刀的形状,不是措辞:**

### ★★★ G1 · `page-owned` 下,一个【只读列】在手机上会整个消失

**这是委托书、DRAFT-0、DRAFT-2 都没有记的一条,而它直接改掉了 `#8` 的列设计。**

`editable-table.tsx:632` 是 `phoneCols = editing ? editableCols : restCols`,
而 `page-owned` 下 `editing` **恒为真**(`:625-626`)。☞ **展开区只画【有 `edit` 的列】。**
于是一个**没有 `edit`** 的只读列:不在手机那一行里(它不是 priority),
**也不在展开区里** —— 它**整个消失**。

⚠ **`#8` 的金额列正是这一种**,而它恰好是那块要被拆掉的 `TABLE-PHONE-3` 注释
**专门护着**的东西(「拿掉的是那一列,不是那个事实」)。
☞ **Tim 的 Q1 裁定:金额列 `priority: true`** —— 它是唯一能让那个数**零次点按**留下来的办法。
★ **`#24` 没有撞上这一条**,因为它没有「只读且非 priority」的列。

### ★★ G2 · `#7` 那两处空【有意思】,`#24` 的 `—` 惯用法搬过来就是错的

`NewWorkOrderForm.tsx:163-165` 白纸黑字写着:一个空格子读起来像「这一栏不重要」,
**而这一栏正是六个月后唯一能回答「这个数可不可信」的东西**。
两处 `<option value="">` 各自带着一句话:物料是**「没有预期」**,依据是**「还没有人说过」**。
☞ **Tim 的 Q4:只读投影里写那句话本身,永远不要 `—`。**
★ 而 `#6` 的物料空**确实**只是一个没用上的槽 —— 那里 `—` 是对的。**同一个文件,两种答案。**

### ★★ G3 · 那道闸对【内联数组字面量】记一条 `unresolved`,**而且照旧退出 0**

`check-editable-name.mjs:185-194` 按 `columns={IDENT}` 在同一个文件里定位那个数组;
定位不到就记 `unresolved`(`:187`)并**照常退出 0**(`:244`)。
☞ **写成内联数组字面量的表,是【没有人守着】的表,而构建是绿的。**
★ **本刀四张全部写成同一个文件里的具名标识符**,`#6`/`#7` 同一个文件所以是**两个互不相同的名字**。
**读数坐实了:12 个调用点 / 查了 12 个 `columns` 区段 / `unresolved` 0 个。**

★ 另外六条(G4–G9)在 §1 的裁定表里,它们改的是判断,不是发现。

---

## 1 · Tim 的九条裁定(2026-09-21,全部照收)

| | 问的是 | 裁定 |
|---|---|---|
| **Q1** | `#8` 的只读金额列在手机上怎么办 | ★★★ **`priority: true`** —— 那个数**零次点按**,正是被拆掉那块注释护的事 |
| **Q2** | 每张表留哪几列在 390px 上 | `#9` 序号+名目 · `#8` 序号+名目+**金额** · `#6`/`#7` 序号+物料 |
| **Q3** | G4 那个下标行键,修还是登记 | ★★ **修,只修 `#8` / `#9`** —— 计数器 uid,**在桥那一处剥掉**,服务端零改动;**记下 `#6`/`#7` 为什么不需要** |
| **Q4** | `#7` 的空怎么投影 | ★ **写那句话本身,永不 `—`** |
| **Q5** | `dirty` 覆盖到哪 | **只覆盖它自己那个数组**;页面级字段留不下提醒 —— **照直记**;站内 `<Link>` 不拦,**而那是对的** |
| **Q6** | `#6`/`#7` 的序号列用哪个键 | **新增 `processing.colSeq: '#'`**,与 `sales` / `purchasing` 同深度同拼写 |
| **Q7** | 加一期钮与比例合计放哪 | **留在组件外面,一个字不动** |
| **Q8** | 顺手量 `IDLE-DRAFT-GRID-HALF-RESTORE` 吗 | ★★★ **量** —— 而且**量出来是什么就报什么**,不复现的话那比那条修订更值钱 |
| **Q9** | 搬家前的 390px 读数 | ★ **付那一次构建** —— 这是旧 markup 还在树上的最后一刀 |

---

## 2 · 建了什么

| 批次 | 提交 | 是什么 |
|---|---|---|
| ① | **`ecbb03ca`** | `#9 TemplateForm` —— 最小的一张,把形状钉下来 |
| ② | **`269fe3a0`** | `#8 NewOrderForm` 付款计划 —— 最重的一张 |
| ③ | **`946f2c76`** | `#6` / `#7 NewWorkOrderForm` —— 两张表一次提交 |

### 2.1 ★★ 服务端**一个字都没改**,而这一次它是【构造上】成立的

`#8` / `#9` 的 uid **放在那一行【旁边】,不放进它里面**:

```ts
type TermLine = { uid: string; line: OrderTermInput }
const termsJson = JSON.stringify(terms.map((x) => x.line))   // ← 没有任何剥离动作
```

☞ **交出去的那个对象从头到尾没有被碰过** —— 载荷与搬家前逐字节同形,
**不是「记得把 uid 剥掉」,是剥不掉**。★ 浏览器读数坐实:两张表的载荷键
**一个 `uid` 都没有**(`#9` 六个键 · `#8` 六个键,与搬家前逐字相同)。

> ★ **第一版写的是 rest 剥离(`({ uid, ...rest }) => rest`),而 eslint 冻结闸拦下了它**
> —— 那会新增一条 `no-unused-vars`。**按名拒是对的,改法比原方案更好**:
> 换成「uid 放旁边」之后,那条不变量从**一句要记住的话**变成了**一个结构**。

### 2.2 ★ `#6`/`#7` 连桥都没有

这个文件**没有 `<form>` 元素**,`createWorkOrder({…})` 收的是一个带类型的实参。
☞ **它没有 `*_json` 可画,那道闸对它也无事可做**(全文件 `name=` 仍然 0 个)。
**这两张是纯粹的外观 + 手机档的活**,与 DRAFT-2 §1.3 说的逐字一致。

### 2.3 ★★ `#6`/`#7` **不需要 uid**,而这写进了代码

`#8`/`#9` 的 uid 是为**删行 / 整个数组换掉**付的账。
`#6`/`#7` 是**定长空槽(5 / 3)**:不加行、不删行、没有任何东西会整片换掉它们 ——
**下标从头到尾指着同一个槽,它本身就是稳定的键。**
☞ 理由**写在类型旁边**,免得下一刀来「统一」。

---

## 3 · 逐表:得到什么,失去什么

| | `#9 TemplateForm` | `#8 NewOrderForm` 付款计划 | `#6` / `#7 NewWorkOrderForm` |
|---|---|---|---|
| ★★ **得到(手机)** | 390px **整页溢出 533/390 → 390/390** | **552/390 → 390/390** | ⚠ 两端都 `NOT MEASURED`(见 §4.3) |
| ★ **得到** | 序号列已有;两处断点的删除钮**合成一个槽** | ★ 同上;**金额靠 priority 留在那一行上** | ★ **第一次有序号列**;★★ **第一次有「未保存」提醒**(此前一个都没有,也没有 IDLE-DRAFT) |
| ⚠ **失去** | 390px 就地打字(4 列)；**删除钮 0 → 1 次点按** | 390px 就地打字(**两个单选 + 条件数字框 + 触发下拉 + 条件日期**)；删除钮 0 → 1 次点按 | 390px 就地打字(2 列 / 4 列) |
| ★ **不变** | `hasFixed` · 比例合计 · 提交禁用 · `lines.length===1` 的删除禁用 | 比例合计 · 提交禁用 · `termsEdited` · `{terms.length>0&&}` 条件 | `filledLines` → `blocked` 与那句琥珀色提示 |

★ **`#8` 失去的那一份是本族至今最大的一次**,而它换回来的是一页**不再横向溢出**的手机屏。

### 3.1 ★★ `dirty` 逐表是一个【说出来的决定】(Tim 的 Q5)

| 表 | 判据 | 为什么是这一个 |
|---|---|---|
| `#9` | **与进门时那一份比** | 这张表单同时服务 `/new` 与 `/[id]/edit`;按「有没有字」算,**编辑一份既有模板会一进门就是脏的**,提醒立刻变噪音 |
| `#8` | **有没有期次** | 它是建单页,进门时 `terms` 恒为空 —— 两种判据在这里是同一件事,取简单的。★ **套模板自动填的也算脏**:判据问的是「会不会丢东西」,不是「谁填的」 |
| `#6` / `#7` | **各自那个数组里有没有内容** | ★★ **两个值,谁都不替谁说话** |

★★★ **`#6`/`#7` 那两个值【不需要合并】,而合并是错的:**
组件是**每个实例各自**挂一个 `beforeunload`、各自由自己那个 `dirty` 把着
(`editable-table.tsx:474` · `:479-483`),而**浏览器无论几个监听器调了
`preventDefault`,只弹一个框**。☞ **合并只会让一张表声称另一张表的状态。**

⚠ **照直记三条盖不住的:**
① **站内 `<Link>`(取消钮)不拦** —— 组件抬头声明过的限制,**而对一颗取消钮那也正是对的:
明说要走的人不该被再问一遍**;
② **页面级字段不在任何一张表里**(`#6`/`#7` 的排产日与备注、`#9` 的名称/说明、`#8` 的供应商与采购行)——
只改它们不会有提醒;
③ `#8` 那一条对 `IDLE-DRAFT-GRID-HALF-RESTORE` 是**缓解,不是修复**(§6)。

---

## 4 · 读数(**每一个都是那支脚本自己的退出行**)

| 量具 | 判词 |
|---|---|
| `npx tsc --noEmit` | **`TSC_OWN_EXIT=0`** |
| `npm run build`(34 条静态检查 + `next build`) | 批次① **`BUILD1_OWN_EXIT=0`** · ② **`BUILD2_OWN_EXIT=0`** · ③ **`BUILD3_OWN_EXIT=0`** · 收尾 **`BUILD4_OWN_EXIT=0`**;冷构建约 **43.6s** |
| `node scripts/check-i18n.mjs` | **`I18N_OWN_EXIT=0`** |
| `node scripts/check-error-swallowing.mjs` | **`SWALLOW_OWN_EXIT=0`**(0 unallowed · 0 queued · 9 allowlisted) |
| `node scripts/check-editable-name.mjs` | ★ **`NAME_OWN_EXIT=0`** —— **12 个调用点 / 查了 12 个 `columns` 区段 / 带 `name` 的控件 0 个 / `unresolved` 0 个** |
| `node scripts/smoke-routes.mjs` | ★ **`SMOKE_EXIT=0`** —— **250 ok · 6 skipped(没数据)· 0 FAILED**;计时 225 条,合计 **499.7s**,中位数 **2060 ms**。★ 后台起(`run_detached.sh`),**12 分钟超过前台 10 分钟的上限** |
| `node scripts/probe-draft3-before.mjs` | **`BEFORE_OWN_EXIT=0`** —— 搬家**之前**的 390px(§4.2) |
| `node scripts/probe-draft3.mjs` | ★★ **`PROBE_OWN_EXIT=0`** —— **24 条全过**(§4.1) |
| `db/gate.py` · 备份 | ★ **没跑,而且不需要** —— 零迁移,`db/` 一个字节没动 |

★ `check-datatable-phone`:**165 → 169**(DataTable 157 · **EditableTable 8 → 12**)。
那一条「静态读不出 1」(`MySelfAssessmentPanel:186`)**原样还在**,本刀没有碰它。

### 4.1 ★★ 浏览器证明(`PROBE_OWN_EXIT=0`,24 条全过)

探针 `scripts/probe-draft3.mjs`,跑在**生产构建**上(`next start`),等的是**水合收尾**(`__reactFiber$…`)。
★★ **它一次提交都不发** —— 判据读的是 `new FormData(form)`,**那就是按下提交会送出去的东西**,而读它不写库。

#### ★★★ `#9` —— 一个派生值管着【另一个字段存不存在】,第一次被量

| 判据 | 读数 |
|---|---|
| 比例模式:币种字段**不存在**,补位的隐藏输入在 | `<select name=currency>` **false** · `<input hidden name=currency>` **true** |
| ★★★ 选了**定额** → 币种字段**长出来了**,隐藏那个退场 | `<select>` **true** · `<input hidden>` **false** |
| 桥只出现一次,而且是一个数组 | `lines_json` **1 次** |
| ★ 载荷里**没有 uid** | 键 = `label · mode · percentage · fixed_amount · trigger_event · days_offset`(与搬家前逐字相同) |
| 格子里的具名控件 | **0 个** |
| ★ 390px 整页横向溢出 | **`scrollWidth 390 / clientWidth 390`** ← 搬家前是 **533 / 390,溢出** |

> ★★ **闸轮把这一条标成「一次外推,不是一次测量」。现在它是一次测量。**
> `page-owned` 此前没有任何一张表的派生值管着**另一个字段的存在与否** ——
> `#24` 只有一个 `dirty` 推导。**今天这一条有了它的第一份证据。**

#### ★★★ `#8` —— 两期、一个条件字段、表外面的合计、以及那颗按不动的钮

| 判据 | 读数 |
|---|---|
| ★★★ **金额列留在 390px 那一行上**(Tim 的 Q1) | 看得见的表头 = **`["", "#", "Label", "Amount"]`** ← **零次点按,与搬家前相同** |
| ★★ 触发改成 `fixed_date` → **条件日期字段长出来了** | 第二行展开区里的 `date` 输入 **1 个** |
| ★★★ **表【外面】的比例合计跟着草稿动** | `Instalments total 90% — the remainder is not scheduled.`(60 + 30) |
| ★★★ **手机上打的字【到了】桥里,而桥只有一份** | `terms_json` **1 次**;`[0]={label:"Deposit", percentage:"60"}` · `[1]={label:"Balance", percentage:"30", trigger_event:"fixed_date", due_date:"2026-12-31"}` |
| ★ 载荷里**没有 uid** | 键 = `label · mode · percentage · fixed_amount · trigger_event · due_date` |
| 格子里的具名控件 | **0 个** |
| ★ 390px 整页横向溢出 | **`390 / 390`** ← 搬家前是 **552 / 390,溢出** |
| ★★★ **合计推到 120% → 提交钮按不动,并且说出了为什么** | `disabled=true` · `Instalment percentages total 120% — more than 100%`;改回 60% → `disabled=false` |
| ★ **1280:390 上打的字出现在格子里** | 两行格子里的名目 = **`["Deposit", "Balance"]`** ← **同一个 store,两个断点** |
| ★★★ **1280:桌面上打的字到了桥里,桥仍然只有一份** | `terms_json` **1 次** · `[0].label="Deposit-desktop"` · `[1].due_date="2026-12-31"` |
| 1280 整页横向溢出 | `1280 / 1280` |

> ### ⚠ ★★ 第一版探针**五条一起红,而红的是探针** —— 照直记
> 判据写的是「展开区里看得见的第 0 个输入框」,**而 `page-owned` 下 `open` 是一个【集合】**:
> 组件展开第二行时**不会**收起第一行(那一支没有 `onSave`,走不到 `begin()` 那条
> 「至多一个键」的约束)。☞ **于是第二行的字被打进了第一行。**
> ★ 改法:按**那一行自己的面板**取控件(`window.__panel(n)`)。
> ★★ **这正是本仓库那条法则的又一张脸:一条判据必须认【它自己那一行】,
> 而不是「屏幕上第一个长得像的东西」** —— 而它值钱的地方在于:
> **那五条红把一个【探针缺陷】报成了【产品缺陷】,差一步就会去"修"一棵没有坏的树。**

★ **两处在册的探针缺陷都避开了:** 每一次 CDP 调用带 **30s 上限**(`PROBE-UNBOUNDED-CDP-WAIT`);
收尾那条 `lsof` 把**本进程**滤掉(`PROBE-LSOF-KILLS-ITSELF`)。
★ **复核(收尾,只读):** 残留探针账号 **0** · `.ephemeral/` **空** ·
一张采购单、一份模板都没有被建出来(探针只读 `FormData`,**一次都没有提交**)。

### 4.2 ★★ 搬家**之前**的 390px(Tim 的 Q9)—— 这是最后一次量得到它

`scripts/probe-draft3-before.mjs`,跑在**未改动的树**上,`BEFORE_OWN_EXIT=0`。

| | `#9` `/purchasing/payment-terms/new` | `#8` `/purchasing/orders/new` |
|---|---|---|
| 表里的控件 | **5 个,5 个都看得见**(就地可填) | **5 个,5 个都看得见** |
| 列 | 表头 5 列,**看得见 4 列** | 表头 6 列,**看得见 4 列** |
| 表宽 | **501px** | **520px** |
| ★★ 整页横向溢出 | **`scrollWidth 533 / clientWidth 390` —— 溢出** | **`552 / 390` —— 溢出** |

☞ ★★★ **两页在搬家之前【都是横向溢出的】,而此前没有任何人量过。**
**DRAFT-2 为 `#24` 留下的那处 `NOT MEASURED` 没有在这一刀重演。**

### 4.3 ⚠ 三处 `NOT MEASURED`,照直标出来

1. ★ **`#6` / `#7` 的 390px,搬家前后【都没有量】。** 委托书点名的浏览器证明是
   「sized to the cut,落在 `#8` 上」,而 `#6`/`#7` 顺着那句话一起没有量。
   **它们过了 tsc、构建、闸与冒烟,而没有一只眼睛看见过它们在 390px 上画出来的样子。**
2. ⚠ **`#18` / `#20` 的服务端收参点仍然没有定位**(闸轮实查:两个表单目录里没有
   `actions.ts` 兄弟文件)—— §8 的范围里按「与本刀同量」算的,**而那一格没有量过**。
3. ⚠ **DRAFT-1 §2.1 那处【验证缺口】原样成立** —— 本刀没有还上它。
   ☞ ★ 但**本刀这四张不在那个缺口里**:三条路由全是建单/建模板页,**不需要数据就画得出来**,
   而且 `#8` / `#9` **真的被看过了**(§4.1 的 24 条)。

### 4.4 冒烟 —— `SMOKE_EXIT=0`

**250 ok · 6 skipped(没数据)· 0 FAILED**,与 DRAFT-2 那一次**逐字相同**。
★ 那 6 条 skipped 与本刀无关(源表空着的那几条,与 DRAFT-1 / DRAFT-2 记的是同一批)。
★ 本刀动过的三条路由**一条都没有出现在 SKIP 名单里,也没有 FAILED**。

---

## 5 · ★★ 两块 `TABLE-PHONE-3` 注释的理由 —— **拆掉了,搬到这里,不许成孤儿**

**DRAFT-2 的 Q5 点名要的那件事,在这一刀兑现。** 两块注释的原文理由:

> **「手机档被拿掉的那一列(金额 / 删除),原样叠在『序号』那一格里 ——
> 拿掉的是那一列,不是那个事实。」**

★ **两块的去处【不一样】,而这一点最要紧:**

| 被拿掉的那一列 | 搬家前怎么活下来的 | 搬家后怎么活下来的 |
|---|---|---|
| **删除**(`#8` + `#9`) | 桌面在自己那一列;手机叠在序号格里 | ★ **`rowActions`** —— 桌面在动作列,手机在**展开区末尾**。⚠ **0 → 1 次点按** |
| **金额**(`#8` 独有) | 桌面一列;手机叠在序号格里,带着自己的列头 | ★★ **`priority: true`** —— **仍然在手机那一行上,零次点按** |

☞ ★★★ **金额那一半【不能】走展开区**,理由是 §0 的 G1:`page-owned` 下展开区只画
`editableCols`,一个只读列会整个消失。**所以那块注释护的事实,是靠换一个载体活下来的,
不是靠换一个写法活下来的** —— 两者的区别正是这一刀差点漏掉的东西。

★ **组件的展开区【是】那份叠加块的正规写法**(DRAFT-2 §3 已经写下这一句),
所以**删除**那一半是「换一个写法,不是丢掉那个事实」;
而**金额**那一半如果照抄那句话,就会把事实丢掉。**一句对了一半的话,比一句全错的话更难发现。**

---

## 6 · ★★★ `IDLE-DRAFT-GRID-HALF-RESTORE` —— **复现了**(Tim 的 Q8)

**这一条从 DRAFT-1 立案起就带着 `⚠ NOT MEASURED`:「本条是读两份源码推出来的,
没有在浏览器里复现过」。本刀在 390px 上真的走了一遍。**

| 步骤 | 读数 |
|---|---|
| 填两期 + 一个抬头字段(`terms_text = "DRAFT3-PROBE-HEADER"`),等 800ms 防抖 | ★ 草稿**存下来了**,而且**存在 `sessionStorage`**(`purchase_orders` 是受限表);`terms_json` 里**两期俱全** |
| 重载那一页 | `[data-draft="found"]` 横幅**出现** |
| 点「恢复」 | ★★★ **抬头字段回来了**(`terms_text = "DRAFT3-PROBE-HEADER"`)· ★★★ **那张网格没回来**(`terms_json = []`,表里 **0 行**)· 「已恢复」横幅**在** |

> ### ☞ 判词:**那条在册的推断【成立】,而它现在是一次测量,不是一次推理。**
> 机制与 `editable-table.tsx:51-73` 那四条逐字吻合:草稿靠
> `form.elements.namedItem(name)` 写 `el.value` 恢复,而 `terms_json` 是一个
> **React 受控**的隐藏输入 —— 写进去的值在下一次渲染就被覆盖,**永远到不了 `terms` state 里**。
> ★★ **而屏幕上说的是「已经恢复了」** —— 这正是那条缺陷说的「比什么都没恢复更坏」。

⚠ ★★ **那条缺陷【不关闭】,它被修订成「已复现」。** 本刀落在它身上的只有一次**缓解**:
`#8` 从今天起有 `beforeunload`(关标签页 / 刷新时会被拦一下)。
☞ **站内 `<Link>` 跳走它盖不住,而那次「看起来成功、其实丢了半张网格的恢复」一个字都没有动。**
★ **DRAFT-2 旁注里那句「它今天还没有发生」从今天起不成立了** —— `#8` 已经搬了,
那条缓解**现在是真的在跑的**。已就地更正。

---

## 7 · 文档

| 改了什么 | 在哪 |
|---|---|
| ★★ `IDLE-DRAFT-GRID-HALF-RESTORE` —— **修订**:`NOT MEASURED` → **已复现**,逐步读数;缓解从「还没发生」变成「已经在跑」 | `known-issues.md` |
| ★ 本报告 —— 九条裁定 · 三个发现 · 逐表得失 · 24 条读数 · 搬家前读数 · 三处 `NOT MEASURED` · **两块 TABLE-PHONE-3 的理由** · 剩下七张的重新定大小 | 本文件 |
| ★ 队列第 4 行:做完了什么、剩下什么 | `forward-queue.md` |

★ **`EDITABLETABLE-NAME-DOUBLE-SUBMIT` 本刀【一个字都没改】,而那是对的** ——
这四张**从来不在它的射程里**,它的「八张只搬了一张,另外七张原样卡着」今天**逐字仍然成立**。

---

## 8 · ★★ 还剩什么 —— 以及**重新定一次大小**(带着四张表的实测)

### 8.1 剩下的七张

`#18 AssayForm` · `#20 OutputAssayForm` · `#21`(采购 `AmendOrderForm`)·
`#22` · `#23`(同一个文件:销售 `AmendOrderForm`)· `#26 FormulaForm` · `#27 BulkPricesForm`。
⚠ **`#22` 进场之前必须先裁 Q7**(它仍然 **AWAITING TIM**,本刀按委托书一个字没碰它)。

### 8.2 ★★★ 重新定大小 —— **范围【往上】修正了,照直说**

**DRAFT-2 §7 的单位是 `#24` = 180 行应用代码。本刀四张的实测:**

| 表 | 每行受控格 | ★ 实测改动行数(增+删) | = 几个 `#24` |
|---|--:|--:|--:|
| `#9 TemplateForm` | 4 | **328** | **1.8 ×** |
| `#8 NewOrderForm` | 4 + 只读金额 | **353** | **2.0 ×** |
| `#6` + `#7`(同一文件) | 2 / 4 | **279** | **1.6 ×** |
| **四张合计** | | **960** | ★ **5.3 ×** |

★ **闸轮当时估的是 3.5–5 ×,实测 5.3 × —— 低估约三成,而【低估的原因】比那个数更有用:**

> ★★ **每张表的成本不由【并列数组的条数】决定,由【每行有几个控件】+【这个文件欠多少条 why-line】决定。**
> `#9` 我按「1 组数组」估成 0.8 ×,实测 1.8 × —— 数组只有一组,**而每行有四个受控格**,
> 外加 uid、`dirty` 判据、以及三段要写清楚的理由。

**于是剩下七张按【每行受控格】重估,而不是按【数组条数】:**

| 张 | 每行受控格 | 行从哪来 | ★ 范围 | 从什么量出来的 |
|---|--:|---|--:|---|
| `#18` `#20` `#26` `#27` | **各 1** | ★ **物质字典**(`substanceOptions`)—— **定长,不加行不删行** | **各 0.5 – 0.8 ×** | 比 `#6`(2 格 / 定长 / 1.6 × 里的一半)还轻;★ **而且【不需要 uid】**,与 `#6`/`#7` 同理 |
| `#22` + `#23` | 2 + 删除勾选 / **0(全非受控)** | `lines` + 三个空槽 | **合计 2 – 3 ×** | 同一文件同一次提交;⚠ `#22` 走 `getAll()`,**收参形状要重写而不是照抄**;⚠ **另加一次 Q7 裁定的落地** |
| `#21` | **3 + 删除勾选** | `lines` | **3 – 4 ×** | 两组并列数组合计 **11 条**(`amend/actions.ts:29-33` + `:56-61`);DRAFT-1 量过它是「本批最挤的一行」(四列里三列是控件) |
| ★ **七张合计** | | | ★★ **7 – 10 ×** | ≈ **1,300 – 1,800 行**应用代码,落在 **6 个页面文件 + ≥4 个服务端收参点** |

★ **落成刀数:两刀,不是一刀** —— `#21` 一张就吃掉上沿的三分之一还多。
★ **建议的切法:** 先 `#18 #20 #26 #27`(四张同形、最轻、零 uid),再 `#21` + `#22`/`#23`(Q7 裁完之后)。

⚠ **三件会把它推到上沿以外的,今天仍然没有答案:**
① **Q7 的裁定** —— 若它要改的是 `editable-table.tsx`,那**这张表本身要重新量**;
② **`#18` / `#20` 的服务端收参点还没找到**(§4.3);
③ **空源表的验证缺口**不在上面的范围里。

### 8.3 ★ 版本号

★★ **整个草稿模型的活从头到尾只背【一个】版本号:`v1.4.28`,由【最后一刀】宣布。**
⚠ **DRAFT-0 / DRAFT-1 / DRAFT-2 / DRAFT-3 一个都没有发过,下一刀也不发** ——
**在最后一刀之前,任何一刀都不许自带版本号。**
