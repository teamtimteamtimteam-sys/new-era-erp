# TABLE-CONVERT-5 交回报告 —— 那六张【量测解开的】表,而【六个手机判断没有一个是抄来的】

> ### ☞ 一句话的读数
> **六张全部转完。390px 上的横向溢出【一张不剩地归零】—— 合计 454px 拖没了。**
> **而【最要紧的那一句】:这六个列选判断,前四刀每一次都是把手机家族已经做过的
> 判断搬过来,这一次【没有东西可搬】—— 六个都是新做的,六个的理由都写在代码里。**
>
> ⚠ **同时要说的那一句:`Traceability:126` 在【桌面】上从不滚变成要拖 292px。**
> 原因是机械的、可指认的(组件的 `sm:whitespace-nowrap` + 那一页 608px 的容器),
> 而它落在【表格列宽】那条「登记、不调整」的裁定里 —— **本刀登记为第四例,没有动它。**
> 详见 §8.3,这是本刀唯一一处变坏的读数,摆在最前面。

---

## 1 · HEAD、树、与 d9b6e76 的差别

| | |
|---|---|
| 分支 | `main` |
| `git fetch origin` | 干净返回 |
| **开工 HEAD** | `d9b6e7610ff66e2f5009b75705a2a3c736279208` |
| **开工 origin/main** | 同上 —— **逐字相等** |
| 开工 `git status --porcelain` | **零行(树干净)** |
| **与预期的差别** | **没有差别。** 委托书说 TABLE-MEASURE-1 没写代码,HEAD 应当仍是 `d9b6e76` —— 实测正是它,一个字节都没漂。 |
| 收工 HEAD | 见 §14(工作提交),推送与部署记录见 §14.2 |
| 迁移 | **零个。** 没有 SQL 改动,一张表结构都没碰。 |

---

## 2 · 人口核对 —— 【对今天的源码,不是对委托书抄一遍】

**六个行号一个都没漂。** 逐条 `sed -n '<行>p` 读回来,六行全部是 `<table` 开标签:

| # | 文件:行 | 读回来的那一行 | 列 |
|--:|---|---|--:|
| ① | `inbound/[id]/edit/AssaySection.tsx:87` | `<table className="w-full border-collapse…` | 5 |
| ② | `inbound/[id]/edit/PricingPanel.tsx:138` | 同上 | 5 |
| ③ | `output/[id]/edit/OutputAssaySection.tsx:50` | 同上 | 5 |
| ④ | `output/[id]/edit/TraceabilitySection.tsx:68` | 同上 | 6 |
| ⑤ | `output/[id]/edit/TraceabilitySection.tsx:126` | 同上 | 7 |
| ⑥ | `stocktakes/[id]/page.tsx:226` | 同上 | 5 |

### 2.1 两道标准筛子,逐张过了 —— 而【第二道差点放过一张】

* **「格子里住着带 `name` 的输入」** —— 六个 `<table>` 块里 **一个都没有**。
  ⚠ **`PricingPanel.tsx` 全文有 3 处 `name=`(94 / 104 / 121 行)**,而表在 137–179 行 ——
  **三处全在它上面那张 `<form>` 里(87–131 行),没有一处在表块内。**
  ☞ 这一条不能按文件数,只能按【块】数:按文件数就会把 ② 误判成 A 类而白白交回。
* **「无列头」** —— 六张**全部**有 `<thead>`,一张都不属于那六张无列头。
* **TABLE-CONVERT-4 那条教训也过了一遍**(`AttendanceGrid` 的行画在 `<table>` 块【外面】):
  六张的 `<tbody>` 里都是就地 `.map()` 出来的 `<tr key=…>`,**没有一张把行交给子组件画**。

☞ **六张全部落在 C(只读账簿),可转。** 与 TABLE-CONVERT-0 §4 的归类逐条相符。

---

## 3 · ★★ 六个手机判断 —— 读数说什么、我选了什么、为什么 ★★

**先把最要紧的事实摆明:这九张从来不在 TABLE-PHONE 家族的工作清单里。**
六张**一处 `hidden sm:table-cell` 都没有**(实测:五个文件 grep 全为 0),
也没有任何一张写过折叠块。**所以下面六个判断没有一个是搬运,六个都是第一次做。**

| # | 表 | 读数【说它需要什么】 | 我选的 priority | 折进展开区 | 理由(一句) |
|--:|---|---|---|---|---|
| ① | `AssaySection:87` | 「基本可读(勉强)」,只差 22px,**露一半的是 `Applied`** | **化验号 · 已应用** | 日期 · 实验室 · 类型 | 身份(也是链接)+ 结论:没应用意味着含量与价格都还停在旧数上,源码自己写着「这是钱没算对」 |
| ② | `PricingPanel:138` | 「基本可读(勉强)」,只差 35px,**露一半的是 `Notes`(本来就是补充)** | **时间 · 旧价 · 新价** | 录入原值 · 备注 | 身份是时间;而**一次改价是一个位移,位移要两个端点**,只留新价就把历史变成一列孤零零的数字 |
| ③ | `OutputAssaySection:50` | **没有读数**(线上零行,表不渲染)—— §3.2 | **化验号 · 已应用** | 日期 · 实验室 · 类型 | ★ **与 ① 同一个判断,而它靠的是 ① 的读数,不是自己的** —— 见 §3.2 |
| ④ | `Traceability:68` | 「读不了」,拖 145px,**行高 145px / 折 8 行**,身份滚到底就丢 | **加工单 · 上游批次** | 层级 · 消耗量 · 供应商 · 到货日 | 一行就是血缘上的【一跳】,而能认出这一跳的就是那两个单号;其余三样是【上游那一批】的属性 |
| ⑤ | `Traceability:126` | 「读不了」,拖 196px,**七列里两列完全看不见**,身份滚到底就丢 | **加工单 · 金属 · 回收率** | 入量 · 出量 · 入侧出处 · 出侧出处 | 身份**真的要两列**(行键就是 `run × metal`);而回收率是这张表存在的理由 |
| ⑥ | `stocktakes:226` | 「之间」,只差 56px,**露一半的是 `Delta`** —— 一次盘点的结论正在屏幕外 | **批次 · 物料 · 差异** | 账面 · 实点 | 结论是差异(委托书点名);身份要两列(批次号不透明,物料名才是人话);账面与实点**成对**折,它们只有摆在一起才有意义 |

### 3.1 ★ 我的选择与读数【哪里一致、哪里不一致】

**三处【正面印证】读数:①②⑥ 被切掉一半的那一列(`Applied` / `Notes` / `Delta`)——
我把 `Applied` 与 `Delta` 提成了 priority,把 `Notes` 折了。**
这正是读数那条横贯观察要的动作:「露一半的那一列是什么,比差多少像素更要紧」。

**一处【我与读数不一致,照直说】:④ 的 `Step`(层级)。**
读数没有点名要留它,但它是行键的一部分,留不留都说得通,**而我折了它**。
理由写在代码里:它只有一个数字,却要在 `table-fixed` 下吃掉整整一列的宽度(§8.4),
而行本来就按层级顺序画。**这是六个判断里最接近可以两说的一个,专门摆出来给 Tim 看。**

**一处【与文件自己的规矩有张力,而我做了取舍】:⑤ 的两列「出处」。**
`TraceabilitySection.tsx` 抬头写着「**出处跟着数字走**」——不说出除的是哪一种数,
这个百分比就没有意义。**而我把 `input_source` / `output_source` 折进了展开区。**
两条支撑,都不是绕过那条规矩:
* **「算不出」那一半仍旧贴着数字走**:算不出时回收率那一格印的【就是具名原因】
  (`input_not_measured` 等),它跟 priority 列一起留在明面上,一个字没动。
* **折走的是「算得出时这个数来自化验还是手录」,而它带着自己的列头躺在展开区里** ——
  §4 点开读回来的原话是 `Input content from: unknown — never recorded`:
  **`unknown` 印成 `unknown`,没有被抹平成 `assay`。那条规矩最硬的一半守住了。**
* 另一条路是 `mode:'scroll'` 把七列全留下 —— **但那正好把本刀要治的病留在原地**
  (196px 横拖、两列完全看不见),所以没选它。
☞ **如果 Tim 认为出处必须与百分比同屏,那是把它们提成 `priority` 的事,一行改动。**

### 3.2 ★★ ③ 没有读数,而它的判断【建立在孪生兄弟身上】—— 照直说 ★★

`OutputAssaySection:50` 线上**画不出来**:`rows.length === 0` 成立,表根本不渲染。
**本刀开工时【独立又查了一次】,不是照抄 TM-1:**

```
assay_results?output_batch_id=not.is.null&deleted_at=is.null   →  Content-Range: */0(零行)
```

☞ 它的孪生是 ①`AssaySection:87`:两处列头签名逐字相同
(`assay.colCode / colDate / colLab / colKind / colApplied`),行字段也逐字相同。
**于是我照 ① 的判断原样给了它,而这一句必须说白:**
> **③ 这一个判断【不是从它自己的读数来的】。** 相同的标记不等于相同的读数 ——
> 产出侧的化验号、实验室名可能比进料侧长,而列宽由最宽的那一格定。
> **它要等线上第一张产出化验落地那天才有真读数。**

★ **但它比 TM-1 那时多了一件【可以量的东西】:转换之后它【会渲染了】。**
空态搬进 `empty` prop 之后,零行不再是「什么都不画」,而是一张有列头的空表 ——
**本刀在真路由上量到了它**(390px 与 1280px 各一次,溢出 0,空态行高 85px,见 §8)。
☞ **有数据的那一张仍然 UNMEASURED;空态的那一张,已经量过了。**

---

## 4 · 展开区:六张逐张点开,读回来的原话

**这是真点开读的** —— 量具对每张表的 `tbody button[aria-expanded]` 逐颗 `click()`,
再把渲染出来的 `<dl>` 按 `dt: dd` 读回来:

| 表 | 展开钮 | 点开之后读回来的原话 |
|---|--:|---|
| `AssaySection:87` | 1 | `Assay date: 2026-07-31` · `Laboratory: —` · `Kind: Final` |
| `PricingPanel:138` | 2 | 第一行 `Entered: 2 SGD` · `Notes: —`;第二行 **`Entered: 53 USD @ 1`** · `Notes: —` |
| `Traceability:68` | 1 | `Step: 1` · `Quantity: 800` · **`Supplier: SUP-2026-0003 Shanghai Yidong Battery Recycle Co.`** · `Received: 2026-06-09` |
| `Traceability:126` | 1 | `Metal in (kg): 320` · **`Metal out (kg): not measured`** · **`Input content from: unknown — never recorded`** · `Output content from: —` |
| `stocktakes:226` | 1 | `Book: 50 kg` · `Counted: 49 kg` |
| `OutputAssaySection:50` | **0** | **没有展开钮 —— 它有 0 行**(线上零条产出化验)。空态行本身没有可折的列。 |

☞ **每一个折走的列都带着自己的列头回来了,一个都不缺。**
特别点名两处,因为它们是「折了会不会说谎」最尖锐的地方:
* `Metal out (kg): not measured` —— **没测过就说没测过,不是 0。**
* `Input content from: unknown — never recorded` —— **`unknown` 就印 `unknown`。**
两句都出自 `app/output/traceabilityShared.ts`(与 PDF 共用的那一份),一个字没重写。

### 4.1 `mode: 'scroll'` —— **本刀一处都没用**

六张**全部**是 `columns` 档。TABLE-CONVERT-4 那处先例(四列并排比较)在这里没有同形的表:
⑤ 看起来最像(七列要横着比),**但它正是溢出最严重的那一张**,
scroll 会把 196px 的横拖原样留下 —— 那与本刀的题目相反。理由写在代码里。

---

## 5 · 空态:一张一张说,而【旧那一支全部删掉了】

| 表 | 转换前 | 转换后 | 旧分支 |
|---|---|---|---|
| `AssaySection:87` | `rows.length === 0 ?` → `<p>` 里 `assay.empty` | 搬进 `empty` prop,**同一个 key** | **已删** |
| `OutputAssaySection:50` | 同形,`assay.empty` | 搬进 `empty`,**同一个 key** | **已删** |
| `PricingPanel:138` | `history.length === 0 ?` → `<p>` 里 `inbound.pricing.historyEmpty` | 搬进 `empty`,**同一个 key** | **已删** |
| `stocktakes:226` | 表体里一行 `colSpan={5}` 的 `stocktakes.noLines` | 搬进 `empty`,**同一个 key** | **已删** |
| `Traceability:68` | **没有空态**(`report.chain.map()` 直接画) | **没给 `empty`** —— 见下 | 不适用 |
| `Traceability:126` | **没有空态**(`report.recovery.map()` 直接画) | **没给 `empty`** —— 见下 | 不适用 |

★ **四处旧分支【逐处删干净】,而这一条是数出来的,不是看出来的**:
§9 的 key 用量表里 **`assay.empty` / `inbound.pricing.historyEmpty` / `stocktakes.noLines`
三个 key 全部停在原值(2 / 1 / 1),一个都没有往上跳** ——
而「往上跳」正是 TABLE-CONVERT-3 §6.1 那处死代码的签名。

★ **两张 Traceability 我【没有】造空态**(委托书明令:今天画什么都不画的,不要发明一句)。
但要照直说一个后果:**组件在 `rows` 为空且没有 `empty` 时会退到它自带的 `t('table.empty')`**。
所以那两张真遇上零行时,屏幕上会多出一句 ——
**用的是组件既有的 key,不是我造的新词**(与 TABLE-CONVERT-3 的 `orders/[id]/page:162` 同一条)。
⚠ 而 `chain` 那一张实际上够不到这条路:`NOTHING_TO_REPORT` 是一个**具名的错误分支**,
报告一旦成功就至少有一跳,零行走的是上面那个具名空状态,不是空表。

★ **一处权限答复【没有】被并进空态**:`TraceabilitySection` 的
`issuesRestricted ? … : issues.length === 0 ? … :` 那三支**一个字没动** ——
它不在这两张表里,而「看不见签发档」与「从未签发」本来就是两件事(FIX-2b)。

---

## 6 · 棘轮与手搓表数

| 维 | 之前 | 之后 | 差 |
|---|--:|--:|--:|
| 手搓 `<table>` | 49 | **43** | **−6** |
| **cellfont** | 346 | **330** | **−16** |
| 其中:标签上(`<table>/<th>/<td>`) | 53 | **37** | −16 |
| 其中:**列描述符里(`className: '…'`)** | 293 | **293** | **±0** |
| `datatable-phone` 调用点 | 150 | **156** | +6 |

★ **−6 就是这六张,一张不多一张不少。**
★ **−16 与 TABLE-CONVERT-0 §4「钉字号」那一栏的合计【逐张相等】**:
  `AssaySection 1 + PricingPanel 1 + OutputAssaySection 1 + TraceabilitySection 12 + stocktakes 1 = 16`,
  **五个文件全部归零**(基线 diff 是 10 行【纯删除】,零行新增)。
★ **列描述符那一栏停在 293 —— 六张表的列定义里一个字号都没有。**
  这一条是本维存在的理由:机械地把 `<td>` 的字号搬进 `Column.className`,
  **那不是还债,是把债换了个拼法。**

构建里那几行原文:

```
   基线:39 个文件在册。本次扫到 43 处。
✓ 没有新增的手搓 <table>。
   基线:98 个文件在册。本次扫到 330 处。
   分账:标签上 37 处(<table>/<th>/<td>) · 列描述符里 293 处(className: '…')。
✓ 没有新增的格子钉死的字号(<table>/<th>/<td> 标签 + 列描述符的 className)。
✓ 手机声明:156 个调用点(DataTable 152 · EditableTable 4) —— columns 模式 155(各自至少一列 priority)· scroll 模式 1(各自带 why) · 静态读不出 1
```

基线按闸门自己的规矩**顺手收紧**(`--update-baseline`),diff 只有删除:
五个文件从 `table` 维退场、五个文件从 `cellfont` 维退场。

---

## 7 · 我渲染的是什么 · 什么状态 · 以及【水合是怎么证的】

★ **三条【真实路由】,线上真数据,没有建任何 probe 路由,没有临时改任何清单。**

| 路由 | 记录 | 喂出来的表 | 行数 |
|---|---|---|--:|
| `/output/12dd44fb…/edit` | `OUT-2026-0118`(draft) | ④⑤ + ③(0 行,转换后画空态) | 各 1 / 0 |
| `/inbound/662bfff7…/edit` | `IN-2026-0001`(draft) | ①② | 1 / 2 |
| `/stocktakes/d915d304…` | `ST-2026-0004`(**posted** —— 只有 posted/cancelled 才走那张只读表) | ⑥ | 1 |

**每一条 id 都先查库确认底下真有行**(§2 那支只读探数脚本),不是随手取第一行。

★ **我量的是哪一个状态:一次性 admin(全权限)。** 这几页是编辑页,权限旗标真的会改表体:
`PricingPanel` 的 `canViewPrices` 为 false 时旧价/新价两列画的是「受限」——**那是更窄的一种**,
所以 admin 是这几张表的【最宽】状态,也是真的会来改这些页的人看到的状态。
⚠ **`canViewPrices=false` 那一种我没有量**(§12)。

★ **除了展开钮,我【没有点任何别的东西】** —— 三条里两条是编辑页。
展开钮是组件自己的纯客户端开关(`useState` 的 `open` 集合),它不写库。

### 7.1 ★★ 水合:本刀的证明是【功能性的】,比数 fiber 更硬 ★★

TABLE-CONVERT-4 花了三次跑才发现页面从没水合,而 TM-1 §8 用故障注入证明了
「组件没画」与「React 没活」读起来一模一样(两边都是 `tables=1`)。

**本刀不需要重做那次故障注入,因为它有一个更强的证据:展开区打开了。**
> `<dl>` 只在 `isOpen` 为真时渲染,而 `isOpen` 来自组件里 `useState` 的 `open` 集合,
> 由展开钮的 `onClick` 翻转。**一个没水合的页面上,那颗钮画得出来但按下去什么都不会发生。**
> §4 那 6 个 `<dl>` 是点完之后读回来的 —— **它们的存在本身就证明客户端 bundle 跑起来了。**

外加计数那一层(与 TM-1 同一支自检,数宿主元素上的 `__reactFiber$…`):

```
                     before(early→late)      after(early→late)
/output/…/edit          145→145                 152→152
/inbound/…/edit          27→159                  27→164     ← 早/晚差 137,水合过程本身被抓到了
/stocktakes/…            31→31                   33→33
```

**六个读数没有一个是 0。** 导航全程走 `localhost`,不是 `127.0.0.1`
(TC-4 §7.1 / TM-1 §8:后者会被 Next dev 的 cross-origin 挡掉 bundle)。

---

## 8 · 行高与溢出:390px 与桌面,前后各一遍

### 8.1 ★ 390×844 —— **本刀的题目,答案是【全部归零】**

| # | 表 | **前:要横拖** | **后:要横拖** | **差** | 前行高 | 后行高 |
|--:|---|--:|--:|--:|--:|--:|
| ① | `AssaySection:87` | +22px | **0** | **−22** | 77px | **41.5px** |
| ② | `PricingPanel:138` | +35px | **0** | **−35** | 77 / 77px | **61.5 / 61px** |
| ③ | `OutputAssaySection:50` | (不渲染) | **0** | — | — | 85px(空态行) |
| ④ | `Traceability:68` | +145px | **0** | **−145** | **145px / 折 8 行** | **61.5px** |
| ⑤ | `Traceability:126` | +196px | **0** | **−196** | 113px | 121.5px ⚠ |
| ⑥ | `stocktakes:226` | +56px | **0** | **−56** | 89px | **81.5px** |

> ### ☞ **合计拖没了 454px 的横向滚动,六张的 `maxScroll` 全部读到 0。**
> **手机上不再有任何一张需要横着拖 —— 推一把,一张都不动。**
> 行高五张里四张变矮,最猛的是 ④:**145px → 61.5px(掉了 58%)** ——
> 转换前它一行要吃掉 390×844 屏幕的六分之一。

⚠ **一处变高:⑤ 的 113 → 121.5px(+8.5px)。** 原因见 §8.2,是同一件事的另一面。

### 8.2 为什么有的格子字变大了 —— **而这是【规矩要的】,不是漏掉的**

转换前 `Traceability` 两张表有 **12 处 `text-xs`(12px)钉在 `<td>` 上**。
**这 12 处一处都没有搬进列定义** —— 搬了就是那条棘轮存在的理由本身
(「机械地把 `<td>` 的字号搬进 `Column.className`,那不是还债,是把债换了个拼法」)。
☞ 于是那些格子回到组件表体的 **14px**。字大了,行就高了。
**这是一处【看得见的、已报的】变化**,与 TABLE-CONVERT-4 在 `NewOrderForm:808` 上报过的同一条。

**没有变的:徽标里那些 `text-xs`。** ①②③ 的「类型 / 已应用」两枚徽标、
② 的汇率侧与 as-of 标记,`text-xs` 都钉在 `<span>` 上(不是 `<td>`),
**原样保留** —— 与 TABLE-CONVERT-4 的 `ApplyPreviewTable` 同一种处置,渲染逐字不变。

### 8.3 ⚠⚠ 1280×844 —— **一处变坏的读数,摆出来**

| # | 表 | 前溢出 | 后溢出 | 前行高 | 后行高 |
|--:|---|--:|--:|--:|--:|
| ① | `AssaySection:87` | 0 | **0** | 37px | 41.5px |
| ② | `PricingPanel:138` | 0 | **0** | 37 / 37px | 41.5 / 41px |
| ③ | `OutputAssaySection:50` | (不渲染) | **0** | — | 85px |
| ④ | `Traceability:68` | 0 | **0** | 65px | **141.5px** ⚠ |
| ⑤ | `Traceability:126` | 0 | **292px** ⚠⚠ | 97px | **161.5px** ⚠ |
| ⑥ | `stocktakes:226` | 0 | **0** | 41px | 41.5px |

**三张 5 列的表在桌面上干干净净**(溢出 0,行高只多了 4.5px —— 组件的 `py-2.5`)。
**变坏的是 6 列与 7 列那两张,而机制是可以指认的:**

```
组件 data-table.tsx:456  <th … 'sm:whitespace-nowrap'>   ← 桌面上列头【不许折行】
两页的容器            <div className="p-8 max-w-2xl">   ← 表可用宽度 608px

⑤ 转换前:列头【折行】,七列硬挤进 608px(Metal in (kg) 那一格只有 65px 宽,
          代价是行高 97px / 折 5 行)—— 这正是 TM-1 §9 登记的【第三例列宽】。
⑤ 转换后:列头不折行,"Output content from" 一格就要 157px,
          七列合计 900px > 608px  →  溢出 292px。
```

☞ **它不是「转换把表撑宽了」,是【同一个容器装不下七列】这件事换了个表现:
转换前表现为「行高 97px、折 5 行」,转换后表现为「横向滚动 292px」。**
☞ **两个补救都是【调列宽】**(给列头解除 nowrap、写死列宽、或把 `max-w-2xl` 放宽)——
而那正是 Tim 裁定「**登记,不调整**」的那件事:variant C 从未规定列宽,
**改一页就是一次没有判据的调整。所以我一个字都没动。**

### 8.4 ★ 列宽:**登记第四例**(委托书 §4 明令:见到就登记,不要调整)

> **`table-fixed` 下,一列的宽度与它装多少字无关 —— 每一列拿到的份额是一样的。**
> 实测(390px,⑤):可用 358px 减去展开钮那格 32px,三个 priority 列各约 108px,
> **一个只装一个数字的「层级」列,与一个装 `PR-2026-0007` 的列,占的宽度完全相同。**
> ☞ 这条直接改变了 ④ 的判断(§3.1:所以 `Step` 折了),
>   也是 §8.3 那 292px 的另一半原因。
> **与 `/finance/balance-sheet`、供应商合规表、TM-1 §9 的 `Traceability:126` 是同一个形状。**
> **登记为第四例。不动。**

### 8.5 整页溢出

**三条路由 × 两个视口 × 前后 = 12 个读数,`documentElement` 溢出【全部 0px】。**
横向滚动**整个关在各自那层外壳里**,页面一次都没有被拖着走。

---

## 9 · i18n key 用量:**逐个对过,两个方向都对上**

取五个文件里用到的**全部 79 个 key**,转换前后各在 `app/ lib/ scripts/` 全量数一遍:

```
合计用量  before = 378      after = 378      差 = 0
往上跳的 key:0 个        往下掉的 key:0 个        掉到 0 的 key:0 个
```

☞ **每一个 key 都是【一比一搬家】**:列头从 section 搬进 client 文件,
空态从三元的一支搬进 `empty` prop —— **没有一处被写了两遍,也没有一处被写丢。**
**「没有一个 key 往上跳」正是 TABLE-CONVERT-3 §6.1 那处死代码的反面证据。**

### 9.1 ★ 一处我自己造出来又抓回来的:**注释也会被数进去**

第一版改完之后,**有三个 key 从 2→3 / 13→14 往上跳**:
`metals.` · `traceability.conservationFlag` · `traceability.kindInbound`。
按 §6.1 那条签名,这本该是三处死代码。**逐处查下去,三处全在我自己写的
`TraceabilityTables.tsx` 的 JSDoc 注释里**(例如 `/** t('traceability.conservationFlag') 或 null */`),
一行代码都没多。

**处置不是「解释一下就算了」,是把注释改掉:**
留着它们,**下一刀的这条检查就钝了一分** —— 那条检查唯一的价值就是「往上跳 = 有鬼」,
而我刚往里掺了三个不是鬼的往上跳。三句注释改成不含 key 字面量的说法之后,
**79 个 key 全部回到原值,差为 0。**

---

## 10 · 我自己拿的主意(除了 §3 那六个判断)

1. **两张孪生表分成两个 client 文件,没有抽成一个共享组件。**
   差别只有链接前缀,抽出来也做得到 —— 但 TABLE-CONVERT-4 对同一组孪生
   (两张金属表)用的就是「两个文件、同一个形状」,**照先例走,不新开一种做法**。
2. **`Traceability` 两张表合进一个 client 文件**(`TraceabilityTables.tsx`)——
   同一页、同一次判断,与 TC-4 的 `AssayTables.tsx` 同形。
3. **单元格文案在【服务端】就用 `traceabilityShared.ts` 调好,传成品字串过界。**
   那三个函数(`recoveryText`/`sourceText`/`kgText`)**同时喂着 PDF** ——
   屏幕这一侧自己拼一遍,「客户手里那张纸」与「屏幕上这一行」就会各说各的,
   而那是 AUD-2 最不能出的错。**列头**则照本仓库既有做法走客户端 `useTranslations()`
   (同一份 `MESSAGES`,§8 的读数确认了前后列头文字逐字相同)。
4. **两处「按行算的 className」搬进格子【里面】那层 `<span>`。**
   ⑤ 的回收率(算不出→灰字 / 算得出→等宽)与 ⑥ 的差异(盘盈绿/盘亏红)
   转换前都是按行算的,而**组件今天没有按行的格子 `className`(已登记的缺口)**。
   本刀**不修组件**,把条件挪进 `render` 里那层 `<span>` —— 渲染出来是同一件事。
5. **棘轮基线顺手收紧**(`--update-baseline`)—— 基线自己的抬头写着「少一处 → 顺手收紧」。
6. **量具自己退出。** `before` 那一跑做完全部工作却没退出(dev 的 detached stdio 管子
   还开着,事件循环不空),于是包装脚本的 `$?` 永远写不进日志。加了一行
   `process.exit(process.exitCode ?? 0)`,**`after` 那一跑因此报出了自己的退出码 0**。

---

## 11 · Tim 该去哪儿走一遍,每一页要什么状态

| 走哪 | 要什么状态 | 看什么 |
|---|---|---|
| `/inbound/662bfff7…/edit` | 任意登录者;**带 `data.view_prices` 才看得到真价格** | 化验区(①)与计价历史(②)。**390px 上推一把:一张都不该动。** 点开每行左边那颗 `›` |
| `/output/12dd44fb…/edit` | 需要 `module.sales.view` 或 `module.processing.view` 才看得到签发档 | 血缘链(④)与回收率(⑤)。**⑤ 在【桌面】上会横滚 292px —— §8.3,那是登记项,不是漏修** |
| `/stocktakes/d915d304…` | **必须是 posted 或 cancelled** —— open 的盘点单走的是点数界面,根本不画这张表 | 只读行表(⑥):批次 · 物料 · 差异三列在手机上都在了 |

★ **六张里有一张【走不了】:`output/[id]/edit` 的化验区(③)。**
**线上一条产出批次化验都没有**,所以它只画得出空态那一句(`assay.empty`)。
要看它带数据的样子,得先在 `/output/<id>/assays/new` 录一张 ——
**本刀没有录**(那是往线上写业务数据,不在题目里)。

---

## 12 · 我【没有】验证的东西

* ★★ **我的字长就是今天线上这几条记录的字长。** ④⑤⑥ 各只喂到 1 行,② 2 行,① 1 行。
  **列宽由最宽的那一格决定,行一多只会更宽或持平。**
  ☞ 但这一条对【转换后】的杀伤力比对转换前小得多,而理由是机械的:
  **手机档是 `table-fixed` + `w-full` —— 表宽被钉死在容器上,再长的字也只会折行,不会溢出。**
  §8.1 那六个 0 因此不靠数据量撑着;**会随数据变化的是行高,不是溢出。**
  **桌面那一侧没有这层保护**(`sm:table-auto`),所以 §8.3 的 292px **是下界**。
* **③ 带数据的样子完全没有读数**(§3.2)。空态量过了,有数据的没有。
* **`canViewPrices=false` 那个状态没量**(② 的旧价/新价会变成「受限」,更窄)。
* **`TraceabilitySection` 的 `failed` 那一支没量** —— 线上这条记录报告是成功的,
  所以那个具名空状态(`NOTHING_TO_REPORT` 等)本刀没有渲染过。
* **两张 Traceability 的零行空表没量** —— 走不到(§5)。
* **只走英文档。** 中文字宽不一样,一次都没量。
* **只量首屏。** 对话框、`<details>`、`/inbound/…/edit` 上另外 10 张 0 宽的折叠表,一个都没展开。
* **硬导航,不是软导航。**
* **触控目标尺寸、可访问性、颜色对比**一律没量,不在题目里。
* **`⑤` 的两列出处折进展开区【是不是对的】,我证明不了** —— §3.1 给的是理由,不是读数。
  这一条要人读,不是仪器能答的。

---

## 13 · 剩下的人口 —— **对今天的源码重新扫的,不是抄委托书**

**手搓 `<table>` 今天 43 张**(扫描 + 按【块】分类,不是按文件):

| 类 | 张数 | 是什么 |
|---|--:|---|
| **A/B** | **16** | 块里住着带 `name` 的输入(受控输入网格 / 表单并列数组)—— 转不了,要先做草稿模型 |
| **C** | **18** | 只读账簿 —— **看起来还能转的那一批** |
| **E** | **3** | 带 `<tfoot>`:`balance-sheet:174` · `trial-balance:148` · **`PayrollGrid:202`** |
| **H** | **6** | **无列头 —— 正是那六张**(`cashflow:147` · `month-end:206` · `reviews/cycles:157` · `PrepaymentPanel:148` · `ForwarderPanels:167` · `forwarders/page:203`) |

★ **那 18 张「C」是【上界,不是待办清单】,而这一条我犯过同样的错要说明:**
我这支分类器只看「块里有没有带 `name` 的输入」——
**而 `AttendanceGrid:48` 就在这 18 张里,TABLE-CONVERT-4 已经把它当作归错类的 B 交回过**
(它的行由 `LineRow` 画在 `<table>` 块【外面】,块里扫不到那 4 个受控 `useState`)。
☞ **同一个盲区,同一支筛子。下一刀要逐张读过才知道这 18 张里真正能转的有几张。**

**其余人口(本刀一个字没碰):**
* **穿着 `tableC` 的 22 处**(25 个文件引用 `tableC`,22 处 `tableC.root`)—— TABLE-STYLE-1 的那一批。
* **Tim 已裁定不在本刀的三张**:`PayrollGrid:202`(它同时是 E,组件没有 `tfoot`)、
  `ForwarderPanels:167`、`forwarders/page:203`。**三张的手机问题按裁定原样留着。**
* **UNMEASURED**:`OutputAssaySection:50` 带数据的读数(§3.2)——
  它现在从「转换人口」挪进了「已转、但只量过空态」。

---

## 14 · 收工核对

| | |
|---|---|
| `npm run build` **自己的退出码** | **0**(`BUILD_OWN_EXIT=0`)· `✓ Compiled successfully in 9.4s` |
| eslint 冻结 | `基线 error 42 · warning 88` / `现在 error 42 · warning 88` · `✓ 没有新增的 eslint 问题。` |
| 元检查 | `✓ check-instrument-selfproof:33 支量具都写了瞄准线;其中 22 支(构建链里的,含本支)都带着覆盖断言。` |
| cellfont | `基线:98 个文件在册。本次扫到 330 处。` · `✓ 没有新增的格子钉死的字号` |
| 迁移 | **零个**。零 SQL 改动。 |
| probe 路由 / 临时改清单 | **一个都没有**(本刀用真路由 + 真会话,两样都不需要) |
| 一次性账号 | 建了 2 个(before / after 各一),**两个都由 `ephemeral.mjs` 的计划删掉**;收工查线上 `tc5-* / tm1-*` 剩余 **0 个**,`.ephemeral/` 空 |
| 量具住在哪 | **全部在 scratchpad,仓库里一个文件都没建**:`tc5-measure.mjs` · `tc5-data.mjs` · 读数 `tc5-readings-{before,after}.json` |

### 14.1 `db/gate.py` —— 自己的退出码、墙钟、四条判词逐字

```
GATE_OWN_EXIT=0
GATE_WALL_SECONDS=290          ← 落在委托书给的 180–700s 区间内,不需要放宽
```

**四条判词,逐字抄回来:**

```
判词【可重建性】:✓ 仓库能从零建出库(prelude 足够,B1/B2 双侧断言见上)
判词【镜像 vs 线上】:✓ 一致(结构、种子、引导、自洽、definer)
判词【行为断言】:✓ db/fixtures 全部通过(建出来的库跑起来是对的)
判词【匿名面】:✓ 线上是基线的子集(基线 327 条)
```

☞ **退出码读的是脚本自己的 `$?`,写进日志再读回来** —— 不是包装器的码。
(同一条教训在 §10-6 的量具上又用了一次。)

---

## 14.2 推送与部署 —— 两个问题**分开问**,并且**等到 SHA 对上才问第二个**

**推送窗口(两边都用 `date -u` 卡):**

| | |
|---|---|
| 推送前 | **2026-09-10T03:35:43Z** |
| 推送后 | **2026-09-10T03:35:47Z** |
| **工作提交** | **`0d558c1eba11f84b73c31f6fd3c7ad7e8bc71c4f`** |

**推送是【取回来核】的,不是读 `git push` 的输出**(TC-4 记过 `PIPESTATUS` 是 bash 的东西、
这个壳是 zsh,所以那个退出码本来就拿不到 —— 而哈希核对本来就是委托书指定的判据):

```
git rev-parse HEAD                     0d558c1eba11f84b73c31f6fd3c7ad7e8bc71c4f
git rev-parse origin/main              0d558c1eba11f84b73c31f6fd3c7ad7e8bc71c4f   → MATCH ✓
git ls-remote origin refs/heads/main   0d558c1eba11f84b73c31f6fd3c7ad7e8bc71c4f   → 远端自己也这么说 ✓
```

**问题一:最新那次部署是哪一次,它是给哪个 SHA 的?**(推送后约 32 秒问的,03:36:19Z)

```json
{"id":6358014724,"sha":"d9b6e7610ff66e2f5009b75705a2a3c736279208","created_at":"2026-09-09T19:48:56Z"}
```

**那是 `d9b6e76` —— TABLE-CONVERT-4 的那个纯文档提交,不是我的。**
**所以我没有去问它的状态**:那一刻的「成功」会是**上一刀部署的成功**,
把它记成本刀的成功,就是这一族反复交代过的那种假绿。

**于是等到 SHA 对上为止:**

| | |
|---|---|
| 推送完成 | **03:35:47Z** |
| 部署登记出现 | **03:38:45Z** |
| **登记滞后** | **178 秒**(TC-4 那次是 165 秒,同一个量级) |
| 部署 id | **6364047812** |
| **它的 SHA** | **`0d558c1eba11f84b73c31f6fd3c7ad7e8bc71c4f`** ← **与 `origin/main` 逐字相同 ✓** |

**问题二(SHA 对上之后才问):这次部署是什么状态?**

```json
{"created_at":"2026-09-10T03:38:46Z","description":"Deployment has completed","state":"success"}
```

> ### ★ **`state=success`,而且是【本刀这个 SHA】的那一次。** ★

☞ 顺带核了一遍 id 与 sha 的绑定(`deployments/6364047812` → `sha: 0d558c1…`),
免得「问了一个 id 的状态,而那个 id 是另一次部署的」。

---

## 14.3 ★★ 收工时抓到的一处泄漏 —— **是我自己的"改进"造出来的** ★★

**§10-6 那条「让量具自己退出」的改动,把 LEAK-1 的原坑请了回来。**

收工核对时查到:**`.ephemeral/` 里剩一份计划,线上剩一个活着的一次性 admin**
(`tc5-1789010666748@test.local`,连着一条 `user_roles` 授权)。

**机制,逐字指认得出来:**

```js
// 我写的(错的):
try { runPlan(); … } catch …          // ← runPlan() 返回的是 promise
process.exit(process.exitCode ?? 0)   // ← 清理在第一个 await 上让出,exit 当场收掉进程
```

`scripts/ephemeral.mjs:149` 的 `runPlan()` **是 async 的**,而我**没有 `await`**。
☞ **这正是 `ephemeral.mjs` 抬头写了整整一段的那个「确定的竞争」** ——
它当初描述的是 survey-phone 的信号处理器与 `process.exit(130)` 抢跑,
**而我用一个手写的 `process.exit` 把同一个竞争复刻了一遍。**

**三件事,都做了:**
1. **那一层设计【按它自己说的兜住了】:清理计划先于线上那些行落盘,所以它还在。**
   `node scripts/reap-ephemeral.mjs` 照计划补删 —— `收割 1 份 · 补删失败 0 份`。
   收工复查:**`tc5-*/tm1-*` 账号 0 个 · 那条 `user_roles` 0 行 · `.ephemeral/` 空。**
2. **量具改对了:`await runPlan()`,再退出。** 错误的经过与正确写法一起写在那支脚本里
   (它住在 scratchpad,不在仓库里)。
3. **§14 那张表里「两个都由计划删掉了」这句话【当时是错的】** ——
   `before` 那一跑确实是计划自己删的,**`after` 那一跑是收割补删的**。以本节为准。

> ### ☞ 这一条值得单独留一句给下一刀:
> **`process.exit()` 与「用完即删」是天生打架的。** 想让脚本自己报退出码,
> 唯一安全的顺序是 **`await` 清理 → 再 exit**;
> 而「日志里印了『cleanup plan ran』」**证明不了清理跑完了** —— 那行字是
> 在 promise 被丢掉之后打的。**本刀就是被那行字骗过一次的。**

## 14.4 收工核对

| | |
|---|---|
| **开工前 HEAD** | `d9b6e7610ff66e2f5009b75705a2a3c736279208` |
| **工作提交** | `0d558c1eba11f84b73c31f6fd3c7ad7e8bc71c4f`(**工作 + 交回报告同一个提交**,委托书 §6 要求) |
| **本提交** | 纯文档 —— 只把 §14.2–§14.4 的推送、部署与那处泄漏的实测填上 |
| 树 | 收工时 `git status --porcelain` **空** |
| 迁移 | **没有**(零 SQL 改动) |
| probe 路由 / 临时改清单 | **一个都没有** |
| 一次性账号 | **线上 0 个**(1 个由计划自删,1 个由 `reap-ephemeral` 补删 —— §14.3) |
| 新增/改动的用户可见字符串 | **没有新 key**;唯一可能新出现在屏幕上的是组件自带的 `table.empty`(§5),以及 §8.2 那批从 12px 回到 14px 的格子 |

> ### ★ **一个提交写不进它自己的哈希。递归在这里停。**
> §14.2 那张表里的部署是**工作提交**那一次的;**本提交自己的哈希与它自己的部署不在本文里**——
> 要它,`git log -1` 即可。**这就是本提交存在的全部理由,我没有假装它不在。**
>
> ☞ **给下一刀:委托书里写的哈希会指向这两个里的某一个。**
> 照 §0 那条规矩办:**树干净 + `HEAD == origin/main` + 差别只是一个纯文档提交 → 继续。**
