# SEARCH-0 —— 勘察与停止闸(2026-09-13)· **没有写一行代码,等 Tim**

> ## ⛔ 这份文件的身份
> **它是一次勘察的结果,不是一份规格。** 它不开工、不建议"先做哪一半"。
> 下面每一条问题都带着**我的建议**与**建议所依据的那次测量**,而**决定是 Tim 的**。
> ★ **我没有替自己回答任何一条然后接着做下去。**

---

## 0 · 一句话的结论

**这块地基比委托书假设的要结实,而它在【三个具体的地方】是空的。**

| 三件活 | 地基状况 |
|---|---|
| **② 找页面与动作** | ★ **最结实**。注册表已经有 **83** 条带名字的去处,而且有一道在册的闸守着它。缺的是 **18** 个"埋着的屏幕"没有自己的名字 —— ★ **包括 Tim 自己举的那个例子(`/inbound/receive`,"field receiving")**。 |
| **③ 找解释** | **一半现成、一半不存在**。手册是结构化的 markdown,**101 个天然锚点**,拿来做全文检索不需要数据库。★ 而「从一条拒绝跳到一段手册」**不存在** —— 869 条拒绝码里,手册提到的是 **0 条**。 |
| **① 找单据** | ★ **最空**。前缀表不存在(散在 **两种**机制、**32** 个前缀里);`code` 这一列**同时装着另一种东西**(274 行是单据、**309 行不是**);而「`0001` 列出所有以 0001 结尾的单据」是一次**后缀匹配**,★ **今天全库 GIN/GiST 索引 0 个、tsvector 列 0 个、`pg_trgm` 没装** —— 19 个 `code` 上的 btree 索引**按构造服务不了它**。 |

---

## 1 · 先说 grilling 改了什么

**`mattpocock-skills:grilling` 按名调用了,而它对这次勘察改了两件事:**

1. ★ **它的规矩「Finding facts is your job, never the user's」把【问 Tim 的清单】砍掉了一大半。**
   我原本准备问的 6 条 —— 「注册表覆盖到哪一层」「有几个前缀」「手册能不能切开」
   「审计留痕答不答得了」「有没有本页主语」「拒绝码有多少条」—— ★ **全部是可以量的,一条都不该问。**
   量完之后它们不是问题,是**读数**,而读数把后面的问题变得具体多了:
   不再是「手册能搜吗」,而是「**手册没有版本号,而你的裁定 ③ 要求显示版本号,那版本号是什么**」。
2. ★★ **它的「设计树 / 前沿」把问题排了序,于是我发现【有几条我以为要问的,其实还轮不到问】。**
   例:「结果列表怎么排序」「一次显示几条」—— 它们挂在「一次搜索到底查几张表」底下,
   而那一条挂在「前缀表长什么样」底下。**前缀表没定,排序就问不出有意义的答案。**
   ☞ 所以下面**只有第一轮的前沿**,10 条。**后面还有至少两轮**,它们的前提就在这 10 条里。

⚠ **grilling 的收尾条件是「前沿为空」,而它现在【不为空】。**
这份文件因此是**第一轮的结束**,不是勘察的结束。

---

## 2 · 第一轮前沿 —— **10 条,全部开着**

> ★ 委托书原话:「EVERY open question it or the survey raised, each with your recommendation
> and its evidence — ★ ALL OF THEM, do not triage the list down to the ones needed to start」。
> **照办:下面 10 条一条都没有被我筛掉,包括我认为答案显而易见的那几条。**

---

❓ **Q1** — **手册的「版本」是什么东西?**

裁定 ③ 要求「结果必须显示这段话来自手册的哪一个版本」。
★ **实测:手册今天【没有版本号】** —— `docs/manual-draft.md` 的头两行是标题和一条 `---`,
`scripts/build-manual.py` 里 `version` / `date` / `issued` **一次都没出现**,
PDF 的文件名里也没有。**今天唯一能机读的"版本"是【最后动过它的那个 commit】**
(`5915c17`,2026-09-08)。

➡️ **建议:在 `manual-draft.md` 的前置区加两行 `version:` 与 `issued:`,由
`build-manual.py` 读出来印在封面上、并写进一份生成出来的索引里。**
理由:git SHA 机读得到,但**它不是一个"发布版本"** —— 一个操作员看到
`5915c17` 学不到任何东西,而看到 `v1.3 · 2026-09-08` 能判断"这比我手上那本新"。
★ 而这件事必须**先于**job ③ 落地:裁定 ③ 是一条**硬要求**,而它今天**没有数据可读**。

---

❓ **Q2** — **job ② 的清单:【扩注册表】还是【另起一份】?**

★ **实测:注册表 `FUNCTIONS` 有 83 条,而 `app/` 底下有 232 条路由。**
差额的分账是量出来的,不是估的:

| | 条数 |
|---|--:|
| 注册表**自己有一条**(带 `navKey` = 有名字) | **83** |
| 只靠父条目**前缀**覆盖(**没有自己的名字**) | **67** |
| ……其中**动作型**(`/new` `/export` `/pdf` `/bulk` `/import`) | 49 |
| ……★ 其中**同级屏幕** = 「埋着的功能」 | ★ **18** |
| 动态段 `[id]` | 73 |
| 孤儿(靠例外表兜住) | 9 |

★★ **那 18 个逐条在这里,而第 10 个就是 Tim 举的那个例子:**
`/finance/bank/statements` · `/hr/kpi/score` · `/hr/leave/balances` · `/hr/leave/calendar` ·
`/hr/leave/grants` · `/hr/leave/holidays` · `/hr/leave/types` · `/hr/reviews/cycles` ·
`/hr/reviews/scale` · ★ **`/inbound/receive`** · `/inventory/reports/ledger` ·
`/inventory/reports/safety` · `/inventory/reports/snapshot` · `/inventory/reports/violations` ·
`/sales/customers/overlap` · `/tools/pricing/calculator` · `/tools/pricing/formulas` ·
`/tools/pricing/metal-prices`

☞ **委托书说的「~20 个」是对的 —— 实测 18,而且现在它是一份【名单】,不是一个数。**

➡️ **建议:扩 `FUNCTIONS`,不要另起一份。** 两个理由,第二个更重要:
① 那 18 条每一条都缺的只是 `navKey` + `permission`,而权限它们本来就得有;
② ★★ **另起一份就是这个仓库反复付账的那件事**(「同一个意思的第二套说法,
就是下一次漂移的种子」—— 这句话**逐字写在 `SearchShell.tsx` 里**)。

⚠ **而「保持它跟上新功能」这件事,今天的闸【只做到一半】,这一条要说准:**
`check-nav-routes.mjs` 的判据 ② 是「每条路由**要么**在注册表里、**要么在某条条目之下**、
**要么**在例外表里」。★ **「在某条条目之下」= 前缀覆盖 = 一个新的埋着的屏幕【不会让它变红】。**
☞ 所以 job ② 要的不只是补 18 条,**还要把判据 ② 收紧**:
一条**不是** `/new` `/export` 这一族的静态路由,**必须有自己的条目**。
**不收紧,这 18 条补完之后,第 19 条照样会安静地长出来。**

---

❓ **Q3** — **单据前缀表放在哪儿?它今天【不存在】。**

★ **实测(线上,一次回滚掉的只读事务):**

| | |
|---|--:|
| 带 `code` 列的表 | **68** 张 |
| `code` 的行 | **584** |
| ★ 长成 `XXX-YYYY-NNNN`(单据形状)的 | **274** |
| ★ **不是**单据形状的 | ★ **309** ← **同一列,另一种东西** |
| 实测在用的**单据前缀** | **32** 个 |

★★ **三条非均匀,逐条:**
① **前缀由【两种】机制铸出来** —— 29 支 `db/functions/next_*_code.sql`,
   **外加 3 个嵌在表镜像里的触发器**(`IN-` `OUT-` `ST-` 写在
   `db/tables/{inbound_batches,output_batches,stocktakes}.sql` 里)。**没有一个地方列全它们。**
② **同一张表可以铸两个前缀** —— `payments` 同时产 `PMT-` 与 `RCPT-`(付款 / 收款)。
③ ★ **同一张表可以同时装单据码与主数据码** —— `customers`(4 条 `CUS-` + 3 条自由格式)、
   `materials`、`suppliers`、`quotes`、`sales_orders`、`shipments`、`containers`、
   `inbound_batches`、`employees` 都是。**「code 长什么样」不是一张表的性质。**

➡️ **建议:铸一张 `document_types(prefix, table_name, route, label_key)`,并且让
`next_*_code` 那一族【从它读前缀】,而不是各自写死。** 这样它**不是第二份定义**,
而是**把散在 32 处的那一份收拢**。⚠ 而这件事**动数据库**,见第 5 节的分刀建议。

---

❓ **Q4** — **「`0001` 列出所有以 0001 结尾的单据」—— 装 `pg_trgm` 吗?**

★ **实测:全库 GIN/GiST 索引 0 个 · tsvector 列 0 个 · `pg_trgm` 没装**(可装版本 1.6)。
而 `code` 上有 **19/21** 张表带 btree 索引 —— ★ **btree 服务得了 `PO-2026-0001`(等值)
与 `PO%`(前缀),【服务不了】`%0001`(后缀)。** 后者今天必然是全表扫。

➡️ **建议:装 `pg_trgm` + 在 `code` 上建 GIN。** 三条理由:
① 它同时买下「后缀」与「中缀」与「拼错一个字」,而 Tim 的三个例子里有两个要它;
② 不装它的替代品是**反转字符串的表达式索引**(`reverse(code)`),它只买下后缀,
   而且**下一个读到它的人认不出那是什么**;
③ ⚠ **它是一次迁移** —— 见第 5 节。
★ **另一条路也请一并裁:把 job ① 收窄成「等值 + 前缀」,把「后缀」砍掉。**
那样**一行迁移都不要**,而 Tim 举的三个例子里会少掉一个。**我不替这件事做主。**

---

❓ **Q5** — **裁定 ② 的「现有审计留痕」,指的是哪一个?**

★★ **实测:没有「一份」审计留痕 —— 有【两种】,覆盖率差三倍。**

| 来源 | 是什么 | ★ 覆盖 31 张单据表里的 |
|---|---|--:|
| `*_history` / `*_log` 表 | 13 张,逐实体 | ★ **6** |
| 行上的 `updated_by` + `updated_at` | 基表自己的列 | ★ **21** |
| 行上的 `created_by` | 同上 | 29(但"建过"不等于"改过") |
| **两样都没有** | `certificates_of_destruction` · `traceability_report_issues` | 2 |

★ **而它有一个【没有人算过】的代价:那 21 张表上,`updated_at` 索引 0 个、
`updated_by` 索引 0 个。** ☞ **裁定 ② 说的「不要新表」成立,而它【不等于】「不动 schema」——
它要 ~21 条新索引,否则每一次点开空搜索框都是 21 张表的全表扫。**

➡️ **建议:走 `updated_by` + `updated_at`,接受 21/31 的覆盖,并把缺的 10 张【按名写进界面】**
(而不是让它们安静地不出现 —— 那正是这个仓库反复付账的「一处缺席被渲染成一个答案」)。
理由:6/31 太少,空状态会常年是空的,而一个常年空的空状态会教会操作员不要点它。

---

❓ **Q6** — **「3 more matches in Finance」里的 Finance,是哪一层?**

★ **实测:`MODULES` 是 9 个** —— purchasing · logistics · operation · sales · finance ·
inventory · hr · tools · settings。
★★ **而 Tim 举的例子里那个词是「Payroll」,它【不是】一个模块** —— 薪资住在 `hr` 底下。
☞ 所以照今天的模块表,那句话会写成「**1 more match in HR**」,而不是「in Payroll」。

➡️ **建议:就用这 9 个模块,并且【接受措辞会变粗】。**
理由:模块是权限求值器 `allows()` 唯一认识的边界(`lib/modules.ts`,而且有一道闸
`check-permission-predicate.mjs` 禁止任何别处再写一份判断)。
★ **想说「Payroll」就得引入第二层边界,而那是一份新的权限模型** —— 不是措辞问题。
⚠ **顺带:这让 Tim 已经裁过的那条泄漏【变窄了一点】** ——「HR 里有 1 条」比
「Payroll 里有 1 条」少说了一件事。**这不改变裁定,但它是裁定的一个事实性更新。**

---

❓ **Q7** — **手机上从哪里进?(这是一条【继承来的】要求,不是我提的)**

★ `docs/forward-queue.md` 的「全局搜索」条目里,CONFIRM-1 已经写下:
> **今天顶栏那个搜索外壳带着 `hidden md:block` —— 768px 以下每一页都不画。**
> Tim 已裁定这**今天不是缺陷**(首页那个大外壳没有宽度隐藏)。
> ★ **「而搜索真建起来的那一天它就变成缺陷了:那时手机上没有任何入口,
> 而今天没有任何人在为它做打算。」**

➡️ **建议:把手机入口算进这一刀的范围,并且用那条现成的判据收尾**
(`scripts/probe-search-shell.mjs` 的 S4 格钉着 390px 的现状)。
☞ 委托书说「两个入口 + 一个快捷键」,★ **而快捷键在手机上不存在** ——
所以手机上真正的入口只有首页那一个,除非顶栏那一格取消隐藏。

---

❓ **Q8** — **手册只有英文。一个用中文的人搜到一段解释,看到什么?**

★ **实测:`docs/` 底下只有一本手册,英文**(`manual-draft.md` 2043 行 / 一份 PDF)。
而 app 是双语的(`messages/en.ts` 与 `messages/zh.ts`)。

➡️ **建议:job ③ 先只对英文界面开,中文界面【明说这一段只有英文】并原样给出。**
理由:与裁定 ③ 同一条精神 —— **把"它是什么版本"说出来**,那么"它是什么语言"也该说出来。
★ **不建议现在翻译手册**:那是一件独立的、比这把刀大的活。

---

❓ **Q9** — **job ③ 的两半:先做哪一半,还是都做?**

★★ **实测把它劈成了两件不一样的事:**

| 半 | 地基 |
|---|---|
| **拿词搜手册**(「partially shipped」→ 那一段) | ★ **现成**。手册是 4 个 PART · **31 个编号小节** · **70 个三级标题** = **101 个天然锚点**,markdown 源码,**在内存里搜就够,一行数据库都不要**。 |
| ★ **从一条拒绝跳到一段手册** | ★★ **不存在,而且是零。** 实测:手册里出现的拒绝码 **0 条**(`grep -cE '[A-Z]{2,}_[A-Z_]{2,}' docs/manual-draft.md` = 0);拒绝码那一侧也**没有任何一条**指向手册小节。**这个映射一条都没有。** |

➡️ **建议:这一刀只做上半(拿词搜手册);把「拒绝 → 手册」单独立一条。**
理由:那是 **869 条**人工判断(「这条拒绝该指到哪一节」),它**不是**一件工程活,
是一件**写作活**,而且它要先有 Q1 的版本号才谈得上"指向哪一版的哪一节"。

---

❓ **Q10** — **委托书说「744 refusal strings」。那个数对不上,而【对不上的方式】要 Tim 知道。**

★ **实测,三个口径,三个数:**

| 量法 | 数 |
|---|--:|
| 委托书说的 | 744 |
| ★ **镜像里(= 线上真源):`db/functions` + `db/tables` 去重** | ★ **869**(+1 条 `RAISE '…'` 写法 = 870) |
| 再并上 `db/migrations`(**变更日志,不是真源**) | **913** |
| 仓库自己最近一次的记载(BUGFIX-1b,2026-09-12) | 909 |

★★ **那个 909 几乎可以肯定是【并上了 migrations】量的** —— 913 与它只差 4。
☞ **而对一支搜索来说这个差额是实的:migrations 里那 ~44 条码,是被改掉的旧版本函数抛的,
【今天没有任何一支函数能抛出它们】。** 照 909 建索引 = 给 44 条**用户永远见不到**的拒绝建索引。

➡️ **建议:用 869(镜像),不要用 909(含日志),更不要用 744。**
⚠ **我没有去查 744 是从哪儿来的** —— 它可能是某一天的正确读数(这个仓库有过
「一个数正确地诞生,然后安静地过期」的先例)。**照直记成对不上,不编一个来历。**

---

## 3 · 一件不在委托书里、而它【已经坏了】的事

★★ **「两个入口」里的那一个,今天对一个真的用户【整个会话都不存在】。**

`SearchShell` 住在**根布局**里,而 App Router **软导航时不重画根布局**。
`AGENTS.md` 记着实测:

```
S2  /me @1280 硬导航   present=true  visible=true  box=200x32   ← 脚本量到的
S3b /me @1280 软导航   present=false visible=false "not in DOM" ← 人看到的
```

☞ **一个人登录后落在 `/`,根布局求值一次得到 `null`,然后他点着链接走遍整个系统 ——
那个 `null` 跟着他一整个会话。**
同一条结构缺陷还有登记在册的一条:`CONFIRM-1-ROOT-LAYOUT-HEADER`(未修,Tim 裁定排队)。

➡️ **建议:这一刀必须修它,否则「两个入口」实际只有一个。**
修法在那条登记里已经写好:**下沉到客户端组件用 `usePathname()`,或者把那个判断移出根布局。**
⚠ **而 Tim 当初排队它的理由要一起读:**
> 「在一刀自己的修法还没有被证明之前,顺手修掉一条潜伏缺陷,会让这次提交
> 看起来修了两件事,而它只量了一件。」

☞ **所以它不该是"顺手修" —— 它该是这一刀【自己的一件活】,带自己的判据。**

---

## 4 · 报价 —— **两个数,而第一个是量出来的**

### 4.1 ★ 过程底价(**本刀 Part A 实测,2026-09-13,同一台机器**)

**这是"一刀不管做什么都要付"的那一笔。** 全部来自脚本自己打出来的那一行:

| 量具 | 实测 | 身份 |
|---|--:|---|
| `db/gate.py` | **494s** | CONFIRMED(脚本自报 wall-clock) |
| `scripts/smoke-routes.mjs` | **736.0s**(计时 223 条) | CONFIRMED(脚本自报) |
| drift 普查 141×2 + urls,**一趟** | **1826s** | CONFIRMED(本刀实测) |
| ……而一刀要**两趟**(改前 + 改后) | **3652s** | CONFIRMED |
| 卡死路由的修补跑 ×2 | **166s** | CONFIRMED |
| 按钮几何探针 ×2 | **211s** | CONFIRMED |
| `npx tsc --noEmit` | ⚠ **NOT MEASURED**(跑了 4 次,没有单独计时) | — |
| `npm run build` | ⚠ **NOT MEASURED**(`run_detached` 截断日志而非重建,出生时间不可靠) | — |
| ★ **过程底价合计(只含量到的)** | ★ **5259s ≈ 88 分钟** | CONFIRMED |

> ★★ **这个数本身就是一条读数:Part A 改了 10 处调用点、加了 1 个档位,
> 而验证花了 88 分钟。** 任何"这一刀很小"的说法都要先减去这 88 分钟。
> ⚠ 而它**还不含**:写文档、写比对器、三次故障注入、以及两次被 10 分钟前台上限
> 打断后的重跑。

### 4.2 ★ 实际工作量 —— **按【测出来的地基】分,不按感觉分**

| 活 | 要动数据库吗 | 依据 | 我的估 |
|---|---|---|---|
| 面板组件 + 两个入口 + 快捷键 | **否** | 两个外壳已存在,3 个文案键已存在 | 中 |
| 修根布局那条(§3) | **否** | 修法已写在登记里 | 小 |
| **job ②** 页面与动作 | **否** | 补 18 条 `FUNCTIONS` + 收紧判据 ② | 小~中 |
| **job ③ 上半** 拿词搜手册 | **否** | 101 个锚点,内存里搜 | 小 |
| Q1 手册版本号 | **否** | 两行前置区 + build 脚本 | 小 |
| ★ **job ①** 找单据 | ★ **是** | `document_types` 表 + `pg_trgm` + GIN | ★ **大** |
| ★ 裁定 ② 的最近清单 | ★ **是** | ~21 条新索引 | 中 |
| ★ 权限诚实的计数 | ★ **是** | 一支 definer 计数函数 × 9 个模块 | 中 |

⚠ **不能报价的,逐条点名:**
* ★ **任何一次查询的生产耗时** —— **今天全库那 21 张单据表合计 196 行**,
  `EXPLAIN` 出来每一支都是 Seq Scan。☞ **那次测量量的是【形状】,不是【成本】,
  它按构造外推不到生产。** 报一个"每次搜索 N 毫秒"就是编一个数。
* **Q3 / Q4 / Q5 三条答之前,job ① 报不出价** —— 三条都改变它的形状。
* **「拒绝 → 手册」那 869 条的写作量** —— 它不是工程活,我估不了。

---

## 5 · 一刀还是几刀 —— **建议【两刀】,而理由只有一条**

> 委托书:「Merge by default; split only for a real reason, and name it.」

★★ **那条真实的理由是:【迁移的边界】,而这个仓库为破窗有一整套成文的代价。**

| | SEARCH-1 | SEARCH-2 |
|---|---|---|
| 内容 | 面板 + 两个入口 + 快捷键 + §3 那条修复 + **job ②** + **job ③ 上半** + Q1 版本号 | **job ①** + 裁定 ② 的最近清单 + 权限诚实的计数 |
| ★ **迁移** | ★ **零** | `document_types` · `pg_trgm` · GIN · ~21 条索引 |
| 破窗 | ★ **不存在**(`AGENTS.md`:没有迁移就没有那个窗口) | 有,要按成文的顺序走(迁移 → gate 绿 → 推送) |
| 需要 Tim 先答 | Q1 Q2 Q7 Q8 Q9 | ★ **Q3 Q4 Q5 Q6** |

**三条支持这么分的话:**
1. ★ **SEARCH-1 一条迁移都没有,于是它【没有破窗】。** 这个仓库对破窗有一个必填字段
   和一整节成文的代价;一刀能做到零,就不该让它非零。
2. ★ **SEARCH-1 不依赖 Q3/Q4/Q5** —— 那三条正是最需要 Tim 想清楚的三条。
   合成一刀,整块活会**卡在数据库那三条上**,而面板本可以先上线。
3. ★★ **SEARCH-1 自己就把「搜索还没建」那句话换掉了。** 今天两个外壳都诚实地说
   「尚未启用」;SEARCH-1 之后它们真的能找到页面与解释 —— **那已经是一件完整的、
   用得上的东西**,而不是半个功能。

⚠ **反对这么分的一条,照直记:** Tim 的第一条理由是「这个仓库反复为『同一件事建两遍』付账」。
**分两刀有再造那件事的风险** —— SEARCH-1 建面板时如果没有为 job ① 留出位置,
SEARCH-2 可能会想再开一个。☞ **处置:SEARCH-1 的面板必须【按三件活设计】,只是其中一件返回空。**
★ 而这一条要写进 SEARCH-1 的委托书,不能靠记。

---

## 6 · 每一个数,以及它是不是量过的

| 数 | 值 | 身份 | 量法 |
|---|--:|---|---|
| `app/` 下路由 | 232 | ★ CONFIRMED | 本刀自写枚举器,与 `check-nav-routes.mjs` 同形 |
| 注册表 `FUNCTIONS` 条目 | 83 | ★ CONFIRMED | 同上 + `check-nav-routes` 自己也报 83 |
| 只靠前缀覆盖的静态路由 | 67 | ★ CONFIRMED | 本刀 |
| ……其中动作型 | 49 | ★ CONFIRMED | 本刀 |
| ★ **埋着的屏幕** | **18** | ★ CONFIRMED | 本刀,**逐条列名** |
| 例外表条目 | 11 | ★ CONFIRMED | `check-nav-routes.mjs` 自报 |
| 模块 `MODULES` | 9 | ★ CONFIRMED | 本刀 |
| 带 `code` 列的表 | 68 | ★ CONFIRMED | 线上,回滚事务 |
| `code` 行数 | 584 | ★ CONFIRMED | 同上 |
| ……单据形状 | 274 | ★ CONFIRMED | 同上 |
| ……**不是**单据形状 | 309 | ★ CONFIRMED | 同上 |
| 在用的单据前缀 | 32 | ★ CONFIRMED | 同上 |
| 铸前缀的机制 | 2 种(29 支函数 + 3 个触发器) | ★ CONFIRMED | 本刀 grep |
| `code` 上有 btree 的 | 19 / 21 | ★ CONFIRMED | 线上 `pg_indexes` |
| `updated_at` 索引 | ★ **0 / 21** | ★ CONFIRMED | 同上 |
| `updated_by` 索引 | ★ **0 / 21** | ★ CONFIRMED | 同上 |
| GIN/GiST 索引 · tsvector 列 | ★ **0 · 0** | ★ CONFIRMED | 线上 |
| `pg_trgm` | ★ **没装**(可装 1.6) | ★ CONFIRMED | 线上 `pg_extension` |
| `*_history` / `*_log` 表 | 13 | ★ CONFIRMED | 线上 |
| 单据表有 `*_history` 的 | 6 / 31 | ★ CONFIRMED | 线上 |
| 单据表有 `updated_by` 的 | 21 / 31 | ★ CONFIRMED | 线上 |
| 单据表有 `created_by` 的 | 29 / 31 | ★ CONFIRMED | 线上 |
| 那 21 张表今天合计行数 | **196** | ★ CONFIRMED | 线上 —— ☞ **所以任何耗时都外推不了** |
| 手册行数 / PART / 小节 / 三级标题 | 2043 / 4 / 31 / 70 | ★ CONFIRMED | 本刀 |
| 手册里的拒绝码 | ★ **0** | ★ CONFIRMED | 本刀 grep |
| 手册版本号 | ★ **不存在** | ★ CONFIRMED | 本刀(源码 + build 脚本 + 文件名) |
| 拒绝码(镜像去重) | ★ **869**(+1 = 870) | ★ CONFIRMED | 本刀 |
| 拒绝码(并上 migrations) | 913 | ★ CONFIRMED | 本刀 |
| 仓库自己的记载 | 909 | 转述(BUGFIX-1b,2026-09-12) | 未复算,但 913 解释得了它 |
| 委托书说的 | 744 | ★ **对不上,来历未查** | — |
| 过程底价 | **5259s ≈ 88 min** | ★ CONFIRMED | 本刀 Part A |
| `tsc` / `npm run build` 耗时 | — | ⚠ **NOT MEASURED** | — |
| 任何一次搜索查询的生产耗时 | — | ⚠ **NOT MEASURED,且今天量不了** | 数据集 196 行 |
| SearchShell 软导航 present | false | 转述(`AGENTS.md` 实测,本刀未复量) | `probe-search-shell.mjs` S3b |

---

## 7 · 那次关于【计数本身可读】的交换 —— 照委托书要求存档

> **委托书原话:「RECORD THAT EXCHANGE in the survey output so a later UAT question
> does not re-derive it.」**

**Claude 向 Tim 提的:** 一个计数**本身就是可读的信息**。一个没有薪资权限的人
可以搜一位同事的名字,从「Payroll 里还有 1 条」学到**那里存在一条薪资记录**,
而从一个 0 学到**那里没有**。☞ 也就是说「内容不给、存在说出来」这条线,
**在计数这一格上把一部分内容也给出去了**。

**Tim 的裁定:计数照给,全都给。**

★ **这条裁定本轮【没有被重新打开】。** 而勘察给它加了**一条事实性更新**(见 Q6):
模块只有 9 个,薪资住在 `hr` 里,**所以那句话实际会写成「HR 里还有 1 条」** ——
比「Payroll 里还有 1 条」少说了一件事。**裁定不变,泄漏面比当初讨论时窄。**

---

## 8 · 下一轮前沿(**还没问,因为它们的前提在第 2 节里**)

照 grilling 的规矩,**一个答案依赖着本轮未决问题的问题,属于下一轮**。点名存档:

* 结果怎么排序、一次显示几条、三类结果怎么混排 —— **等 Q3**(前缀表的形状决定一次查几张表)
* 防抖多少毫秒 / 要不要服务端缓存 —— **等 Q4 Q5**(索引决定每次查询的代价)
* 计数是每次击键都算,还是只在"看起来搜完了"的时候算 —— **等 Q4 Q6**
* 快捷键用哪一个组合、与浏览器的冲突怎么办 —— **等 Q7**(它决定面板在手机上存不存在)
* 面板要不要记住上一次搜索 —— ★ **等一条 PDPA 判断**,而 Tim 的反访问日志裁定就在旁边
* 「供应商页里搜索默认限定在这家供应商」的范围怎么读 —— ★ **本轮已经量过地基**(见下),
  但**具体措辞等 Q3**

> ★ **顺带把最后那一条的地基报了,因为它是量过的:**
> **今天【没有】一个机读的「本页主语」概念。** `ListPage` 只有 `breadcrumb` / `title`;
> `RecordHeader`(19 页在用)拿的是 `fields: {label, value}[]`,而它的文件头明写
> **「本组件只把它们排开,不认识其中任何一个」**。
> ☞ **唯一现成的结构化信号是 URL 本身**:`/suppliers/[id]` 里,路由前缀给出**类型**、
> `[id]` 给出**身份**,而路由前缀→注册表条目的映射 `check-nav-routes.mjs` 已经在用。
> ➡️ **建议:从 URL 读,不要让 73 条动态路由各自声明一遍。**

---

## 9 · 我【没有】做的事

* ★ **没有写一行产品代码。** `git status` 在这份文件之外是干净的。
* ★ **没有替自己回答上面任何一条然后接着做。**
* ★ **没有重新打开 Tim 的三条裁定。** Q6 与 Q10 是**事实性更新**,不是异议。
* **没有查 744 的来历** —— 照直记成对不上。
* **没有复量 `SearchShell` 软导航那条**(转述 `AGENTS.md` 的实测)。
* **没有翻译手册、没有装 `pg_trgm`、没有建任何索引。**

---

## 10 · 等什么

**等 Tim 答第 2 节那 10 条。**
★ **Q3 · Q4 · Q5 · Q6 答完,SEARCH-2 才报得出价;
Q1 · Q2 · Q7 · Q8 · Q9 答完,SEARCH-1 就可以开工。**

⛔ **在那之前不开工。**
