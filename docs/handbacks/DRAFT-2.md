# DRAFT-2 —— 草稿模型表搬家,**第 2 刀(共三刀)**(2026-09-21)

> ### ★ 一句话:**那条挡了八张表的坑【裁定了】,给它配了一道闸,搬了一张表证明它 —— 然后停在了说好的地方。**
> 委托书要搬 **五张**(#6 #7 #8 #9 #24)。**开工闸上的 grilling 把批次顺序换了个个儿**,
> 理由是 **#24 是这五张里唯一一张真的碰得到那条坑的表**;Tim 照收,并把停止线画在 #24 之后。
> ☞ **本刀搬了 1 张(#24),#6 #7 #8 #9 顺延** —— 它们**从来不在那条坑的射程里**(§1.3)。

**开工闸:** 工作树干净;`HEAD` = `origin/main` = `git ls-remote origin main` =
`6ae37364f2c3bd643a7d838d587457309b542de2`(DRAFT-1 收尾,仅文档)。

**零迁移** —— `db/` 一个字节没动。于是**没有备份、没有 gate、没有破窗**。

---

## 0 · grilling 改了什么 —— 五个发现,逐条量过

Tim **七条全部照收**,并把停止线从「五张全搬完」改成「**#24 搬完即止**」。

### ★★★ G1 · **(b) 不修那个机制,它拿走那个机制的燃料**

这是这条裁定最容易被读错的一句,而两份在册文档都没有写。
组件照旧把 `c.edit(...)` 画**两遍**(桌面格 `hidden sm:block` + 手机展开区)。
**(b) 变的是【格子里不再有 `name=`】**:页面持有那个数组,草稿经表**外面**一个隐藏的
`*_json` 交出去,而那一个**只画一遍**。

☞ ★ **于是「在 EditableTable 里实现选定的答案」这件事【没有机械内容】** ——
`editable-table.tsx` 里**没有一行代码是为 (b) 改的**。
它改的是**调用点的形状**,而组件那一侧落下的是**一段抬头**与**一道闸**(见 Q2)。

**实测坐实了这一点:** 390px 展开一行之后,`DOM 12 个数字输入 / 看得见 2 个` ——
★ **双渲染原样在那里。**

### ★★★ G2 · (c) 出局的那条真理由:**它会在手机转屏的那一刻把打的字弄丢**

`sm` = **40rem = 640px**(Tailwind v4 默认;`app/globals.css` 里 `--breakpoint-*` 覆盖 **0 处**,实测)。
★ **一台 390×844 的手机转成横屏是 844px 宽 —— 它跨过 `sm`。**

(c) 在那一刻把活着的那一份**卸载**、把另一份**挂上**;一个**非受控**的 `<input name=…>`
挂上来带的是 `defaultValue` —— **字没了。**
★★ **今天的代码【不会】丢**(藏起来的那一份还在 DOM 里拿着那些字)。
☞ ★★★ **(c) 等于拿一个安静写错数的坑,换一个安静丢字的坑,而后者今天并不存在。**

★ 连带两条:**没有纯 CSS 的 (c)**(`disabled` 的控件确实不提交,**而 CSS 设不了 `disabled`**);
**(c) 要修两遍**(`DataTable` 是同一个机制的另一头,而 (b) 让两头对那八张同时失效)。

### ★★ G3 · `known-issues` 里 (c) 那一行**高估了自己的证据** —— 已就地更正

原话:「要一次 JS 媒体查询 —— **SSR / 水合的老坑**,本仓库别处避着它」。

| 量的是 | 读数 |
|---|---|
| `grep -rn "水合\|hydration" docs/known-issues.md` | **2 处** —— 一处是探针就绪的说明,一处**就是这句话自己**。☞ **没有一次在册的水合事故在它背后。** |
| 那条回避在不在案 | ★ **在,而理由不是水合。** `SearchEntry.tsx:274-277` 逐字:「…那会在 TS 里【再写一遍】Tailwind 的 `md` 断点,于是 768 这个数在仓库里有两份…**一个源。**」 |
| 仓库到底用不用 `matchMedia` | ★ **用。** `feedback.tsx:23-25`(带 `typeof window !== 'undefined'` 守卫)—— 只是它问的不是断点。 |

☞ **回避的理由是【断点的值只许有一个源】,不是 SSR / 水合。**
★★ **而裁定不靠这一条** —— 押着 (c) 的是 G2,那一条是量出来的。

### ★★ G4 · DRAFT-1 点名的那个风险**不成立**,而另一个成立

DRAFT-1 的 grilling 点名「`keySig` 那个补种 effect + 下标行键」是 `page-owned` 的具体风险。逐行读下来:

* `editable-table.tsx` 的补种 effect 开头就是 `if (mode !== 'all-rows') return` —— ★ **它对 `page-owned` 根本不跑。**
* `drafts` 那个容器**只在 `all-rows` 时播种**;`page-owned` 一个草稿都不存(`draft = toDraft(row)`,恒等)。

☞ ★ **那个被点名的风险不在那里。** 真正在的是另一个:**`open` 是按 `rowKey` 存的。**
用下标做键时,`removeLine(i)` 与 `onApplyTemplate`(整个数组换掉)会让行在**同一个键底下挪动** ——
**一行展开着,显示的却是另一行。** ★ **数据是安全的**(数组由页面持有),**这是一处观感问题。**

⚠ **对 `#24` 它不发生** —— 那张表**没有加行也没有删行**,五个槽的下标从头到尾不动。
它要紧的是**下一刀的 `#8` / `#9`**,那两张都会删行。

### ★★ G5 · 那条缺陷**仍然是 0/8**,所以**没有一个跑在真路由上的红→绿**

实测今天全部八个 `<EditableTable>` 调用点,`name="` 命中 **0 / 0 / 0 / 0 / 0 / 0 / 0 / 0**
(DRAFT-1 量的是当时的 4 个)。

☞ ★★★ **而更要紧的是这一句:一个【数 DOM 副本】的探针在 (b) 之下会【一直红】**,
因为 (b) 不改那个双渲染。**所以 DOM 那一侧根本证不了 (b)。**
★★ **这条裁定唯一一个真红得起来的测试是那道闸**(Q2);
**浏览器那 12 条是【正面证据】,不是红→绿。** —— 见 §4.1 与 §5。

---

## 1 · Tim 的七条裁定(2026-09-21,全部照收)

| | 问的是 | 裁定 |
|---|---|---|
| **Q1** | (b) 还是 (c),八张一次裁完 | ★★★ **(b)** —— 页面持有 + 一座 JSON 桥 |
| **Q2** | 这条规矩配不配一道闸 | **(ii)** 抬头 **+** `scripts/check-editable-name.mjs`,进构建链 |
| **Q3** | 五张各传什么 `dirty` | **都传真的**;`#8` 那条对 `IDLE-DRAFT-GRID-HALF-RESTORE` 是**缓解不是修复**(而且**今天还没发生**) |
| **Q4** | 空槽表手机上留哪一列 | **加一列只读「序号」并 priority**;**不许用 `scroll` 模式**;那条死路**单独立案** |
| **Q5** | 390px 就地打字没了,认不认 | **认**,逐表记;两块 TABLE-PHONE-3 注释的理由**搬进交回报告,不许成孤儿** |
| **Q6** | `page-owned` 那五个死标签 | **收窄类型** —— 那一支只要 `{ expand }` |
| **Q7** | 批次顺序 | ★★ **对调** —— 批次② = `#24`(它才验得了那条裁定);`#6 #7 #8 #9` 顺延 |

### 1.1 ★ 停止线(Tim 亲画):**本刀到 #24 为止**

**理由是他的:** `#24` 搬完,**那条裁定就是被证过的**;而留下的四张
**从来不在那条坑的射程里** —— 于是「提前收工」的代价是**最小的那一个**。

### 1.2 ★ 委托书本来要改的一件事,这里照直记

委托书的批次① 写着「**在 EditableTable 里实现选定的答案**」。
☞ ★ **按 (b),那件事没有机械内容(G1)。** 批次① 因此落下的是:**一段抬头 + 一道闸 + 一次类型收窄**。
**照直说出来**,免得后来的人去 `editable-table.tsx` 里找一段并不存在的 (b) 实现。

### 1.3 ★★ 留下的四张为什么「从来不在射程里」—— 逐张量过,不是推的

| # | 今天怎么交参 | 那条坑碰不碰得到它 |
|--:|---|---|
| **#6 / #7** `NewWorkOrderForm` | ★ **连 `<form>` 都没有** —— `createWorkOrder({…})` 是一个**带类型的实参**(`:48-65`);全文件 `name=` **0 个** | ★★ **碰不到。** 它压根不走 `FormData` |
| **#8** `NewOrderForm` 付款计划 | ★ **早就坐在桥上** —— `terms_json`(`:372`)→ `JSON.parse`(`orders/new/actions.ts:137`) | ★★ **碰不到**,而且**服务端零改动** |
| **#9** `TemplateForm` | ★ 同上 —— `lines_json`(`:108`)→ `parseLines`(`payment-terms/actions.ts:121`) | ★★ **碰不到**,服务端零改动 |

☞ ★ **所以那四张是【外观 + 手机档】的活,不是【正确性】的活。** 它们晚一刀不损失任何东西。

---

## 2 · 建了什么

### 2.1 批次① · 裁定落地(`9feff8ed`)—— **一张表都没搬**

| 落点 | 是什么 |
|---|---|
| `editable-table.tsx` **抬头** | (b) 的裁定 + Tim 的三条理由(已是这棵树的形状 / (c) 转屏丢字 / (c) 要修两遍)· 那道闸的判据与故障注入读数 · ★ **G5 那句「没有红→绿」照直写在里面** |
| `editable-table.tsx` **抬头** | ★★ **DRAFT-1 欠的那一处落点,付掉了** —— 「零消费者的能力留着」那条裁定**照抄 `DRAFT-1.md` §3.1 的引用块,一字未改** |
| `scripts/check-editable-name.mjs` **(新)** | `<EditableTable>` 的 `columns` 区段里出现 `name=` 就变红。**`render` 与 `edit` 一视同仁**;**`footer` 刻意不在判据内** |
| `package.json` | 进 `npm run build` 链(排在 `check-datatable-footer` 之后)+ `check:editablename` |
| **类型收窄(Q6)** | `labels` 从 `EditableTableCommon` 搬进判别式:`page-owned` 那一支只要 `{ expand }` |
| **两处标记** | `DRAFT-1.md` §3.1 与 `forward-queue.md` 第 4 行的「**还欠一处落点**」—— **划掉 + 写收据** |

> ★ **关于「划掉」而不是「删掉」:** 委托书说的是「移除」,队列第 4 行说的是「付掉那一笔的人才删得动它们」。
> 这里取的是**划掉留着 + 一行收据** —— 那个标记**不再读作一笔未付的账**,而**删掉一行标记
> 和从来没写过它,在读的人眼里没有区别**。☞ **要硬删,说一声,一条命令的事。**

#### ★★ 那道闸的故障注入 —— **它真的红得起来,而且红在该红的地方**

| 做了什么 | 读数 |
|---|---|
| 往 `GoalsEditor` 一个 `edit()` 的格子里放 `name="probe_objective"` | ★ **`RED_OWN_EXIT=1`**,点名 `app/hr/reviews/GoalsEditor.tsx:146` |
| 改成放进 `footer`(**那正是 (b) 那座桥该待的地方**) | ★ **`FOOTER_OWN_EXIT=0`** —— 放行 |
| 全撤 | ★ **`CLEAN_OWN_EXIT=0`**,`git diff` 空 |

☞ ★ **中间那一条和第一条一样要紧:一道把解法也拦下来的闸,读起来就是坏的。**

⚠ **它不与 `check-datatable-phone.mjs` 共用那三个解析工具,理由写在它抬头里:**
那一支的 `blankComments()` **只抹注释、不抹字符串正文**,而本闸要按括号配对,必须连字符串正文一起抹;
改那一支去共用**会改掉它 `assertPinned` 钉住的两个计数**。
★ **两份 `blankComments` 会漂开是一个真的风险,记下来,不假装它不存在。**

### 2.2 批次② · #24(`6504dd45`)

| 变的是什么 | 前 → 后 |
|---|---|
| 行的持有者 | **DOM(非受控)** → ★ **页面 state**(`lines: LineDraft[]`) |
| 交参 | **15 个 `line_{material,qty,price}_${i}`** + `for (i<20) get()` → ★ **一个 `lines_json`** + `JSON.parse` |
| 表 | 手搓 `tableC` `<table>` → `<EditableTable mode="page-owned">` |
| 手机档 | **没有**(三列原样堆着) → 序号 + 物料留在表里,**编辑在展开区** |
| 「未保存」提醒 | **没有** → `beforeunload`(`dirty` = 任一槽有字) |

★ **逐行的判据一个字没改** —— 空槽照旧跳过,数量/单价照旧**独立**拒空拒负,
`errNoLines` 照旧在写库之前判。**AGENTS.md 的两道闸:界面那一道不是保护,这一道才是。**
★ 新的那一面**照直拒**:载荷坏掉、或者不是一个数组 → `fieldErrors.lines`,
**不当成「没有行」**(两者不是同一件事,错得也不是同一个方向)。

★ **下标烧进行里(`LineRow.i`)**:`render(row)` 与 `edit(draft, set)` **都收不到下标**,
而 `page-owned` 下 `set` 是一条按名拒绝,页面必须走自己的 setter —— 这一格非知道「我是第几槽」不可。
☞ 靠 `indexOf` 捞是一条**看不见的、一次 `map` 就会断掉的**依赖;烧进行里是一条看得见的。
★ **它不进 `lines_json`。**

---

## 3 · #24 逐条:得到什么,失去什么

| | |
|---|---|
| ★★★ **得到(正确性)** | **那条坑对它退场了。** 搬家前它是 `get()` 那一种坏法:**手机上打的字整个丢掉,而且不报错。** 今天格子里 `name=` **0 个** |
| ★★ **得到(手机)** | 它从前**没有任何手机档** —— 三列原样堆在 390px 上。今天:序号 + 物料留在表里,编辑在展开区,**整页横向溢出 0**(`scrollWidth 390 / clientWidth 390`) |
| ★ **得到** | `beforeunload`;失败重渲染时草稿**按构造**留在 React state 里 |
| ★ **得到(Q4 的新列)** | 一列只读**序号**。★ **今天这张表没有它** —— 五个空槽内容完全相同,不给序号,手机上就是**五行「—」**,认不出在改哪一行。这一列 `#8` / `#9` 早就有(「序号」) |
| ⚠ **失去** | ★ **390px 上的就地打字。** 今天在格子里直接填;搬完之后**每一行要先点开**。这是组件的既定契约(抬头 ④),不是本刀的选择 |
| ⚠ **变了** | 物料/数量/单价在手机上变成**只读投影**,编辑在展开区 —— 与 DRAFT-1 给 `#10` 记的那一条逐字相同 |

> ### ★ Q5 的交代:两块 `TABLE-PHONE-3` 注释的理由,**本刀一个字都没有动它们**
> 那两块住在 **`#8 NewOrderForm:890-899`** 与 **`#9 TemplateForm:184-189`**,
> 而 **`#8` / `#9` 顺延到下一刀** —— ☞ **所以本刀没有拆掉任何一块,它们原样在树上。**
> ★ **下一刀拆它们时欠的那句话,先写在这里,免得到时候成孤儿:**
> 它们记的是**「手机档被拿掉的那一列(金额 / 删除),原样叠在序号那一格里 ——
> 拿掉的是那一列,不是那个事实」**。组件的展开区**就是那一份叠加块的正规写法**,
> 所以搬过去是**换一个写法,不是丢掉那个事实**。
> ⚠ ★★ **而对 `#8` 那一块,损失比 `#10` 当初【大】**:一行付款计划带着**两个单选 + 一个条件数字框 +
> 一个触发下拉 + 一个条件日期** —— 今天**全都在 390px 上就地可填**,搬完之后**全在一次点开之后**。

---

## 4 · 读数(**每一个都是那支脚本自己的退出行**)

| 量具 | 判词 |
|---|---|
| `npx tsc --noEmit` | **`TSC_OWN_EXIT=0`** |
| `npm run build`(**34 条静态检查**,新的那一条在内 + `next build`) | 批次① **`BUILD1_OWN_EXIT=0`** · 批次② **`BUILD2_OWN_EXIT=0`** |
| `node scripts/check-i18n.mjs` | **`I18N_OWN_EXIT=0`** |
| `node scripts/check-error-swallowing.mjs` | **`SWALLOW_OWN_EXIT=0`**(0 unallowed · 0 queued · 9 allowlisted) |
| `node scripts/check-editable-name.mjs` | **`NAME_OWN_EXIT=0`** —— 8 个调用点 / 8 个 `columns` 区段 / 带 `name` 的控件 **0** |
| `node scripts/smoke-routes.mjs` | **`SMOKE_OWN_EXIT=0`** —— **250 ok · 6 skipped(没数据)· 0 FAILED**;231 条路由 + 各类探针;计时 225 条,合计 **724.7s**,中位数 **3062 ms** |
| `node scripts/probe-draft2.mjs` | **`PROBE_OWN_EXIT=0`** —— **12 条全过**,见 §4.1 |
| `db/gate.py` · 备份 | ★ **没跑,而且不需要** —— 零迁移,`db/` 一个字节没动 |

★ `check-datatable-phone` 的读数从 **164(DataTable 157 · EditableTable 7)**
变成 **165(157 · 8)** —— 多出来的一个正是 `#24`。那一条「静态读不出 1」
(`MySelfAssessmentPanel:186`)**原样还在**,本刀没有碰它。

### 4.1 ★★ 浏览器证明(390px 与 1280px,`PROBE_OWN_EXIT=0`,十二条全过)

探针 `scripts/probe-draft2.mjs`,跑在**生产构建**上(`next start`),等的是**水合收尾**(`__reactFiber$…`)。
★★ **它一次提交都不发** —— 判据读的是 `new FormData(form)`,**那就是按下提交会送出去的东西**,而读它不写库。

| 判据 | 读数 |
|---|---|
| ★★★ 格子里的具名控件 | **0 个** |
| ★★★ 桥只出现一次 · 并列数组退场 | `lines_json` **1 次** · `line_*` 具名字段 **0 个**(★ **搬家前是 15 个**) |
| 390 / 桥里五个空槽 | `lines.length = 5` |
| 390 / 收起时格子只读 | DOM **10** 个数字输入 · **看得见 0 个** |
| 390 / 展开后 | DOM **12** / 看得见 **2** ← ★★ **双渲染照旧在。(b) 不修它。** |
| ★★★ **390 / 手机上打的字【到了】桥里** | `lines[0] = {"material_id":"5d4c…","qty":"77","price":"10.5"}`,`lines_json` **1 次** |
| 390 / 其余四槽还是空的 | 四个 `{"material_id":"","qty":"","price":""}` |
| 390 / 整页横向溢出 | `scrollWidth 390 / clientWidth 390` —— **不溢出** |
| 1280 / 格子里就地编辑 | DOM 12 / **看得见 10**(五行 × 两格) |
| ★ 1280 / 390 上打的字出现在格子里 | 第一行两格 = **`["77","10.5"]`** ← **同一个 store,两个断点** |
| ★★★ **1280 / 桌面上打的字【到了】桥里,桥仍然只有一份** | `lines[0].qty="77"` · `lines[1].qty="9"` · `lines_json` **1 次** |
| 1280 / `line_*` 具名字段 | **`[]`** |

> ### ★★★ **这十二条【不是】一次红→绿 —— 照直说一遍**
> **(b) 不改那个双渲染**,所以一个**数 DOM 副本**的判据在 (b) 之下**会一直红**。
> 上面证的是**结果**:桥只有一份、格子里没有具名控件、而桥**带着那个断点上打的字**。
> ☞ ★ **真红得起来的那个测试是 §2.1 那道闸的故障注入。**
> ⚠ **而且今天【没有一页是红的】** —— 八个调用点 `name=` 全 0(G5),
> 所以**根本不存在一个跑在真路由上的红→绿**。**这一句写在这里,不让人去推。**

★ **复核(收尾,只读):** 残留探针账号 **0** · `.ephemeral/` **空** ·
`quote_lines` 里 `qty ∈ {77, 9}` 的行 **0 条** · `quotes` 仍然 **3**(与 R11 计数同)。
☞ **一张报价都没有被建出来。**

★ **两处在册的探针缺陷都避开了:** 每一次 CDP 调用带 **30s 上限**(`PROBE-UNBOUNDED-CDP-WAIT`);
收尾那条 `lsof` 把**本进程**滤掉(`PROBE-LSOF-KILLS-ITSELF`)。

#### ⚠ 一处 `NOT MEASURED`,照直标出来

委托书点名要量 **`#24` 在 390px 上【今天】的表现**,并说「**能顺手量就量,量不到就标出来**」。
☞ ★ **上面那个 `scrollWidth 390 / clientWidth 390` 是【搬家之后】的读数。**
**【搬家之前】那一个没有量** —— 量它要把旧 markup 再构建一次、再跑一次探针,
而那**不是顺手**(一次生产构建 + 一次 `next start`)。
**⚠ 所以:搬家前 `NOT MEASURED`;搬家后【不溢出】,已量。两者不要混读。**

### 4.2 冒烟 —— `SMOKE_OWN_EXIT=0`

**250 ok · 6 skipped(没数据)· 0 FAILED。** 231 条路由 + 各类探针;
计时 225 条,合计 **724.7s**,中位数 **3062 ms**(DRAFT-1 是 893.9s / 3724 ms)。

★ **那 6 条 skipped 与本刀无关** —— 它们是源表空着的那几条
(`attendance_periods` · `assay_results` · `commission_agreements` · `management_packs` …),
**与 DRAFT-1 记的是同一批**。☞ ⚠ **DRAFT-1 §2.1 那处【验证缺口】原样成立,本刀没有还上它。**

★ **本刀动过的那一条 `/sales/quotes/new` 没有出现在 SKIP 名单里,也没有 FAILED** ——
它是一张建单页,**不需要数据就画得出来**;而它真正被看过的证据在 §4.1,不在这里。
**一个绿的冒烟不是一次目视 —— 这一张两样都有。**

★ 另记:**临时行体检报了滞留行**(冒烟自己打的那一行:「处置由人决定」)。
按那个检查自己的规矩:**报告,不清扫** —— 本刀一行都没有动过它们。

---

## 5 · 文档

| 改了什么 | 在哪 |
|---|---|
| ★★ `EDITABLETABLE-NAME-DOUBLE-SUBMIT` —— **修订,不是关闭**:裁定 (b) · 八张只搬了一张 · **另外七张原样卡着** · 那道闸 · **G5 那句「没有红→绿」** | `known-issues.md` |
| ★★ (b)/(c) 那张表:(c) **划掉出局**;**G3 的更正就地写在旁边**(回避是真的,理由是【断点只许一个源】,不是水合);★ 另起一节写 **G2** 那条转屏丢字 | 同上 |
| ★ **立案 `EDITABLETABLE-SCROLL-PHONE-UNEDITABLE`** —— `scroll` + 可编辑 = 手机上改不动;**今天 8/8 全是 `columns`,零个消费者**;**本刀不修,照直记** | 同上 |
| ★ `IDLE-DRAFT-GRID-HALF-RESTORE` 旁注:`#8` 的 `beforeunload` 是**缓解不是修复**,⚠ **而且今天还没有发生**(`#8` 在下一刀) | 同上 |
| ★ 两处「还欠一处落点」**划掉 + 收据** | `DRAFT-1.md` §3.1 · `forward-queue.md` 第 4 行 |
| ★ 队列第 4 行:本刀做了什么、剩下什么、**带着组件在手上之后我会怎么定剩下的大小** | `forward-queue.md` |

---

## 6 · 还剩什么

* ★★ **`#6` · `#7` · `#8` · `#9`** —— **下一刀第一批**。它们**从来不在那条坑的射程里**(§1.3),
  所以是**外观 + 手机档**的活。⚠ 三件已经欠着的话:
  ① **Q4 的序号列**:`#6`/`#7` 也要加(`#8`/`#9` 已有);
  ② **G4 的下标键**:这两张**会删行**,`open` 那个键会在行底下挪 —— 观感问题,要说出来;
  ③ **§3 那两块 `TABLE-PHONE-3` 注释的理由**,拆的时候要搬进那一刀的交回报告。
* ⚠ **`EDITABLETABLE-NAME-DOUBLE-SUBMIT` 的另外七张**:`#18` `#20` `#21` `#22` `#23` `#26` `#27`。
  裁定有了、闸有了、样板有了(`#24`),**剩下的是七次同样的转换 + 七个服务端收参形状**。
  ★ **`#21` 是最重的一张:两组并列数组,合计 11 条**(`amend/actions.ts:29-33` + `:56-61`)。
* ⚠ ★★ **Q7(Tim 的那条)仍然开着,`#22` 进场之前必须裁掉** —— 组件把行内动作放进手机展开区,
  而 `TABLE-STYLE-1 / R1`(Tim,2026-09-09)裁的是动作列在 390px 上不折。**本刀按委托书一个字没碰它。**
* ⚠ **`#3 PayrollGrid` 欠一次 390px 重量**(R12)—— 搬完之后才还得上,今天未还。
* ⚠ **`DATATABLE-UNCONTROLLED-NAME-DOUBLE-SUBMIT` 开着** —— 那道新闸**只管 `EditableTable`**,
  `DataTable` 那一头今天仍然只有一句注释守着(写在闸自己的瞄准线里)。
* ⚠ **验证缺口照旧成立**(DRAFT-1 §2.1):**一个绿的构建不是一次目视。**
  ☞ ★ 本刀这一张**不在那个缺口里** —— `/sales/quotes/new` 是一张建单页,**不需要数据就画得出来**,
  而且它**真的被看过了**(§4.1 的十二条)。
