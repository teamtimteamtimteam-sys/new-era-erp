# TABLE-CONVERT-7 交回报告 —— 【六张里能转的是 0 张,而上一刀说"两张"是我读漏了一段】

> **本刀转换 0 张表,改动的只有记录。**
>
> ★★ **两条更正,一条比一条要紧:**
> ① **`TABLE-FOOTER-1 §4` 把 `balance-sheet` 判成「纯 `<tfoot>`,完整解开」—— 那是错的。**
>    它的 `sectionBlock()` 每节画一条分组抬头 + 一条夹层小计,×3 节。
>    **实测渲染出来:6 条带 colSpan 的行。** 错因照直写:那一刀读了它的 `<tfoot>`,
>    **没有读 `sectionBlock`**。
> ② 于是「六张里两张完整解开」的实数是**一张**(`PayrollGrid`),
>    **而那一张卡在草稿模型后面。表尾能力今天仍然【零个消费者】。**
>
> ★ 委托书说「expect the answer to be fewer than six」——**答案是 0。**

---

## 1 · HEAD 与树

| | |
|---|---|
| 期待的 HEAD | TABLE-STYLE-2 的最后一个提交或其纯文档后继 |
| **开工前 HEAD** | `f94f1dd5001bd365dec0d23e7af2903391116420` |
| 与期待的差别 | `f94f1dd` 是 `2a4d465`(TABLE-STYLE-2 工作提交)的**纯文档后继**:`git show --stat` 只有 `docs/handbacks/TABLE-STYLE-2-round2.md` 一个文件,**零处代码** → **PROCEED** |
| 开工前树 | 干净 · `HEAD == origin/main` ✓ |
| **收工后 HEAD** | 见 §14 |

> ### ⚠ 委托书里两处与源码对不上的地方,照直报
> 1. **「the divider it had to restore on PayrollGrid」—— 没有这回事。**
>    TABLE-STYLE-2 **根本没有给 `PayrollGrid` 穿上 tableC**:它量出横滚 414→450px 之后
>    **停手并整份放回**。今天 `PayrollGrid.tsx:202` 仍然是
>    `<table className="w-full border-collapse border border-gray-300 text-sm">`,
>    `tableC` 出现 **0 次**。没有分隔线被"恢复"过。
> 2. **「The 9 tables TABLE-STYLE-2 just dressed」—— 是 4 张,不是 9 张。**
>    那一刀:**穿好 4 张** · **两张枢轴表上上一刀就穿好了** · **停在 3 张之前**。

---

## 2 · ★★ 六张逐张读完 —— **能转的是 0 张** ★★

### 2.1 判决表(**源码读一遍 + 真浏览器渲染数一遍,两条独立路数**)

| # | 表 | 拦住它的 | **渲染出来的证据**(1280px,真数据) | 能转? |
|--:|---|---|---|:-:|
| 1 | `finance/balance-sheet:174` | ★ **分组抬头 + 夹层小计**(`sectionBlock():101`,×3 节) | 14 数据行 · **6 条带 colSpan 的行** · tfoot 2 行<br>`Assets` / `Assets — Totals` / `Liabilities` / `Liabilities — Totals` / `Equity` / `Equity — Totals` | ❌ |
| 2 | `finance/trial-balance:148` | **分组抬头**(按科目类型,`:167`,colSpan 写两份) | 23 数据行 · **5 条分组抬头** · tfoot 1 行 | ❌ |
| 3 | `finance/payables:173` | **夹层小计**(每个账龄桶一条,`subtotal-${gi}`) | 13 数据行 · **4 条夹层小计** · tfoot 0 | ❌ |
| 4 | `finance/receivables:169` | **夹层小计**(同上) | 9 数据行 · **3 条夹层小计** · tfoot 0 | ❌ |
| 5 | `finance/pnl:160` | **分组抬头 + 夹层小计 + 夹在中间的毛利/净利** | 1 数据行 · **8 条带 colSpan 的行** · tfoot 0 | ❌ |
| 6 | `hr/payroll/PayrollGrid:202` | ★ **表尾确实解开了它** —— 但格子里是**受控输入**(12 处 `DecimalInput`/`patch`) | 7 数据行 · **0 条带 colSpan 的行** · **tfoot 1 行** | ❌ **等草稿模型** |

> **★ 第 6 行是全表唯一「0 条拦路行 + 有 tfoot」的那一张** ——
> 也就是说**表尾能力的唯一真消费者就是它**,而它被另一个能力卡住。

### 2.2 ★ 更正 `TABLE-FOOTER-1 §4`

那一刀写的是:

> | 1 | `finance/balance-sheet:193` | ✅ **完整解开** | 纯 `<tfoot>` 两行,正是本能力的形状 |

**实测不是。** `balance-sheet` 的行**不住在 `<table>` 块里**,住在 `sectionBlock()`(:101)——
而那个函数里有 `<td colSpan={3}>`(分组抬头)与 `<td colSpan={2}>`(夹层小计),
**每节各一条,调用三次**。渲染出来 **6 条**,和读源码算出来的 3+3 **对得上**。

☞ **错因不是判断错,是【没读到】:** 那一刀读了 `:165–215`(`<tfoot>` 那一段),
**没有往上读到 :101**。**这与本族记了一整族的那个教训是同一条**——
`TABLE-STYLE-1 §3.2` 早就写过「`balance-sheet` / `pnl` / `cashflow` 的行住在块外面,
普查按块统计就数不到」。**我在 TABLE-FOOTER-1 里没有把那一条用在自己身上。**

> ### ★★ 而这正是「加能力」与「吃能力」分开的价值兑现的地方 ★★
> **如果 TABLE-FOOTER-1 顺手把 `balance-sheet` 转了**,这个读漏
> **会带着一张转坏的表一起上线**(6 条结构行会被压成 14 行数据 + 一个表尾)。
> 它没有转任何东西,所以这次读漏的代价**只是一行写错的记录,而不是一张坏掉的报表**。

### 2.3 处置:**不硬转,不在这里建缺的能力**

委托书 §1 明令:「**DO NOT BUILD THE MISSING CAPABILITY HERE**」。照办。
六张里**五张已经穿着 `tableC`**(外观正确、markup 保留),
**`PayrollGrid` 是唯一的例外** —— 见 §4。

---

## 3 · 每张转换后的表:**没有。本刀转换 0 张。**

委托书 §6-3 要「per converted table: 手机判断 / 表尾在 390px 的行为 /
TABLE-FOOTER-1 的折叠合计规则站不站得住」。**三条都没有对象:**

* **没有新的手机判断** —— 零张表被转换。
* **表尾在 390px 上的行为【仍然没有被消费过】。**
* ★ **「折叠合计不显示」那条规则,本刀【仍然无法验证】。**
  委托书说「YOU ARE THE FIRST CONSUMER — measure what that rule actually does」——
  **我不是消费者,因为没有一张表转得成。** TABLE-FOOTER-1 把那条规则标成
  「**reasoned, not measured**」,**这一刀之后它仍然是 reasoned, not measured。**
  ☞ **第一个能验证它的人,是草稿模型那一刀之后转 `PayrollGrid` 的那一刀。**

**§6-4(展开区)· §6-5(空态)· §6-9(key 计数)同理**:零张转换 → 零个展开区、
零处空态搬迁、**key 计数两个方向都是 0**(`git diff` 里 `app/` 一个文件都没有)。

---

## 4 · ★ 13 张仍是变体 A —— **「不是组件就是 tableC」这句话今天不成立**

委托书 §1 说「After it, every table in the system either uses the component or wears tableC」。
**实测不是。** 现扫(与棘轮逐字同判据):**手搓 39 处 / 35 文件,其中穿 `tableC` 的 26,没穿的 13。**

| 类 | 处数 | 点名 |
|---|--:|---|
| **B 类(格子里有受控输入)** | **11** | `BulkFxGrid:88` · `AttendanceGrid:48` · `PayrollGrid:202` · `GoalsEditor:153` · `ContainerPanels:120` · `NewWorkOrderForm:95`/`:134` · `NewOrderForm:866` · `TemplateForm:167` · `QuoteLinesEditor:81` · `PermissionMatrix:120` |
| **TABLE-STYLE-2 量出会变差、停下的** | **2** | `ForwarderPanels:167` · `forwarders/page:203` |

* **11 张 B 类是【刻意】不穿的,而这是对的**:TABLE-STYLE-1 的工作单是「**转不动**的那 23 张」,
  B 类**转得动**(等草稿模型),转的时候外观跟着一起对 —— **先穿再转是白做一遍。**
* ☞ **但委托书那句「their APPEARANCE is already correct」是【假的】** ——
  实测这 11 张今天仍是 `border border-gray-300` 的全边框变体 A。**已写进记录,免得被当成已完成。**
* **`PayrollGrid` 两头都占**:既是 B 类,又是 TABLE-STYLE-2 停下的那张。
  **它是全仓唯一一张【既不是组件、也不穿 tableC、还带着一个已量出并已被接受的手机代价】的表。**

---

## 5 · 棘轮与手搓表计数:**一个数都没动**

| | 开工前 | 收工后 |
|---|--:|--:|
| 手搓 `<table>` | **39** | **39** |
| cellfont | **309** | **309** |
| `<DataTable>` 调用点 | **159 / 131 文件** | **159 / 131 文件** |
| `<EditableTable>` 调用点 | **6** | **6** |

**本刀 `app/` 下一个文件都没改**,所以这四个数**本来就不该动** —— 它们不动是预期,不是成绩。

---

## 6 · 我渲染了什么,在什么状态下,以及水合

**六条真路由 × 390px 与 1280px,线上真数据,一次性 admin(全权限)。**
**这一次渲染的目的不是"验证改动",是【把 §2 那个判断从"读源码"抬成"量出来"】。**

| 路由 | 表 | 数据行 | 拦路行 | fiber |
|---|---|--:|--:|:-:|
| `/finance/balance-sheet` | 1 | 14 | **6** | ✓ |
| `/finance/trial-balance` | 1 | 23 | **5** | ✓ |
| `/finance/payables` | 1 | 13 | **4** | ✓ |
| `/finance/receivables` | 1 | 9 | **3** | ✓ |
| `/finance/pnl` | 1 | 1 | **8** | ✓ |
| `/hr/payroll/new` | 1 | 7 | **0** | ✓ |

**水合:** 逐张表问 `__reactFiber$`(`react-dom` 只有在客户端真的跑过之后才挂它),
**六张全部带 fiber**,页面 fiber 数 149 / 380 / 446 / 408 / 89 / 206。

**没渲染出来的:** 见 §12。

---

## 7 · 行高与溢出:**没有量前后,因为没有前后**

本刀没有改任何渲染路径,**所以"前后"这两个字没有对象**。
`/finance/payables` 在 390px 上仍然溢出(`COPY-1-PAYABLES-OVF` 记着的 480px),
**本刀一 px 没加也没减。**

---

## 8 · 我自己拿的主意

1. **把 §2 的判断【渲染一遍再写】。** 委托书只要求"读每一张",而一次读漏
   正是这一刀要更正的东西 —— **所以我用真浏览器数了 DOM 里带 colSpan 的行,
   与读源码算出来的数对上了才写。** 一次读源码得出的结论,这一族已经错过一次。
2. **不给 `balance-sheet` 硬转,也不在这里建行模型。** 委托书 §1 明令,且
   TABLE-FOOTER-1 的分离原则这一刀刚刚兑现了价值(§2.2)。
3. **记录写进 `known-issues.md` 与 `forward-queue.md`,不只写进交回报告。**
   委托书 §3 说得对,而这一族的母条 `RAW-TABLE-PHONE-SWEEP` 已经有 §八 那个先例
   —— **我照它的形状写,不另发明一种。**
4. **更正 `forward-queue` 里 `RAW-TABLE-PHONE-WRAPPED` 的「UNMEASURED」**:
   那 9 张里 6 张已转,剩 3 张**已经量过了**(TABLE-MEASURE-1 + TABLE-STYLE-2)。
   **它们等的是裁定,不是测量。** 原文保留在下面,不删。
5. **委托书两处与源码对不上的地方照直报**(§1 的两条),不当作笔误吞掉。

---

## 9 · Tim 该在哪里看,和每一页要什么数据

> **本刀没有改任何页面,所以【没有东西可走】。** 要看的是两份记录:

| # | 看什么 | 在哪 |
|---|---|---|
| 1 | ★ **结案条**:六张的判决 · 13 张变体 A · 从未验证过的五条 · 估算自己的历史 · 还缺的六个能力 | `docs/known-issues.md` 的 **`TABLE-CONVERT-SWEEP`** |
| 2 | 队列里那条「UNMEASURED」的**状态更正** | `docs/forward-queue.md` 的 `RAW-TABLE-PHONE-WRAPPED` 抬头 |

**如果要眼见为实地确认 §2 那个"0 张"**,这三页最能说明问题(都不需要特殊数据):

* **`/finance/balance-sheet`** —— **6 条结构行**(三节抬头 + 三条小计)夹在 14 行数据里。
  **这就是"它不是纯 tfoot"的那张图。**
* **`/finance/pnl`** —— 1 行数据配 **8 条结构行**,最极端的一张。
* **`/hr/payroll/new`**(需要在职员工,线上有 7 名)—— **唯一一张 0 拦路行 + 有 tfoot 的表**,
  也就是表尾能力将来的第一个消费者。⚠ 它今天仍是变体 A,且 390px 上横滚 414px。

★ `/hr/payroll/[id]/edit` **进不去** —— 线上 `payroll_periods` 只有 1 条且是 `posted`,
那条路由 `redirect` 掉(TABLE-MEASURE-1 §7 记过,本刀复核仍成立)。

---

## 10 · 我没能验证的

1. **★ 「折叠的合计不显示」那条规则仍然是 reasoned, not measured**(§3)——
   本刀没能成为它的消费者。
2. **`hr/reviews/cycles:158` 仍然没有渲染过**(`review_cycles` 是空表)。
3. **`PayrollGrid` 的空态行没渲染过**(线上恒有在职员工)。
4. **`ContainerPanels:120` 与 `:306` 并排的样子没看过**(线上那个货柜没有发货单)。
5. **只量首屏 · 只走英文档 · 硬导航 · 一次性 admin(全权限)**;权限为 false 的状态没量。
6. **`/finance/pnl` 只有 1 行数据** —— 8 条结构行是真的,但**一张只有一行数据的损益表
   不能证明它在数据多时也是这个形状**(虽然结构行的条数由 `sectionBlock` 的调用次数决定,与数据量无关)。

---

## 11 · 全部 76 张表的最终去向

> **76 是 `TABLE-CONVERT-0` 普查那一刻的手搓表总数**;
> 今天的实数用与棘轮逐字相同的判据现扫。

| 去向 | 处数 | 说明 |
|---|--:|---|
| **已转成 `<DataTable>`** | **159 处 / 131 文件** | 含本来就在用组件的页面;**七刀转换累计换掉 37 张手搓表**(76 → 39) |
| **已转成 `<EditableTable>`** | **6 处** | CONV-2 的 A/B 两类 |
| **手搓,穿着 `tableC`(外观正确、markup 保留)** | **26 处** | 无列头 4 · 枢轴 2 · E 类 5 · 其余只读/表单表 15 |
| **手搓,仍是变体 A** | **13 处** | **B 类 11**(等草稿模型,刻意不穿)+ **TABLE-STYLE-2 停下的 2**(等裁定) |
| **手搓合计** | **39 处 / 35 文件** | 与棘轮基线一致 |

**记录已经带上了那两节:**

* ✅ **【从来没有验证过】** —— 五条,写在 `known-issues.md` 的 `TABLE-CONVERT-SWEEP` §四:
  哪几张因为线上没有那样的数据而从未渲染 · **「一个字段都没丢」只是标记层面的口径** ·
  **`PayrollGrid` 414px 横滚且身份列丢失(一张录入工资的表)** ·
  **`ForwarderPanels` 删除钮只露一半** · 只量首屏/英文档/全权限。
* ✅ **【这个估算自己的历史】** —— 三条,§五:
  **交下来的 54 错在三个方向**(注释里的假阳性 · `count(/<th/)` 无词边界 · 按文件问 `overflow-x` 漏 12 个文件)·
  **普查按【标记】分类,而拦路的问题全是关于【行为】的** ·
  **`name=` 那把筛子报 18 张可转,真数 4 张,漏报 78%**。
  ☞ 可复用的一句写在那里:**扫描器回答「文字在不在范围里」,而「能不能转」是一个关于行为的问题;解药不是更好的正则,是【读】。**
* ✅ **还缺的六个能力** —— §六:夹层小计 · 分组抬头 · 草稿模型 · 逐行格子 `className` ·
  表体 15px token · 双渲染。

---

## 12 · 收工

### 12.1 `db/gate.py` —— **`GATE_OWN_EXIT=0`**

**wall-clock:** 它自己报的三判词 **363s**;外部量到整支 **462s**(含 `db/check_grants.py`)。
**两个数都在 180–700s 里,不用放宽。** 本刀**一行 SQL 都没改**,跑含闸是照本族的规矩。

```
判词【可重建性】:✓ 仓库能从零建出库(prelude 足够,B1/B2 双侧断言见上)
判词【镜像 vs 线上】:✓ 一致(结构、种子、引导、自洽、definer)
判词【行为断言】:✓ db/fixtures 全部通过(建出来的库跑起来是对的)
   判词【匿名面】:✓ 线上是基线的子集(基线 327 条)
```

### 12.2 `npm run build` —— **`BUILD_OWN_EXIT=0`**

```
── eslint 冻结闸 ─────────────────────────────────────────────
基线  error 42 · warning 88
现在  error 42 · warning 88

✓ 没有新增的 eslint 问题。
```

```
✓ check-instrument-selfproof:34 支量具都写了瞄准线;其中 23 支(构建链里的,含本支)都带着覆盖断言。
```

```
   基线:94 个文件在册。本次扫到 309 处。
   分账:标签上 16 处(<table>/<th>/<td>) · 列描述符里 293 处(className: '…')。
```

**TABLE-FOOTER-1 的表尾闸(委托书 §7 点名):**

```
✓ check-datatable-footer:表尾在两个断点上各自跨满整行;折走的合计没有丢;无 footer 的表不长 <tfoot>。
```

> ⚠ **它绿,而它守着的能力今天【零个消费者】** —— 那道闸证的是"组件的表尾是对的",
> **不是**"有人在用它"。这两件事的区别,正是本刀 §2.2 那条更正的由来。

**手搓 `<table>`:** `基线:35 个文件在册。本次扫到 39 处。` —— **39,与开工前相同。**
**四个数(39 / 309 / 159 / 6)全部未动 —— 本刀 `app/` 下一个文件都没改,它们本来就不该动。**

### 12.3 推送与部署

*(推送后回填)*
