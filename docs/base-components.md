# BASE-1 · 基础组件层(表格 · 动效 · 拒绝态)

2026-09-02。本刀**不转换任何页面** —— 187 个已上线页面一个像素都没有变,
而这一条是**证出来的**(见文末「怎么证的」)。没有 DDL。

BRAND-1 装了九个 shadcn 组件与品牌 token,IA-BUILD-1 搭了外壳(九个模块、面包屑、
dock、淡底、浮动层的磨砂)。本刀是**页面底下那一层**:一个按钮按下去有什么反应、
一张表在手机上变成什么、一个人怎么知道刚才那一下生效了。
逐个模块的页面转换**不在本刀里**。

---

## 〇 · 先更正三件事(EXISTENCE FIRST)

委托书要求"三次连着的刀都发现机制已经在了 —— 去查,别假设"。查了,三条里有两条要更正。

### ① 「九个组件零采用」——**错的,是我自己第一次量错了**

第一次用 `grep "components/ui/button\""` 量,得到 0 采用。**那个 grep 要求双引号**,
而仓库里的 import 用的是**单引号**。改用解析式统计之后:

| 组件 | 取样页之外的真实采用 |
|---|---|
| button | `app/login/SubmitButton.tsx` |
| card / input / label | `app/login/page.tsx` |
| 其余五个 | 0 |

也就是说 **`/login` 是一个真的、已上线的、已经转换过的页面**(LOGIN-1 做的)。
这件事直接改了本刀的一个决定 —— 见下面「输入框的推移为什么默认关着」。

### ② FE-0 的「排序:一个都没有」——**结论错,而它的数没错**

FE-0 写的是「`sortBy` / `sortKey` / `sortDir` / `toggleSort` 全仓库 0 次命中」,
**这句话是对的**。它由此推出「用户在任何一张表上都不能点表头重排」,而**这一步错了**:
真正的实现叫 `sortableTh`,走 URL 参数 + 数据库 `ORDER BY`,grep 的那四个名字都不是它。

BASE-1 逐页量的结果:

| 能力 | FE-0 说 | 实测 |
|---|---|---|
| 用户可改的排序 | **0** | **8 页**:/inbound · /materials · /output · /customers · /suppliers · /processing · /metal-prices · /finance/fx |
| 用户可用的分页 | 「局部有,大部分没有」(28 处 `.range()`) | **17 页** |
| 列显隐 | **0** | **0** ✓ FE-0 说对了 |

**这不是吹毛求疵,它改了组件的形状** —— 见 §1「两种排序模式」。

### ③ 「201 张表是不是三类形状」——**不是三类,是一类**

逐张解析 201 个 `<table>` 块(161 个文件,与 FE-0 的数一致):

| | |
|---|---|
| **会重复的账簿**(块里有 `.map`) | **198 / 201** |
| 静态明细表 | **3** |
| 列数分布 | 2–4 列 **58** · 5–6 列 **88** · **≥7 列 52** |
| 最宽的几张 | /inbound **13** · /finance/assets **12** · /materials 与 /output 各 **11** |

**结论:一种答案可以覆盖全部。** 它们不是「账簿 / 两列明细 / 摘要」三类,
它们是同一类,只在**宽度**上不同。真正的难题只有一个:**那 52 张宽表怎么上手机。**

---

## 一 · 表格:`app/components/ui/data-table.tsx`

### R3 · 手机上的画法:【留下的列 + 一行展开】

**不是卡片流。** 一张账簿存在的意义是**顺着一列往下比** ——
60 行变成 60 张卡片之后,那件事就再也做不了了。卡片流是最常见的响应式答案,
而它恰好毁掉这张表唯一不可替代的能力。

**也不是只给横向滚动。** 那是把桌面版缩小,R3 明确不要。

所以:手机上留下**声明过的那几列**(身份 + 那个要紧的数),
其余的收进每行左边那个「›」,点开是一段带标签的 `<dl>`。

**列的优先级每张表自己声明,组件不猜。**
「猜前 N 列」在有些表上一定挑错:/inbound 的前三列是批次号、物料、供应商,
而人在手机上要找的是**批次号、物料、余量** —— 供应商要让位。

> **没有声明 priority 的表,组件【当场按名拒绝】**(`DATATABLE_NO_PHONE_COLUMNS`),
> 而不是默默退回「前 N 列」或「横向滚动」。
> 本仓库对这个形状有一长串先例:不声明重量基准就拒、缺一个交易日就拒、
> 没录上限就拒绝作判断。**一次响亮的拒绝,好过一个悄悄挑错列的默认值。**
> 它拒得起,是因为本刀没有转换任何页面:今天没有一个页面会碰到这条路,
> 而每一次转换都会在第一次渲染时就撞上它。

### R4 · 四个能力,默认【全关】

排序 / 筛选 / 分页 / 列显隐都是显式开关,默认 false。
**把组件换上去不会让任何一页凭空多出一个控件** —— R4 要的就是这个。

### ★ 两种排序模式 —— 「永不声称排了它没拿到的东西」

失败模式是静默的:一页只取了一屏数据,却给出一个排序控件;
人按下去,以为看到的是**全体里最大的那个**,其实只是这一屏里最大的。

处置**放在类型上**,而且比"提示一句"更硬:

```ts
type SortingClient = { sorting: { mode: 'client'; coverage: 'complete' } }
type SortingServer = { sorting: { mode: 'server'; coverage: Coverage
                                 active: …; href: (key, dir) => string } }
```

* `mode: 'client'` —— 组件自己排。**类型上只接受 `coverage: 'complete'`。**
  拿到的是一部分,就**打不开它** —— 编译期就打不开。
  一句提示要人去读;一个类型错误不需要任何人记得。
* `mode: 'server'` —— 表头渲染成**链接**,由页面已有的 URL 参数去数据库排。
  **这一档是为上面那 8 页 / 17 页留的路**:它们的排序是在数据库里对**全体**排的,
  换成客户端排序**是一次降级**,而且正好是上面那个静默失败。
  这时候排序看得见全体,所以**不出警告**,只报"这一屏几行 / 全体几行"。

> 也就是说:**转换刀不必为了用上这个组件而放弃已经正确的服务端排序。**

### 不引 TanStack Table —— 和前两次同一条标准

BRAND-1 拒 shadcn 的 data-table 时说的是「data-table 不是一个组件 ——
排序与列显隐是**缺失的功能**,不是样式」。本刀同意,并按同一条标准判这个依赖:

* 我们要的四件事,对着**已经在内存里的、一页大小的**数组,各是一个比较器、
  一个谓词、一次 slice、一个 `Set`;
* TanStack 真正值钱的地方(服务端分页、虚拟滚动、分组、faceted filter)**我们刻意都不要**;
* 它**完全不碰**这一刀最难也最值钱的那部分 —— 390px。那部分无论如何要手写;
* 而采用它意味着 201 张表在转换时都要改写成 `columnDef` 形状 ——
  **那是比"换掉标记"大得多的迁移面**。
* 列描述符两边都要有(priority 是每张表声明的),而我们这一份可以小、
  并且是**照着手机形态设计**的,TanStack 那一份不是。

### 390px 实测(不是断言)

真浏览器(chrome-headless-shell + CDP)、真服务端渲染、真会话,量的是 `getComputedStyle`
与 `scrollWidth`,不是看截图:

| | 390 × 844 | 1280 × 900 |
|---|---|---|
| 页面横向溢出 | **无**(390 ≤ 390) | **无**(1280 ≤ 1280) |
| 表格 scrollWidth / clientWidth | **342 / 342**(是**放得下**,不是滚动) | 1260 / 1260 |
| 13 列里看得见 | **3 列**(批次号 · 物料 · 余量)+ 展开钮 | **13 列** |
| 点开一行 | `aria-expanded=true`,`<dl>` 出现,**其余 10 列全部拿得到**,**仍然不溢出** | — |

**实测逼出来的两条改动**(都记下来,因为它们不是设计出来的,是量出来的):

1. **手机上用 `table-fixed`,桌面上恢复 `auto`。**
   auto 之下任何一个格子都能把整列撑宽 —— 实测一枚「无库存 · out of stock」小片
   把第三列撑到 260px,把物料那一列挤成**每行一个字**。
   只剩三列的宽度里,一个格子无权决定整列宽度。
2. **表头桌面上不折行(`sm:whitespace-nowrap`)。** 实测 1280px 下「库存状态」
   被折成每行一个字。手机上**不加**这一条:那里是 table-fixed,nowrap 会顶出格子。

---

## 二 · 动效清单 —— 四条,全部为状态服务

R1:**动效只用来反馈状态,永不装饰。** 这是一个一天用八小时的工具;
入场动画、页面转场、会跳的按钮,到第三天只剩烦人,还会让页面**感觉**比实际慢。
下面四条就是全部 —— 没有第五条。

| 状态 | 画法 | 写在哪 | 为什么这个状态值得反馈 |
|---|---|---|---|
| **成功** | 一次扫过的浅绿,1.1s 退回原样 | CSS `@keyframes base-flash-ok` | 写完之后屏幕上常常什么都没变(数字本来就是那个数),于是分不清「存好了」和「点漏了」 |
| **校验失败** | 红边 + 两下 3px 横向推移,260ms | CSS `@keyframes base-nudge-err` | 焦点常常不在出错那一格;颜色是静态的,一次极短的位移把眼睛**带过去** |
| **加载中** | 低振幅骨架脉冲(1 → 0.55)· 按钮转圈 | CSS `@keyframes base-skeleton` / `base-spin` | 「在取数」和「取回来是空的」在屏幕上一模一样;而按下去没反应的按钮会被**再按一次**(IA-BUILD-1-fu1 那条缺陷的原文) |
| **数字变了** | ~15 行 `requestAnimationFrame`,easeOutCubic,420ms | **JS**(`feedback.tsx` 的 `CountUp`) | 一个总数悄悄从 812 变成 1,043,和它一直是 1,043,在屏幕上一样 |

外加一条**过渡**(不是动画):`base-pressable` —— 按下的即时回应。
R1 说得明白:**按下去是什么反应,是按钮自己的性质**,不是事后洒上去的东西。

### 减弱动效:关掉的是【动】,不是【状态】—— 实测

**把状态本身也一起关掉,是把可访问性偏好读成了「这个人不需要知道结果」。**

实测方法:CDP 模拟 `prefers-reduced-motion`,把六个类各挂到一个注入元素上,
读 `getComputedStyle` 的 `animation-name` / `transition-property`。
**直接量 CSS,不依赖"点一下按钮看它有没有动"** —— 后者要靠某次交互恰好发生。

| 类 | no-preference | reduce | |
|---|---|---|---|
| `base-flash-ok` | `base-flash-ok` | **`none`** | ✓ |
| `base-nudge-err` | `base-nudge-err` | **`none`** | ✓ |
| `base-skeleton` | `base-skeleton` | **`none`** | ✓ |
| `base-spin` | `base-spin` | **`none`** | ✓ |
| `base-reveal` | `base-reveal` | **`none`** | ✓ |
| `base-pressable` | `background-color, transform` | **`none`** | ✓ |

**而状态一个都没丢**(同一次实测,量的是真实渲染出来的元素):

| | reduce 之下 |
|---|---|
| 骨架 | 仍在场、仍是 `rgb(229, 238, 244)`(`--brand-muted`),只是不再脉冲(opacity 固定 0.75) |
| 校验失败 | 仍是 `aria-invalid="true"`,边框仍是 `rgb(183, 91, 83)`(`--brand-destructive-fill`) |
| 按钮 pending | 仍 `disabled` + `aria-busy`,只是圈不转 |
| `CountUp` | 直接落在终值(JS 里读 `matchMedia`,不走 rAF) |

### 第二次拒绝 motion.dev —— 理由和 LOGIN-1 那次**不同**

LOGIN-1 拒它是因为 **CSS 25 行就够了**。这一次的理由更硬,两条:

1. 上面四种状态里**三种是 CSS 原生的**;第四种需要一个逐帧循环,
   而那是 **15 行 `requestAnimationFrame`,不是一个 40KB 的依赖**。
   **那 15 行就是 motion.dev 要顶替的全部东西。**
2. **更要紧的一条:** motion.dev 这一类库要么带全局样式、要么把组件包成它自己的元素,
   而 **BRAND-1 拒 tw-animate-css 正是因为它要一次全局 import** ——
   BRAND-1 的 187 页安全论证**整个建立在"不往全局加任何东西"上**。
   引它就等于把那条论证花掉,换四个 CSS 做得到的效果。

> 本刀自己加的那一个全局文件(`app/base-motion.css`)照 BRAND-1 的先例办:
> 新文件、只加不改、类名一律 `base-` 前缀,而 `base-` **作为类名**在本刀之前
> 全仓库 0 次出现(量过;`messages/en.ts` 里那 5 处 `base-currency` 是英文正文,不是类名)。
> 于是它们在声明级证明里全部落在「新增」那一栏。

---

## 三 · 拒绝态:一种画法替掉十几种

**词没有漂,漂的是颜色。** 精确量:`t('common.restricted')` **41 处、横跨 32 个文件**,
而直接包着它的元素上有 **12 种不同的 className 写法**(另有 14 处不直接落在标签上 ——
`title={…}` 或三元里的取值)。写法举例:`text-gray-400 italic` / `text-sm text-gray-600`(11 处)/ `text-gray-500` /
`font-mono text-xs` / 甚至 `text-3xl font-bold text-gray-300`。
于是**同一句话在不同页面上分量完全不同**:有的像一句正式的拒绝,有的像一行注释,
有的像忘了填。

`<Refusal>` 只做一件事:把那十几种收敛成一种(Tim 在 sampler 上指的变体 C,填充小片)。

**四个字眼共用一种颜色。** 各配一个颜色听着更"有信息",但那正是**十种画法当初长出来的方式**
—— 每一页各自决定这一句该有多重。**区分由词承担**(词没有漂),颜色只承担「这是一句拒绝」。
将来若真要分色,那要是一次集中的、看得见的决定,不是十页各挑一个灰。

### ★ 「替掉十种」在本刀里【只做到一半】—— 而这是 R5 与验收条件的直接冲突

验收条件写的是「**一种拒绝画法,替掉十种**」;R5 写的是「**本刀不转换任何页面**」。
这两条在这一项上**直接撞车**,因为那十种画法**不在页面里,在共享组件里**:

| 组件 | 今天的画法 | 被多少文件用 |
|---|---|---|
| `app/components/MaskedValue.tsx` | `text-gray-400 italic` | 8 |
| `app/components/ActorName.tsx` | `text-gray-500` | 7 |
| | | **合计 14 个文件** |

也就是说,**只要把这两个共享组件改成 `<Refusal>`,14 个已上线文件的样子当场就变了。**

**处置:R5 赢,并且照 BRAND-1 的先例把这个取舍写下来** ——
BRAND-1 遇到 R2(产品字体是 Google Sans)与 R3(不许改已上线页面)相撞时,
判的是 R3 赢:字体在取样页里加载、演示,全站切换留给一次单独的、看得见的决定。
这里逐字同一条:

* **本刀交付的是那一种画法本身** —— `<Refusal>` 建好、量过对比度、在取样页上
  与"今天的五种真实写法"并排画出来;
* **把 `MaskedValue` / `ActorName` 换过去,是转换刀的第一件事**,
  而且它是**性价比最高的一件**:改 2 个文件,14 个文件的拒绝态一次收敛。
* 这句话必须写下来,否则下一个人会以为收敛已经发生了 —— **它没有。**
  今天线上仍然是十几种写法。

### ★ 一处对变体 C 的修正(量出来的)

| | |
|---|---|
| 片内文字 `--brand-text` on `--brand-accent` | **12.59:1** ✓ AA |
| ★ 填充色 `#E1F5FF` **对着页底** `#F1F9FE` | **1.05:1** |
| ★ 填充色对着白面 `#FFFFFF` | **1.12:1** |

也就是说:**IA-BUILD-1 换上淡底之后,那个「填充小片」在真实页面上几乎看不出是一个片。**
(变体 C 是在 BRAND-1 那天定的,那时页底还是纯白 —— 这不是当时判错,是底色后来变了。)

这**不算 WCAG 失败**:1.4.11 管的是 UI 控件,而它是一段静态文字,文字本身 12.59:1。
但它让这个变体**做不到它被选中时要做的那件事**。

处置:**补一条 1px 边** `--brand-muted-text` `#62738C` ——
对页底 **4.53:1** · 对白面 **4.83:1** · 对片内填充 **4.30:1**,三处都远在 3:1 之上。
**颜色一个字节没改**,只是给它一个看得见的边界。

---

## 四 · 卡片与面板要不要磨砂 —— **建议不要**

R2 把这一条留给了判断(「淡底落地、看得出效果之后再说」)。判断如下:

**一张卡片不是浮动层。** 顶栏与 dock 是 sticky/fixed 的,页面内容**真的从它们底下滚过去**
—— 那正是磨砂要透出来的东西,IA-BUILD-1 也正是对着"合成色"量的那 0.90。
而一张卡片底下**什么都不经过**:它压着的就是那层淡底,一个不动的纯色。
于是磨砂**没有东西可以透出来**,却要为此降低卡片里每一个数字的对比度 ——
**付出了代价,买不到那个效果。**

> 但这是一个**看着才能定**的事,所以取样页 §④ 把两个都画出来了(实心白面 vs 与顶栏同一档的 0.90 磨砂)。
> **Tim 指哪个算哪个**;上面这段是默认值,不是结论。

---

## 五 · 刻意留给转换刀的东西

| 留下的 | 为什么现在不做 |
|---|---|
| **187 个页面本身** | R5。逐模块转换是后面的刀 |
| **`MaskedValue` / `ActorName` 换成 `<Refusal>`** | 改 2 个文件会当场改变 **14 个已上线文件**的样子 —— R5 不允许。**但它是转换刀最该先做的一件**(见 §3) |
| **每张表的 priority 列** | 那是**每张表自己的判断**,不是一次能批量决定的事。组件会在第一次渲染时按名拒绝没声明的表 |
| **那 8 页的排序换不换皮** | 它们今天是对的(数据库里对全体排)。换成 `mode: 'server'` 只换外观,**别顺手换成客户端排序** —— 那是降级 |
| **列显隐要不要在某页默认开** | R4:加能力可以,**悄悄改变某一页默认显示什么不行**。哪一页开、开哪几列,是那一页的决定 |
| **输入框推移默认关着** | `/login` 两个输入框都写着 `aria-invalid`。默认打开就等于**在一个已上线页面上改了行为** —— 见下 |
| **全站字体仍是 Arial** | BRAND-1 的结论没变:换字体值得是一次单独的、看得见的决定 |
| **`app/brand-sampler/` 整个目录** | 用完即删。删的时候连 `scripts/smoke-routes.mjs` 里那几行排除、以及 `check-base-isolation.mjs` 的 `ALLOWED_PREFIXES` 一起删 |

### 输入框的推移为什么默认关着

这一条值得单独写,因为它是**第 〇 节那个量错**直接带出来的:

`/login` 是已上线的一页,而它两个输入框都写着 `aria-invalid={fieldsInvalid || undefined}`。
如果校验失败的推移**默认打开**,本刀就在一个已上线页面上改了行为 ——
登录失败时输入框会开始抖。**R5 说 187 页一个像素都不许变,这一条正好会撞上去。**

所以它是显式开关 `nudgeOnInvalid`,默认 false ——
**与 R4 对排序/分页那四个开关的处置逐字同一条:能力建好,但不自动生效。**

---

## 六 · 怎么证的(R5)

三道,**互相补位**,缺一条都证不完整:

1. **声明级(postcss,`scripts/check-css-declarations.mjs`)** ——
   把两棵树的构建产物 CSS 按「上下文 + 选择器 + 属性」建索引,逐条比:
   > **既有 1,938 条声明:0 改值、0 消失、+58 条新增。**
   * 对照树是 `git worktree` 检出的 HEAD `919175e`,**真跑了一次构建**,不是拿旧产物。
     怎么重跑(下一刀照做):
     ```bash
     git worktree add --detach ../.before <对照 commit>
     cp -al node_modules ../.before/node_modules   # 硬链接,秒级、不占空间
     cp .env.local ../.before/.env.local
     (cd ../.before && npx next build)
     npm run build
     node scripts/check-css-declarations.mjs ../.before .
     git worktree remove --force ../.before
     ```
     ⚠ **不要用软链接接 node_modules** —— Turbopack 会拒:
     `Symlink [project]/node_modules is invalid, it points out of the filesystem root`。
     也**不要**把对照树放在项目目录之外再软链回来,同一个原因。
   * **故障注入验过**:把 `--brand-muted` 从 `#E5EEF4` 改成 `#E5EEF3`(一个十六进制位),
     检查退出码 1 并点名 `:root ⟩ --brand-muted #e5eef4 → #e5eef3`;撤回后回 0。
   * ★ **第一版这条检查是【吵的】**:它把所有 `@font-face` 的声明摊进同一个桶
     (上下文字符串都是 `@font-face`),于是**两次构建里字体块的先后顺序不同**
     被报成 4 条"改了值",而字体一个字节都没变。
     这正是 BRAND-1 记过的教训 —— **一个吵闹的检查和一个瞎掉的检查一样坏**。
     修法:每个 `@font-face` 用自己的 family/weight/style/unicode-range 做身份。
2. **隔离(`scripts/check-base-isolation.mjs`)** —— 声明级证明有一个它证不到的洞:
   一个新组件如果被某一页 import 了,那一页**真的变了**,而声明级看到的只是"多了些新声明",
   完全合法。所以补这一道:
   > **781 个文件里,组件目录与取样页之外 0 处 import、0 处 `base-*` 类名。**
   * `/login` 的 4 处**按行登记成基线**(不是把那四个组件从守卫里删掉 ——
     删掉的话以后任何一页偷偷用上 button 都不会再被抓到);
   * 基线**比实际宽也会红**:登记了 4 处却只找到 3 处,同样退出码 1;
   * **故障注入验过**:往 `app/inbound/page.tsx` 塞一条 import 和一个 `base-skeleton`,
     两类都被点名、退出码 1;撤回后回 0。
3. **真浏览器实测** —— 见 §1 的 390px 那张表。**判据是量出来的数,不是一张看起来还行的截图。**

> **两条合起来才是完整的证明:** 既有声明一条没动 ⇒ 老类名的样子没变;
> 新增的只能通过新类名生效 ⇒ 而没有任何既有页面用到那些新类名。

---

## 七 · 一件【顺带修掉的、不是本刀造成的】漂移

`db/gate.py` 第一次跑是**红的**:`lib/database.types.ts` 与线上 schema 不一致(20 行)。

**它不是 BASE-1 造成的。** 本刀没有 DDL,唯一动过库的是幽灵授权那次 `DELETE`(8 行数据),
而删数据不可能改变生成类型。`lib/database.types.ts` 在本刀里也一直是未改动状态。

**它是什么:** 把类型重新生成一遍再 diff,**全部 40 行差异都是同一件事** ——
五个泛型辅助类型(`Tables` / `TablesInsert` / `TablesUpdate` / `Enums` / `CompositeTypes`)
多了一对括号:

```diff
-  TableName extends DefaultSchemaTableNameOrOptions extends {
+  TableName extends (DefaultSchemaTableNameOrOptions extends {
-    : never = never,
+    : never) = never,
```

**表、列、函数一处都没差。** 也就是说线上 schema 与仓库里的类型**内容完全一致**,
变的只是 supabase CLI 生成器的**写法**(CLI 版本差异)。

**为什么它能积到今天没人发现:** BRAND-1 / LOGIN-1 / PDF-1 / IA-BUILD-1 都是没有 DDL 的前端刀,
按仓库的规矩**不跑 gate**(BRAND-1 原文:「没有 DDL:不适用备份、迁移、窗口与 db/gate.py」)。
于是 CLI 升级带来的这点漂移可以一直躺着 —— **不跑的门不会红**。

**处置:** 按 gate 自己给的指示重新生成并提交。语义上是个空操作,但把门恢复成绿的,
下一次真的 schema 漂移才看得见。

---

## 八 · ★ 组件库这条规矩(PRE-ACCOUNT-1,2026-09-04)—— **一半有闸,一半没有,说清楚**

Tim 在账号发放前定下的规矩,两句:

> **(a)** 新页面用组件库 —— `ListPage` · `RecordHeader` · `DataTable` ·
> `EditableTable` · `RefusalBlock`/`RefusalPage` · `padding` 槽,按模板定义的用法;
> **(b)** 库里缺一项这一页需要的能力时,**先把能力加进库,再用它** ——
> 不是内联"就这一次"。

### 先量了树,再设计闸(而这一步改掉了本来要建的东西)

| 量的是什么 | 数 |
|---|---:|
| `app/**/page.tsx` | **198** |
| 其中 import `ListPage` | 100 |
| 渲染 `DataTable` 的页 | 86 |
| 用 `RecordHeader` 的文件 | 19 |
| **`app/` 下手搓 `<table>` 的文件 / 处**(不含 `app/components/ui/`) | **66 / 76** |

> ☞ **顺带更正一个数:**裸 grep 数到 70 个文件 / 81 处,`survey-pages.mjs` 报 77 处。
> **差额全部是【注释里的 `<table>`】** —— 五处,逐条看过,全是在说
> 「这里从前是一张手写表,已经换成 DataTable 了」的说明文字
> (`app/inbound/page.tsx:330` · `app/operation/orders/[id]/page.tsx:226` ·
> `app/operation/processing/[id]/LossPanel.tsx:129` ·
> `app/sales/commissions/CommissionsTable.tsx:15` · `app/hr/reviews/cycles/page.tsx:12`)。
> **CONV-8 的手机闸被它自己的注释骗过去过**,所以本闸先剥注释再匹配。

**66 个文件在册 ⇒ 一道"不许手搓表格"的闸今天会对着 66 个文件变红。**
委托自己的判据:「一份一开始就很长的例外清单,是这条规矩不对的证据」。
CONV-10 的判据:「一道会对着正确代码变红的闸,两刀之内就会被加白名单绕过去,
那比没有这道闸更坏」。**两条都指向"今天建不了闸"。**

**但"只印不拦"也不够** —— 这条规矩存在的理由是**下一刀要新建一批页面**
(建账号 / 建角色 / 录 KPI)。一份没人看的普查拦不住新债。

### 所以走第三条路:**棘轮**(`scripts/check-component-library.mjs`)

本仓库已经为这条办法付过四次账:`masked-reads-baseline.json` ·
`auth-error-baseline.json` · `currency-messages-baseline.json` ·
`check-base-isolation.mjs` 的 `KNOWN_CONVERSIONS`(它把办法写成了一句话:
**「多出来的每一处仍然会红」**)。

* 冻结今天的 **66 个文件 / 76 处**(`scripts/component-library-baseline.json`);
* **多一处 → 红,点名 `file:line`**;少一处 → 只提示,请顺手收紧;
* **这份基线只会缩短。** 它不是白名单 —— 方向相反:白名单随新债增长,基线随还债缩短。
  这句话写在**基线文件自己的 `__NOTE__` 里**,任何一刀想把它改大都留在 diff 里;
* 已进 `npm run build`(在 `check-base-isolation` 之后),另有 `npm run check:complib`。

**故障注入(两个方向都验过,而两个方向缺一不可):**

```
A 干净树                                        → EXIT 0(66 个文件在册,扫到 76 处)
B 新建一张【手搓表格】的页面(cut C 的形状)
    app/settings/accounts/new/page.tsx          → EXIT 1
      ✗ 新增 1 处 …  基线 0 → 现在 1
        app/settings/accounts/new/page.tsx:7    ← 点名到行
C 【同一张页面】改写成用 <DataTable>            → EXIT 0
    并且 check-datatable-phone 同时 EXIT 0      ← 正确的新页面【不会被罚】
D 删掉注入                                      → EXIT 0,基线 shasum 全程未变
                                                   (16717c75…)
```

> ★ **C 那一格是本闸能不能活下去的关键。** 一道只证明"坏代码会红"的闸,
> 证不了它不会对着**好代码**红 —— 而那正是白名单的由来。

### ★★ (b) 那半条:**UNENFORCED —— 本仓库【没有】机器在检查它** ★★

**它没有机械特征。** 一段本该抽成组件的内联代码,和一段本就该内联的代码,
**长得一模一样**。造一个假装在查它的检查,只会走上 `check-masked-reads` 抬头
记下的那条路:「一个夸大自己的检查会被人忽略,而一个被忽略的检查比没有更坏 ——
因为它让人以为那块地面有人看过」。

**所以 (b) 是一条【评审规矩】,写在这里,由人执行:**

> 库里缺这一页需要的能力时,**先把能力加进 `app/components/ui/`,再用它。**
> 不要内联"就这一次" —— `padding` 槽(CONV-9)与 `RecordHeader`(CONV-8)
> 都是按这条路加进来的,那是它成立过的证据。

### ⚠ 给下一刀(cut C)的一处【会撞车】的提醒

**Tim 的 (a) 与 BASE-1 的隔离闸,在 cut C 要建的那批页面上是【对着干的】。**

`scripts/check-base-isolation.mjs` 守着"这批基础组件**还没有人用**":
`button` · `card` · `input` · `label` · `select` · `textarea` · `alert` · `badge` ·
`table` · `feedback` · `add-row-panel` —— **11 个,今天一个都不许在 `/login` 之外被 import。**
而 cut C 要建的是**表单页**(建账号 / 建角色 / 录 KPI),它们最需要的正是
`input` / `label` / `select` / `button`。

**照着建下去,`npm run build` 会红。** 而正确的处置**不是**加白名单:

> 照 CONV-0(`refusal` 毕业)与 CONV-1(`data-table` 毕业)的先例走 ——
> **把你真正采用的那个组件从 `GUARDED` 里【拿掉一条】,并在这里写下是哪一刀、
> 采用在哪几页。** 那张清单本来就设计成"随转换推进自然缩短",不是一张过期白名单。
> 写下这一条,是因为一个不知情的人撞上红灯时,**最省事的动作是加白名单** ——
> 而那会把这道闸对剩下 10 个组件的守护一起废掉。

---

## 九 · ★ C-1(2026-09-04):`input` · `label` · `select` 毕业了 —— 而这道闸对它们【已经花掉了】

§八 末尾那条「给下一刀的提醒」兑现了:C-1 要把 `/settings/accounts` 的邀请面板
重写成「建账号 + 当场设初始密码」(没有邮件服务,Tim 当面发密码),
那是一张真正的表单页,`input` / `label` / `select` 正是它需要的东西。

**处置照 CONV-0(`refusal`)与 CONV-1(`data-table`)的先例:从 `GUARDED` 里拿掉三条,
不加白名单。**

### 拿掉三条【不够】—— 还要删 `KNOWN_CONVERSIONS` 里两行(实测)

```
只改 GUARDED、不动 KNOWN_CONVERSIONS  → EXIT 1
    ✗ 基线对不上:登记了 4 处,只找到 2 处。
```

三个组件离开 `GUARDED` 之后,`importRe` 再也匹配不到它们,`known` 从 4 掉到 2,
而 `KNOWN_CONVERSIONS.size` 仍是 4 —— 那道「基线比实际宽」的自检当场变红。
所以 `app/login/page.tsx → input` 与 `→ label` 两行必须一起删。
**委托书没有提到这一步**;是跑出来的。

### ★★ 说清楚:这道闸对这三个【没有后继】★★

| 毕业的组件 | 接替它的是什么 |
|---|---|
| `refusal`(CONV-0) | 拒绝态只剩一份实现 —— 要漂就得改那一个文件 |
| `data-table`(CONV-1) | 必填 `phone` prop · `check-datatable-phone.mjs` · 渲染期 throw |
| **`input` / `label` / `select`(C-1)** | **没有。本仓库【没有任何机器】在检查谁 import 了它们。** |

这不是疏忽,是这道闸的性质:它守的是「还没有人用」,
而一个已经被采用的组件不可能再满足那条断言。写下来,是因为
**一道被高估的闸比没有闸更坏**(`check-masked-reads` 抬头记着这句话)。

### 故障注入(两个方向,缺一不可)

```
A 把【仍在 GUARDED 里】的 card 注入 app/welcome/page.tsx   → EXIT 1
    import  app/welcome/page.tsx:1  → @/app/components/ui/card    ← 点名到行
B 把【已毕业】的 input 注入同一个位置                       → EXIT 0  ← 好代码不被罚
   撤掉注入:app/welcome/page.tsx 与 HEAD 逐字节相同(git diff 0 字节)
   脚本自身 shasum:45ee2cc8…(改动前) → 8d43f3ac…(改动后)
```

### ★ 这道闸当场抓到了 C-1 自己的一次越界,而处置是【退回来】

写 `CreateAccountPanel` 时用了 `Card` / `CardContent` / `Button`,
**而 Tim 的裁定只毕业了 input / label / select 三个。**
闸 EXIT 1,点名 `app/settings/accounts/CreateAccountPanel.tsx:31 → button`。

> **处置:把按钮与卡片退回原生标记,不顺手把 button/card 一起毕业掉。**
> **一道闸自己抓到的越界,如果由撞上它的人当场放宽,那道闸就不存在了。**

☞ **留给下一刀:一张真正的表单页需要 `button`,几乎肯定还需要 `card`。**
下一次建表单页会立刻再撞一次 —— 那时该由 Tim 裁定是否一并毕业,
而不是由撞上的人自己决定。

### 规矩 (b) 在本刀的执行情况

**没有触发。** 这一页需要的三种控件库里都有,一处都没有内联"就这一次"。
按钮与卡片走原生是【闸的裁定】,不是"库里缺能力" —— 两者不要混为一谈。

---

## 附:文件清单

| 文件 | 是什么 |
|---|---|
| `app/base-motion.css` | 四条状态动效 + 减弱动效。**只加不改,`base-` 前缀** |
| `app/components/ui/data-table.tsx` | 表格。手机形态 · 两种排序模式 · 四个默认关着的能力 |
| `app/components/ui/refusal.tsx` | 拒绝态的唯一画法 |
| `app/components/ui/feedback.tsx` | `CountUp`(唯一的 JS 动效)· `Skeleton` · `useFlashOnChange` |
| `app/components/ui/button.tsx` | 加了 `pending`(转圈 + 禁用 + `aria-busy`) |
| `app/components/ui/input.tsx` | 加了 `nudgeOnInvalid`(**默认关**) |
| `app/brand-sampler/Base1.tsx` | 取样页第二节:13 列的表 · 拒绝态前后对照 · 四种动效 · 磨砂 A/B |
| `scripts/check-base-isolation.mjs` | R5 的机器守卫 |
| `scripts/check-css-declarations.mjs` | 声明级视觉证明 |

---

## 十 · ★ BTN-1(2026-09-06):档位定下来了 —— 而【一个按钮不够】★

Tim 的裁定,一句话:**「一个看起来像普通动作的破坏性动作,是缺陷,不是不一致。」**
所以这一刀先定档位,再转换 —— 反过来做会把一个真实的区别抹平。

### 10.1 五个档位,以及【不靠颜色】怎么分开

| 档 | 什么时候用 | 底 | 字 | 边 | 字重 | 左竖条 |
|---|---|---|---|---|---:|---|
| `default` | 这一页存在的那个动作 | ocean 实底 | 白 | 无 | 500 | 无 |
| `secondary` | 列表控件 / 行内控件 / 取消 | 透明 | text | border-strong | **400** | 无 |
| `reversal` | 撤销一个**已过账**的状态 | 透明 | text | border-strong | 500 | **虚线 3px** |
| `destructive` | 真的销毁或作废一条记录 | destructive 淡底 | destructive-text | destructive/40 | 500 | **实线 3px** |
| `warning` | 系统在告诉你一件正在发生的事 | warning 实底 | 白 | 无 | 600 | 无 |

**★ 颜色是最后一条判据,不是第一条。** 一个只能靠颜色分辨的破坏性动作,
把最危险的那一格押在**一部分人根本用不了的信道**上。四条不经过颜色的判据:

1. **有没有实底** —— 只有 `default` 与 `warning` 是实心的。一个区域只该有一个实心钮。
2. **字重** —— `secondary` 是唯一的 400。它是唯一"更轻"的档。
3. ★ **左竖条** —— `destructive` 实线、`reversal` 虚线,**同一处几何,一实一虚**。
   别的档没有。这一条是纯几何,完全不读颜色。
4. **动词形状** —— `destructive` 是 Delete/Void;`reversal` 是 Un-/Re-。
   ★★ **而【动词形状是最不可靠的一条】,BTN-3 与 BTN-3b 各抓到过它说谎:**
   `fx/DeleteButton` 写着 Delete 做的是软删除;`RoleForm:166` 同样;
   `DecideControls:89` 写着 Cancel 做的是补一条等额反向记账。
   **判据永远是 handler 做了什么,不是脸上那个动词。**

> ### ★★ 作废 vs 撤销:这条界线 BTN-3b 之前【只是默契】,现在写下来 ★★
>
> 上面 destructive 那一格写着「真的销毁**或作废**一条记录」,而 `作废` 这两个字
> 一直没有被压力测试过。BTN-3b 撞上 `void_review`:它**什么都不删**、理由必填、
> 还专门返回 `employment_facts_unchanged: true` —— 照字面读它像撤销。
> Tim 在 BTN-3b 裁定,跟随 `VoidInvoiceControl` 的先例,并要求把界线写在这里:
>
> **作废杀死【单据】;撤销是在单据继续活着的前提下,回退一个【已过账的状态】。**
>
> 照这条读:`void_review` 是**作废**(`void` 是终态,那份评估作为单据到此为止)→
> `destructive`;`cancel_leave_request` 是**撤销**(补一条等额 release,
> 请假单的账继续成立)→ `reversal`。
> **两者都不删任何行 —— 所以"删不删行"分不开这两个档,不要用它当判据。**

**探针实测(`scripts/probe-button-tiers.mjs`,18 页 / 31 处库按钮):**
同一 `(variant,size)` 跨页几何**全等** —— `default/default` 13 处 / 11 页
`h=32 r=8px pl=10px pr=10px w=500`;`secondary/sm` 8 处 / 8 页 `h=28 … w=400`。

### 10.2 ★ 撤销档为什么是一个真的档,不是 destructive 的一种写法 ★

实测 **57 个按钮**做的是"撤销一个已经过账的状态":`Reopen` · `Unpost` ·
`Unapply` · `Unreconcile` · `Unmatch` · `Turn GST off`。**它们不删任何东西,
审计痕迹全留着。**

> 把它们画成 destructive 红,等于教会操作员**「红 = 我会丢数据」**——
> 而那句话在他第一次点 `Reopen` 时就是**假的**。
> **一条被教错的规则,比没有规则更坏。**

它们也不是 `default`:**没有任何一页是为了 Reopen 而存在的。**

### 10.3 ★ 两个量出来的【库自身缺陷】—— 先修库,再转换 ★

这一节是 §八(b)「库里缺能力时先把能力加进库」那半条**实际运转了一次**的记录。
两处都是**先修库、后转换**,不是转换途中顺手改的。

**① 禁用态:`disabled:opacity-50` 实测 1.45:1 —— 比它要取代的写法【更差】**

```
库原来的  disabled:opacity-50  →  底 #80bfd6 字 #c0dfeb  =  1.45 : 1
手写的    disabled:bg-gray-400 →  白字 on #9CA3AF        =  2.54 : 1
现在      disabled-bg #DDE7EF + brand-text #182B4B       = 11.27 : 1  ✓
```

**照原样转换会让禁用态比现在更看不清** —— 而 `docs/silent-disable-inventory.md`
里还有 **9 处**只灰不说的提交钮(不是委托书写的"十二处";那份清查自己写着
「未修,按伤害排序(9 处)」,共查 14 处、已修 5 处)。
**判据是【清楚地不能按】,不是【淡】**:淡 = 可能还没加载完;平 + 灰底 = 明确关着。

**② `destructive` 变体的字色低于 AA**

`--brand-destructive-fill` 那个 4.53:1 是**对着白底**量的,而这个变体把字画在
一层淡底上。实测 `#B75B53` on `#F9F2F1` = **4.01:1,不合格**。
已另出 `--brand-destructive-text` `#AA4F48`(同一淡底 4.85:1 ✓)。

> ☞ **一个 token 标着"合规",不等于它在【这个用法】里合规。底换了,数就变了。**

### 10.4 ★ 组件库棘轮现在有【两个维度】★

**委托书的验收条款本来是量不出来的:**它要求「组件库棘轮的基线必须缩短」,
而 `check-component-library.mjs` 当时只匹配 `<table`,**一个按钮都看不见**。
Tim 的裁定是**把这一维补上**,不是把条款划掉。

| 维度 | BTN-1 之前 | BTN-1 之后 |
|---|---|---|
| `table` | 66 文件 / 76 处 | **66 / 76(原样,没有放宽)** |
| `button` | 205 文件 / 389 处 | **138 / 265**(−67 文件 / −124 处) |

**新债到达的速度是量出来的:** C-1b(`8afa0b7`,09-04)量到 383/200;
BTN-1 开工前(`6302ae2`,09-06)重量 **391/205**。**两天 +8 处。**
一次性转换而不留闸,三个月后就回到原处 —— 这一维就是为了那件事。

**★ 一个量出来的陷阱:旧写法只看得见 40% 的按钮。**
`/<button[\s>]/` 要求标签名后**同一行**还有一个空白字符,而多行标签
(`<button` 独占一行)匹配不上。实测 266 处里 **158 处是多行的**。
后果不是"数字偏小",是**这道闸对多行写法完全失效**。改成 `(?=[\s>]|$)`。
对 `table` 维度**无影响**(实测新旧同为 66/76 —— 本仓库的 `<table>` 全部同行书写)。

**故障注入(四格,全部实测):**

```
A 干净树                                        → EXIT 0
B 注入一个【多行】手写 <button>                  → EXIT 1,点名 app/fi-probe/page.tsx:4
C 同一个按钮改用 <Button>                        → EXIT 0  ← 正确代码【不会被罚】
  并且 check-base-isolation 同时                → EXIT 0
D 删掉注入                                      → EXIT 0,基线 shasum 全程未变(1a51ecdcbecd)
```

> ★ **C 那一格仍然是关键**(§八 的同一条):一道只证明"坏代码会红"的闸,
> 证不了它不会对着**好代码**红 —— 而那正是白名单的由来。

### 10.5 这道闸【看不见】什么 —— 说清楚,别高估它

> **它数的是"有没有用库",不是"档位对不对"。**
> 一个本该是 `destructive` 的按钮写成了 `default`,棘轮**一声不吭**。
> 档位由人评审,判据在 §10.1 与 `button.tsx` 抬头。

`scripts/probe-button-tiers.mjs` 补上一部分(几何一致 / 无颜色区分 / 禁用态可读),
但它只走**取样的 18 页**,而且**看不见只在某个状态下才出现的按钮**
(`/settings/accounts` 的建号面板要先点开)。那些在
`docs/manual-walk-list.md` §15,由人走。

**★ 探针自己也差点报了假绿。** L3(禁用态对比度)第一版读的是
`getComputedStyle().color`,而它报的是**没有褪色的**值 —— 把
`disabled:opacity-50` 注回去,这条断言照样绿。**是故障注入发现的,不是推的。**
现在把 element opacity 逐层乘进去再算。委托书两次点名的正是这个坑
(UI-1d fu2、FIX-2b 的头一版角色探针),这是第三次。

---

## 十一 · ★ CONFIRM-1(2026-09-06):确认对话框 —— `app/components/ui/confirm-dialog.tsx`

40 处原生 `window.confirm` 换成一个对话框。**委托书说的是 56 处,那个数把
【注释】也数了进去、同时漏了一处没有 `window.` 前缀的 `confirm(`** ——
两个方向上都错(记在 AGENTS.md「一句注释可以污染将来对它自己的计数」)。

### 它存在的理由,一行就说得完

> **`/finance/bank/statements/[id]` 这一页,【对账】的时候会说出报表代号
> (`bank.reconcileConfirm` 用着 `{code}`),而【删除】的时候一个字都不说。**

同一页、同一个读者、同一份单据 —— 一个动作点名,另一个不点名。
**那不是谁偷懒,那是 40 个原生对话框买来的东西:** 一个够不着样式、
说不出主语、而且【机器点不到】的确认步骤,没有任何压力让它把话说清楚。

### ★ 更正一句本刀自己说过的话:树里【原本有三处】,不是两处

CONFIRM-1 的 Step 2 报告写的是「没有现成的确认组件,只有两处先例
(`TaskModal` 与 `IdleWatcher`)」。**那句话是错的。**第三处是
`app/components/ReasonPrompt.tsx`(AUDEL-2,**八个调用点**)——
一个完整的确认界面:标题 + 后果 + 必填理由 + 确认/取消。

它没被找到的原因**不是它藏得深**,而是那次搜索按的是【名字】
(`modal|dialog|overlay` 与 `role="dialog"`),而它两样都不叫。
法则与补救记在 AGENTS.md「一次勘察只看得见代码碰巧给它起的名字」。

**这一条写在这里,是因为 FIX-2a 记过同样的三跳:一句话被写进报告、
报告被写进下一份委托书、委托书被当成事实读。** 在它被记下来的地方更正它,
比在下一刀的报告里再说一次有用。

### API:`subject` 是必填的,而它有闸

`<ConfirmButton>`(确认钮**直接**调 `onConfirm`,不跨 await)与 `useConfirm()`
(Promise,给塞不进按钮的调用点),共用同一个 `ConfirmDialog`。

> ★【更正「34 处 + 6 处」这个拆分 —— 落地之后不是这样】★
> 那是设计时的估算。**实测落地:`<ConfirmButton>` 43 处 JSX 开标签,
> `useConfirm()` 【0 个调用点】。** 原先划给 Promise 那一支的 6 处
> (表单 onSubmit、带条件的行内判断、map 里的行内 onClick)最后都用组件写成了 ——
> 条件那几处的办法是**在条件的两侧各渲染一个控件**(`DictSection` 的
> 停用/重新启用、`StatusPanel` 的破坏档/普通档),而不是在一个 onClick 里分支。
> **`useConfirm()` 因此是【今天没有消费者的导出】。** 记在这里而不是删掉它:
> 它是本刀刻意留的一个形状,而一个没有消费者的导出**必须被说出来**,
> 否则下一个人会以为它在用,或者以为它是死代码而顺手删掉 —— 两种误读都要避免。
`subject: string` 是必填 prop,另有 `scripts/check-confirm-subject.mjs` 拦住
「填了等于没填」。驳回是默认:Escape 关、点外面关、初始焦点在【取消】上、
焦点关在里面、关掉还给触发钮。

### ★★【主语【无条件】渲染,所以它不许带这一页遮着的东西】★★

**这是 Tim 在 CONFIRM-1 定下的一条规矩,而它是本刀的目的与权限模型
【正面相撞】的唯一一处:**

> 本刀要「说清楚你在确认什么」;权限模型要「不许说这个读者看不到的东西」。
> **`subject` 走的不是 `MaskedValue`,它照着字面画。**
> 所以:**把主语里的每一样东西,对照【同一页对同一个读者遮住了什么】过一遍。**

**实测抓到过一次(`app/operation/processing/[id]/CostPanel.tsx`)**:
两条同类型的成本条目,最自然的区分办法是把【金额】拼进主语 ——
而那一列走的正是 `MaskedValue` + `canViewPrices`。照着做,
**一个没有看价权限的人会在对话框里读到一个他在表里读不到的数。**
现在主语是「成本种类 · 备注」,金额刻意不在里面,理由写在那一行旁边。

**★ 判据是【逐个消费者】,不是【逐个组件】。** 一个组件可以被好几页用:
`app/components/finance/FinanceAttachmentsPanel.tsx` 同时服务
`/finance/payments/[id]` · `/finance/payables/[batchId]` ·
`/finance/receivables/[saleId]` · `/finance/expenses/[id]` ——
**在其中一页安全的主语,在另一页未必安全。** 要问的是
「这个字段,在【每一个】用到这个组件的页面上,对【每一个】读得到那一页的角色,
都是可见的吗」。文件名是(它就印在表里);金额不是。

### ★ `scripts/check-confirm-subject.mjs` —— 四条判据,四次故障注入(基线 0)

**只有类型是守不住 `subject` 的。** 编译器拦得住「不写」,拦不住「写了等于没写」,
而那三种写法正是本刀开工前那 40 处的实际长相:

| 判据 | 抓的是 | 注入 | 实测 |
|---|---|---|---|
| `EMPTY` | 空串 / 只有空白 | `subject={''}` | ✗ 点名 `DeleteButton.tsx:22`,EXIT 1 |
| `SENTENCE` | 主语其实是一整句旧消息 | `subject={t('suppliers.deleteConfirmTitle')}` | ✗ 印出那句英文 `"Delete this supplier?"`,EXIT 1 |
| `NOT_FROM_SCOPE` | 主语不引用作用域里的任何东西 | `subject="Supplier"` | ✗ 点名,EXIT 1 |
| `COVERAGE` | **解析器自己漏抓** | 把 `subject=` 那条正则改成 `subjectXX=` | ✗ 38 个文件全部点名,EXIT 1 |

**判据 ② 必须下到【英文原文】,不能只看代码。** `subject={t('materials.deleteConfirm')}`
在代码里长得完全正常 —— 一个 t() 调用、一个存在的键;坏的地方在**它指向的那句话**
(`'Delete this attachment?'`)。所以脚本把 `t('a.b.c')` 解析到 `messages/en.ts`,
拿真英文去问有没有 `"this "`。**一条不下到原文的检查会把这一类全部放过,
而这一类是 40 处里的 28 处。**

#### ★★ 判据 ④ 是这一刀最值得留下的那一条:**一道会漏抓而不吭声的闸,比没有闸更坏** ★★

头一版跑出来 **43 处**,而 `grep -c '<ConfirmButton'` 全库数出来 **48 处**。
那 5 处差额后来查明是**注释里提到组件名**(正是 AGENTS.md
「一句注释可以污染将来对它自己的计数」那一条,穿着新衣服又来了一次)。

**可当时没有任何东西能证明这一点。** 一个解析器悄悄跳过五个调用点,
和它抓到了五个坏调用点、而代码本来就干净 —— **两种情况的输出长得一模一样,
都是「EXIT 0,一切干净」。** 所以这个数字不能只被人核对一次,它必须是一条断言:

> **一个文件里有几个真的 JSX 开标签(跳过注释行),就得抽出几处主语。少一个,
> 本检查【自己】红。**

注入时把 `subject=` 那条正则弄瞎,输出是 `0 处 subject / 43 个 JSX 开标签` ——
**它没有报「干净」,它报了「我瞎了」。那正是这条断言存在的全部理由。**

**进了 `npm run build`**(只读仓库文件,秒级,够不着数据库 —— 不该占 gate 那 300 秒),
基线 **0**,没有例外表:本刀把 40 处全部转完,所以下一个人加一处不点名的确认当场红。

### ★ 理由型的八处:输入框搬进对话框 —— 以及【刻意不搬】的那一处

八处「撤销/重开要一句理由」的动作,原来的形状都是
**「先在页面上敲理由 → 按钮才亮 → `window.confirm` 再问一次要不要」**。
两步问的是同一件事,而那个灰盒子答不出**这是哪一个期间/哪一张单**。
`ConfirmContent.reason` 让对话框自己要那句话(空白判据 `reason.trim() === ''`
逐字抄自 `ReasonPrompt`,而它又与数据库的 `btrim` 逐字对应),于是七处收成一步:
**同一个字符串、同一个参数位、同一个动作,只是问的时机从「先填」变成「确认时填」。**

**★ 第八处刻意没有搬:`app/finance/invoices/[id]/VoidInvoiceControl.tsx`。**
带分录的发票除了理由还要一个**冲销日**(`void_invoice` 的
`REVERSAL_DATE_REQUIRED`),而对话框只放得下一个理由框。
**把两个必填项拆到两块屏幕上比两个都留在面板里更坏** —— 人会以为面板填完就齐了,
然后在对话框里撞上第二个。所以那一处只加主语,理由与冲销日原样留在面板里。
**这是一处刻意的不一致,写下来是为了让下一个人不必再推一遍。**

### ★ 那条「点两次」的自制确认退休了 —— 而委托书说有两处,实际只有一处

`app/tools/tasks/[id]/TaskHeader.tsx` 旁边写着这么一句注释:

> 「确认走的是【第二次点击】,不是 `window.confirm`:后者在测试里点不到。」

**那不是一句吐槽,那是本刀的委托书。** 原生确认框对冒烟隐形,于是这一处宁可
自己再实现一遍确认。约束没有了(`ConfirmDialog` 是真 DOM,探针点得到),
所以自制那一层跟着退休,`confirming` 这个 state 一起消失。

**委托书说的第二处(`app/settings/accounts/CreateAccountPanel.tsx`)不存在。**
那个文件里的 `confirm` 是一个**密码确认输入框**(`confirm !== password`),
与「点两次确认」毫无关系;它的提交钮是一次点击、没有任何确认步骤。
**按行为搜了全树**(`confirming` / `setConfirming` / `点两次` / `click again`),
自制两段式确认**全树只有 TaskHeader 一处**。
`suppliers/new` 与 `sales/customers/new` 的「再按一次提交」看着像,**不是** ——
那是服务端先拒、再带 `ack_near_duplicate` 重提,判据在服务端,
把它搬进客户端对话框等于在页面上再实现一遍近重复判断,正是本仓库禁的那件事。

---

## 十二 · ★ BTN-2(2026-09-06):链接态 —— 以及【本刀最有价值的产出是一次更正】★

**这一刀落地的是 26 处转换 + 2 个可访问名字。委托书写的是 108 处。**
不必美化这个差:**它在碰第一行代码之前就把最有用的活干完了 ——
两个数都是错的,其中一个错了二十倍。**

### 12.1 两个被更正的数,连同它们是怎么错的

| 委托书 | 实测 | 方法 |
|---|---:|---|
| 链接态 **65** 处 | **24 / 27 / 55** | 见 12.2 |
| 图标钮 **43** 处,其中 **41** 处无名 | **2** 处无名 | 见 12.3 |
| BTN-1 遗留 **36** 个混合文件 | **36 属实**,但已被 CONFIRM-1 化掉 4 → 开工时 **32** | `git show --stat 5c71875` 的 .tsx ∩ 剥注释后仍命中 `<button` |
| 棘轮 button 维基线 **138 / 265** | 树上实际 **231** | CONFIRM-1 化掉 34 处后**没有收紧基线** |

**测法(下面每一个数共用这一套,抄数的人请把方法一起抄走):**
遍历 `app/**/*.tsx`(不含 `app/components/ui/`),先用棘轮自己的 `stripComments`
剥注释,再逐个 `<button` 向前走 —— 跟踪引号状态与花括号深度找到开标签真正的
结尾,所以**多行标签与 `className={cond ? … : …}` 都解析得对**。
★ **同一次运行里第二条独立路径断言 N**:`split('<button')` 后逐段复验边界字符,
与正则扫描器不共用一行代码,两者不等就 `exit 3`。
**故障注入四格(实测):**

```
A 干净树                                              → 205 = 205,EXIT 0
B 把正则退回 BTN-1 踩过的那个坑 /<button /(要求同行空格) → 101 vs 205,EXIT 3
C 改瞎第二条路径(只认空格)                            → 205 vs 101,EXIT 3
D 还原                                                → 205 = 205,EXIT 0
```

> B 那一格值得单独看:**101 对 205 —— 旧写法只看得见一半**,
> 这正是 §10.4 记下的那个陷阱,换一个身份又出现了一次。

### 12.2 链接态:65 从来没有在这棵树上成立过

| 判据 | 处数 |
|---|---:|
| 委托书自己的判据(`text-blue-600` **且** `hover:underline`) | **24** |
| \+ `text-blue-600` 配裸 `underline`(1)、`text-blue-700`(2) | **27 —— 全部蓝色链接态** |
| \+ 红色链接态 19、灰色链接态 8、导航项 1 | **55 —— 全树所有带下划线的 `<button>`** |

★ **把同一个解析器跑在 `6302ae2`(BTN-1 开工前)与 `5c71875`(BTN-1 落地)上,
两次都是 24。** 65 不是"后来变小了",是**从来没有成立过**。
最接近的结构性解释:`text-blue-600` 在 `app/**/*.tsx` 里出现 **298 次**,
其中落在 `<button>` 上的只有 24 次 —— 其余在 `<Link>` / `<a>` / `<span>` 上。
**一次没有限定在 `<button>` 上的 grep,会落在那 298 里的某处。**

**导航还是动作:24 处逐个读了 handler,导航 0 处。** 全部是 state setter、
server action 或 transition。四处 `handleDownload` 是**先请服务端签 URL、
再 `window.open`** —— URL 在服务端回话之前并不存在,所以它做不成 `<Link>`。

**硬写死的品牌色:27 处全部是 Tailwind 默认值,0 处用 token。**
而这次替换**降低**了对比度,这一点要说在前面:

```
今天   text-blue-600  #2563EB on white = 5.169 : 1
之后   text-primary(--brand-ocean-fill #007FAD) = 4.527 : 1   ← 过 AA,过了 0.027
```

☞ 记在队列里的不是这一个实例,是**这个规律**:这已经是第三次
`--brand-ocean-*` 落在合规线上下一个零头之内。**一套总是差着百分之几过线的
色板,是关于色板的发现,不是关于三把刀的发现。**

### 12.3 ★ 图标钮:43/41 的真值是 2 —— 而【怎么测出来的】比这个数值钱 ★

完整技法已提成 `AGENTS.md`「问『有没有哪一处满足 P』时,枚举 P 住的那个空间」。
这里只留结论与复现:

**先犯了一次本仓库点名过的错。** 第一版分类器报 **40 处无名按钮** ——
它把 `{labels.save}` 和 `{t('common.save')}` 当成了"没有文字"。
**我知道它错了,只是因为我把那 40 行打印出来读了一遍。
一个没有打印出来的计数会照原样发出去。**

**并且这一版的输出复现了 BTN-1 的数:** 同一个坏分类器跑在 `6302ae2` 上
给出 **41 处,其中 39 处无 aria-label**,对着 BTN-1 报的 **43 / 41**。
**同一族错误,同一个提交。** 43/41 就此从记录里划掉。

**换成枚举目录之后:** 6506 个键 × 两种语言里,**只有 6 个值不含任何字母**,
其中落在按钮上的只有 `tasks.nodes.up`(`↑`)与 `tasks.nodes.down`(`↓`)。
另有 4 个真图标钮(遮罩 · 汉堡 · 头像 · 语言开关)**本来就有 `aria-label`**。
全 `app/` 下 `<svg>` 一共 **5 个**、`lucide-react` 引用 **1 处**、
`size="icon"` 调用点 **0 处** —— **这棵树上几乎没有图标按钮。**

**处置(Tim 在闸上裁的 (ii)):名字补上,但【不转换】。**
Tim 的规矩说的是「不许把没有名字的图标钮转成 `size="icon" variant="ghost"`」,
它**没有**说「必须让它继续没有名字」。而 `NodeTree.tsx` 另外 8 个手写钮是
BTN-3 的,只转这两个只会让这一页更花。所以:看得见的仍然是箭头,
`aria-label` 走与别处同一套 i18n,两种语言都有(`Move step up` / `上移此步骤`)。

### 12.4 ★ `size="inline"` —— §八(b) 又运转了一次:库里缺能力,先加进库 ★

**转换不是免费的,而代价是量出来的。** `variant="link"` 在这棵树上
**一个调用点都没有**(grep 到的两处 `variant="link"` 是 `ReasonPrompt` 自己的
prop,与库无关)。照 `size="default"` 转过去,每一处会从
「句子里的一段字」变成 **h=32px、左右各 10px 内边距的盒子** —— 行高当场变。

所以先给库加了 `size="inline"`(`h-auto gap-1 rounded-sm p-0 align-baseline`),
**而它必须多做一件事**:

> 基础层写着 `disabled:bg-disabled-bg`(#DDE7EF)。那是给**有底的档**准备的。
> 行内档**没有底**,留着它等于禁用时凭空长出一个灰矩形 ——
> 一个本来只是一段字的东西,**禁用后反而比启用时更像盒子**。
> 所以 `size="inline"` 里写了 `disabled:bg-transparent`。

☞ **与 §10.3 同一课的另一面:一个 token 在【有底的档】里合规,
不等于它在【没有底的档】里还是同一件东西。** §10.3 是换了底、数变了;
这一次是**根本没有底**。

禁用后落在白底上的字色 = `--color-disabled-text` = `--brand-text` `#182B4B`,
实测 **14.13 : 1**(它取代的 `disabled:text-gray-400` 是 **2.54:1**,
`disabled:opacity-50` 是 **2.14:1**)。判据仍是 BTN-1 那一条:
**【清楚地不能按】,不是【淡】** —— 禁用后它读起来就是一段普通正文。

### 12.4b ★ 一个新档位落地,而唯一能量它的探针【看不见它】—— 所以补了两页

`probe-button-tiers.mjs` 取样 18 页,而 **BTN-2 改过的 20 个文件里,一页都不在那 18 页上**。
于是 `size="inline"` 落地之后,探针照样 **29 条断言全绿** —— 它绿的是别人的按钮。

> **§10.5 自己写过这句话:「这道闸看不见什么,说清楚,别高估它」。
> 这一次它看不见的是【本刀刚加的那个档】。一条绿掉的断言,和一条
> 根本没有被求值的断言,在退出码上仍然是同一个字节。**

处置:把 `/purchasing/orders/new` 与 `/operation/processing/new` 加进取样清单
(这两页的「加一行」「加一个投入」是**无条件渲染**的 link/inline,L1 拿得到 ≥2 处去比)。
探针的分组逻辑是通用的(按 `(variant,size)` 分组),所以**不必为新档写新断言**。

**加完之后它量出来的东西:**

```
✓ L1 link/inline   6 处 / 3 页 全等 · h=22 r=4px pl=0px pr=0px w=500
                                       ↑ 盒子没了(default 档是 h=32 pl=10 pr=10)
```

★ 顺带把一件**看得见的改动**记下来,免得被当成缺陷报回来:
`w=500` —— 库的基础层是 `font-medium`,而这 26 处**今天继承的是周围正文的 400**。
转换之后它们比周围的字**略重一点**。这是刻意的:字重属于档位,不属于尺寸,
在 26 个调用点各写一个 `font-normal` 正是本族要消灭的债。

**故障注入(四格,全部实测):**

```
A  干净树 / 扩清单【之前】       → 29 条断言,红 0,EXIT 0  ← 但 link/inline 根本没被求值
A′ 干净树 / 扩清单【之后】       → 32 条断言,红 0,EXIT 0  ← 正确代码不会被罚
B  给一个 link/inline 加 px-2 h-8 → **EXIT 1**,点名两种几何:
                                    h=22 r=4px pl=0 pr=0 w=500 | h=32 r=4px pl=8px pr=8px w=500
D  从副本还原(不是 git checkout)→ 32 条断言,红 0,EXIT 0,回到 6 处 / 3 页 全等
```

> **A 与 A′ 的差是这一格的全部意义:同样是 EXIT 0,一个求值了新档,一个没有。**

### 12.5 转了 26 处,不是 27 —— 那一处为什么留着

`app/purchasing/orders/new/NewOrderForm.tsx:714`(单价展开钮)**没有转**。

它是 27 处里**唯一**一个子节点是「一段合成出来的金额 + 一个条件嵌套 span
(汇率与折算)」的。库的基础层带 `whitespace-nowrap`,而 `<Button>` 是
`inline-flex` —— 那个 span 会变成一个独立的 flex 项、且整体不再换行。
今天它是 `inline-block`,长标签**会换行**。
**在 390px 上这是一次真的版式改动,而本刀没有这个授权。**
留给 BTN-3 连同它旁边那些一次性签名一起判。

### 12.6 数字总账

| | 之前 | 之后 |
|---|---|---|
| 棘轮 `table` 维 | 66 文件 / 76 处 | **66 / 76(原样)** |
| 棘轮 `button` 维(**记在基线文件里的**) | 138 文件 / 265 处 | **115 / 205** |
| 棘轮 `button` 维(**树上实际的**) | 121 文件 / 231 处 | **115 / 205(−6 文件 / −26 处)** |
| 链接态 `<button>`(蓝) | 27 | **1**(12.5 那一处) |
| 无可访问名字的按钮 | 2 | **0** |
| BTN-1 的混合文件 | 32 | **27** |

★ **`138/265` → `121/231` 那一段【不是本刀干的】,是 CONFIRM-1 化掉的 34 处
从未被收紧。** 本刀先把基线收到树上的实际值(自成一步),再转换、再收一次,
**免得一次提交看起来做了 61 件事而实际做了 26 件。**

---

## 十三 · ★ BTN-3(2026-09-06):撤销档、行内的档位 —— 以及【委托书点名的那两个按钮,不是树上那 24 个】★

**这一刀落地的是 63 处转换 + 三个行内档位 + 一条把"没被求值"变成红的探针断言。**
委托书里的四个数,四个都重量过,**三个是错的**;而最要紧的那一条不是数,是一句话。

### 13.1 ★ 头号发现:`type` 那一句,照着做会改掉 8 张线上表单的提交行为 ★

委托书写着「**两个按钮没有 `type` 属性**(`ContainerPanels.tsx:290` 在表单里、
`TaskHeader.tsx:102` 不在),BTN-2 原样保留了它们」。

**实测:树上有 24 个手写 `<button>` 没有 `type`,其中 8 个【就在 `<form>` 里】** ——
`ContainerPanels` 5 处、`LanesPanel` 3 处。在表单里,**没有 `type` 就是 `type="submit"`,
那个【缺席】就是提交线路本身。** 而委托书点名的那两处是 **291** 与 **103**,
并且**早已是 `<Button>`** —— 它们是 BTN-2 转换后的那一对,不是这棵树的population。

> ★ **所以那句话不是"少算了 22 个",是【一句描述了一个它没有查过的总体的话】,
> 出现在一把第一条规矩写着「不许改行为」的刀里。** 照字面执行 ——
> 「只有两个,别的都补上 `type`」—— 会让 8 张线上表单的按钮从提交变成不提交,
> 而**页面不会报错、构建不会红、冒烟照样 200**:表单只是按下去没反应。

处置:转换器**按解析出来的偏移量重写开标签**,除 `className` 外一个属性都不碰,
**`type` 的缺席原样保留**。转换后复量:9 个 `<Button>` 无 `type` 且在 `<form>` 里
(8 个本刀转的 + `ContainerPanels:290`),与转换前逐处对得上。

### 13.2 四个数,重量之后

| 委托书 | 实测 | 方法 |
|---|---:|---|
| 撤销档还剩 **~53** 处 | **6 处** | 见 13.3 —— 差着 **9 倍** |
| 一次性签名 **92** 处 | **117**(树内唯一)/ 176(文件内唯一) | className 频次;92 在 BTN-1 那两个提交上、五种定义下**都复现不出来**(104–136) |
| 红 **19** + 灰 **8** | **19 + 8,一字不差** | 带 `underline` 且带红/灰字色的手写 `<button>`;27 处里 **7 处行号漂了 1–2 行** |
| 无 `type` **2** 个 | **24 个手写 + 11 个 `<Button>`** | 见 13.1 |
| BTN-1 开工前 **391/205** | **389/205** | 逐行(棘轮的算法)与逐处(本刀的)**两种数法都给 389** |

**测法(抄数的人请把方法一起抄走):** `app/**/*.tsx`(不含 `app/components/ui/`),
先剥注释,再逐个开标签**跟踪引号状态与花括号深度**找到真正的结尾,所以多行标签与
`className={cond ? … : …}` 都解析得对。**同一次运行里第二条独立路径**用
`split('<button')` 逐段复验边界字符,两者不等就 `exit 3`。

**故障注入四格(实测,退出码从脚本自己的日志读):**

```
A  干净树                                     → A 205 === B 205,EXIT 0
B  把路径 A 弄瞎(/<button /,要求同行空格)     → A 101 vs B 205,EXIT 3
C  把路径 B 弄瞎(只认空格)                    → A 205 vs B 101,EXIT 3
D  还原                                       → A 205 === B 205,EXIT 0
```

> ★★【B 那一格【第一次没有咬人】,而那才是本节最值钱的地方】★★
> 第一版的「弄瞎」写的是 `/<button[\s>]/`(BTN-1 踩过的那个字符类),
> 它回来是 **EXIT 0**。按理必须红 —— 查下去才发现:**对整份源码来说 `\n` 真的是
> `\s`,那两条正则等价。BTN-1 的陷阱从来不在字符类里,在 `.split('\n')` 里。**
> 而几分钟后,同一个陷阱**咬了本刀自己的 `<form>` 嵌套判据**(逐行 `match`),
> 把 24 个无 `type` 的按钮全判成"不在表单里"。
> **一次没有咬人的注入是信息,不是麻烦** —— 已提成 `AGENTS.md` 的一条法则,
> 连同这个陷阱的名字。

### 13.3 撤销档:57 是三个命名空间的合计,而**手写的只剩 6 处**

**枚举的是【空间】,不是调用点**(`AGENTS.md` 那条技法):走遍 en 目录 **6508 条**
词条 → 短到能当按钮脸的 **3547 条** → 读起来是撤销动词的 **27 条**,再逐条反查渲染点。

```
已经在 tier="reversal" 上(CONFIRM-1 以 <ConfirmButton> 落地)  11 处
仍是手写 <button> 的撤销动词                                     6 处  ← 本刀的活
<Button variant="reversal"> 的调用点                             0 处  ← 委托书说 BTN-1 转了 4 处
```

六处逐个读过,而**其中三处读下来【不是】撤销档:**

| 处 | 读到什么 | 档 |
|---|---|---|
| `AttendanceGrid:103` Reopen · `ReconcileWorkspace:282` Unmatch · `MyExpenseClaimsPanel:154` Withdraw | 撤销一个已提交/已匹配的状态,痕迹全留 | **reversal** |
| `DictSection:113` · `LocationActiveToggle:34` Reactivate | **同一个按钮两个动词**(停用 ⇄ 重新启用),而停用没有撤销任何已过账的东西 | **secondary** —— 一条虚线竖条在按钮写着"停用"的那一半时间里是**假话** |
| `DraftBanner:61` Restore the draft | 恢复的是**本地未保存的草稿**,从来没有过账过 | **default**(它是那条横幅的主动作) |

**另外两处是【动词与行为对不上】,方向相反:**

* ★ `finance/fx/[id]/edit/DeleteButton.tsx` 写着 **Delete**、画成红色链接,
  而它调的是 `softDeleteFxRate(id, reason)` —— **软删除,理由必填,记录全留**,
  它自己的文案键就叫 `withdrawReasonPrompt`。**一个撤销穿着破坏档的衣服。**
  → **reversal**,文案不动(改名是 COPY 的事)。
  顺带:它是**全树最后一个原生对话框**(`window.prompt` + `alert`),
  归 BTN-4(替掉它是 `ReasonPrompt` 的活)。CONFIRM-1 那 40 处一个不剩,
  仅存的 3 处 `window.confirm` 全在 `confirm-dialog.tsx` 自己的注释里。
* ★ `ReconcileWorkspace:299`(`bank.unignore`)是一处**撤销**,而 BTN-2 把它转成了
  `variant="link"` —— 因为它当时画得像个链接。**一把"只换画法"的刀,把一个档位
  按画法定了。** → 改回 **reversal**。这一页现在三处撤销并排,档位才是真的。

### 13.4 ★ 行内的档位 —— §八(b) 第三次运转,而这一次两个档到了行内会自己散架

25 处红/灰链接态(27 减去 `ReasonPrompt` 的 2 处,那两处是 BTN-4 的)今天全部是
**表格单元格里、或句子里的一段字**。两条路都堵着:

* 照 `size="default"` 转 → 每处变成 h=32px 的盒子。**BTN-2 §12.5 正是为了不做这次
  未经授权的版式改动才留下了一处没转。**
* 留在 `variant="link"` 上 → **重新发一遍 BTN-1 存在的理由**:
  「一个看起来像普通动作的破坏性动作,是缺陷,不是不一致。」

而 `size="inline"` 与这两个档**直接冲突**,这是读代码读出来的,不是推的:

```
size  inline      : p-0                     ← 把 destructive/reversal 的 pl-3.5 压掉,
variant destructive: pl-3.5 + 3px 左竖条        竖条于是画在字底下
                     bg-destructive/8       ← 行内一个词会变成一个淡红色小方块,
                     border-destructive/40     比转换前【更像盒子】
```

**处置(`compoundVariants`,三对):保留纯几何的那一条,丢掉只对盒子成立的那些。**

| 组合 | 留 | 丢 |
|---|---|---|
| `destructive/inline` | **实线** 3px 左竖条 + 缩进 | 底、描边 |
| `reversal/inline` | **虚线** 3px 左竖条 + 缩进 | 底、描边 |
| `secondary/inline` | 400 字重 + 正文色 | 描边、hover 底 |

三对都补了 `hover:underline` —— **这 25 处今天全都有,悬停反馈一个字不改**。

**★ 对比度:这一次转换【提高】了对比度,与 BTN-2 那次相反。**

```
今天  text-red-600  #DC2626 on white = 4.829:1   text-gray-500 #6B7280 = 4.834:1
之后  --brand-destructive-text #AA4F48 = 5.356:1  ← 过 AA 过了 0.856
      --brand-text #182B4B(secondary/禁用)      = 14.132:1
参考  BTN-2 的 link 是 4.527:1,只过了 0.027
```

**没有一处比 BTN-2 那 0.027 更贴线,所以委托书那条"更贴线就停下来报告"没有触发。**
色板那条队列条目原样留着 —— 它是关于色板的,不是关于这几刀的。

### 13.5 逐处的档位判读(25 处,按【handler 做了什么】分族)

| 读到什么 | 处数 | 档 |
|---|---:|---|
| 服务端删掉一行已存的记录 | 7 | `destructive/inline` |
| 服务端解绑一个引用(集装箱 ⇄ 提单,理由留痕) | 2 | `destructive/inline` —— 记录都在,但**从这个屏幕撤不回来** |
| **从一张还没保存的草稿里删掉一行**(纯客户端,零服务端调用) | 7 | **`secondary/inline`** |
| 撤销 / 软删除 | 3 | `reversal/inline` |
| 对话框脚上的 Cancel | 5 | `secondary` / `ghost`(见下) |
| 句子里的会话动作(Not you? Log out) | 1 | `link/inline` |

> ★ **那 7 处草稿行的"移除"是本节的判断核心。** 它们**没有服务端调用**:
> `setLines(ls => ls.filter(...))`、`removeRow(key)`。**什么都还没存过。**
> 把它们画成红色,等于在低一层重复 BTN-1 要消灭的那条假规则 ——
> 「红 = 我会丢数据」,而这里红色的代价是**你十秒前打的那一行字**。

**★ Cancel 那五处:Tim 裁的是"接受它们变成盒子",而其中两处的前提不成立。**
裁定的理由是「Cancel 挨着一个**实心**提交钮时,全树别处都是一对按钮」。
实测:`VoidInvoiceControl` / `ConvertControl` / `GoalsEditor` 的提交钮确实是实心 →
`secondary`。而 `ReleaseControl` 与 `DeclineControl` 的提交钮**自己就是
`secondary/sm`**(BTN-1 转的),两个 secondary 并排会把"动作"与"取消"抹平 →
这两处取 **`ghost/sm`**:同样的盒子,没有描边,**不靠颜色也分得出哪个是动作**。

### 13.6 ★ 探针:把"没有被求值"从静默变成红 ★

`probe-button-tiers.mjs` 的 L1 对着 `list.length < 2` 的组是 **`continue`,一声不吭**。
于是一个新档位可以完整落地、被这支探针整支跑过,**而它那条断言从来没有被求值** ——
BTN-2 §12.4b 付过一次这笔账,而它当时的修法是**加两页**,没有动这个静默。

**本刀把"被看见"本身写成断言**(`REQUIRED_GROUPS`,`AGENTS.md`「覆盖率本身必须是
一条断言」的又一次落地):每个组合必须在取样里出现到它的门槛,否则整支红并点名。
取样另加四页静态 + 一页动态,**每一页都渲染本刀改过的东西**:

| 页 | 它证的档 | 实测 |
|---|---|---|
| `/hr/leave/holidays` · `/purchasing/licences` | `destructive/inline` | 15 处 / 2 页 全等 `h=18 r=4px pl=8px pr=0px w=500` |
| `/finance/journal/new` · `/purchasing/payment-terms/new` · `/purchasing/orders/new` · `/operation/processing/new` | `secondary/inline` | 6 处 / 4 页 全等 `h=22 r=4px pl=0px pr=0px w=400` |
| `/settings/dictionaries` | `secondary/xs` | 34 处 / 2 页 全等 |
| `/logistics/lanes` | `default/sm` · `secondary/sm` | 12 处 / 4 页 · 20 处 / 10 页 |
| `/finance/fx/{id}/edit`(**动态,id 从线上现取**) | `reversal/inline` | 1 处 |
| `/finance/settings`(原有) | `reversal`(盒子形态) | L2 虚线竖条 2 处 |

**★ 这一节最值钱的是它【第一版没做到的那件事】,以及为什么不许把它糊过去 ★**

第一版跑完,`COVER reversal/inline` 报 **0 处** —— 而那正是这条断言存在的理由:
**六个撤销档调用点【全部】住在 `[id]` 路由里,而取样清单只收静态路径。**
补了动态 id 解析之后仍然只到 **1 处**,原因是线上数据本身:

* 第一版的 id 查询是一句 `select=id&limit=1`,取到的 task **是软删除掉的**,
  页面 404、`RENDER` 当场变红 —— **「取一行」与「取一行【这一页画得出来的】」
  不是同一个问题**,而冒烟的 id 解析器早就为同一件事维护着一张 27 张表的清单。
* 换成对账台之后仍然渲染不出:全库两张对账单,一张 `open` 但**已软删除**、
  两行都是 `unmatched`(画不出 Unmatch/Restore);另一张有 `matched`+`ignored` 行
  但**已经对完**,而那一页对 `status==='reconciled'` 是 **redirect**。

> ★ **所以 reversal/inline 到不了 2,不是取样偷懒,是【线上没有能渲染它的数据】。**
> 而"没有数据"绝不允许被写成"这一档是绿的" —— 那正是本仓库
> 「一个瞎掉的检查必须说【我瞎了】」那一条。

**处置不是降低门槛了事,是换一条【一处实例就成立、而且更强】的断言:**

```
L2 行内两档同几何(destructive/inline vs reversal/inline)
```

它断的是 §10.1 ③ 那条设计律本身:**行内只剩竖条这一条不经过颜色的判据,
而竖条要分得开,两个档的盒子必须先长得一模一样。** 跨页全等证不了这件事,
这一条证得了,并且它对着**任何一处** reversal/inline 都成立。
门槛降到 1 的那一格在输出里**连同理由一起印出来**,不是悄悄降的。

### 13.6b ★ 一次量出来的【转换代价】:`shrink-0` 让每一个转过来的按钮在窄的 flex 行里【压不动了】

**版式探针在 `/logistics/lanes` 上报了 +41px,而点名的元凶是【一个库按钮】** ——
也就是本刀刚转过去的那一个。这一处做了 A/B,因为"是不是我弄的"只能靠量:

| | 390px 的溢出 | 元凶 |
|---|---:|---|
| 本刀之前(从备份还原那一个文件) | **+15px** | `button.rounded bg-blue-600 px-3 py-1 …`(手写钮) |
| 照原样转换 | **+41px** | `button.group/button …`(库按钮) |
| 加上 `shrink whitespace-normal` | **+12px** | `form.flex items-end gap-2 …`(表单自己,与按钮无关) |

**机制,读代码读出来的:** 库的基础层写着 `shrink-0` 与 `whitespace-nowrap`,
两条都是**刻意的** —— 一个被挤扁、文字被截断的按钮比一个溢出的行更坏。
而这三张表单是**不换行的 flex 行**,手写钮当年**可以被压缩、也可以折行**。
于是转换在这一页上**把一个可压缩的东西换成了一个不可压缩的东西**。

> ★ **这不是"库写错了",是【转换有代价,而代价要量】。**
> `size` 反而更小(`px-3`→`px-2.5`、`text-sm`→`text-[0.8rem]`),
> 光看内边距会得出"应该更窄"的结论 —— **而它更宽,原因在两条与尺寸无关的类上。**

**处置:在这四个调用点解开这两条**(`shrink whitespace-normal`)。
它是**这一行的版式性质**,不是档位性质,所以留在调用点是对的
(与 BTN-2 §12.4b 反对"在 26 个调用点各写一遍 `font-normal`"不矛盾:
字重属于档位,能不能被压缩属于它所在的那一行)。

★★ **给 BTN-3b 的一句话,因为这条对【每一次】转换都成立:**
> **把一个手写钮转进一个不换行的 flex 行之前,先在 390px 上量一遍。**
> `shrink-0` + `whitespace-nowrap` 是库的两条硬性质,而它们只在窄屏上显形。

顺带记下这一轮版式探针的全貌(19 条路由,**全部是本刀改过的页**):
**可用 16 / 19**;三处 U1 不过 —— `/operation/processing/new` **+416px**、
`/purchasing/payment-terms/new` **+257px**(两处元凶都是**手写 `<select>`**,
与按钮无关,BTN-2 已经把前者立过案)、以及上面那处 `/logistics/lanes` **+12px**。
一处 U2 不过(`/purchasing/payment-terms/new` 一张表被非滚动祖先裁掉)。
**这三处一处都不是本刀造成的,而 `/logistics/lanes` 本刀让它【好了 3px】。**

### 13.7 数字总账

| | 之前 | 之后 |
|---|---|---|
| 手写 `<button>` | 205 处 / 115 文件 | **142 / 91**(−63 处 / −24 文件) |
| `<Button>` 调用点 | 150 | **213** |
| 一次性签名(树内唯一 className) | 117 | **91** |
| 混合文件(同一文件里手写与库并存) | 32 | **21** |
| 混合文件(BTN-2 的定义:BTN-1 碰过、仍有手写的) | 27 | **18** |
| `variant="reversal"` 调用点 | 0 | **6** |
| 无 `type` 且在 `<form>` 里的按钮 | 8 手写 + 1 库 | **0 手写 + 9 库(逐处对得上)** |

**没有转的,连同理由:**
* `NewOrderForm:715`(单价展开钮)—— BTN-2 §12.5 留下的那一处,**本刀再留一次**,
  并且**明确入队**:它是一次 390px 的版式测量,该和手机那一族一起做,
  不该由第三刀继续用散文传下去。
* **9 处不是按钮**(6 处 `aria-expanded` 展开器、2 处整行可点、1 处纯字形)——
  与 BTN-1 数到的 9 处逐处相同,原样不动。
* 其余 **91 处一次性签名 → BTN-3b**,连同下面这条残余风险。

### 13.8 ★ 残余风险,写出来而不是藏着

> **按【标签】找撤销档,看不见一个【动词与行为对不上】的撤销。**

本节 13.3 的枚举法是穷尽的 —— 对**标签**而言。而 `fx/DeleteButton` 恰恰证明了
这类东西存在:它写着 Delete,做的是撤销。**同一个形状反过来也一定有**:
一个写着别的词、做的却是撤销的按钮,这套办法一处都找不到。

**这与"一次勘察只看得见代码碰巧给它起的名字"是同一条法则**(`AGENTS.md`),
只是那里是组件名,这里是按钮脸上的动词。

**关掉它的办法是确定的,代价也是确定的:读完那 91 处一次性签名的 handler。**
**那是 BTN-3b 的活**,并且是它比"把剩下的转完"更值钱的那一半。

---

## 十四 · ★ BTN-3b(2026-09-06):读完 91 个 handler —— 以及【树上唯一没有确认步骤的硬删除,穿着全场最淡的衣服】★

**本刀落地 74 处转换。而它最值钱的产出不是转换,是【读 handler 读出来的六处
动词与行为对不上】,其中一处不是档位问题,是安全问题。**

### 14.1 ★★ 头号发现:两个都叫「移除」的按钮,画法的轻重【正好反了】★★

| 处 | 脸上写着 | 画成什么 | handler 实际做什么 |
|---|---|---|---|
| `tools/tasks/[id]/NodeTree.tsx:117` | `labels.remove` | `className="text-red-700"` —— **一行裸红字,没有边、没有底、没有任何几何** | `supabase.from('task_nodes').delete()` —— **一次硬删除,行没了** |
| `sales/customers/ContactsPanel.tsx:120` | `contacts.remove` | `border-gray-400 … hover:bg-gray-50` —— **中性灰盒子,和旁边的「编辑」一模一样** | `soft_delete_counterparty_contact` → `UPDATE … deleted_at`,**行还在** |

> ★ **真的会销毁一行的那个,穿的是全树最淡的一件;什么都不销毁的那个,
> 反而比它重。** 而 `NodeTree.tsx` 里 `ConfirmButton` 出现 **0 次** ——
> **这是树上唯一一处【没有确认步骤的硬删除】,而它同时是最不像按钮的那个按钮。**

**处置:** NodeTree:117 → `destructive/inline`(拿到实线左竖条);
ContactsPanel:120 → `reversal/xs`(虚线左竖条)。
★ **那个缺失的确认步骤【本刀不补】** —— 这一刀的第一条规矩是不许改行为,
而加一个对话框是改行为。**它进 BTN-4 的交接清单第一条,并且进人工走查清单**,
因为它是一句今天就能靠"告诉六个人"止损的话。

### 14.2 另外五处动词与行为对不上

| 处 | 脸上写着 / 画成 | handler 实际做什么 | 定档 |
|---|---|---|---|
| `settings/roles/RoleForm.tsx:166` | `common.delete`,红 | `softDeleteRole` → `deleted_at` + `is_active=false` | **reversal** —— `fx/DeleteButton` 那个形状的**第二例** |
| `hr/leave/[id]/DecideControls.tsx:89` | `leave.cancel`,红 | `cancel_leave_request`:**对每条 draw 追加一条等额 release**,一行都不删 | **reversal** —— 正是 Unapply/Unreconcile 的形状 |
| `hr/reviews/ReviewActions.tsx:122` | `reviews.void`,红 | `void_review`:`status='void'`,理由必填,**不回滚已发生的雇佣事实** | **destructive** —— 见 14.3 那条被写下来的界线 |
| `finance/gst/GstControls.tsx:87` | 琥珀色 | `correct_gst_return`:**INSERT 一份新期间行**,原件仍是 `filed` | **secondary** —— 它既不撤销也不销毁,是一次向前的过账 |
| `finance/month-end/page.tsx:200` | `t('reviews.filter')` | 一个月结页的筛选提交 | **文案缺陷,文案不动**(改名是 COPY 的事),档位按行为定 |

★ **`leave.cancel` 那一处还带出一件事:全树只有这一个「Cancel」是一次域内撤销,
另外八个 `common.cancel` 都只是关掉一个对话框。** 同一个词,两种后果。

### 14.3 ★ 一条本来只是"惯例"的界线,现在写下来了 ★

Tim 在本刀裁定:`void` 归 `destructive`,跟随 `VoidInvoiceControl` 的先例。
**而他同时要求把界线写进 §10.1,不要再靠默契:**

> ### **作废杀死【单据】;撤销是在单据继续活着的前提下,回退一个【已过账的状态】。**

这一句是必要的,因为 §10.1 的表里 destructive 那一格写着「真的销毁**或作废**一条记录」,
而 `作废` 这两个字此前**从来没有被压力测试过** —— `void_review` 什么都不删、
理由必填、还专门返回 `employment_facts_unchanged: true`,照字面读它像撤销。
**按上面那条界线读,它是作废:那份评估作为单据到此为止(`void` 是终态,不在流水线上)。**
而 `cancel_leave_request` 相反 —— 它补一条等额的反向记账,请假单本身的账继续成立。

### 14.4 非按钮:那个「9 处」是【按标记数出来的】,而标记数不到没有标记的东西

BTN-1 与 BTN-3 都数到 9 处非按钮(6 `aria-expanded` + 2 整行可点 + 1 字形)。
**本刀复量:`aria-expanded` 确实是 6 处,一处不差。** 而**按行为读**,
光是这 91 处里就还有:

* `LanguageSwitcher:37` —— `role="switch"` + `aria-checked`,**一个开关,不是按钮**;
* `ModuleBar:275` —— `absolute inset-0 bg-black/30`,**一块遮罩**;
* `AssetActions:80` · `CreateAccountPanel:105` · `UserRow:142` ——
  **三个展开器,而它们【都没有 `aria-expanded`】**;
* `PurposePanel:107/120/134` · `SafetyStatePanel:76` —— **分段/单选控件**
  (当前项是 `disabled`),转一个成员会改变整组的含义。

> ★★ **「9 处」不是数错了,是【它数的东西和它的名字对不上】:
> 一个从 aria 标记推出来的计数,看不见一个【没有那个标记】的控件。**
> 这与 AGENTS.md 那条勘察法则(「一次勘察只看得见代码碰巧给它起的名字」)
> 是同一条,只是那里是组件名,这里是无障碍标记。
> ☞ `AssetActions:80` 缺 `aria-expanded` 是一处**无障碍缺陷,本刀只报不修**。

**本刀因此留下 17 处不转**,逐处带理由(见 14.6)。

### 14.5 一个【运行期才定得下来】的档位

`sales/orders/[id]/TransitionPanel.tsx:56` 用**一个** `<button>` 渲染
`confirmed` / `closed` / **`cancelled`** 三个动作,而它们共用**同一个中性 className** ——
一次销售订单的取消,与一次确认长得一模一样。

**而两个目录之外的 `suppliers/[id]/edit/StatusPanel.tsx` 早就把这件事做对了**:
它按 `DESTRUCTIVE_TRANSITIONS` 分流,危险的那支走 `<ConfirmButton tier="destructive">`。
**同一个问题,同一棵树,一处解了、一处没解。**

处置:`variant={to === 'cancelled' ? 'destructive' : 'secondary'}` ——
**照抄树里已有的那条运行期判据,不发明第二种问法。**
`inbound/…/AssayForm.tsx:313/326` 同理:哪一个是主动作**随 `applyBlocked` 移动**,
转换后由 `variant={applyBlocked ? … : …}` 承接,与它原来的 className 三元一一对应。

★ **顺带更正本刀自己的一个判断:** 开工时我以为 `StatusPanel:85` 要转成运行期变体。
读下去发现**那个手写钮只是 `!isDestructive` 那一支** —— 危险的那支上面就是
`ConfirmButton`。所以它是静态 `secondary`,而 `cls` 那个常量**必须留着**
(第 115 行的 `ConfirmButton` 还在用它)。**读代码之前的推断,又一次比代码更复杂。**

### 14.6 转了什么、没转什么

| | 处数 |
|---|---:|
| 一次性签名(树内唯一 className,空白归一后) | **91** |
| 转换 | **74** |
| 留着不转 | **17** |

**转换后按档位:** `secondary` 33 · `default` 32 · `reversal` 3 · `destructive` 2 ·
`link` 1 · **运行期决定 3**(TransitionPanel ×1、AssayForm ×2)。

**17 处不转,分四类:**
1. **非按钮 10 处** —— 3 展开器(`ModuleBar` 209/255/305)、1 遮罩(275)、
   1 开关(`LanguageSwitcher:37`)、1 整行可点(`CountList:109`)、
   4 分段控件(`PurposePanel` 107/120/134、`SafetyStatePanel:76`);
2. **另有 3 个展开器** —— `AssetActions:80` · `CreateAccountPanel:105` · `UserRow:142`,
   与①同类,只是**没有 `aria-expanded` 标记**;
3. **归 BTN-4 的 3 处** —— `ReasonPrompt` 77/114/122(它整支要折进 `confirm-dialog`,
   **现在转等于把同一份活干两遍,还保证一次冲突**);
4. **已入队的 1 处** —— `NewOrderForm:716`,BTN-2 与 BTN-3 各留过一次,
   它是一次 390px 的版式测量。

★ `PurposePanel:120`(「清除待定」)**单独说一句**:它本身是个正经动作,
读下来该是 `secondary/sm`。**留着不转的理由是它属于 107/134 那一组分段控件** ——
**只转一组里的一个成员,会让那一组从"三个同类选项"变成"两个选项加一个动作"**,
而那是一次没人要求过的语义改动。

### 14.7 数字总账

| | 之前 | 之后 |
|---|---|---|
| 手写 `<button>` | 142 处 / 91 文件 | **68 / 48**(−74 处 / −43 文件) |
| 组件库棘轮 · `button` 维 | 91 文件 / 142 处 | **48 / 68** |
| 组件库棘轮 · `table` 维 | 66 / 76 | **66 / 76(原样,没有放宽)** |
| 混合文件(同一文件手写与库并存) | 22 | **12** |
| `variant="reversal"` 调用点 | 6 | **9** |
| 无 `type` 的手写 `<button>` | 11 | **6** |
| 无 `type` 且在 `<form>` 里 | **0 手写 / 9 库** | **0 手写 / 9 库(逐处对得上)** |
| 探针断言 / 取样 | 45 条 / 25 页 | **84 条 / 43 页(153 个库按钮)** |

★ **委托书写的「24 个无 `type`,其中 8 个在 `<form>` 里」是【上一刀之前】的数。**
BTN-3 早已把那 8 处转成了 `<Button>`;本刀开工时实测是 **11 个无 `type`,
其中在 `<form>` 里的是 0 个**。那条规矩仍然成立(`button.tsx` 不设 `type` 默认值,
所以缺席原样穿过转换),**而它点名的那个总体已经不存在了。**

### 14.8 探针:三条修正,而其中两条是【本刀自己的断言错了】

1. ★ **L4 第一版是【假红】。** 它去 `button.tsx` 里取变体的类名串,分隔符写的是
   `'\n        '`(8 空格)—— 而值那一行缩进 **10** 空格,**8 空格是它的前缀**,
   于是取回来的是 `"\n        destructive:"`,**一个类名都没有**,三条断言全红,
   而库里那三样一样不缺。**一条看不见那个性质的断言,红和绿都不是证据。**
   注入五格实测:干净树绿 · 抽掉 `pl-3.5` 红 · 抽掉 `before:w-[3px]` 红 ·
   虚线变实线红 · **拿旧的瞎 helper 对着干净树跑,复现那次假红**。
2. ★ **L1 对行内档不该比高度,而这条 BTN-3 已经写下、只是没写进 L1。**
   BTN-3 在 L2 里量过并写明:`size="inline"` 就是 `h-auto`,行内档的高度按定义
   等于周围那行字。L1 照旧把 `h` 比进去,**此前没红只是因为取样里
   destructive/inline 两处都住在 `text-xs` 的表格里**。本刀铺到 `/tools/tasks/{id}`
   (正文 `text-sm`)之后当场两种几何。**这不是回归,是 L1 一直在问一个
   已经被判定不该问的问题,而旧取样恰好让它答对了。**
3. **取样从 25 页扩到 43 页(7 页动态)。** 开工前实测:那 91 个按钮住在
   **44 个目录**里,而**其中 32 个目录一条路由都不在旧清单上**。

**新加的 `L2b`(盒子形态两档同几何)与 `L4`(库源码层的左竖条)** 是为了
`reversal/sm` 与 `reversal/xs` 各自**只有一处调用点**这件事 ——
照 BTN-3 的处置:**不降门槛,换一条一处实例就成立、而且更强的断言。**

**五页试过又拿掉了,连同理由**(RENDER 那条断言正是为此存在):
`/settings/roles/{id}/edit` **那条路由不存在**(真实是 `/settings/roles/{id}`)·
`/sales/commissions` 表单在 `/new` 不在列表页 · `/tools/pricing/calculator`
的钮在 `{res && …}` 里(要先算一次)· `/operation/handovers` 与 `/finance/claims`
**两张表实测 0 行**。
★ 最值得读的是 `/sales/commissions/new`:**路由对、组件在,而按钮仍然不存在** ——
`CommissionForm:67` 有一句 `if (agents.length === 0) return (…)`,
而 `service_vendor` 供应商实测 **0 行**。

**两个组合诚实地宣告为"本刀这一处走不到",连同理由印在输出里:**
`destructive/sm`(`ReviewActions:122`——线上 `performance_reviews` **0 行**;
★ 但**这个组合本身是证过的**,L2b 拿它与 `reversal/sm` 比过同几何)与
`default/xs`(五个调用点**无一**在页面加载时存在)。

### 14.9 版式与对比度

**390px 版式探针,17 条本刀改过的路由:`USABLE 17/17`,U1 与 U2 各 17/17,
溢出 0、裁切 0。** BTN-3 留下的那句「把手写钮转进不换行的 flex 行之前先在 390px 上量」
**照做了,而这一次量下来没有一处需要解开 `shrink`/`whitespace-nowrap`** ——
BTN-3 那次的 A/B 是被一条 +41px 的失败触发的,这一刀没有失败可归因。

**对比度:没有一处比 BTN-2 那 0.027 更贴线,所以"更贴线就停下来报告"没有触发。**
`L3 禁用态对比度` 实测最差 **11.27:1**(19 处)。
★ 三处**琥珀色当动作用**(`GstControls:87` · `HoldReleaseControls:83` ·
`RetentionPanel:176`)按行为改判,**而"琥珀=警告档"这个用法本身归入队列里那条色板问题**
—— `warning` 在 §10.1 的定义是「系统在告诉你一件正在发生的事」,不是一个人按的动作。
