# 信息架构 · 已建成 —— IA-BUILD-1

**2026-09-02。** 前一刀 PDF-1(`6503a5c`)。本刀有 DDL(`user_dock`),
备份 `evoltrya-backup-2026-09-02-1639.dump`（TOC 5250 条，已验证）。

前置的勘察是 `docs/information-architecture-scoping.md`（IA-0，只读）。
**那份文件在三处是错的,本文件逐条更正** —— 见 §0。设计裁定(D1–D7、M6、R1–R3)
是 Tim 的,不在这里重新讨论。

---

## 0 · 先说三处更正 —— 它们更正的是【已经写下来的裁定与勘察】

### F1 · 审批链【没有配置界面】,这一刀之后仍然没有

D7 的裁定写着「配置审批链从此是系统管理员的事,而不是财务的事」,并接受了
「Choo-er TEH 不再配置她自己是一级审批人的那条链」这个后果。

**实测:那句话的后半在本刀之后仍然不成立 —— 因为它本来就不成立。**

* `ApprovalsPanel` 是【只读】的,而且是刻意的(它的抬头写着为什么:开启审批要
  同时配齐三个值,其中两个是业务决定,所以不给一个按下去就会被拒的按钮);
* `app/` 底下**没有任何东西**写 `approvals_enabled` / `approval_level1_role_code` /
  `approval_level2_role_code` / `approval_threshold_base` 这四列 ——
  `app/finance/settings/actions.ts` 只导出一个 `setPeriodLock`;
* 线上那一行 —— `enabled=false · level1=finance · level2=cfo · threshold=1000` ——
  是**直接改库**改出来的。

所以本刀搬走的是**那扇窗**,不是一个控制器。**配置审批链在本刀之后仍然不是任何人
在界面上做的事。** 这句话也印在 `/settings/approvals` 屏幕上
(`finance.approvals.noConfigUi`),不只写在注释里。

> **排队事项(本刀不建):审批链没有配置界面。**
> **触发点:同事测试轮要试审批。** 今天开启审批 = 一次数据库操作 + Choo-er TEH
> 的邮箱确认。要在那一轮之前决定是建界面,还是接受用数据库操作走一次。

### F2 · 执照登记簿【本来就不在 module.finance.view 后面】

勘察 M2 说它在财务权限后面。实测(CMPL-1 落地时就是这样):

| | 判据 |
|---|---|
| `company_compliance` 的 RLS | select = `module.suppliers.view` · write = `module.suppliers.edit` |
| 面板自己 | `can('module.suppliers.view')` / `.edit` |

`module.finance.view` 把的是**它此前寄居的那一页**(`/finance/company`),不是这块面板。
所以 D7 在这一条上是一次**搬家**,不是一次**改码** —— 码本来就是对的。

### F3 · M6 在今天【不授出任何东西】

实测 live 的 `role_permissions`:**没有任何角色持有 `module.stocktakes.view`
而不持有 `module.inventory.view`**;`module.materials.view` 同理。
所以把盘点与物料降成库存的二级**不困住任何人**,而"只有盘点权限的人也给库存视图"
这条今天**授出零个角色**。它仍然是对的,只是它买的是明天。

**而且它不需要任何特判** —— 见 §2 的推导。

---

## 1 · 顶栏此前在手机上是什么样 —— F4,本刀最该被记住的一个数

实测,稳定别名之前的那一版,**登录着**,量的是真实渲染:

| 角色 | 视口 | 导航项 | 导航内容宽 | **不横向滚动够得到几项** |
|---|---|---|---|---|
| admin | 1440 | 21 | 1834px | 16 / 21 |
| admin | **390** | 21 | 1834px | **4 / 21** |
| warehouse | 1440 | 19 | 2337px | 10 / 19 |
| warehouse | **390** | 19 | 2337px | **2 / 19** |

**注意第二行:权限最少的角色,顶栏反而最长** —— 因为「· 受限」把每个标签都撑长了
(warehouse 比 admin 宽 503px)。于是一个仓库工在手机上打开系统,
**最先看见的两样东西都是他打不开的**。

这不是"待优化",这是本刀之前的状态。它是 Tim 裁定手机拿另一种形态(A3)的依据。

---

## 2 · 建成的结构

### 2.1 三份清单,各回答一个问题(`lib/modules.ts`)

| | 是什么 | 谁读它 |
|---|---|---|
| `MODULES` | **九个一级模块**,Tim 给的顺序 | 顶栏 |
| `SCOPES` | **15 个权限范围** | 页面守卫 `requireModule(MOD.x)`,178 处 |
| `FUNCTIONS` | **每一个二级条目**(77 条,其中 11 条跨属主) | 顶栏二级、手机抽屉、财务子导航、dock |

★ **为什么必须分开**:九模块方案把 suppliers / materials / stocktakes / customers /
inbound / output / pricing 从一级模块降成二级条目,**但它们的权限码一个都没有降级** ——
`/suppliers/*` 仍然由 `module.suppliers.view` 把门,因为那是 suppliers 表 RLS 上
写着的那个码。**导航的层级与权限的范围是两件事**,而它们此前共用一份清单。
分开之后:**导航整个重排,169+ 处页面守卫一行未改。**

### 2.2 一级模块【没有 permission 字段】—— 可进性是推导的

一个模块进不进得去 = **它名下有没有任何一条二级条目进得去**
(`lib/moduleAccess.ts` 的 `entries.some(...allows...)`)。

写第二份谓词就是第二份定义,而它与二级条目的判据之间没有任何东西保证同步。

**它顺手把 M6 变成了一件不需要特判的事**:盘点是库存名下的一条二级条目,
它进得去,所以库存这个一级就进得去。不需要 `widen`、不需要新码、不需要一行特判。
而进去之后看到的是一张**逐条诚实**的菜单,不是一个打得开却零行的库存页
(NAV-REG-1 为物流记下的那个陷阱)。

### 2.3 九个一级与它们的二级(实测渲染,admin)

| 模块 | 二级(顺序即屏幕顺序) |
|---|---|
| **采购** | 采购首页 · 采购订单 · 收货差异 · 付款条件 · 供应商 · 合同 · **公司执照登记簿** · 佣金【双】· 收货【双】· 定价【双】· 公式【双】· 计价器【双】· 金属行情【双】· 已删除记录【双】 |
| **物流** | 货代 · 航线 · 货柜 · 运费单【双】 |
| **运营** | 工单 · 加工单 · 在制 · 交接 · 产出【双】· 批次毛利【双】· 已删除记录【双】 |
| **销售** | 报价 · 销售订单 · 客户 · 客户重叠 · 佣金【双】· 定价【双】· 公式【双】· 计价器【双】· 金属行情【双】· 已删除记录【双】 |
| **财务** | **32 条,分六组**(见下) |
| **库存** | 现况 · 库位 · **盘点** · **物料** · 库存报表【双】· 收货【双】· 产出【双】· 已删除记录【双】 |
| **人力** | 概览 · 员工 · 部门 · 考勤 · 薪资 · 休假 · 报销 · 培训 · 评估 · KPI |
| **任务** | 任务 |
| **设置** | 权限 · 字典 · 批量导入 · **审批链** |

**财务的第三级(唯一有第三级的模块):**
报表(试算/损益/资产负债/现金流量/现金预测/价格敞口/库存报表/批次毛利)·
分录(分录/手工分录)· 应收(应收/发票/贷项)· 应付(应付/收付款/开支/报销/固定资产/运费)·
期末(月结/薪资付款/成本结算/重估/成本差异/关账/GST/预提税/月度包)·
配置(财务设置/公司抬头/汇率/银行)。

### 2.4 模块名是【按钮】,不是链接

Tim 的 D2:不做模块目录页,顶栏本身就是目录。可导航的地址**全部**是二级条目,
包括模块自己的落地页(财务的试算平衡 = `/finance`)。
这顺手解决了「设置」根本没有 `app/settings/page.tsx` 这件事:**它不需要有。**

### 2.5 手机拿的是另一种形态,不是压窄的桌面

< 640px:九个模块收进**一个菜单按钮 + 一张全高抽屉**,每一行展开自己的二级,
财务展开到第三级;dock 变成一条**固定底栏**。
同一份注册表、同一份判据、同一句「受限」。

抽屉底部另有一段**「关于你」**(`/me`、`/my-reviews`)—— 手机顶栏放不下它们
(见 §5 的实测),所以它们**换了地方,没有消失**。

---

## 3 · 面包屑:深路由的判据是【算出来的】

> **深度 = 路径段里,不是动态段 `[…]`、不是路由组 `(…)`、也不是叶子动作词
> `new`/`edit` 的那些段的个数。深 ⟺ 深度 ≥ 3。**

三条排除各有理由:一个 id 不是架构里的一层(它是同一层上的一行);路由组根本不
出现在 URL 里;`new`/`edit` 是**对当前这一层的动作**,不是更深的一层
(`/hr/employees/new` 读起来是"员工 · 新建",不是"人力 › 员工 › 新建")。

实测落在 190 条路由上:深度 1 → 45 · 2 → 121 · **3 → 22 · 4 → 1**,
**深的一共 23 条 —— 与 Tim 的 D4 和勘察 PART F/3b 的 22+1 逐条对上。**

清单由 `scripts/gen-deep-routes.mjs` 生成到 `lib/deepRoutes.generated.ts`,
`npm run build` 比对,过期就红。**为什么不手写那 23 条**:勘察 E2/4 记着
`finance/Subnav` 两份清单的漂移隐患 —— 一份手写的深路由清单是同一个形状,
加一页的人不会想到来改它,于是那一页永远没有面包屑,而没有任何东西会说出来。
(本刀落地时它就响了一次:新增两页之后比对当场变红。)

面包屑的三截也是机械拼的:模块名(不是链接)› 注册表里最长前缀那条二级条目 ›
剩下的每一段一句 `breadcrumb.<段>`。**动态段整段跳过**,与判定深浅时跳过 `[id]`
是同一条理由。那 21 个段名的两语文案由 `check-i18n` 的 MANIFEST 从生成文件**现读** ——
加一条带新段的深路由,少一句文案就构建变红。

**实测**:`/inventory/reports/snapshot` → `Inventory › Reports › Snapshot`;
`/inventory` → **不画**(一个字节都没有)。

---

## 4 · dock

### 4.1 存在哪,为什么

`public.user_dock`:**一人一行**,`hrefs text[]`,FK 指向 `auth.users`(级联删除),
四条 RLS 策略全部锁在 `user_id = auth.uid()` —— **别人读不到,管理员也读不到。**

★ **为什么是"一行 + 一个数组",而不是"一人多行"** ★ 因为要分得开**三种**状态:

| 库里 | 意思 | 屏幕 |
|---|---|---|
| **没有这一行** | 从来没有动过 | 画按权限的默认 |
| **有行、数组为空** | **本人清空了** | 就画空的,**不补回默认** |
| 有行、数组非空 | 这是他排的 | 画这些 |

一人多行表达不了中间那一种:零行既是"没动过"也是"清空了"。
**"缺席 ≠ 空"是这个仓库反复付账的那条区别**,这里从模式层面就分开。

**为什么不是 cookie / localStorage**:系统里已有的唯一一项偏好(语言)存在 cookie 里,
那是**每台设备一份**。Tim 在手机上和桌面上用同一套 dock。

### 4.2 一项进不去的时候(Tim 的 4b)—— 三态,实测

| 状态 | 屏幕上 | 可点 | 有移除按钮 |
|---|---|---|---|
| `open` | 标签 | ✅ | 编辑态下有 |
| `restricted` | 「员工 · 受限」 | **❌** | **✅**(他得能自己清掉) |
| `gone`(地址已不在注册表里) | 「/some-retired-feature · 已下架」 | **❌** | **✅** |

**可见性在【渲染时】算,不在【存的时候】算** —— 存的只有地址,每次渲染重新问注册表。
所以权限被收走的第二天,那一项自己就变成「受限」,不需要谁去跑一次清理。
写侧**故意不判权限**:写侧一挡,渲染侧就会有人觉得不必再判 —— 那才是危险的地方。

### 4.3 默认(4c)

按**权限**取一份候选清单里的前 8 条。Tim 的原话是"按角色",落成"按权限" ——
效果相同(权限就是角色带来的东西),但**一个人可以同时持有几个角色**,按权限
根本不会遇到"两个角色的默认怎么合";而且渲染顶栏时权限**已经查过了**(React cache)。

实测:**live 的 11 个角色,每一个的默认 dock 都非空** —— 连一个零模块权限的
`employee` 也有一条(金属行情,它的读策略是 `USING(true)`)。

### 4.4 一处实测出来的迟钝,以及为什么它值得修

服务端动作写完之后要 `revalidatePath('/', 'layout')` —— 而外壳在**每一页**上,
所以整棵树要重画。实测(稳定别名,按下「×」之后逐秒采样):

| 1s | 2s | 4s | 8s |
|---|---|---|---|
| 没变 | 没变 | 没变 | **变了** |

也就是说:人按了移除,**屏幕上五秒钟什么都没发生**,而库里其实已经写完了。
**一个按下去没反应的控件读起来就是坏的。** 加了 `useOptimistic`:那一格立刻变,
真值到了再对齐,失败时 React 自己回滚、错误由 `role="alert"` 说出来。
**存的是什么一个字没变,变的是等待期间屏幕说的是哪一句话。**

### 4.5 它不是第二套导航(4d)

加项只能从注册表里已有的条目加(服务端动作对着 `FUNCTIONS` 核,核不上就拒绝),
所以 dock 里**不可能出现顶栏到不了的功能**。

---

## 5 · 淡底与磨砂(R1–R3)

### R1 · 内部页面的底色

`body { background: var(--brand-bg) }` = **#F1F9FE**,品牌指南白纸黑字给的 BACKGROUND。

| 文字 | on #FFFFFF(之前) | **on #F1F9FE(现在)** |
|---|---|---|
| `--foreground` #171717(正文,187 页在用) | 17.93:1 | **16.84:1** ✓ |
| `--brand-text` #182B4B | 14.13:1 | **13.27:1** ✓ |
| `text-gray-600`(全站次级文字最常用的一档) | 7.56:1 | **7.10:1** ✓ |
| `text-gray-500` | 4.83:1 | **4.54:1** ✓(最险的一档) |

**每一档都掉了一点点,没有一档掉出 AA。** 125 处 `bg-white` 的卡片与表格原样保留,
它们现在是**浮在淡底上的白面**。

### R2 · 磨砂只给浮动层

顶栏 · dock · 下拉 · 抽屉。**表格不磨砂,卡片与面板这一刀也不磨砂**(Tim 的裁定)。

### R3 · 值是怎么定的,以及【是哪一条约束定的】

顶栏是 sticky 的,页面内容从它底下滚过去 —— 所以对比度必须对着**合成色**量,
不是对着 token 量。最坏的真实底:`bg-gray-900` #111827(全站最深的面,62 处)。

| 玻璃上的文字 | AA(4.5:1)允许的**最低不透明度** |
|---|---|
| `--brand-text` #182B4B | 0.526 |
| **`--brand-muted-glass` #55657D** | **0.868 ← 就是这一条定的值** |

**AA 允许的最大透明度是不透明度 0.868。本刀取 0.90 —— 比允许的最大值更不透明,
也就是更淡的玻璃**,富余 3.2 个百分点。这正是 R3 要的 "a hint of glass"。

合成之后的实测:

| 底 | 合成色 | `--brand-text` | `--brand-muted-glass` |
|---|---|---|---|
| 最坏 #111827 | #E7E8E9 | **11.52:1** ✓ | **4.83:1** ✓ |
| 正常(淡底 #F1F9FE) | #FEFEFF | **14.02:1** ✓ | **5.88:1** ✓ |

**顺手修掉的一处**:「· 受限」此前用 `text-gray-400`(#9ca3af,on white **2.51:1**),
而 D5 的全部内容是**那个名字要读得出来**。现在它用 `--brand-muted-glass`,
最坏 4.83:1。一条读不清的具名限制,和一处缺席差得不多。

模糊 `blur(10px)`。与 `/login` 那条不同,这里底下是**页面内容**(文字、表格线、
色块),有真的细节可以糊 —— login 那条实测 ΔE76 只有 0.0495,因为它底下是平滑渐变。

---

## 6 · D7 的两次搬家:谁多了、谁少了(对着 **live 授权**,不是种子文件)

### 6.1 公司执照登记簿 → `/purchasing/licences`

**码没变**(`module.suppliers.view` / `.edit`,见 F2);变的是外面那道门:
`module.finance.view` → `module.purchasing.view`。

| | 之前(要 finance.view + suppliers.view) | 之后(要 purchasing.view + suppliers.view) |
|---|---|---|
| admin · gm · finance · auditor | 看得见 | 看得见 |
| **procurement** | **看不见**(不持 finance.view) | **看得见 ← 唯一的变化** |
| cfo | 页面进得去、面板「受限」 | 同左 |
| operations · warehouse · sales · hr · employee | 进不去 | 进不去 |

**多了一个:procurement。少了:没有人。** Tim 的裁定(A2):
"一个看不见管着他能收什么料的执照的采购角色,本来就是错的形状。"

### 6.2 审批链 → `/settings/approvals`

**码跟着搬**:`module.finance.view` → `action.manage_permissions`。

| | 之前 | 之后 |
|---|---|---|
| admin | 看得见 | 看得见 |
| **gm · finance · auditor · cfo** | **看得见** | **看不见 ← 变化** |
| 其余 | 看不见 | 看不见 |

**live 的真人受影响两位**:一个 `finance` 账号、一个 `cfo` 账号
(live `user_roles`:admin 14 · cfo 1 · finance 1)。
**这正是 Tim 要的那条分离** —— 但请连着 F1 一起读:被拿走的是**一扇只读的窗**,
不是一个控制器,因为控制器不存在。

### 6.3 原处留了指路,而且【进不去的人也看得见它去哪了】

`/finance/company` 与 `/finance/settings` 各留一条交叉引用;进不去的人看到的是
「…… · 受限」,不是一处缺席(D5 对交叉引用同样适用)。地址取自注册表
(`FN.licences.href` / `FN.approvals.href`),搬第二次时它不会指向 404。

---

## 7 · 逐角色实测(live 授权,本地渲染,探针账号用完即删)

| 角色 | 九个里进得去 | 顶栏上具名「受限」的 | 执照 | 审批链 |
|---|---|---|---|---|
| admin | 9/9 | (无) | 可进 | **可进** |
| gm | 9/9 | (无) | 可进 | 拒绝 |
| auditor | 8/9 | 设置 | 可进 | 拒绝 |
| cfo | 6/9 | 人力 · 任务 · 设置 | 可进(面板受限) | 拒绝 |
| finance | 8/9 | 人力 | 可进 | 拒绝 |
| **procurement** | 8/9 | 人力 | **可进(新增)** | 拒绝 |
| operations | 8/9 | 人力 | 拒绝 | 拒绝 |
| warehouse | 8/9 | 人力 | 拒绝 | 拒绝 |
| sales | 7/9 | 人力 · 设置 | 拒绝 | 拒绝 |
| hr | 4/9 | 物流 · 运营 · 财务 · 库存 · 设置 | 拒绝 | 拒绝 |
| employee | 2/9 | 物流 · 运营 · 财务 · 库存 · 人力 · 任务 · 设置 | 拒绝 | 拒绝 |

**没有任何角色的导航条上少了一个模块名** —— 九个永远都在,进不去的写着「· 受限」。

> ★ **一处值得单独说的结果:`employee`(零模块权限)现在有 2/9 是"进得去"的** ★
> 采购与销售 —— 因为**金属行情**挂在这两个模块底下,而它的读策略是 `USING (true)`。
> 这是 Tim 在 D6 里点名的那个例外落地之后的样子:**界面不许比数据严。**
> 他进去之后看到的是一张菜单,里面只有「金属行情」可点,其余每一条都写着「· 受限」。
> 这是对的,但它是一处**可见的变化**,记在这里。
>
> ★★【UI-FIX-1(2026-09-02)更正:这一句仍然对,但它挂靠的位置搬了家】★★
> ⑦ 把金属行情搬进了**工具**,于是 `employee` 现在是 **1/9 —— 工具**,
> 不再是「2/9 —— 采购与销售」。**观察本身没有错**(界面不许比数据严,
> 零模块权限的人仍然进得去他读得到的那一页),错的只是模块的名字。
> **上面整张表都受 ⑦ 影响**,搬家之后的完整版见 `docs/nav-registry.md` §8.4。

---

## 8 · 手机(390px)的实测

| 页面 | 视口 | 文档宽 | 横向溢出 |
|---|---|---|---|
| `/`(工作台) | 390 | **390** | **0px** ✓ |
| `/inventory` | 390 | 839 | 449px —— **全部来自 `<table>`** |
| `/finance/invoices` | 390 | 898 | 508px —— **全部来自 `<table>`** |

**外壳本身在 390px 上不溢出一个像素。** 剩下的溢出是**既有的**宽表问题
(FE-0 PART C/e:161 个含表文件里 134 个没有横向滚动容器),越界元素**全部是
`<table>`,没有一个是导航**。那是另一把刀,本刀不碰。

落地过程中修掉的两处**本刀自己造成的** 390px 缺陷,记下来因为它们都很容易漏:

1. **顶栏右侧那一组溢出 6.45px**(实测宽 265.75,右边缘 396.45,视口 390)——
   于是整个文档横向滚动。修法不是缩字,是把 `/me`、`/my-reviews` 在 <sm 时
   挪进抽屉的「关于你」一段。
2. **dock 在横向可滚动的页面上被拉到了【文档宽度】**:`fixed inset-x-0` 在
   `/finance/invoices` 上实测宽 **898px**(视口 390),于是右半条停在屏幕外,
   而它看起来只是"东西有点多"。加 `w-screen` 钉回视口。

---

## 9 · 落地过程中被门抓到的三条真缺陷

记下来,因为它们都属于"漏了不会报错"的那一类:

1. **一个模块只要名下有一条带 `group` 的跨属主条目,它自己没有 `group` 的条目就
   整批不见了。** 实测:物流只画出 1 条(运费),货代/航线/货柜三条全没了;
   库存只画出 1 条(报表),六条全没了;运营只画出 1 条(毛利)。
   修法:第三级**只给财务算**(`FINANCE_MODULE_ID`),外加渲染层一道兜底
   (分了组也把未分组的画在后面)。**同一个形状不该只有一道防线。**
2. **`dockActions.ts` 丢掉了 `auth.getUser()` 的 error**(3 处)——
   `check-auth-error-swallowing` 抓的。后果是一次网络抖动会被读成"这个人没有 dock",
   于是他排好的快捷栏被默认值悄悄顶掉。现在按
   `AuthRetryableFetchError` = 判断不出 / 其余 = 确立的否定 分开。
3. **深路由清单过期**(新增了两页)—— `gen-deep-routes` 比对当场变红。
   这条门在落地的当天就证明了自己不是摆设。

---

## 10 · 门的改动

| 文件 | 改了什么 |
|---|---|
| `scripts/gen-deep-routes.mjs` | **新增**。生成 + 比对深路由清单与面包屑段名 |
| `scripts/check-i18n.mjs` | MANIFEST 加 `breadcrumb.` 前缀,后缀集合从生成文件现读;`licenceErrorCodes` 路径跟着搬 |
| `scripts/check-permission-predicate.mjs` | 渲染层名单换成 `nav/` 三件;②**收窄到跨模块条目** |
| `package.json` | build 链里加 `gen-deep-routes` 比对 |

★ **②那一条的收窄要说清楚,因为它看起来像放松** ★
本条不变量的原话是"**跨模块**功能的入口必须由注册表派生"。此前 `FUNCTIONS` 里
**只有**跨模块的两条,所以"遍历全部条目"与"遍历跨模块条目"恰好是同一件事。
本刀把 `FUNCTIONS` 扩成了每一个二级条目(单属主的也在里面),两者从此不同:
一条单属主条目的"返回列表"链接(`app/suppliers/new` → `/suppliers`)**不是**
第二份定义,它就是同一个模块里的一条普通链接。
**这不是放松,是把它收回到它本来的范围** —— 11 条跨模块条目一条不漏,全部照旧受管。
落地时它抓到三处真的手写跨模块入口(`app/pricing/page.tsx` → `/metal-prices`、
`app/suppliers/page.tsx` → `/contracts` 与 `/commissions`),已改成从注册表取地址。

---

## 11 · 本刀【没有】做的

* **审批链的配置界面** —— F1,排队,触发点是同事测试轮;
* **卡片与面板的磨砂** —— R2 明说这一刀不做,等淡底落地看得出效果再判;
* **宽表在手机上的横向滚动容器** —— 134 个文件,既有问题,另一把刀;
* **`module.settings.view` 这个码** —— 铸码是迁移级动作,NAV-REG-1 的 R3 里
  Tim 保留给自己的裁定。设置的可进性因此与其余八个一样,由二级条目推导;
* **`docs/exec-views-plan.md` 的更新** —— 勘察 G0 点名了 7 行过期内容,仍然排队;
* **`/finance/claims` 与 `/finance/cash-forecast` 丢掉守卫返回值** —— 那两处已由
  GUARD-FIX-1 修掉,`check-permission-predicate` 的 ④ 现在盯着 178 处调用点。

---

## 12 · CHART-0(2026-09-02)—— Tim 在部署系统上看到的五件事

本节记 CHART-0 第一部分改掉的东西。图表勘察在 `docs/charts-scoping.md`。

### 12.1 ① 浮层的玻璃太透 —— 而【真正的病是模糊一帧都没生效】

Tim 报的是"菜单太透,底下的字能读"。能读是真的,**但透明度只是从犯。**

**实测(稳定别名,admin 会话,1440×900。做法:把菜单自己的字藏起来只留玻璃,
截图量整块菜单区域最亮/最暗合成像素之比 = 底下那一页透过来的对比度。)**

| | 读透对比度 |
|---|---|
| 菜单开在 `/finance/invoices` 的密表上 | **1.260:1** ← 能读 |
| 菜单开在 `/tasks` 的看板上(Tim 看到的那一屏) | **1.216:1** ← 能读 |

1.2:1 的**锐利**文字读得出来。而它锐利,是因为那 10px 模糊**一帧都没生效**。

**根因:下拉是顶栏的后代,而顶栏自己就是一个 Backdrop Root。**
`backdrop-filter` 的滤镜源只到"最近的 Backdrop Root 之内、画在它背后的东西";
带 `backdrop-filter` 的元素自己就开一个 Backdrop Root。下拉从顶栏**底下**垂出去,
那一块是空的。**判据(逐像素相减,把下拉自己的底色与内容全部去掉只留滤镜):
平均差 0.000,最大差 0 —— 一个字节都没变。**

**同一个根因还砸了手机抽屉,而那一处更严重。**
带 `backdrop-filter` 的元素同时是 `fixed` 后代的**包含块**。抽屉写的是
`fixed inset-0`,对齐的于是是顶栏不是视口。**实测 390×844:抽屉 94px 高、
面板 105px 高、里面 583px 内容** —— 九个模块挤在一条缝里,而 IA-BUILD-1 的
抬头写的是"一张全高的抽屉"。**这个缺陷此前没有任何人报过。**

**修法**:顶栏自己不再带 `backdrop-filter`,玻璃改由一个铺满它的子元素
`.nav-glass-underlay`(`absolute inset-0; z-index:-1`)来画。顶栏于是既不是
Backdrop Root 也不是包含块,而看上去逐像素不变。

**每一个浮动层的值(它们【共用一个 token】`.nav-glass`,所以只改了一处)**

| 层 | 改前 | 改后 | 模糊改前 | 模糊改后 |
|---|---|---|---|---|
| 顶栏 `<header>` | 白 0.90 | 白 **0.94**(移到 `.nav-glass-underlay`) | 生效 | 生效 |
| 模块下拉 `[role=menu]` | 白 0.90 | 白 **0.94** | **失效** | **生效** |
| 手机抽屉面板 | 白 0.90 | 白 **0.94** | **失效** | **生效** |
| dock(桌面竖栏 / 手机底栏) | 白 0.90 | 白 **0.94** | 生效 | 生效 |
| 表格列菜单 `ui/data-table.tsx:231` | 白 0.90 | 白 **0.94** | 生效(不在顶栏里) | 生效 |
| sampler 面板 `brand-sampler/Base1.tsx:196` | 白 0.90 | 白 **0.94** | 生效 | 生效 |

**六层共用一个 token,所以是改了一次。** 模糊半径两边都是 `blur(10px)`,没动。

**把 0.216 与 0.018 分开说 —— 因为 Tim 要的是"大幅降低透明度",
而实测说透明度不是主因:**

| | 读透(最坏情形,`/finance/invoices` 密表) |
|---|---|
| 0.90 + 模糊失效(今天) | 1.260:1 |
| 0.90 + 模糊生效 | **1.044:1** ← 光修模糊就到这 |
| **0.94 + 模糊生效(本刀)** | **1.026:1** |
| 0.96 + 模糊生效 | 1.017:1 |

**修模糊拿走 0.216,收透明度只拿走 0.018。** 仍然收到 0.94,理由有二:
Tim 要"浮层必须**遮住**它浮着的东西";而模糊糊得掉细笔画、糊不掉一整片暗色
(全站最深的 `bg-gray-900 #111827` 有 62 处),那一片只能靠不透明度压。
**没有再往上收到 0.96**,因为那时读透只剩 1.017:1,底下那层就**看不出来**了 ——
Tim 的原话是 "perceptible, NEVER legible",看不出来是把另一头做过了头。

**菜单自己的字对着最坏合成色(实测,不是推算)**

| | 最坏合成 | `--brand-text` | `--brand-muted-glass` |
|---|---|---|---|
| 0.90 / 模糊失效 | `rgb(229,229,229)` | 11.22:1 | 4.70:1 |
| **0.94 / 模糊生效** | `rgb(252,252,252)` | **13.77:1** | **5.78:1** |

两档都过 AA;本刀把最紧那一档的余量从 0.20 抬到 1.28 个百分点。

### 12.2 ② 九个模块根逐个查过 —— 别名【只有一个】

判据:**别名**(重定向或渲染得与另一条相同)→ 删;**真落地页**(有自己的内容)→ 留。

| 模块 | 二级里的根条目 | 那一页今天是什么 | 判词 |
|---|---|---|---|
| 采购 | `/purchasing` 「Purchasing home」 | **全文 14 行,主体是 `redirect('/purchasing/orders')`** | **删** |
| 运营 | `/processing` 「Processing runs」 | 251 行加工单列表(日期区间 + 分页) | 留 |
| 库存 | `/inventory` 「Overview」 | 439 行库存与物料平衡(到岸成本估值) | 留 |
| 人力 | `/hr` 「Overview」 | 154 行待处理看板(准证到期 / 试用期 / 证书到期)+ 在职摘要 | 留 |
| 财务 | `/finance` 「Trial Balance」 | 190 行试算平衡 | 留 |
| 任务 | `/tasks` | 61 行看板,且是本模块**唯一**一条 | 留 |
| 销售 | 没有 `/sales` 条目 | 也没有 `app/sales/page.tsx` | 无 |
| 物流 | 没有 `/logistics` 条目 | 也没有 `app/logistics/page.tsx` | 无 |
| 设置 | 没有 `/settings` 条目 | 也没有 `app/settings/page.tsx`(D2:它不需要有) | 无 |

**采购那一条的第二个独立证词**:`scripts/smoke-routes.mjs:587` 早就把它记成
`'/purchasing': [307]`;而 `app/purchasing/Subnav.tsx` 的 `ITEMS` **从来就没列过它**
(第一条是 `/purchasing/orders`)。三处彼此独立,指向同一个结论。

**财务 REPORTS 组里那条「Reports」——【不是】别名,保留。**
它是 `/inventory/reports`(`inventory.subnav.reports`),一个跨属主条目
(库存 + 财务,`group: 'finance.group.reports'`),渲染的是**四张只读报表的入口卡片**
(snapshot / violations / safety / ledger),与财务自己那六条报表毫无重叠。
**但要照直记一件事:它在财务菜单里的标签就是「Reports」,而它所在的组也叫
「Reports」——「报表」套着「报表」,在那个位置上等于什么都没说。**
本刀**没有**改它:那一条 navKey 同时供库存菜单用(在那里它读作"库存 → 报表",
完全通顺),改标签会同时改掉另一处。**这是一个留给 Tim 的措辞裁定,不是一个缺陷。**

**★ 删除为什么安全 —— 以及为什么这条理由【只对采购成立】★**
模块名在顶栏上是**展开菜单的按钮,不是链接**(D2)。
**所以一级永远到不了模块根。** 一条二级条目删掉,它指的那一页就从导航上消失了。
采购那一条是例外,**只因为它指的那一页本身就是一次跳转**,而跳转的终点
`/purchasing/orders` 就在同一个菜单的下一行、自己是一条条目。
换句话说:**这一刀删的是重复,不是入口。** 若照同样手法去删
`/finance`、`/inventory`、`/hr`,那三页就会**从导航上彻底消失** ——
那不是去重,是把页面弄丢。

顺带:`purchasing.subnav.overview` 两个语种的文案一并删掉(它没有第二个读者)。

### 12.3 ③ 长菜单现在自己说它还有下文

**实测**:财务菜单 **1249px 内容装在 628px 里**(1440×900,admin),
**16 条完全在视野之外**,而屏幕上没有任何东西说这件事。

**选的不是渐隐,也不能只靠滚动条。** 第一版只做了滚动条,实测把它否了:
量 `offsetWidth − clientWidth`,在 headless Chrome 152 / macOS 上是 **0px**;
拿一张最小页面单独试三种写法(无样式 / `::-webkit-scrollbar{width:10px}` /
`scrollbar-width:thin`)**三种都是 0** —— 这台机器上的 Chrome 无条件用覆盖式
滚动条,CSS 掰不动。**一个演示不出来的信号不算修好。**

**最终的修法:我们自己画一行字**(`ModuleBar.tsx` 的 `MoreBelow`),
贴在滚动容器底边,说的是**条数**不是"往下滚"——条数可核对,手势不可核对;
数的是**完全落在视野外**的行(逐个量 `offsetTop`,不按平均行高估算,
因为分组标题/缩进项/普通项高度并不相同);**滚到底就消失**(那时它是假的)。

| | 实测 |
|---|---|
| 桌面 1440×900,财务菜单 | 「↓ 16 more below」;滚到底 → **消失 ✓** |
| 390×844 抽屉里展开财务 | 面板 788px 装 1835px、34 行 → 「↓ 19 more below」 |

`::-webkit-scrollbar` 的样式**留着当增强**(桌面版 Chrome/Safari 上通常会把
覆盖式换成常驻式),但**那是通行说法不是本刀量到的事实**,所以它不承重。

### 12.4 ④ dock 搬到左边一条竖栏

桌面 = 左竖栏(与顶栏分开:一条管结构,一条管个人);手机 = 仍是底栏。
两边都可收起,**收起状态跟着人走**(`user_dock.collapsed`,不是 cookie)。
布局在 `app/layout.tsx`:`<TopNav>` 之下开一个 flex 行,
`<DockRail>` 与内容列是两个兄弟(取数从 `TopNav` 搬进了 `DockRail.tsx`)。

**★ 那一列 boolean 逼出来的二义,以及它本来会吃掉谁的 dock ★**
`user_dock` 靠 hrefs 区分三态,而 hrefs 此前是 `NOT NULL DEFAULT '{}'`。
**只要为了记一个 `collapsed` 就得建行,那一行的 hrefs 会拿到 `'{}'` = "本人清空了"。**
于是:一个人只是把 dock 收起来,系统就认为他把 dock 清空了,
展开之后看到一条空栏,而他自己排的那几项**再也回不来**。
迁移因此把 hrefs 改成**可空并去掉默认值**:`NULL` = 从没动过,`'{}'` = 清空了。
连带 `resetDock` 从 `DELETE` 整行改成 `UPDATE SET hrefs = NULL` ——
否则"恢复默认 dock"会顺手把收起/展开的选择也抹掉,而那是两件事。
(线上 `user_dock` 当时 **0 行**,所以这条陷阱伤到的是第一个收起 dock 的人,不是存量数据。)

**实测(本地 production build,admin 会话)**

| | 竖栏宽 | 内容宽(1440) |
|---|---|---|
| 展开 | **192px** | 1248px |
| 收起 | **48px** | 1392px |

**宽表在它旁边怎么样(`/finance/invoices`,该表最小宽约 866px,
且这一页【没有】自己的横向滚动容器 —— FE-0 记的 134 页之一):**

| 视口 | 展开(192px) | 收起(48px) |
|---|---|---|
| 1440 | 表 1184px,**不横向滚** | 表 1328px,**不横向滚** |
| 1024 | **整页横向滚 +66px** | 不横向滚 |
| 768 | **整页横向滚 +322px** | 整页横向滚 +178px |

**照直说:竖栏把"开始横向滚"的门槛从约 914px 抬到了约 1058px。**
横向滚起来时竖栏是 flex 流里的一项,会跟着滚出屏幕左侧。
**收起来能拿回 144px,而这正是"可收起"存在的理由之一。**
1440 及以上两种状态都不横向滚。

**收起态不是消失**:桌面留一条 48px 图标竖栏(每项 `title` + `aria-label` 是
与展开态**逐字相同**的全名;受限/已下架照样画成灰图标 + title 里那句话,
不是拿掉 —— 4b 在收起态同样成立);手机留一条把手(26px)。
`lucide-react` 此前全仓库 0 处引用,本刀是第一处读者。

**跟着人走,实测**:收起 → 换页仍是收起 → **换一个全新的浏览器上下文
(同一个人、另一台"设备")仍然是收起**;并且 dock 里 8 项一项没少、
`data-dock-default=1` —— **收起没有被读成"清空"。**

手机底栏:展开 37px / 收起 26px,`body` 给它留的 `padding-bottom` 是 52px,
两种状态都盖不住最后一行。

### 12.5 ⑤ /login 标语两行各自向中间收 8%

用**百分比**而不是固定 px,正是为了 Tim 点名要查的那件事
("窄卡上两条缩进会吃掉小得多的宽度"):百分比对着内容盒算,**卡片越窄缩进越小**。
实测(`createRange` 量**墨迹**边缘 —— span 是 `display:block`,盒子永远占满宽,
`text-align` 在盒子上看不出来):

| 视口 | 内容盒 | 左缩进 改前→改后 | 右缩进 改前→改后 | 两行左边缘相差 改前→改后 |
|---|---|---|---|---|
| 1280 | 360px | 0.0 → **28.8px** | 0.0 → **28.8px** | 200.2 → **142.6px** |
| 390 | 310px | 0.0 → **24.8px** | 0.0 → **24.8px** | 150.2 → **100.6px** |

墨迹宽 L1 186.91px / L2 159.83px。390px 上第一行右边缘落在 211.7px,
离内容盒右边(310)还有 98px —— 两条缩进都没把任何一行挤到换行,**参差仍然读得出**。
断点仍在逗号处、20px / 字重 500 / 行高 1.15、两种语种下英文都不翻译,一个字没动。

### 12.6 ★ 一条【躺了五刀】的误报:可达性检查把"打不开"读成了"到得了" ★

**这不是 CHART-0 造成的,但它是 CHART-0 跑出来的,所以在这里记清楚。**

CHART-0 改了导航注册表(删掉 `/purchasing` 那条别名)与整个外壳布局,
于是按 `scripts/smoke-routes.mjs` 自己的建议跑了 `--reach=operations`。它红了:

```
✗ operations 预期走不到的 /login 现在走得到了
```

**而 /login 并没有多出任何入口。** 全仓库 `grep` 过:`app/` 与 `lib/` 里
**没有任何一处** `href="/login"`;`/login` 只出现在 `redirect('/login')`
(登出、set-password)、`isPublicPath` 的 import 和注释里。
照着这个检查自己的算法(从 `/` 出发、只收 200 页面里的 `href="`、
碰到 `data-access-denied` 就不再往下走)复跑一遍:**爬完同样的 187 页,
`/login` 不在 `seen` 里。**

**真正的原因在检查自己身上。** 判据写的是

```js
const unreachable = openable.filter((r) => !seen.has(r))          // ← 只在【打得开的】里挑
const gone = [...EXPECTED_UNREACHABLE[role]].filter((x) => !unreachable.includes(x))
```

一条路由有**三**种状态,而 `gone` 只认两种:

| | 在 unreachable 里? | 被判成 |
|---|---|---|
| 打得开、走得到 | 否 | 「走得到了」✓ |
| 打得开、走不到 | 是 | 「走不到」✓ |
| **打不开** | **否** | **「走得到了」✗** |

第三种把**够不着**读成了**到得了** —— **与这个仓库反复付账的「缺席 ≠ 空」
是同一个形状,只是这一次发生在检查自己身上。**

**它从哪一刀起必然失败。** LOGIN-1-fu1(`210c4d6`)让【已登录的人】打开
`/login` 时重定向走。实测(稳定别名,operations 会话):

```
/login  已登录(operations) → HTTP 307  Location: /suppliers  content-type: text/plain
/login  未登录              → HTTP 200  content-type: text/html
```

而 `openable` 是拿一个**已登录**会话试出来的。所以从 `210c4d6` 起,
`/login` 永远进不了 `openable` → 永远进不了 `unreachable` → `gone` 永远含着它。
**这条误报对三个角色都成立,与改了什么无关。**

**为什么五刀没人发现**:上一次跑绿是 GUARD-FIX-1(`7759848`)记下的
`REACHFIN2_EXIT=0`,而 `7759848` **早于** `210c4d6`。这个检查默认不跑
(它太慢,GUARD-FIX-1 的抬头解释过为什么改成显式开启),于是从 LOGIN-1-fu1
到 BASE-1 之间**没有任何一次运行**。CHART-0 是那之后的第一次。

**修法:直接问那个真正的问题。**

```js
const gone = [...EXPECTED_UNREACHABLE[role]].filter((x) => seen.has(x))
```

"走得到"本来就只与**爬到没爬到**有关,与打不打得开无关。
★ **这不是把断言放松** ★:一条真的多出了入口的路由会进 `seen`,照样响。
**放松的写法是把 `/login` 从 `EXPECTED_UNREACHABLE` 里删掉** ——
那是"把债划掉不是还债",而且会顺手把"哪天真给 `/login` 加了入口"这件事
一起盖掉。`unexpected`(打得开却走不到)那一半**一个字没动**。

**这条检查自己的抬头早就写过这句话**:「误报比漏报更坏,它教人忽略这条检查」——
那是首跑时把 `/settings/permissions` 报成"走不到"之后写下的。同一条道理,
这一次落在它自己身上。

---

## 13 · UI-FIX-1(2026-09-02)—— 改名一个模块,搬动五条功能

> **【日期上的一句话,免得下一个人以为哪里错了】** 本节里所有标着 **2026-09-02**
> 的东西 —— Tim 的裁定、live 授权的那次取数、逐角色的求值 —— **都是 09-02 做的**,
> 而**这一刀的提交落在 09-03 的凌晨**(跨了午夜)。两个日期都是真的,没有一个是推出来的。
> 本仓库对"照着上一个文件名往前加一天"付过账,所以这里把话说明白而不是挑一个看起来齐整的。

**先说一件与内容无关、但影响这份文档怎么读的事。**
UI-FIX-1 的指令写着"预期 HEAD 是 `526e119`(BASE-1)"。**实际的 HEAD 是
`c7e0a3a`**,中间隔着 `ced7a26`(CHART-0)与 `c7e0a3a`(CHART-0-fu1)。
**于是那份指令里的 ①②③④⑤ 在它被执行之前就已经建好了** —— 全部由 CHART-0
交付,实测与数字记在上面的 §12。本节只记 UI-FIX-1 真正新做的那一半:
**⑥ 改名、⑦ 五条搬家、⑧⑨ 记录、⑩ 报告**。
①–⑤ 的复核结论写在 §13.4,那里逐条对着完成定义核过,没有一条是"想必已经好了"。

### 13.1 ⑥ 一级模块「任务」→「工具」

Tim 的理由:任务一个人撑不起一个一级模块(**2 页**,对着财务的 54 页),
而他**不要**把它折进别的模块,所以这一格改成**小工具的去处**。

| 动了什么 | 从 | 到 |
|---|---|---|
| `MODULES[].id` | `tasks` | `tools` |
| `MODULES[].navKey` | `nav.tasks` | `nav.tools` |
| `messages/en.ts` `nav.tools` | (无) | `Tools` |
| `messages/zh.ts` `nav.tools` | (无) | `工具` |
| `FUNCTIONS` `/tasks` 的 `modules` | `['tasks']` | `['tools']` |

**★ `nav.tasks` 留着,而那不是一处忘记删的死键 ★**
它有两个活读者:`FUNCTIONS` 里 `/tasks` **那一条二级条目自己的标签**
(工具菜单里那一行仍然读作「任务」——**改名换的是模块,不是那一页**),
以及 `SCOPES` 里 `/tasks` 拒绝页的标题。

**★ 权限范围一个字没动 ★** `SCOPES` 里 `/tasks` 仍然是 `module.tasks.view` ——
那是 `tasks` 表 RLS 上写着的码,与顶栏叫什么无关。这正是 `lib/modules.ts`
抬头 §一 那条「导航重排,门一个字不动」。**没有铸新码,没有迁移。**

### 13.2 ⑦ 五条搬家 —— 全部经多属主注册表,零 DDL

搬完之后每个模块的菜单(从注册表直接导出,不是手抄):

| 模块 | 条数(前 → 后) | 变化 |
|---|---|---|
| 采购 | 13 → 8 | **−5** `/pricing` `/pricing/formulas` `/pricing/calculator` `/metal-prices` `/deleted` |
| 物流 | 4 → 4 | — |
| 运营 | 7 → 6 | **+1** `/inbound` · **−2** `/margin` `/deleted` |
| 销售 | 10 → 5 | **−5** `/pricing` `/pricing/formulas` `/pricing/calculator` `/metal-prices` `/deleted` |
| 财务 | 32 → 31 | **−1** `/inventory/reports` |
| 库存 | 8 → 7 | **−1** `/deleted` |
| 人力 | 10 → 10 | — |
| **工具**(原任务) | **1 → 5** | **+4** `/pricing` `/pricing/formulas` `/pricing/calculator` `/metal-prices` |
| 设置 | 4 → 5 | **+1** `/deleted` |

> ★【财务那一行容易读错,所以点出来】★ `/margin` 搬成"财务独占"**没有给财务的
> 菜单加东西** —— 它本来就是"运营 + 财务"双属主,财务那一侧一直看得见它。
> 这一刀收窄的是**运营**那一侧。所以财务只 **−1**(库存报表搬回库存),不是 +1−2。
> **这张表是从注册表直接算出来的**(两份 `lib/modules.ts` 同时 import,逐模块取差集),
> 不是手抄的 —— 手抄的第一版在四行上都算错了。

`scripts/check-permission-predicate.mjs`:**FUNCTIONS 76 条(4 条跨模块)** ——
跨模块的四条现在是 `/commissions`、`/inbound`(**三属主**)、`/output`、
`/finance/freight`。

#### 13.2.1 定价 → 工具,独占 ——【这一条推翻了 D6 as built】

**被推翻的是什么。** IA-BUILD-1 的 **D6** 把定价与行情同时挂在采购与销售之下,
理由原话是「定价服务采购(payable %、扣杂)与销售(报价)两侧;塞进任何一侧
都会让另一侧看不见」。**2026-09-02,Tim 就定价这一件事推翻了它。**
那段推理**不再生效**,并且已在 `lib/modules.ts` 共有区里就地改写 ——
**留着一句作废的理由比删掉它更危险**,因为下一个读者会以为它还作数。

**★ 被推翻的是这一族的摆放,不是多属主机制 ★** 机制仍然在用,而且本刀刚给它
添了第一条三属主的条目(`/inbound`)。

**判据一个字没动**:仍然是 `module.pricing.view`。搬的是它**在哪个菜单里**,
不是**谁进得去**。

#### 13.2.2 金属行情 —— 这一条【问过 Tim 才动】,而那不是客套

指令写着「`/pricing` 整体」,并明确要求:**若金属行情是单独放置的,要问,不要猜。**
**查下来它确实是单独放置的**,三处都与 `/pricing/*` 那一组不同:

1. 它是 `FUNCTIONS` 里**单独的一条**,不在定价那三条的任何分组里;
2. 它走**另一条路由树**(`app/metal-prices/`,四页),不是 `/pricing/*` 的子路径;
3. 它带**全站最松的判据** `{ all: [] }`(任何登录用户),因为 `metal_prices`
   的 SELECT 策略是 `USING (true)` —— 读是公开的,只有写受管。

**Tim 2026-09-02 的裁定:一并搬进工具,采购与销售两侧都不再有这个入口。**
**判据一个字没动** —— 仍然是 `{ all: [] }`,任何登录用户都看得见这个入口。

> **顺带记一件不会坏的事:** `lib/dock.ts` 的候选清单把 `/metal-prices` 当作
> **兜底的最后一条**(靠的正是那个最松的判据,好让零模块权限的人也不会看见空
> dock)。**属主换成工具不影响它** —— dock 认的是 `href`,不是属主模块。

#### 13.2.3 被删记录 `/deleted` → 设置,独占 ——【★ 唯一一条真的改了谁进得去 ★】

Tim 裁定这一页是**审计性质**的,不是日常的,所以它由**设置那一侧的码**把关。

**选的是 `action.manage_permissions`,而这是一个需要说明的判断。**
「设置的谓词」没有一个现成的东西可指:**系统里根本没有 `module.settings.view`
这个码**(IA-BUILD-1 明确地没有铸它,铸码是 Tim 保留的裁定)。设置这个一级的
可进性是从它名下三条二级**推导**的。于是只有两个候选:

* **推导出来的那个并集** = `manage_permissions` OR 字典编辑 OR 批量导入。
  **否掉了**:一个**能改物料字典**的人因此看得见审计页,不连贯。
* **`action.manage_permissions`** —— 它同时把着 `/settings/permissions` 与
  `/settings/approvals`,也就是设置这个模块**治理的那一半**。**取这个。**

**★ 实测的代价,对着 live 授权(`role_permissions`,2026-09-02),一条不隐瞒 ★**

| 角色 | 改动前看得见 | 改动后 | |
|---|---|---|---|
| admin | ✓ | ✓ | |
| **auditor** | ✓ | **✗** | ★ 见下 |
| **cfo** | ✓ | **✗** | |
| **finance** | ✓ | **✗** | |
| **gm** | ✓ | **✗** | |
| **operations** | ✓ | **✗** | |
| **procurement** | ✓ | **✗** | Tim 预期 |
| **sales** | ✓ | **✗** | Tim 预期 |
| **warehouse** | ✓ | **✗** | Tim 预期 |
| hr | ✗ | ✗ | 本来就没有 |
| employee | ✗ | ✗ | 本来就没有 |

**9 个角色 → 1 个角色。** Tim 预期会失去的是采购 / 仓库 / 销售 —— **它们确实
失去了**;**而一并失去的还有 auditor、gm、cfo、finance 与 operations。**

> ★★【留给 Tim 再看一眼的一条:auditor】★★
> 一页被判为「审计性质」,而执行这条裁定的结果是**把审计角色本人挡在外面**。
> `auditor` 今天持有那六个模块码的全部,是**除 admin 外唯一能看全所有六类被删
> 记录**的角色。本刀**照裁定执行**,把这件事照直写在这里,而不是替他改判 ——
> 但如果这不是他要的,改法只有一行:把谓词写成
> `{ all: [], any: ['action.manage_permissions', <审计那个码>] }`。

**行一级的过滤一字未动。** 视图 `deleted_records` 每一行仍然自带 `permission`
列并由 `has_permission` 裁决,所以**进来之后看得见哪几类**与从前完全一样。
本谓词只回答「这一页对你有没有意义」。

#### 13.2.4 收货 `/inbound` 多一个属主:运营 ——【纯粹的增加】

Tim 的理由:车间需要知道什么料到了。
**没有任何人失去东西**:判据一个字没动(`module.inbound.view`),采购与库存两侧
的入口原样留着,运营的菜单里多了一条。
**它是注册表里第一条【三】属主的条目** —— `modules` 本来就是数组不是一对,
这一条只是第一次用到第三格,机制一行未改。

#### 13.2.5 批次毛利 `/margin` → 财务,独占 ——【★ 权限明令不变 ★】

Tim:它是一个财务数字,不是一个车间数字。

**★ 谓词逐字不变:`data.view_prices AND (module.finance.view OR module.processing.view)` ★**
**摆在哪、与谁进得去,是两个不同的问题,本刀只动第一个。**

**为的记录:`/margin` 是多属主的【第一个】先例,而那条先例仍然作数。**
MAR-1 让它同属加工与财务;NAV-REG-1 引的正是**它的 AND 合取**,作为「注册表
既需要 OR 也需要 AND」的证据。**那个谓词原封不动地活着** —— 收窄的只是属主。

**★【一处必须更正的话:运营那一侧的入口【确实】没了,而这是搬家的直接后果】★**
本刀起草时写过一句「`/processing` 页头那个 `/margin` 链接留着,因为它读的是判据
不是属主」。**那句话是错的,读代码时被抓到:**
`app/processing/page.tsx` 的页头渲染的是 **`getFunctionAccess('operation')`** ——
**它按【属主模块】取条目**,再对每条问判据。属主收窄成财务,那一条就不再进这个列表。

**实测这个页头前后渲染什么:**

| | 条目 |
|---|---|
| before | `/processing/orders` `/processing` `/processing/wip` `/processing/handovers` `/output` **`/margin`** **`/deleted`** |
| after | `/processing/orders` `/processing` `/processing/wip` `/processing/handovers` **`/inbound`** `/output` |

**少了 `/margin` 与 `/deleted`,多了 `/inbound`** —— 三条**全部**是本刀明令的搬家
(`/deleted` 那一条连门都换了),**所以这是指令要的结果,不是一处回归**:
Tim 的原话就是「他们现在从财务底下到达它」。**而 `/inbound` 出现在这里,正是 ⑦
「车间需要知道什么料到了」那一条自己长出来的样子** —— 没有写一行页面代码。

**"走得到"因此仍然成立,只是路不同了**:见 `docs/nav-registry.md` §8.2 ——
11 个角色里没有任何一个丢掉 `/margin`。一个只持 processing + `view_prices` 的人
(live 上今天没有这个角色)会在顶栏看见**财务可进**,因为 `/margin` 自己就撑开了它,
进去之后只有 `/margin` 可点、其余写「· 受限」。

> ★★【顺带抓到一处【本刀之前就有】的重复,报告但不修】★★
> 那个页头的注释写的是「**加工模块名下的跨模块功能**」,而 `getFunctionAccess('operation')`
> 返回的是**运营名下的全部条目**,不只是跨模块的那些。于是页头把
> `app/processing/Subnav.tsx` 硬编码的那 **4 条(工单 / 加工单 / 在制品 / 交接班)
> 又渲染了一遍** —— 子导航在上,页头在右,同样四个名字出现两次。
> **这不是 UI-FIX-1 造成的**(前后都是 4 条重复),但 ⑦ 让这个页头的内容变了,
> 所以它是在这一刀里被看见的。**修法是一行**(`.filter(f => f.modules.length > 1)`,
> 与注释的措辞对齐),**但改它就是改渲染,超出本刀的范围** ——
> 而且"页头到底该放哪几条"是一个产品判断,不是一个笔误。
> **连同下一刀那两处活动态高亮一起处置。**

#### 13.2.6 库存报表 `/inventory/reports` → 库存,独占 ——【这是纠错,不是去重】

CHART-0 ② 查过它**不是别名**(§12.2),把「报表套着报表」的措辞问题留给了 Tim。
**2026-09-02 Tim 裁定:它是【库存】报表,摆在财务底下是一个错误。**
那一页自己印的就是 “Inventory reports — read-only views over stock as it stands”。

**★ 谓词逐字不变:`module.inventory.view` ★** 改动前进得去的人,改动后一个不少。
**`group` 一并去掉** —— 只有财务有第三级,留着一个 `finance.group.*` 而属主里
没有 finance,是一个没有读者的字段。

**顺带解掉的那个措辞问题**:财务的「报表」组里不再有一条叫「Reports」的东西。

### 13.3 两条「权限不变」的证明 —— 逐角色求值,不是断言

**★ 为什么必须探,而不是"看谓词没改所以没变" ★** 谓词确实一个字没改,但
**入口是由属主模块渲染的**,而拒绝是由 `requireFunction` 判的 —— 两者若不同源,
"摆放收窄"就会悄悄变成"访问收窄"。本仓库对这一族已经付过账(OPS-15 那条
"一条规则两个实现,迟早各错一次")。所以判据是**真的开一次那一页**。

见 `docs/nav-registry.md` 的实测段。结论:

* **`/margin`** —— 一个**只持有 processing + data.view_prices**(没有 finance)的
  角色,改动后**仍然打得开** `/margin`。他现在从**财务**的菜单里看见它。
* **`/inventory/reports`** —— 改动前打得开的人(任何持 `module.inventory.view`
  的角色:admin / auditor / finance / gm / operations / procurement / sales /
  warehouse)**改动后一个不少地仍然打得开**,只是从库存菜单进。

**★ 而那份实测给的结论比"这两条不变"更强 ★**
它对 **11 个角色 × 76 条条目**逐个求了「走得到吗」并取前后差集:
**唯一走不到了的东西是 `/deleted`**,而那是 Tim 明令的那一条。
**其余四条搬家,没有任何一个角色丢掉任何一页。**

**★ 关于 `/margin` 那个探针,有一件必须照直说的事 ★**
Tim 要的是「拿一个**真角色**探」。**live 上今天没有那个形状的角色** ——
同时持有 `module.processing.view` 与 `data.view_prices` 的只有 admin / auditor / gm,
**而这三个都同时持有 `module.finance.view`**。所以那一探用的是**合成权限集**
(`allows()` 唯一的输入形状),并另外做了谓词字面量的逐字比对。
**不是没探,是那个角色不存在** —— 顺带说明 `/margin` 谓词里"加工"那一半
**今天没有任何 live 读者**。全部推理见 `docs/nav-registry.md` §8.3。

**★ 部署之后又证了第三遍,而那一遍最硬 ★** 在稳定别名上拿 **11 个真角色**逐个
**把那两页真的打开了**:`/margin` 5 个人、`/inventory/reports` 8 个人,
**与搬家前逐字相同**;`/deleted` 从 9 个人收到 **1 个(admin)**。
顶栏也逐角色核过:**每个角色都是九个一级**,进不去的写「· Restricted」,
而且十一个角色的数字与 §8.4 的预测**一个都没偏**。
表在 `docs/nav-registry.md` §九。

**⑦ 有一处【可见的】副作用,它不是缺陷但必须报出来。** 模块可进性是从二级条目
**推导**的(§2.2),所以搬走一条**独自撑着某个模块**的条目,那个模块名就会从
「可进」变成「· 受限」:`cfo` 6/9→4/9、`operations` 与 `warehouse` 8/9→6/9、
`sales` 7/9→5/9、`hr` 4/9→2/9、`employee` 2/9→1/9(并新得到**工具**)。
**九个模块名一个都没有从顶栏上消失** —— 进不去的写着「· 受限」,那正是 D5。
**逐条的因果与"页面一个都没丢"的证明见 `docs/nav-registry.md` §8.4。**

### 13.4 ①–⑤ 的复核 —— 逐条对着完成定义核过

| | 完成定义要的 | 状态 | 证据 |
|---|---|---|---|
| ① | 玻璃不再让页面文字读透,对着最坏合成报数 | **CHART-0 已交付** | §12.1:1.260:1 → **1.026:1**(`/finance/invoices` 密表,worst case)。**六个浮动层共用一个 token** `.nav-glass`(0.90 → **0.94**,blur 10px),所以只改了一处 —— **它们本来就没有被各自设过值**(本刀自己点了一遍,**实测还有第七处**跟着它走:表格的列筛下拉 —— 见 §13.4b)。顶栏另有一份逐字相同的 `.nav-glass-underlay`(承载元素不同,不是第二个值)。菜单自己的字对最坏合成:brand-text **13.77:1**、muted-glass **5.78:1**,两者都过 AA |
| ② | 九个模块根逐条判词,别名删掉,真页面留下 | **CHART-0 已交付** | §12.2 九行表齐全:别名 **1 条**(`/purchasing`,已删),真页面 **5 条**(留),**3 个模块根本没有根页面**(销售 / 物流 / 设置)。**Tim 那个安全前提的答案是"否"**:一级是**展开菜单的按钮,永远到不了模块根**,所以删条目一般会**弄丢页面**;采购那一条是例外,只因为它指的那一页本身就是一次跳转、终点就在同一菜单的下一行 |
| ③ | 长菜单可见地说明还有下文,按财务真实长度演示 | **CHART-0 已交付** | §12.3:自绘的一行「↓ 16 more below」(不是滚动条 —— 本机 Chrome 无条件用覆盖式滚动条,实测三种写法都是 **0px**,一个演示不出来的信号不算修好)。桌面 1440×900 财务菜单 16 条;**390px 抽屉里展开财务 19 条**;滚到底那一行消失 |
| ④ | 桌面左竖栏 / 手机底栏,两边可收起,收起状态活过一次登出 | **CHART-0 已交付** | §12.4:竖栏 **192px**,收起 **48px**(图标宽的一条,不是消失)。宽表实测:竖栏把"开始横向滚"的门槛从约 **914px** 抬到约 **1058px**,收起拿回 **144px**。收起状态存 `user_dock.collapsed`(**跟着人走**),同一支迁移把 `hrefs` 改成可空 —— 否则"为记一个偏好而建行"会把"没动过"静默变成"清空了" |
| ⑤ | 标语两行横向靠拢,390px 上复核 | **CHART-0 已交付** | §12.5:两行各 `padding-inline-*: 8%`(**百分比,所以窄卡缩进自己更小** —— 正是 Tim 点名要查的那件事)。1280:缩进 0 → **28.8px**,两行左边缘相差 200.2 → **142.6px**;390:缩进 0 → **24.8px**,相差 150.2 → **100.6px**,参差仍然读得出。断点仍在逗号处、20px / 字重 500 / 行高 1.15 / 两种语言都不翻译 —— 一个字没动 |

### 13.4b ① 的"逐层报值"—— 本刀独立点过一遍,不是转述 CHART-0

① 要求「**检查 IA-BUILD-1 处理过的每一个浮动层,逐个报值;共用 token 就改一次并说明,
分别设过就是本身值得报告的一件事**」。CHART-0 写的是"六个浮动层共用一个 token"。
**本刀把这句话自己验了一遍**(`grep -rn 'nav-glass' app/`),结果如下 ——
**它们【全部】只声明类名,没有任何一处自己写 `background-color` 或 `backdrop-filter`:**

| # | 浮动层 | 位置 | 用的是 |
|---|---|---|---|
| 1–2 | **顶栏**(两条)| `app/components/TopNav.tsx:52` / `:98` | `.nav-glass-underlay` |
| 3 | **模块下拉菜单**(桌面)| `app/components/nav/ModuleBar.tsx:265` | `.nav-glass` |
| 4 | **手机抽屉面板** | `app/components/nav/ModuleBar.tsx:306` | `.nav-glass` |
| 5 | **dock 左竖栏**(桌面)| `app/components/nav/Dock.tsx:274` | `.nav-glass` |
| 6 | **dock 底栏**(手机)| `app/components/nav/Dock.tsx:312` | `.nav-glass` |
| (+) | 表格的列筛下拉 | `app/components/ui/data-table.tsx:231` | `.nav-glass` |
| (—) | 取样页 | `app/brand-sampler/Base1.tsx:196` | `.nav-glass`(不是生产页面) |

**所以答案是【共用一个 token,改一处】,而不是"分别设过"** ——
`.nav-glass` 与 `.nav-glass-underlay` 是同一个值的两个承载(`--brand-surface` 94% +
`blur(10px)`),CHART-0 只动了这一处,六层同时跟着走。
**表格的列筛下拉是第七个用它的地方**,IA-BUILD-1 的清单里没有点它的名,
但它同样跟着这一次收紧走 —— 记在这里,好让下一次改这个 token 的人知道自己动了几层。

**★ 全站唯一一处【分别设过】的磨砂,在 `/login` 的卡片上 ★**
`app/login/login.module.css` 的 `.card` 自己写着 `blur(12px)` + 14% 透明度,
**不走 `.nav-glass`**。它**不是**一个浮动层(它不浮在页面内容上,底下是一张渐变),
而且那条模糊**实测几乎什么都没做**(ΔE76 = 0.0495 —— 渐变太平滑,12px 邻域里
本来就是同一个颜色),玻璃感全部来自那 14% 的透明度。
**分开设是对的,但它确实是一处独立的值,按 ① 的要求点名在此。**

### 13.5 ★【本刀改错了一条【可达性断言】,而这条检查自己把它抓了回来】★

**这一节记的是一次【被抓到的错】,不是一次成功的修改。**

`scripts/smoke-routes.mjs` 的 `EXPECTED_UNREACHABLE.operations` 里有一条
`/metal-prices`,旧注释给的理由是:

> metal_prices 的读策略是 `USING (true)`,任何人都**打得开**这一页,
> 而**入口挂在 pricing 模块里,operations 没有那个模块** —— 于是"打得开却走不到"
> 对他成立,并且**是有意的**。

**本刀先把它删了**,推理是:⑦ 之后行情在**工具**底下,而 **operations 进得去工具**
(它持 `module.tasks.view`,`/tasks` 就在工具底下),所以那个入口该走得到了。
**跑了一遍 `--reach=operations`,红的:**

```
✗ operations 打得开却走不到:/metal-prices
REACHOPS_EXIT=1
```

**★ 于是断言恢复原状,而【真正被更正的是写在那里的理由】★**

旧注释那句「入口挂在 pricing 模块里」**在 ⑦ 之后确实已经不成立**了 ——
入口现在挂在工具底下,operations 有工具。**它仍然走不到,原因是另一件事:**

> **这条检查只跟着【服务端 HTML 里已经存在的】链接走,而顶栏的模块菜单是
> 【点开才渲染】的**(`ModuleBar` 的 `{isOpen && …}`,初始 state 为 `null`)——
> **于是整个第二级对这个爬虫都是不可见的。**

爬虫能扩散,靠的是 **dock 的那几条 + 每个模块自己的 `Subnav` + 页内链接**。
而 `/metal-prices` 的页内入口**只有两处,两处都要 `module.pricing.view`**:

* `app/page.tsx` 首页那条「行情陈旧」待办(`permission: 'module.pricing.view'`);
* `/pricing` 那一页的第三张卡(进那一页要 `module.pricing.view`)。

operations 两个都拿不到 → 爬虫走不到 → **断言成立,只是成立的理由换了。**

#### ★★ 一个必须说清楚的区别,它不是文字游戏 ★★

| | operations 能到达 `/metal-prices` 吗 |
|---|---|
| **对【人】** | **能。⑦ 之后能了。** 点顶栏「工具」→ 菜单里就有「金属行情」,可点 |
| **对【这个爬虫】** | **不能。** 那张菜单不在 HTML 里,它看不见 |

**这条断言度量的是后者。它没有说谎,但它的"走不到"比人的"走不到"【宽】。**
这个差别是 **IA-BUILD-1 把二级搬进点开式菜单之后【整体】出现的**,不是这一刀造成的
—— 而**也正因为如此,不该靠删断言来把它抹掉**:删掉之后,这条检查对
`/metal-prices` 就再也不会说话了,而它本来是能说话的。

**要缩小这个差别,得让二级出现在服务端 HTML 里(或者给这条检查一个能展开菜单的
爬法)。那是一件单独的活**,与下一刀的活动态高亮属于同一片区域。

> ★【为什么这一整节留在文档里,而不是把删掉又改回来的过程抹平】★
> **因为那条旧理由现在【是错的】,而错在一个会骗人的方向上**:下一个读它的人会
> 以为"给非定价角色一个入口"这个产品判断还没有做 —— **它已经在 ⑦ 里做掉了**。
> 断言该留,理由该换,而这两件事必须分开说。
> 顺带,这也是"跑那条慢检查"的一次直接回报:**推理是错的,而检查是对的。**

**两跑的实测,并排放着 —— 只有判词那一行不同:**

| | 断言集里有 `/metal-prices` 吗 | 走到 | 打得开(静态) | 走不到 | 判词 |
|---|---|---|---|---|---|
| 第一跑(删掉之后) | **否** | 186 | 35 | 3 | **`REACHOPS_EXIT=1`** ✗ `/metal-prices` |
| 第二跑(恢复之后) | **是** | **186** | **35** | **3** | **`REACHOPS2_EXIT=0`** ✓ |

**三个数字逐字相同** —— 爬到的东西一模一样,变的只有"这 3 条里有没有一条是意料之外的"。
**(而 `scripts/smoke-routes.mjs` 相对 HEAD 是【纯注释】改动:断言集与改之前逐字节相同。)**
两跑之间同样跑完了快的那一半:**234 ok / 8 skipped / 0 FAILED**。


### 13.6 本刀【没有】做的

* **多属主路由的活动态高亮** —— 指令明说留给下一刀,连同第二级的同一个缺陷。
  ⑦ 之后有 4 条跨模块条目(`/commissions`、`/inbound` 三属主、`/output`、
  `/finance/freight`),这个缺陷的暴露面比从前**大**了一条。
  **顺带记下它在面包屑里的样子**:`lib/navTrail.ts` 的 `breadcrumbTrail` 取的是
  `entry.modules[0]` —— **一个多属主的条目在面包屑上只报第一个属主**,而
  `moduleIdsForPath` 明明返回全部。那是同一个缺陷的第三处穿法,一并留给下一刀。
* **⑧⑨⑩ 一件都没建** —— 按指令只记录、只报告。见 §14 / §15 / §16。

**★ 另外四件是【本刀路过时发现的】,全部报告、一件都没修 ★**
它们不在 UI-FIX-1 的范围里,集中列在这里,好让 Tim 在一个地方看完:

| # | 发现的东西 | 为什么不在这一刀修 |
|---|---|---|
| 1 | **`/processing` 页头把 Subnav 的 4 条又渲染了一遍** —— 那句注释写着"跨模块功能",而 `getFunctionAccess('operation')` 返回的是运营名下的**全部**条目(§13.2.5)。**本刀之前就有**,前后都是 4 条重复 | 修法是一行,但"页头该放哪几条"是**产品判断**,不是笔误。与下一刀的活动态高亮同一片区域 |
| 2 | **`lib/dock.ts` 冒充了一个不存在的闸** —— 注释写着「`scripts/check-dock.mjs` 把这条钉死」,而**那个脚本不存在**。人手加 dock 的那一侧确实有机制(`dockActions.ts` 对 `FUNCTIONS` 核),但**默认候选清单没有任何构建期检查**:某一刀删掉一个地址,`defaultDock()` 会**静默跳过它** | 注释**已经就地更正**(冒充存在的闸比没有闸更坏);**建那个闸**是新加一道 gate,超出本刀范围。本刀手工核过 16 条候选全在 |
| 3 | **`--reach` 看不见第二级** —— 顶栏菜单点开才渲染,爬虫只跟服务端 HTML 里的链接(§13.5)。于是它的"走不到"比人的"走不到"宽 | 要么让二级进服务端 HTML,要么给检查一个能展开菜单的爬法 —— **两者都是单独一件活**,而且都要先有产品判断 |
| 4 | **`/margin` 谓词里"加工"那一半今天没有 live 读者** —— 同时持 `module.processing.view` 与 `data.view_prices` 的三个角色(admin/auditor/gm)**都同时持有 finance**(§13.3) | 这是一条**观察**,不是缺陷:谓词写的是规则,不是今天的花名册。记下来是因为它让"拿真角色探"这件事今天做不到 |

---

## 14 · ⑧ 记录:留给【全站变更历史】那份勘察的一个问题

**不在这里决定 —— 那个机制还不存在。**

Tim 问:有了全站变更历史之后,`/deleted` 还需不需要?
**按他给的措辞记下来:**

> 历史回答的是「**这条记录是谁改的、什么时候改的**」—— 它**挂在一条你已经找得到
> 的记录上**。
> 被删记录回答的是「**什么不见了**」—— 而**一个被删掉的东西没法从它自己出发被找到**。
>
> 所以真正的问题是:**那个历史机制会不会【按动作类型、跨表】查得动。**
>
> * **会** → `/deleted` 塌缩成历史的一个过滤视图,这一页可以退休;
> * **不会** → `/deleted` 仍然独立存在。

**这个问题归那份勘察,不归本刀。** 本刀只把 `/deleted` 搬进设置并换了它的门
(§13.2.3);**这一次搬家不预设它将来退不退休**。

---

## 15 · ⑨ 记录:Tim 要放进【工具】的四件,留给后面的勘察

**一件都没建。** 下面是勘察要收的清单。

### 15.1 一个日历,含新加坡公共假期

日期散在每一个模块里 —— 采购单到期、化验期限、质保金到期、执照到期、月结、
班次 —— **而没有任何一个地方看得见一个月的全貌**。

> ★★【必须记下的那条连接,它可能比日历本身更要紧】★★
> **`index_market_calendar` 自建成起【一直是空的】**,每一次报价期均价都**按名
> 拒绝**,而记录在案的理由正是「**没有权威的假期来源**」。
> **一份好到能画日历的假期数据,很可能也就好到能填那张表。**
> **勘察日历的人必须顺手查一件事:一个来源能不能同时服务两处。**
> 那不是顺带的好处 —— 它是一条**今天正在按名拒绝**的功能路径。

### 15.2 一个单位换算器

公吨 / 千克 / 磅 / 短吨。

### 15.3 定价

**已经做了**,见 §13.2.1 / §13.2.2 —— 定价三条 + 金属行情已经在工具底下。

### 15.4 一个汇率查询 ——【★ Tim 裁定:它跟着汇率源的修复一起来,不单独做 ★】

**今天 USD 根本换不到 SGD**:唯一记下过的中间价是 **2026-07-31**,远在 **4 天**
上限之外。所以**一个建在它上面的查询,每次都会答「无法确定」**。

**这件事因此归排队中的行情源那一摊,不是一个小工具。**
**★ 不许把它当成一个"顺手做掉的小东西" ★** —— 它会准确地做出一个只会拒绝的页面。

---

## 16 · ⑩ 报告:`/hr/reviews` 与 KPI 那几页到底各是什么 ——【一个都没合,只报告】

**Tim 问它们重不重叠、能不能并成一页。这问题从名字上答不了,所以下面是实测。**

### 16.1 各自是什么

| | `/hr/reviews`(绩效评估) | `/hr/kpi`(KPI 框架) |
|---|---|---|
| **它记的是什么** | **一份单据**:某个人在某一段期间的一次评估 | **一套度量**:5 条组织目标 → 30 份职位模板 → 每人 5 条被抄下的考核条目 |
| **表** | `performance_reviews`、`review_goals`、`review_cycles`、`review_rating_scale` | `kpi_organisation`、`kpi_position_templates`、`kpi_cycles`、`kpi_entries`、`positions` |
| **字段** | `review_type`(probation/annual)、`period_*`、`reviewer_employee_id`、`status`、`rating_code`、`summary_text`、`self_assessment_text`、`probation_outcome`、**`new_monthly_salary`(受限列,`data.view_pay`)** | 组织侧:`code/title/weight_pct/definition/month3_target/month6_target/measurement_evidence`;个人侧:`kpi_ref/title/weight_pct/target_text/org_codes`、**`score` 0–5**、**`score_kind`(judged / computed)**、`computed_basis`、`evidence_note` |
| **工作流** | **一台状态机**:`draft →(self_review)→ submitted → approved → acknowledged`,外加 `void`(第六个状态,不在流水线上 —— 靠作废重开更正)。18 个服务端动作 | **没有状态机**。周期是 `draft/open/closed`,带 **M3 / M6 两道关口**;打分是往条目上写一个数 —— **但今天没有任何界面做得了这个动作,见 §16.1b** |
| **谁用** | HR + 评估人(默认按部门经理);本人在 `/my-reviews` 看自己那一份 | **打分的人**看 `/hr/kpi`(组织记分卡 + 职位矩阵 + 谁到了谁没到);**被考核的人**在 `/me` 看自己那五条(`my_kpi_entries`) |
| **读 / 写** | 列表读 `performance_reviews_masked`(薪酬列在视图里遮掉);写全部经服务端动作 | `/hr/kpi` **整页只读**:`kpi_organisation`、`kpi_position_linkage_matrix`、`positions`、`employees_masked` |
| **门** | `requireModule(MOD.hr)`;行一级另有 `data.view_reviews` / `data.view_pay` | `requireModule(MOD.hr)` |
| **live 行数(2026-09-02)** | `performance_reviews` **0** · `review_goals` **0** · `review_cycles` **0** · `review_rating_scale` 4 | `kpi_entries` **0** · `kpi_cycles` **0** · `kpi_organisation` **5** · `kpi_position_templates` **30** · `positions` 6 |

### 16.1b ★【一处实测出来的、名字上完全看不出来的不对称:KPI 那一侧【没有写的界面】】★

`kpi_entries` 有 `score` / `score_kind` / `computed_basis` / `evidence_note` 四列,
库里也有两支写它的函数 —— `db/functions/score_kpi_entry.sql`(打一个分)与
`db/functions/assign_position_kpis.sql`(把职位模板抄到一个人名下)。

**★ 实测:`app/` 底下【一个调用者都没有】。★**
（`grep -rn 'score_kpi_entry\|assign_position_kpis' app/ lib/` → 0 处,
`database.types.ts` 的类型声明不算。）

**所以这两页今天根本不在同一个建设阶段上:**

| | `/hr/reviews` | `/hr/kpi` |
|---|---|---|
| 读的界面 | 有 | 有 |
| **写的界面** | **有 —— 18 个服务端动作**,一条完整的状态机 | **没有。一个都没有。** |
| 写的能力 | 有 | **只在库里**(两支函数,没有屏幕碰它们) |

**这件事对 Tim 的问题很要紧,而它从名字上一点也看不出来:**
`/hr/kpi` 是一扇**只读的窗**,开在一套**写入路径还没有界面**的框架上;
`/hr/reviews` 是一条**建完了的**单据流水线(只是还没有人跑过它)。
**"要不要把它们并成一页"因此还不是今天该问的问题** ——
其中一页连"打一个分"这个动作都还没有地方做。

### 16.2 它们互相引用吗 ——【实测:一处都没有】

* `app/hr/kpi/**` 里 **没有任何一处** 读写 `performance_reviews` / `review_goals` /
  `review_cycles` / `review_rating_scale`;
* `app/hr/reviews/**` 与 `app/my-reviews/**` 里 **没有任何一处** 读写 `kpi_*`;
* 数据库里 **没有** 一个视图或函数同时碰两族表;
* **唯一同时出现两者的地方是 `/me`** —— 而那是员工自己那一页上**两个各自独立
  的板块**,不是一次连接。

**并且这条隔离是【写下来的裁定】,不是偶然。** `db/tables/kpi_cycles.sql` 的表注
记着 Tim **2026-08-29** 的裁定:**不复用 `review_cycles`,尽管形状一模一样**——

> 「**共用周期,是两个模块悄悄变成一个的方式。** 第一次有人开一个 HR 评估周期,
> 每一块 KPI 屏幕都会继承它。**五个重复的列,对上一次永久的耦合。**」

以及 `db/tables/review_goals.sql` 那句更根本的:

> 「**没有权重、没有逐条打分** —— 一旦有了分数,谈话就会围着分数转,而不是围着
> 结果转。」

**而 KPI 的全部内容就是 0–5 乘权重。两者是设计上的对立面,不是偶然的重复。**

### 16.3 判词 —— 它是 (b),**但 Tim 给 (b) 的措辞有一半是错的**

**是 (b):两件听起来像、实际不同的东西。不是 (a),也不是 (c)。**

* **不是 (a)「定义一把尺 + 照它打分」两阶段。** 若是两阶段,评估会**照着 KPI
  的条目打分** —— 而 `review_goals` **刻意没有权重、没有逐条分数**,评估的产出是
  **一个档位 + 一段书面结论**(4 档:Outstanding / Exceeds / Meets / Below)。
  两者的**产出形状不同**,不是同一把尺的两头。
* **不是 (c) 两刀各建一遍的重复。** 隔离是 2026-08-29 **明令裁定**的,理由写在
  表注里,而且 KPI 侧**主动不碰** `review_goals` 的三条 SELECT 策略。

> ★★【但 Tim 给 (b) 的措辞要更正一半】★★
> 他写的是「**关于工厂的业务 KPI**,对**关于人的绩效考核**」。
> **前半对,后半也对,但那条分界【不在两页之间】—— 它在 KPI 这一页【内部】。**
> `kpi_organisation` 的 5 条确实是**工厂的**(执照与法规就绪 25% · 产线设备就绪
> 与试车 25% · 关键现金流连续性 20% · 商务就绪与已签业务 15% · 厂区安全环保合规
> 就绪 15%);**但它们【向下摊到职位、再抄进每个人名下】**,而 `kpi_entries`
> **带 `employee_id` 和一个 0–5 的分数** —— **那一头是关于人的。**
> 所以两页**都**以给一个人打分收尾。**它们真正的区别是【问的问题不同】:**
> * **KPI**:「**这个人对工厂今年要做成的那五件事,交付了多少?**」——
>   自上而下,加权,可累加成 roll-up。
> * **评估**:「**这个人这段时间干得怎么样,以及接下来怎么办?**」——
>   叙事的,一个档位,带**试用期结论**与**新月薪**,要签字。

### 16.4 建议 ——【不要合并】,而真正的缺口是另一件事

**建议:不合并。理由有三条,按分量排:**

1. **合并会让【被明令保护的那件事】失守。** `review_goals` 没有分数是**刻意的**;
   KPI 全是分数。并成一页就必须二选一,而任一选择都推翻一条已经裁定过的判断。
2. **两者的听众和节奏不同。** KPI 按 **M3 / M6** 关口逐月打分;评估按**试用期 /
   年度**走一条要签字的状态机,还牵着薪酬(`new_monthly_salary`,受限列)。
3. **今天没有证据说它们重复。** 交易表**两侧都是 0 行** —— 一个从来没跑过的
   流程与一个从来没打过分的框架,**"它们重复了"这句话还没有任何实测支撑。**
4. **★ 而且它们还不在同一个阶段上(§16.1b)★** —— KPI 那一侧**连打分的界面都没有**。
   **合并一个建完的东西与一个只建了一半的东西,得到的是把前者拆开去等后者。**

**★ 但报告里真正值钱的一条是这个:实测查出来的缺口【不是重复,是断连】★**

**两页之间一处引用都没有。** 于是一个评估人坐下来给某人写年度评估时,
**看不到这个人的 KPI 分**,尽管那正是「他交付了多少」最硬的那份证据。
**这是"加一处引用",不是"合并两页"** —— 而且它比合并**便宜得多、可逆得多**。

**还有一条便宜的清理,与合不合并无关:** `review_cycles` 与 `kpi_cycles`
**形状逐字相同、两张都 0 行**。Tim 2026-08-29 刻意不合它们,**那条理由今天仍然
成立**(共用周期 = 两个模块悄悄变成一个);**记在这里只是为了让下一个看见这两张
表的人不必再推一遍这条推理** —— 表注里写着「形状是刻意保持一致的,好让将来真要
合并时代价还是小的」。

**Tim 决定合不合。本刀只把它们各是什么摆出来,一行代码都没动。**
