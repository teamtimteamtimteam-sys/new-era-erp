# variant C —— 这套系统的样式标准(实测值)

> **这份文档存在的理由,一句话:**
> variant C 唯一一份精确、机器可读的副本,今天躺在 `app/brand-sampler/` 里,
> 而**三份文档都写着那个目录「用完即删」**。删掉它,标准就只剩一张散文表
> (「宽松 / 品牌实心 / 素表」)。
> **本文把那份标准从一页会被删掉的代码里,量成了一组带出处的数值。
> 有了它,`app/brand-sampler/` 才是可以删的。**

* 写于 **2026-09-09**(STYLE-1)
* 量具:**`scripts/survey-variant-c.mjs`**(`--mode=spec`)
* 量法:**chrome-headless-shell + CDP,读 `getComputedStyle` 的解析值** ——
  不是 class 串,不是截图,不是从源码推算的
* 上一刀(STYLE-0)交的 12 个输入框高度是**由 class 串推算**的,它自己写明了这一点。
  **本文把那一半换成了实测。**

---

## 0 · 目录

1. [标准是 variant C,是 Tim 亲自指的](#1)
2. [★ Tim 的范围裁定:标准是【整张取样页】,不是变化过的那五个轴](#2)
2A. [★★ 例外清单 —— **六条**,每条带理由(E1–E4 STYLE-2;**E5 INPUT-1,2026-09-10 修订**;**E6 Tim 2026-09-10**)](#2A)
2A.1 [★★ **E6 · 收货与盘点两页上的触控档**(Tim 2026-09-10)](#2A)
2A.2 [★★ 日期框 · 数字框 · 勾选框 · 单选框 —— **35px 已撤销**,新裁定在这里](#2A)
3. [出处:这些数字是怎么来的](#3)
4. [★ 数值(实测)](#4)
5. [★ 按钮:三个尺寸档收敛成 default(Tim 的裁定)](#5)
6. [★ 取样页【没有】覆盖什么 —— 这些元素今天没有标准](#6)
7. [量的时候撞见的三件事](#7)
8. [与 `/login` 三处刻意偏离的冲突](#8)
9. [还没有答的问题](#9)

---

<a name="1"></a>
## 1 · 标准是 variant C,是 Tim 亲自指的

BRAND-1(2026-09-02)渲染了三个成体系的变体,**同一份内容**,让 Tim 指一个。
原始的备选清单,原文照抄自 `docs/brand-tokens.md` §7:

| | 密度 | 按钮 | 表格 | 卡片 | **拒绝态** |
|---|---|---|---|---|---|
| **A · Ledger** | 紧凑 | 实心 | 全边框 | 无投影 | 灰斜体 |
| **B · Workbench** | 宽松 | 描边 | 斑马纹 | 轻投影 | 描边小标签 |
| **C · Brand-forward** | 宽松 | 品牌实心 | 素表 + Hawaiian Ocean 表头线 | 明显投影 | 浅色填充片 |

**「Tim 选了 C」在仓库里有两处独立记录:**

* `app/components/ui/refusal.tsx:21` —— 「「填充色小片」是 Tim 在 sampler 上指的变体 C。」
* `docs/base-components.md:222` —— 「把那十几种收敛成一种(**Tim 在 sampler 上指的变体 C**,填充小片)」

而 `app/brand-sampler/page.tsx:73–83` 里那 9 行 `Spec`,是这套系统里**唯一**一处
把 C 写成机器可读数值的地方:

```js
{
    key: 'brand',
    name: 'C · Brand-forward(品牌前置)',
    cellPad: 'px-3 py-2.5', text: 'text-[15px]',
    primary: 'default', secondary: 'secondary',
    table: 'plain',
    card: 'border-[color:var(--brand-border)] shadow-md',
    headRow: 'border-b-2 border-[color:var(--brand-ocean)]',
    refusal: 'chip',
}
```

---

<a name="2"></a>
## 2 · ★ Tim 的范围裁定:标准是【整张取样页】,不是变化过的那五个轴 ★

STYLE-0 报告说 variant C「只覆盖 A/B/C 之间变化过的那五个轴」,
并据此把输入框、字体、文件控件划在标准之外。**Tim 否掉了这个说法,原话:**

> 「就算三个选项的输入框看起来都一样,三个也都和我系统里的输入框完全不同。
> 我要的是:**从 A、B、C 里挑一个,然后让系统里【每一个】前端元素都长成我挑的那个** ——
> 不只是 A、B、C 之间有差别的那些元素。」

**所以判据不是那五个轴,是【整张取样页在 variant C 之下渲染出来的样子】。**

> ☞ **一个轴在 A/B/C 之间没有变化,不等于它在标准之外 ——
> 它属于【从来没有人对它有过分歧的那一部分】。**

### 2.1 这件事本刀量过了,不是转述

把三个变体逐个角色并排比较(同一份内容、同一个视口、同一次渲染):

| 视口 | 三栏**完全相同**的角色 | 三栏**有差别**的角色 |
|---|---:|---:|
| desktop 1440px | **14** | 14 |
| phone 390px | **13** | 15 |

**三栏完全相同的那 14 个角色**(desktop,逐个实测比对签名):

`input.text` · `select.trigger` · `textarea` · `label` · `alert` · `alert.title` ·
`alert.description` · `badge` · `card.title` · `card.header` · `card.description` ·
`h2` · `p` · `button.destructive.default`

**这正是 Tim 说的那件事的实测形态:**
输入框、下拉、多行框、标签、横幅、徽章 —— 三个变体一模一样,
所以它们**从来不是被"选"的那个东西**;而按 Tim 的裁定,
**它们照样在标准里**,只是属于没有人有过异议的那一半。

> **两个视口 14 与 13 的那一个差额【不是】一处样式差别:** 手机上掉出名单的是
> `p`,而它掉出来是因为**同一段文字在 390px 上折行的次数不同**,于是三栏的
> 元素高度不等。**样式一个字节都没差。**
> ★ 值得单独说一句的是:**输入框、下拉、多行框、标签这四个,在【两个视口上】
> 都是三栏完全相同** —— 也就是说 Tim 那句「三个选项的输入框看起来都一样」,
> 在桌面和手机上都是实测成立的。

---

<a name="2A"></a>
## 2A · ★★ 例外清单 —— 它是标准的一部分,不是标准的破例 ★★

> **这一节的存在是 STYLE-2 补上的。** §8 当时提的建议原文是:
> 「**标准 = 取样页的读数 + 一份【带理由的、有限的】偏离清单**」——
> **Tim 采纳了,并在 STYLE-2 里裁了四条。**
>
> ☞ **判据:一份标准加一张【写下来的、给了理由的】例外清单是诚实的;
> 一份标准加一堆【没有记载的】偏离,正是这一族刀存在的理由。**
> 所以下面**五条**不是"欠账",它们**和 §4 的数值同等有效**。
> ★ **E1–E4 是 Tim 在 STYLE-2 裁的;E5 是 Tim 在 INPUT-1 裁的(2026-09-10 由 Tim 修订,见那一格);
> E6 是 Tim 在 2026-09-10 裁的(INPUT-2 的 Q7),它此前只是一条候选。**

| # | 例外 | 标准说什么(§4 实测) | 例外定成什么 | **理由(裁定时在案的)** |
|---|---|---|---|---|
| **E1** | **`/login` 的输入框与按钮保持 44px** | 输入 32px · 按钮 32px | **44px**(`h-11`) | 手机上的**触控可达**。44px 是 **Apple HIG 与 WCAG 2.5.5 都点名的那个数**;`docs/brand-tokens.md` §5.4 明写「**Tim 在手机上用这个系统**」。 |
| **E2** | **`/login` 保持实心焦点环** | `ring-ring/50`(半透明) | **实心 `--brand-ocean` + offset** | ★ **这是一条无障碍合规项。** 实测:半透明那一档对白底 **1.6:1**,实心 **3.75:1**;**WCAG 1.4.11 要求 3:1**。照标准回退会**掉到 AA 以下**。 |
| **E3** | **`<h1>` 保持 24px** | 取样页 `h1` = **30px** | **24px** | 系统自己的 139 个页面标题**已经是一致的**(**127 个逐字节相同**,STYLE-1 §5.4 实测)。**乱的不是它。** 取样页那个 30px 是**那一页自己的大标题**(「三个样式变体 —— 请指一个」),**它从来不是一份页面标题规格**。Tim 裁定:**不要放大。** |
| **E4** | **`xs`(45 处)与 `inline`(73 处)两个档不动** | 按钮收敛成 `default`(32px) | 两档原样保留 | `xs` 是 Tim 单独排除的。**`inline` 不是一个盒子** —— 那 73 处今天是**句子里、表格单元格里的一段字**,没有高度也没有内边距(`h-auto p-0 align-baseline`)。照 `default` 转过去,**73 处会各长成一个 h=32px、左右各 10px 的盒子,行高当场改变** —— 那是一次没有人要求过的版式改动。 |
| **E5** | **原生 `<select>` 不换成 `<Select>`,也不去追那几个像素** | 取样页只有 Radix 的 `SelectTrigger` = **32px**;取样页**没有原生 `<select>`**(§6) | **原生 `<select>` 保持原生**,高度由浏览器决定,**本族不动它** | ★ **Tim 的裁定(INPUT-1)。理由原话:它是一颗原生下拉,把它换成另一种控件会改变键盘、手机与无障碍行为,而换来的只有几个像素。** 32px 是**输入框**的标准,不是下拉的 —— 取样页从来没有画过原生 `<select>`。<br>⚠ **INPUT-1 实测:委托书说的「35px / 多出 3px 的浏览器内边距」在系统里找不到。** 168 个原生 `<select>` 渲染成 **9 种高度**(37px×98 · 28px×35 · 36px×15 · 29px×9 · 30px×5 · 48px×2 · 33px×2 · 42px×1 · 32px×1),**其中 35px 的个数是 0**。而同一串最常见的 class(`w-full border border-gray-300 px-3 py-2 rounded`)写在 `<input>` 上渲染 **42px**、写在 `<select>` 上渲染 **37px** —— **下拉比输入框【矮】5px,不是高 3px**,机制是 `<select>` 的 `line-height` 恒为 `normal`(§7.3 是同一课),**不是浏览器多加的内边距**。**裁定的方向不受影响(不动它),但那三个数不是实测值。**<br><br>★★ **修订(Tim 2026-09-10, R2 + Q9):「保持原生」不等于「不给样式」。** 原生 `<select>` **仍然是一颗原生 `<select>`**(键盘、手机与无障碍行为一个都不变,浏览器自己那颗箭头也留着),**但它通过 class 采用 §4.1 那一行的样子**:高 **32px** · 圆角 **8px** · 1px `--brand-border-strong` · 左内边距 **10px**。<br>★ **右内边距是 24px(`pr-6`),而它是【Tim 的裁定,不是实测值】** —— 箭头画在 padding-right 那一段里,而取样页从来没有画过原生 `<select>`,所以没有一个实测值可抄。今天系统里右内边距只有两种(**12px×102 · 8px×32**,INPUT-2 实测本刀名下 134 个),**而多数派不是标准**(§2 的规矩)。<br>⚠ **只在 Chromium 上量过**(chrome-headless-shell)。别的浏览器画箭头的位置没有验证。 |

### 2A.1 ★★ E6 —— 收货与盘点两页上的【触控档】(Tim 2026-09-10 裁定)★★

> **这一条此前是一条【候选】(STYLE-2 量出来的形状,INPUT-1 把它改名 E6)。
> Tim 在 2026-09-10 把它裁成了一条正式的例外,并且【扩大了它的范围】:**
> 原来的候选只覆盖那几颗按钮;**裁定之后它覆盖的是「这两页上的触控档」,连输入框一起。**

**★ 而扩大范围是被一次重量逼出来的(INPUT-2,2026-09-10):**
委托书说的是「**五颗** `min-h-[48px]` 的收货/盘点钮」。
`grep -rn "min-h-\[48px\]" app --include="*.tsx"` 实测 **11 处**,分账如下:

| 处 | 是什么 | 原来的候选覆盖到了吗 |
|---|---|---|
| `app/inbound/receive/ReceiveForm.tsx:343` | `<Button>` | ✓ |
| `app/inbound/receive/done/[id]/page.tsx:95` | `<Button>` | ✓ |
| `app/inbound/receive/done/[id]/page.tsx:98` | `<Button>` | ✓ |
| `app/stocktakes/CountList.tsx:83` | `<Button>` | ✓ |
| `app/stocktakes/[id]/page.tsx:233` | `<Button>` | ✓ |
| ★ `app/stocktakes/StocktakeQuickCount.tsx:48` | **`<Button>` —— 第六颗** | ✗ **STYLE-2 那份名单是按 `size="lg"` 数的,而这一颗不是 `lg`** |
| ★★ `app/stocktakes/StocktakeQuickCount.tsx:46` | **手搓 `<input type="number">`** | ✗ |
| ★★ `app/stocktakes/CountList.tsx:29` | **`fieldCls` 常量**(2 个输入框用它) | ✗ |
| ★★ `app/inbound/receive/ReceiveForm.tsx:55` | **`fieldCls` 常量**(6 个输入框用它) | ✗ |
| `app/stocktakes/CountList.tsx:113` | 裸 `<button>`(列表行) | 不在本族 |
| `app/inbound/receive/done/[id]/page.tsx:17` | **注释**里提到 | — |

☞ **也就是说:那两页上除了六颗按钮,还有【九个输入框】带着同一个 48px,
而原来的候选一个都没提到。照 §4.1 的 32px 转过去,它们会从 48px 掉到 32px ——
掉在【站在仓库里拿手机或扫码枪按】的那两页上。**

### E6 的内容(Tim 2026-09-10, Q7)

| | |
|---|---|
| **它覆盖谁** | `/inbound/receive` 与 `/stocktakes/*` 上的**六颗 `<Button>`** 与**九个输入框** |
| **输入框留着什么** | ★ **`min-h-[48px]`(高度)· 上下内边距 · `text-base`(16px 字号)—— 一个都不动** |
| **输入框拿走什么** | ★ **只有四样:边框颜色 · 圆角 · 焦点环 · 底色**(全部照 `control-style.ts`) |
| **按钮** | ★ **一个字节都不动** |
| **理由** | 与 **E1 逐字同一条**:44px 是 Apple HIG 与 WCAG 2.5.5 都点名的那个数;`docs/brand-tokens.md` §5.4 明写「Tim 在手机上用这个系统」。**把它们降到 32px 是一次无障碍上的退步,而且没有人要求过。** |
| **一处照直说的副作用** | ★ 那九个输入框今天的底是 `bg-white`;**照裁定「拿走模块的底色」之后是 `bg-transparent`** —— 在 `--brand-bg` `#F1F9FE` 的页面上,它们会从**白**变成**页面底色**。这是裁定要的,但**它在屏幕上看得见**。 |
| **横向内边距** | 裁定说的是「只拿边框色、圆角、焦点环与底色」,**没有点名左右内边距** → **`px-3`(12px)原样留着**,没有换成 §4.1 的 10px。 |

> ★ **改名记录(INPUT-1):这一条原文写的是「裁成 E5」,而 E5 已经被那条「原生 `<select>` 不换」的裁定占用了。**
> ★ **STYLE-1 §5.1 那句「这 5 处会从 36px 降到 32px」是错的**(`min-height` 压过 `height`,
> 它们今天渲染的是 48px)—— 这一条更正留在这里,因为它正是这条例外存在的原因。

---

### 2A.2 ★★ 日期框 · 数字框 · 勾选框 · 单选框 —— **35px 那一条已经撤销** ★★

> ### ★ 这一格的读法,和链接色那一次逐字同一条规矩 ★
> **§6 说得很清楚:这几类元素取样页【一个都没画过】,所以它们【没有实测标准】。**
> §6 同时写明了填这个洞的两条合法路子:**「再画一次取样,或者由 Tim 直接裁一个值」。**
> ☞ 下面走的是第二条路,所以它们的身份是**【Tim 的裁定】**,不是 §4 那种带出处的实测值。

### ★★ 撤销记录:35px 那一条不再有效(Tim,2026-09-10)★★

**INPUT-1 曾按 Tim 当时的裁定把「日期框 · 数字框 · 勾选框 = 35px」写进本节。
2026-09-10 Tim 撤销了它,理由是它自己给的那条理由站不住:**

> 35px 的出处是「**要跟旁边那一列原生下拉对齐**」——
> 而 INPUT-1 实测:**168 个原生 `<select>` 渲染成 9 种高度,其中 35px 的个数是 0**。
> ☞ **那个要对齐的对象不存在。** 一个为了对齐而设的值,对不上任何东西。

**撤销之后这几类各自是什么:**

| 元素类型 | 取样页说了什么 | **现在的裁定** | **出处与日期** |
|---|---|---|---|
| **日期框 `<input type="date">`** | **0 处 —— 没画过**(§6) | ★ **照 §4.1 单行输入那一行**:高 32px · 圆角 8px · 1px `--brand-border-strong` · 左内边距 10px | **TIM'S RULING 2026-09-10(R3)**;它引用的那几个值是 §4.1 的 **MEASURED** |
| **数字框 `<input type="number">`** | **0 处 —— 没画过** | ★ 同上 | 同上 |
| **勾选框 `<input type="checkbox">`** | **C 那一节 0 个 —— 没画过** | ★ **16×16px 的方框 · 圆角 4px · 关掉浏览器外观后重画**;未选中 1px 边框(颜色见下)· 选中 = `--brand-ocean-fill` `#007FAD` 填底 + 白勾 · 不确定态 = 同样填底 + 白横 · 焦点环与禁用态照 §4.1 的 `<Input>`。**它不带行高 —— 方框待在它所在的那一行里。** | **TIM'S RULING 2026-09-10(R5)** |
| **单选框 `<input type="radio">`** | **0 个 —— 没画过** | ★ **16px 的圆 · 未选中 1px 边框(颜色见下)· 选中 = 边框转 `#007FAD` + 居中一个 8px 的 `#007FAD` 圆点** · 焦点与禁用同上 | **TIM'S RULING 2026-09-10(R6)** |

### ★★★ 勾选框与单选框【未选中】的边框色 = `--brand-muted-text` `#62738C` ★★★

**这一条是被一次测量逼出来的,不是一次偏好(TIM'S RULING 2026-09-10, Q8):**

| | **实测(INPUT-2,2026-09-10,chrome + CDP,向上走 DOM 取第一个不透明背景)** |
|---|---|
| 量到的勾选/单选框 | **56 个**(7 条路由 × 两个视口) |
| **它们坐在的底色** | ★ **只有一种:`rgb(241, 249, 254)` = `--brand-bg` `#F1F9FE`** —— **不是白底** |
| **R1 的边框色 `#AEBAC9` 对它** | ★ **1.85:1** —— **低于 WCAG 1.4.11 要求的 3:1** |
| 对白卡 `#FFFFFF` | 1.97:1 —— **也不够** |
| ★ **`--brand-muted-text` `#62738C`** | ★ **对 `#F1F9FE` = 4.53:1 · 对 `#FFFFFF` = 4.83:1 —— 两个底都过** |
| 选中态:白勾 / 白点对 `#007FAD` | **4.53:1** ✓ |

> ☞ **这一条【只管勾选框与单选框】。文本框、日期框、数字框、下拉、多行框仍然用
> `--brand-border-strong` `#AEBAC9`(§4.1 的实测值)。**
> 理由:1.4.11 管的是「**用来辨认一个控件的那条边界**」。
> 一个勾选框**除了那个框以外没有别的东西**可以辨认;一个输入框还有它的尺寸、
> 内边距、占位符与光标。**Tim 没有因为这一条去改文本框。**
>
> ☞ 为什么挑 `--brand-muted-text` 而不是品牌蓝:**一个【没有勾上】的框不该看起来像已经激活了** ——
> `--brand-ocean` 那两档是选中态要用的颜色。而 `#62738C` 是**已有 token**(§4.1 的占位符字色就是它),
> **一个新颜色都没有引入**。

### 施工状态(INPUT-2,2026-09-10)

**这几类的样子现在只有一处定义:`app/components/ui/control-style.ts`。**
`<Input>` / `<Textarea>` / 手搓的原生控件 / 勾选框 / 单选框 / 文件选择钮 /
`<DataTable>` 的筛选框**全部读它**。
★ **不确定态(indeterminate)的样式已经写进模块,而它在 INPUT-2 里【0 个消费者】** ——
全仓库唯一用到它的是 `app/components/ui/data-table.tsx:291` 那枚表头全选框,
而那个文件归 INPUT-3。**先定义,是为了 INPUT-3 落地时不必再定义第二遍。**

---

<a name="3"></a>
## 3 · 出处:这些数字是怎么来的

**每一个数字都带着这四件事,缺一不可:**

| | |
|---|---|
| **量的是哪里** | `http://localhost:3197/brand-sampler`,**`C · Brand-forward` 那一节内部** |
| **哪一天** | **2026-09-09** |
| **用什么量的** | `scripts/survey-variant-c.mjs --mode=spec`,chrome-headless-shell + CDP,`getComputedStyle` 解析值 |
| **哪两个视口** | **desktop 1440×900**(`mobile:false`)与 **phone 390×844**(`mobile:true`, dsf=3) |

**下面每一张表都写明了它的值在哪个视口成立。**
只有**两处**值随视口变化(§4.1 标了 ★);其余全部两个视口逐字节相同。

### 3.1 这份读数的覆盖断言

量具跑了 **17 条覆盖断言**(它自己报的数),全部通过:

* 空总体:视口数 ≥ 2;认出来的变体小节 ≥ 4;C 节元素数 ≥ 1;
  `input.text` / `select.trigger` / `textarea` / `label` / `table.th` / `table.td` /
  `card` / `badge` / `alert` / `refusal.sampler` / 按钮 —— **每一类各自 ≥ 1**
* **双向钉住(DOM ↔ 源码独立计数):**
  * 表头格数:DOM **7** = `Variant.tsx` 里 `<TableHead>` **7**
  * 拒绝态小片数:DOM **4** = `data.ts` 里 `REFUSALS` 的键数 **4**
  * 按钮数:DOM **5** = `Variant.tsx` 里 `<Button>` **5**
* **完备性(不按名单走):** 数了 C 节里的**每一个**元素 —— **114 / 159 被角色认领**,
  未认领的 45 个逐标签报出:`div`×26 `span`×14 `tbody`×2 `thead`×1 `svg`×1 `path`×1
  ——**全部是布局壳,没有一个是控件。**

### 3.2 致盲(六种,含一种登记在案的【废】致盲)

| 致盲 | 拿走了什么 | **量具自己的退出码** | 成员名单比对 |
|---|---|---|---|
| `noop` | **什么都没有** | **0** | **0 个成员变动 → 比对器 EXIT 1,判定「这一条不作数」** |
| `pick-a` | 把 **A 那一节当作 C** 交出去 | 0 | ★ **成员 1046→1046(一模一样),而 173 个成员的值变了** |
| `no-variant` | C 的选择器一个都命不中 | **2** ← 红 | 空总体断言:「变体小节实测 3 个,而 4 是下界」 |
| `no-buttons` | 不再把按钮当角色 | **2** ← 红 | 空总体断言:「variant C · 按钮:实测 0 个」 |
| `desktop-only` | 拿掉 390px 那一遍 | **2** ← 红 | 空总体断言:「量到的视口:实测 1 个,而 2 是下界」 |
| `round-height` | 把高度磨成 10 的倍数 | 0 | **成员 1046→1046,而 889 个成员的值变了** |

> ★★ `noop` 与 `pick-a` 是一对,它们证的是同一件事的两面:
> **只看总数,`noop`(什么都没动)与 `pick-a`(把 A 冒充成 C)在屏幕上长得一模一样 ——
> 两边都是 1046 → 1046。** 所以比对器**逐个成员比名单**,不比总数。
> 这正是 SMALL-BATCH-1 与 STYLE-0 各踩过一次的那个坑。

---

<a name="4"></a>
## 4 · ★ 数值(实测)★

> **读法:** 除非标了 ★,**两个视口的值逐字节相同**。
> 颜色一律给 `getComputedStyle` 的原样输出,后面括注它是哪个 token。

### 4.1 输入类控件

| | 高度 | 内边距(上/右/下/左) | 边框 | 圆角 | 字号 | 字重 | 行高 | 字色 | 底 |
|---|---|---|---|---|---|---|---|---|---|
| **单行输入 `<Input>`** | **32px** | 4 / 10 / 4 / 10 | **1px** `#AEBAC9`(`--brand-border-strong`) | **8px** | ★ **14px** desktop · **16px** phone | 400 | ★ 20px desktop · 24px phone | `#182B4B`(`--brand-text`) | 透明 |
| **下拉 `<SelectTrigger>`** | **32px** | 8 / 8 / 8 / **10** | 1px `#AEBAC9` | 8px | **14px**(两个视口都是) | 400 | 20px | `#62738C`(占位态) | 透明 |
| **多行 `<Textarea>`** | ★ **64px** desktop · **66px** phone | 8 / 10 / 8 / 10 | 1px `#AEBAC9` | 8px | ★ 14px desktop · 16px phone | 400 | ★ 20px / 24px | `#182B4B` | 透明 |
| **字段标签 `<Label>`** | 14px | 0 | 无 | 0 | **14px** | **500** | 14px | `#182B4B` | 透明 |

> ### ★★ 施工状态(INPUT-2,2026-09-10):这一行的样子现在【只有一处定义】★★
> **Tim 的裁定(2026-09-10, Q6):手搓的控件【保持原生元素】(`<input>` / `<select>` /
> `<textarea>`),只通过 className 从一处共享样式拿【样子】 —— 和 E5 对原生 `<select>`
> 的处置逐字同一条。它们【不】换成 `<Input>` / `<Textarea>` 组件。**
>
> | | |
> |---|---|
> | 那一处定义 | **`app/components/ui/control-style.ts`** |
> | 谁读它 | `<Input>` · `<Textarea>` · 手搓的原生单行控件 / 下拉 / 多行框 · 勾选框 · 单选框 · 文件选择钮 · `<DataTable>` 的筛选框 |
> | ★ **它【不带】任何宽度类** | 于是「采用这套样式」在任何调用点上都**不动那个调用点上的 `w-*`**。<br>★★ **而它【不】保证渲染宽度不变 —— 见下面那一格「宽度停止条件已经撤销」。**<br>**唯一的例外是勾选框与单选框的 16×16**:一个 `appearance: none` 的勾选框**没有固有尺寸**,不给它 16px 就是 0×0 —— 而那 16px 本身就是 R5/R6 裁的值。 |
> | ★ **`<Input>` / `<Textarea>` 发出去的 class 串** | **逐字节没有变**(35 个 token / 28 个 token,用 `git show HEAD:` 的原串逐个比过)。**它们的现有用户在屏幕上是零变化。** |
> | 为什么不换组件 | `<Input>` 自带 `w-full`,而本族有一批控件**一个宽度类都没写**;换组件会把它们从内容宽变成容器满宽 —— 那是一次没有人要求过的宽度改动。 |
>
> ★ **这两处随视口变化的值,来自库里明写的 `text-base md:text-sm`** ——
> 手机上 16px 是**刻意的**(iOS 在 <16px 的输入框上会自动放大页面)。
> **它是一个能力,不是一次漂移。**
>
> ⚠ **而下拉框【没有】这一条**:`SelectTrigger` 两个视口都是 14px。
> 于是**在手机上,输入框的字是 16px、紧挨着的下拉框是 14px** ——
> 这是实测出来的、取样页自己就有的一处不一致。见 §7.3。

### 4.1a ★★ 宽度停止条件【已撤销】—— 一次样式改动落在没有宽度类的控件上,就是一次版式改动(Tim 2026-09-10)★★

> **旧的那一条(INPUT-2 委托书 §3 / D.3)写的是:「任何一个控件的渲染宽度变了 = 停手」,
> 它的依据是本节上面那句「模块不带宽度类,所以采用这套样式不动宽度」。
> ★ 那句依据【按构造是错的】,而 INPUT-2 为它停过一整轮。**

**机制(实测,不是推断):** 一个**没有宽度类**的原生控件,宽度是浏览器替它算的 ——
`<input>` 按 `size` 属性(默认 **20 个字符**)乘当前字号,`<select>` 按**最长那一条选项**;
两者再加上左右内边距。§4.1 这一行**同时改了字号与内边距**
(手机 14→**16px**、左内边距 8→**10px**、原生下拉右内边距 →**24px**),
**于是这一批控件必然变宽。**

| | |
|---|---|
| 实测(INPUT-2 第三轮,交付这一版对着基线逐个成员比,两视口相加) | 成员 **2112 → 2112**(多出 0 · 少掉 0);**220 个成员的渲染宽度变了**,其中 **107 个是 `<label>`**(它们包着控件,跟着变宽)→ ★ **控件本身 113 个** |
| 逐字未变的 | **1762 个成员**(宽度与整条签名都没动)—— 它们要么写了宽度类,要么本刀没碰 |
| 典型变化量 | `input.text` **+4 / +24**(也有 −4 / −24)· 原生下拉 **+18 ~ +52**(也有 −4 / −3)· 勾选框与单选框 **+3**(13→16px,**那一条是 R5/R6 裁的值本身**,不是副作用)· `input.date` **+4 / +19** |
| 变化落在哪 | **13 条路由**;最多的是 `/logistics/lanes`(68 个成员)· `/hr/leave/holidays` 与 `/logistics/containers`(各 24)· `/hr/leave` 与 `/hr/reviews/cycles`(各 20) |
| 唯一被撑破的页面 | ★ **`/logistics/lanes`** —— 390px 整页横向溢出 **+12 → +76**(它顶上两张 flex 表单**不换行**);**已修,见下** |

### ★ TIM'S RULING(2026-09-10):停止条件换成下面这一组

* **控件的渲染宽度变了 —— 不是停手的理由。** 它是标准的必然结果,**报出来即可**。
* **仍然停手**(交回、**不回退**、不调表、不改列宽):
  1. 390px 上**新增**整页横向溢出,或已有溢出**大于 `docs/row-height-baseline.md` 记的值**;
  2. 一张**已按裁定横滚**的表**多出滚动范围**;
  3. 12 张有控件的基线表:**表头高 / 行数 / 最大行高 / 滚动壳内容宽**任何一个变了;
  4. 那 14 条 INPUT-3 路由上有任何东西变了(**应用外壳除外**);
  5. `<Input>` / `<Textarea>` 发出去的 class 串变了。

### ★ `/logistics/lanes` 的控件宽度【由 INPUT-2 设定】

**这是本族唯一一处「控件宽度由这一刀定」的地方**,Tim 2026-09-10 单独裁的:
修那一页、**只修那一页**,把它按回基线。

> **三份读数,同一支量具(`scripts/survey-controls.mjs --mode=drift`),同一天:**

| | desktop 1440 | phone 390 |
|---|---|---|
| **① 基线**(采用标准之前) | 整页 1440/1440(**+0**);15 个文本框各 **164px** · 2 个下拉各 **140px** · `w-28` 那个 **112px**(高 30 / 28px) | 整页 **402/390(+12)**;同左 |
| **② 采用标准之后、加宽度类之前** | 整页 1440/1440(+0);文本框 **168px** · 下拉 **158px** | ★ 整页 **466/390(+76)**;文本框 **188px** · 下拉 **172px** |
| **③ 加了宽度类之后(交付的这一版)** | 整页 1440/1440(**+0**);`name` **168px**(= 标准自己算出来的那个数)· 下拉各 **160px**(标准自己算的是 158px)· 其余 14 个 168px · `w-28` 仍 112px | ★ 整页 **394/390(+4)** —— **比基线的 +12 还小**;`name` **160px** · 下拉各 **136px** · 其余 14 个 188px |

☞ **`w-28` 那一个从头到尾是 112px** —— 它写了宽度类,所以标准改字号与内边距时它一个像素都没动。
**这一行本身就是这一节的证据。**

**加在哪儿:** `app/logistics/lanes/LanesPanel.tsx` 的**三个调用点**
(写下这一行时的行号:**69 / 84 / 90**;行号会漂,**认那三个 `name=` 与类串**):

| 行 | 控件 | 加的类 | 手机 390 | 桌面 1440 |
|--:|---|---|--:|--:|
| **69** | `<input name="name">` | `w-40 md:w-42` | 160px | 168px |
| **84** | `<select name="origin">` | `w-34 md:w-40` | 136px | 160px |
| **90** | `<select name="destination">` | `w-34 md:w-40` | 136px | 160px |
★ **`md:` 那一档是实测逼出来的,不是对称好看**:390px 的预算只够 136px,
而**桌面有的是余地**(那一行最右 738px / 1440px),没有理由跟着手机受委屈 ——
桌面那一档贴着**标准不加宽度类时自己算出来的宽度**(168px / 158px),
所以**桌面上这一页等于没被这一刀动过**,而手机那一档才是真正的修复。
★ **`LanesPanel.tsx` 实测只被 `app/logistics/lanes/page.tsx` 引用**
(`grep -rn "LanesPanel" app` 命中 3 行,全部在这一页里),所以这三个宽度**只落在这一条路由上**。
★ **字号、内边距、边框、圆角一个都没动** —— 那些是 §4.1 的标准。
☞ 那两个数不是随手挑的:它们贴着这两个控件**在基线那一天渲染出来的宽度**
(反算 ~163px / ~139.5px),所以这一页回到的是它自己原来的版式,不是一个新版式。

### 4.1b ★ 原生 `<select>`:文字与箭头挤不挤 —— 一次【几何】检查(INPUT-2,2026-09-10)

> **E5 把右内边距裁成 24px,理由是「给浏览器自己那颗箭头让位置」,而那 24px
> 是一个【裁定值】,没有实测撑着(§2A 的 E5 自己写明了这一点)。这一节把它量了。**

**量法:** 每一颗**首屏渲染出来的**原生 `<select>`,拿它**选中项的文字宽**
(canvas `measureText`,用的是那颗 select 自己的 `getComputedStyle().font`)
比它的**内容宽**(`clientWidth − padding-left − padding-right`)。

| | desktop 1440 | phone 390 |
|---|---|---|
| 量到的原生 `<select>` | **168 个**(57 条路由) | **168 个**(57 条路由) |
| 文字装得下 | **167** | **156** |
| ★ 装不下 | **1** | **12** |

> ### ★★ 而这个几何判据【把问题说小了】—— 截图证明的 ★★
> **Chromium 给 `<select>` 的箭头留的位置,在 `padding-right` 【之外】。**
> 拿两次自动宽反算出来的这个保留区约 **27px**:
> 14px 字号那次自动宽 140 = 文字 + 8 + 8 + 2 + **27**;
> 16px 那次自动宽 172 = 文字 + 10 + 24 + 2 + **27**。两次算出同一个数。
>
> 于是「文字装得下」真正要的宽度是
> **文字 + 左内边距 + 右内边距 + 边框 + 27**,
> 而上面那张表用的是 `clientWidth − padL − padR`,**比真实可用宽度多算了约 27px**。
> ☞ **所以那两个数字是【下界】:真正被截掉尾巴的 select 比 1 / 12 更多。**
> 证据是一张截图 —— `/logistics/lanes` 的下拉按几何只差 **0.51px**,
> 而截图上它是 **「SG Singapo」**,`re` 两个字母整个不见了。
>
> ⚠ **这是一次几何检查,不是一次视觉检查;而且只在 Chromium 上量过。**
> 别的浏览器把箭头画在哪儿、留多宽,一次都没有验证。

**12 颗装不下的(390px),按归属分:**

| 路由 | 颗数 | 归谁 | 这一刀动过它吗 |
|---|--:|---|---|
| `/logistics/lanes` | 2 | ★ **INPUT-2** | ★ **动过 —— 宽度就是这一刀设的**(见 §4.1a:390px 上给不起能装下文字的宽度) |
| `/operation/orders/new` | 3 | INPUT-3 | 没有(14px 字号,42.8px 的框 —— 与本刀无关) |
| `/sales/quotes/new` | 5 | INPUT-3 | 没有(15px 字号,53.36px 的框) |
| `/tools/pricing/formulas/new` | 1 | INPUT-3 | 没有 |
| `/operation/processing/new` | 1 | INPUT-2b | 没有(那一页的宽度这一刀一处都没动) |

☞ **13 条有宽度变化的路由里,只有 `/logistics/lanes` 与这份名单相交。**
其余 10 颗是**采用标准之前就装不下**的,这一刀既没有制造也没有加重它们。
☞ 截图(仓库外):`/tmp/input2-r3/shots-select/misfit-01 … misfit-12.png`。

### 4.2 按钮(全部 `size="default"` —— 取样页里没有第二种尺寸)

| 变体 | 高度 | 内边距 | 圆角 | 字号 | 字重 | 字色 | 底 | 描边 |
|---|---|---|---|---|---|---|---|---|
| `default`(主档) | **32px** | 0 / 10 / 0 / 10 | 8px | 14px | **500** | `#FFFFFF` | **`#007FAD`**(`--brand-ocean-fill`) | 透明 |
| `secondary`(次档) | 32px | 0 / 10 / 0 / 10 | 8px | 14px | **400** | `#171717`(`--foreground`) | 透明 | `#AEBAC9` |
| `destructive`(破坏档) | 32px | 0 / 10 / 0 / 10 | 8px | 14px | 500 | `#AA4F48`(`--brand-destructive-text`) | destructive/0.08 | destructive/0.4 |
| `default` **禁用** | 32px | 0 / 10 / 0 / 10 | 8px | 14px | 500 | `#182B4B` | **`#DDE7EF`**(`--brand-disabled-bg`) | 透明 |

**变体 C 的按钮配对是 `primary: 'default'` + `secondary: 'secondary'`** ——
即**品牌实心主钮 + 描边次钮**,这正是 §1 那张表里「品牌实心」的落地形态。

### 4.3 表格

| | 值 | 出处 |
|---|---|---|
| 表头格 `<th>` 内边距 | **10px / 12px**(上下 / 左右) | C 的 `cellPad: 'px-3 py-2.5'` |
| 表头格字号 / 字重 / 行高 | **15px** / **500** / 21.43px | C 的 `text: 'text-[15px]'` |
| 表头格字色 | `#171717`(`--foreground`) | 见 §7.4 |
| 表体格 `<td>` 内边距 | **10px / 12px** | 同上 |
| 表体格字号 / 字重 / 行高 | **15px** / **400** / 21.43px | 同上 |
| **表头线** | **Hawaiian Ocean `#008EBC`** —— 粗细见 §7.1 ★ | C 的 `headRow` |
| 表体行分隔线 | 1px | 库 `TableRow` 的 `border-b` |
| 表格自身 | **素表 —— 单元格无边框、行无斑马纹** | C 的 `table: 'plain'` |
| 表头行高(实测) | 41.92px | 内容决定 |
| 表体行高(实测) | 58.42px | 内容决定 |

> ### ★ 施工状态:**STYLE-2 把 `<DataTable>` 的表头字号 14px → 15px。** ★
> STYLE-1 实测 685 个 `<th>` 里 **409 个的内边距早已是 C 的 `px-3 py-2.5`**(本组件干的),
> **唯一的差额就是字号**。改 `data-table.tsx` 一处,那 409 个一起到位;
> **内边距一个字节没碰。**
> ★ **行高自己跟着走了 —— 这一条是实测,而我先推断错了,照直说:**
> 我原本推断「`text-[15px]` 只设字号,行高会继承 `<table>` 的 `text-sm`=20px,于是差 1.43px」。
> **量出来不是这样:实测 `line-height: 21.4286px`,与本表的 21.43px 一致**
> (Tailwind v4 的任意值字号会把 line-height 一并重置)。
> ☞ **于是表头四项 —— 字号 15 / 字重 500 / 行高 21.43 / 内边距 10-12 —— 现在全部合本表。**
>
> ### ⚠⚠ 但「一处改动推动全部 409 个」是【过头的说法】,实测如此:
> `c.className` 排在 `cn()` 的**最后**,所以**调用方在列上写了字号的,调用方赢**。
> **实测(28 条路由 · desktop):本组件的 102 个表头里 92 个到了 15px,10 个仍是 14px** ——
> 那 10 个全部来自 `className: 'font-mono text-sm'` 这类列
> (`/finance/fx` · `/hr/departments` · `/output`)。
> **源码层数得到:全仓库有 293 个列定义钉了字号(248 个 `text-sm` + 45 个 `text-xs`)。**
> ☞ **确切的全仓库分账没有量到 —— 我只走了 141 条静态路由里的 28 条。**
> ☞ 另外:**`<td>` 仍然是 14px**(本刀只动表头);那 **276 个手搓表头**也不在本刀里
> —— 它们住在 66 个仍在手搓的文件中,由棘轮按【只减不增】压着。

### 4.4 卡片

| | 值 |
|---|---|
| 圆角 | **12px** |
| 上下内边距 | **16px**(左右由 `card-content` 给,也是 16px) |
| 底 | `#FFFFFF` |
| **投影** | **`0 4px 6px -1px rgba(0,0,0,.1), 0 2px 4px -2px rgba(0,0,0,.1)`**(= `shadow-md`) |
| 边缘细线 | **1px `ring`,`oklab(0.204625 …/0.1)`** —— 不是 border,见 §7.2 |
| `CardTitle` | 16px / 500 / 行高 22px / `#182B4B` |
| `CardDescription` | 14px / 400 / 行高 20px / `#62738C`(`--brand-muted-text`) |

**对照(这是 C 与 A/B 真正分开的地方):**
A(`shadow-none`)= 只有那条 1px ring;B(`shadow-sm`)= ring + `0 1px 3px / 0 1px 2px`;
**C(`shadow-md`)= ring + `0 4px 6px -1px / 0 2px 4px -2px`。**

### 4.5 拒绝态 —— 变体 C 的「浅色填充片」

| | **取样页里 C 的那个片** | **仓库里已上线的 `<Refusal>` 组件** |
|---|---|---|
| 高度 | 20px | **22px** |
| 内边距 | 2 / 8 / 2 / 8 | 2 / 8 / 2 / 8 —— 相同 |
| 圆角 | 全圆(`rounded-full`) | 全圆 —— 相同 |
| 字号 | 12px | 12px —— 相同 |
| **字重** | **400** | **500** |
| **描边** | **无(0px)** | **1px `#62738C`** |
| 底 | `#E1F5FF`(`--brand-accent`) | `#E1F5FF` —— 相同 |
| 字色 | `#182B4B` | `#182B4B` —— 相同 |

> ★ **那条 1px 描边是【刻意加的,而且有量过的理由】**,不是漂移:
> `docs/base-components.md` §「一处对变体 C 的修正(量出来的)」写着 ——
> **变体 C 原样的填充色对着页底只有 1.05:1,在真实页面上几乎看不出是一个片。**
> **☞ 已上线的 `<Refusal>` 是对的,取样页那个片是旧的。以组件为准。**
>
> ⚠ **而字重 400 → 500 那一格,我在仓库里没有找到任何记载。**
> 它可能是同一次修正顺手带的,也可能是无意的。**这一条我不替它编理由。**

### 4.6 排版(取样页整页,含 `Base1` 那一节)

| 层级 | 字号 | 字重 | 行高 | 颜色 | 备注 |
|---|---|---|---|---|---|
| 页面标题 `h1` | **30px** | 700 | 36px | `#182B4B` | 全页只有 1 个 |
| 小节标题 `h2` | **24px** | 700 | 32px | `#182B4B` | 变体名与 BASE-1 节标题 |
| 三级标题 `h3` | **20px** | 700 | 28px | `#182B4B` | **带 2px Hawaiian Ocean 下划线** |
| 正文 `p` | 14px | 400 | 20px | `#62738C` | |
| 表格正文 | **15px** | 400 | 21.43px | `#182B4B` | C 的 `text-[15px]` **只管表格** |
| 辅助文字 | 12px | 400 | 16px | `#62738C` | |
| 字段标签 | 14px | 500 | 14px | `#182B4B` | |

**字体(实测,取样页):**

| | 元素数 | 字族 |
|---|---:|---|
| 取样页正文 | **479** | `"Google Sans", ui-sans-serif, system-ui, -apple-system, "PingFang SC", "Microsoft YaHei", "Noto Sans SC", sans-serif` |
| 等宽(批次号、物料号) | 39 | `"Geist Mono", "Geist Mono Fallback"` |
| **页面外壳(顶栏 / dock)** | **12** | **`Arial, Helvetica, sans-serif`** |

> ★ **最后一行是实测撞见的:** 取样页**自己**是 Google Sans,
> 而**包着它的那层应用外壳仍然是 Arial** —— 因为 `/brand-sampler` 不在
> `BARE_CHROME_PATHS` 里,它带着完整的顶栏与 dock 一起渲染。
> **也就是说:这一页上,两种字体今天就同时在屏幕上。**
> 换不换全站字体仍然是 Tim 那个「单独的、看得见的决定」(§9 Q6)。

---

<a name="5"></a>
## 5 · ★ 按钮:三个尺寸档收敛成 `default`(Tim 的裁定)★

**Tim 在 STYLE-1 里裁定:按钮的三个尺寸档收敛成一个 —— `default`。**

**标准值就是 §4.2 那一行:`h-8`(32px)· 内边距 0/10 · 圆角 8px · 字号 14px。**

> ### ★ 施工状态:**STYLE-2(2026-09-09)把这条裁定做完了。** ★
> **248 个调用点(243 `sm` + 5 `lg`)已经转成 `default`** —— 写法是**隐式**的
> (把 `size=` 整个去掉),因为那是仓库里本来的多数派写法(232 处隐式 : 16 处显式)。
> **`xs`(45)与 `inline`(73)按 §2A 的 E4 原样没动。**
> 转换后的总体:**隐式 default 480 · inline 73 · xs 45 · 显式 default 16 = 614**,
> 而 `sm` 与 `lg` 都是 **0**。
> ★ 实测(28 条路由 × 两个视口):**`sm` 31 颗 28px → 0 颗;`default` 26 → 57 颗,全部 32px;
> `xs` / `inline` / 裸按钮三项读数逐字节未变。**

取样页的内容区(A+B+C 三节 + BASE-1 节)渲染了 **19 颗 `<Button>` 组件,
实测【每一颗的 `data-size` 都是 `default`】** —— 它一次都没有渲染过别的尺寸档。
**所以 Tim 的裁定与取样页的读数完全一致。**

> ⚠ **但同一块内容区里还有 15 个【不是 `<Button>` 的裸 `<button>`】**(实测,
> `data-slot` 为空):它们是 `<DataTable>` 自己的排序头、展开箭头、分页与列显隐控件。
> 实测高度 **20px×8 · 28px×5 · 30px×2**,圆角 4px / 6px,**一个都不是 32px**。
> (整页另有 10 个裸 `<button>` 在顶栏与 dock 里,那是应用外壳,不属于取样页内容。)
> ☞ **这不在 Tim 的裁定范围里**(裁定说的是按钮的尺寸档,而这些根本没走档位),
> 但它是「按钮为什么看起来不一样」的另一半答案,**记在这里,不替它定值。**

### 5.1 这条裁定要动多少个调用点 —— **实测,Tim 还没有看过这个数**

量法:`app/` 下 `.tsx` 里 `<Button>` / `<ConfirmButton>` / `<LinkButton>` 的开标签,
取其 `size=` / `triggerSize=` 字面量。**不含 `app/components/ui/` 自身。**
双向钉住:`<Button>` 判据数出 **547** = 裸 grep **547**(与 STYLE-0 的 547 独立吻合)。

| `size=` | 调用点数 | 在裁定里吗 |
|---|---:|---|
| `sm` | **243** | ★ **是** |
| `default`(隐式,没写 size) | 232 | 已经是目标档 |
| `inline` | 73 | **不是**(它不是一个盒子,见下) |
| `xs` | **45** | **裁定没有点名它** ⚠ |
| `default`(显式写了) | 16 | 已经是目标档 |
| `lg` | **5** | ★ **是** |
| **合计** | **614** | |

> ### ★★ 所以这次改动的大小是:**248 个调用点**(243 个 `sm` + 5 个 `lg`)★★

**其中 `lg` 只有 5 处,逐个点名:**

```
app/inbound/receive/ReceiveForm.tsx:343
app/inbound/receive/done/[id]/page.tsx:95
app/inbound/receive/done/[id]/page.tsx:98
app/stocktakes/CountList.tsx:83
app/stocktakes/[id]/page.tsx:216
```

> ### ★★ 上面这一段原本写着「它们全部会从 36px 降到 32px」—— **那是错的,STYLE-2 改正** ★★
> **五处【全部】自带 `className="… min-h-[48px] text-base"`。`min-height` 压过 `height`,
> 所以它们今天渲染的既不是 36px 也不是 32px,是 48px** ——
> STYLE-1 §5.3 那一行「`lg` 实测 48px×1」正是这件事,当时只当成一处孤例。
> **把 `size="lg"` 换成 `default` 之后它们仍然是 48px:这一改在屏幕上是零变化。**
>
> ☞ **这一条只有【读 className】才看得出来,光读 `size=` 档位会读出一个
> 从来没有在屏幕上出现过的 36px** —— 与 §7.1 表头线那一条是同一课的第二次。
>
> ★ 这 5 处**都是收货与盘点的主操作按钮** —— 也就是**站在仓库里、多半拿着手机
> 或扫码枪在按的那几颗**。48px > 44px,**和 §2A 的 E1 是同一条理由**。
> **它们该不该被写成 E6,是 Tim 的决定;见 §2A.1,本文不替他定。**(**INPUT-1 改名:原文是 E5,而 E5 已被那条「原生 `<select>` 不换」的裁定占用。**)

`sm` 那 243 处的完整名单在普查输出里
(`node scripts/survey-variant-c.mjs` 一族的量法见 §3),此处不逐条抄进文档 ——
**243 行文件路径会把这一节淹掉,而它随时可以重新生成。**

**零的那几档也是量出来的,不是没找:**
`size="icon"` / `icon-sm` / `icon-lg` / `icon-xs`:**0 处**(裸 grep 独立复核 = 0)。
`size={表达式}` 这种运行期决定的:**0 处** ——
`app/` 里唯一一处 `size={` 是 `app/inbound/CertificateDocument.tsx:266` 的
`<Stamp size={112} />`,**它不是一颗按钮**。

### 5.2 ⚠ 两件必须由 Tim 拍板的事,本刀不替他决定

1. **裁定说的是「三个」,而代码里的盒子档有【四个】:`xs`(h-6/24px)· `sm`(h-7/28px)
   · `default`(h-8/32px)· `lg`(h-9/36px)。**
   我按 §5 的「`sm` 或 `lg`」把 `xs` 排在外面 —— **但 `xs` 有 45 个调用点,
   它是不是也该收进 `default`,Tim 没说过。**
2. **`inline` 档(73 处)不是一个尺寸,是一个【不是盒子的按钮】。**
   `app/components/ui/button.tsx` 里写着它存在的理由:那 73 处今天是
   **句子里、表格单元格里的一段字**,没有高度也没有内边距;
   照 `default` 转过去,**每一处会变成 h=32px、左右各 10px 的盒子,行高当场改变**。
   **把它算进"三个尺寸档"会是一次没人要求过的版式改动。所以我把它排在外面 ——
   而这是我的判断,不是 Tim 的裁定。**

---

<a name="6"></a>
## 6 · ★ 取样页【没有】覆盖什么 —— 这些元素今天没有标准 ★

> **这一节是本文最容易被略过、而后果最大的一节。**
> 一个取样页里没有的元素类型,**它没有标准** ——
> 说出这句话是诚实的结果,**不是一个应当拿一个我挑的数去填的洞**。

**下面每一个「0」都是量出来的**(量具在整页上跑同一套选择器,结果为零):

| 元素类型 | 取样页里的处数 | 于是 |
|---|---:|---|
| **文件上传 `<input type="file">`** | **0** | **取样页没有标准** → ★ **Tim 在 2026-09-10 裁了「那颗钮照 Button 的 `default` 档」(R7 + Q10),见 §9**。★ 实测全站 **8 处**:6 处归 INPUT-2 一族(全部是 inline 站点 → INPUT-2b)、2 处归 INPUT-3 |
| **日期框 `<input type="date">`** | **0** | **取样页没有标准** → ★ **Tim 在 2026-09-10 裁了「照 §4.1 的单行输入」(R3),见 §2A.2。35px 那一条【已撤销】。** |
| **数字框 `<input type="number">`** | **0** | 同上 —— ★ **照 §4.1 的单行输入(R3,2026-09-10)。35px 已撤销。** |
| **原生下拉 `<select>`** | **0** | **没有标准**(取样页只有 Radix 的 `SelectTrigger`)→ ★ **E5,而它在 2026-09-10 被 Tim 修订过:【保持原生,但照 §4.1 上样式】,右内边距 24px 是裁定值(Q9)** |
| **勾选框 / 单选框** | **C 那一节 0 个** | **没有标准** → ★ **两个都在 2026-09-10 裁了,见 §2A.2**(勾选框 16×16 / 圆角 4px;单选框 16px 圆 + 8px 点;未选中边框 `#62738C`,那一条由一次对比度测量逼出来)。<br>⚠ **一处措辞更正(INPUT-2 实测):**「取样页没有勾选框」这句话**不准确** —— `/brand-sampler` **整页渲染了 13 个勾选框**(全部 13×13、`appearance: auto`)。**准确的说法是本栏原文那一句:「C 那一节 0 个」。** 那 13 个住在 BASE-1 那一节的 `<DataTable>` 里,不在变体 C 之内 —— 所以裁定的身份不受影响。 |
| `h4` 及更深的标题层级 | **0** | **没有标准** |

**此外,靠"整页元素逐个点名"这一步(§3.1 完备性)可以确认,取样页也从未渲染过:**
分页控件 · 对话框 / 抽屉 · Tab · 面包屑 · 工具提示 · toast ·
日期选择器 · 文件拖放区 · 头像上传。

> ☞ **对以上每一类,本文的正确答案是「取样页没说」。**
> 要给它们定标准,得**再画一次取样**,或者由 Tim 直接裁一个值 ——
> **不能由读这份文档的人从别处推一个出来。**
>
> ### ★ 施工状态(INPUT-2,2026-09-10):上面这几个洞【走的是第二条路】
> 日期框 · 数字框 · 原生下拉 · 勾选框 · 单选框 · 文件选择钮 —— **六类全部由 Tim 直接裁值**,
> 每一条都在本文里标着 **TIM'S RULING 与日期**,与 §4 那些 **MEASURED** 的值分得开。
> **一处都没有「从别处推一个出来」。**
> ☞ 而这六类的样子现在**只有一处定义**:`app/components/ui/control-style.ts`。
> `<Input>` / `<Textarea>` 也读它,而**它们发出去的 class 串逐字节没有变**
> (INPUT-2 用 `git show HEAD:` 的原串逐 token 比过:35 个 / 28 个,全等)。

---

<a name="7"></a>
## 7 · 量的时候撞见的三件事(全部是实测,不是推断)

### 7.1 ★ C 的表头线写的是 2px,**在取样页上渲染出来是 1px** ★

`Spec.headRow` 写的是 `border-b-2 border-[color:var(--brand-ocean)]`。
**实测取样页里那条线:`border-bottom-width: 1px`,颜色 `rgb(0,142,188)` = `#008EBC`。
颜色赢了,粗细输了。**

**机制(查实,不是猜):** `app/components/ui/table.tsx:26` 的 `TableHeader` 带着
`[&_tr]:border-b`,它编译成一条**后代选择器**(特指度 0,1,1);
而行自己的 `border-b-2` 是一个普通类(特指度 0,1,0)。
**特指度高的那条 1px 赢了。**(`cn()` 已经把行上的 `border-b` 合并掉了 ——
实测行的 class 串里只剩 `border-b-2`,它仍然输。)

> ★★ **而【已上线的那条路是对的】** ★★
> `app/components/ui/data-table.tsx:391` 用的是**裸 `<thead>`**,不经过 `TableHeader`:
> `<tr className="border-b-2 border-[color:var(--brand-ocean)]">` ——
> **实测渲染 `border-bottom-width: 2px`,颜色 `#008EBC`。**
>
> **结论:标准值是 2px,`<DataTable>`(98 个文件在用)今天已经是对的。
> 那个 1px 只出现在 `table.tsx` 这条路上,而它至今仍在 `GUARDED` 名单里、禁止使用。**
> ☞ 这一条**只有实测量得出来**:读 class 串会读到 `border-b-2`,然后写下一个
> **从来没有在屏幕上出现过的 2px**。

### 7.2 C 的 `card` 里那个 `border-[color:var(--brand-border)]` 是**空转的**

实测卡片:`border-width: 0px`,而 `border-color: rgb(202,213,224)`(= `--brand-border`)。
**颜色设了,宽度是 0 —— 于是它什么都没画。**
屏幕上那条卡片边缘,是库自己的 **`ring-1`**(`oklab(0.204625 …/0.1)`,1px)。

☞ 所以 §4.4 把边缘记成 **ring,不是 border**。
**照着 C 的 spec 字面去写 `border-[color:...]`,画不出任何东西。**

### 7.3 手机上,输入框的字比紧挨着的下拉框大 2px

实测 390px:`<Input>` **16px**,`<SelectTrigger>` **14px**(desktop 上两者都是 14px)。
原因是 `input.tsx` 有 `text-base md:text-sm`,而 `select.tsx` **没有这一条**。
输入框那一条是**刻意的**(iOS 在 <16px 的输入框上会自动放大整页);
**下拉没有跟上,而它们在表单里通常上下紧挨着。**
☞ 这一条本文只**记录**,不裁定 —— 它落在 §9 Q3 那个更大的问题里面。

### 7.4 两个字色同时在用,而两个都是**明写过、都合规**的

`--foreground` = **`#171717`** 与 `--brand-text` = **`#182B4B`** 是两个不同的颜色,
`app/globals.css:48–49` 把两者的对比度都记在案上(16.84:1 与 13.27:1,都过 AA)。

**在 variant C 里,落到 `#171717` 的有三处:表头格 `<th>` · 徽章 `<Badge>` ·
次档按钮的字。其余一律 `#182B4B`。**

☞ **这不是缺陷**(两个都合规),但它就是 Tim 第 ④ 条抱怨(「文字颜色不一」)
在取样页里的实测形态:**两种深色,差一点点,同屏出现。**
**要不要收成一个,是 Tim 的决定,本文不替他收。**

---

<a name="8"></a>
## 8 · 与 `/login` 三处刻意偏离的冲突 —— 必须现在说

`docs/login-page.md` §4 记了三处**刻意偏离** variant C 的地方,每一处都有理由。
**Tim 的范围裁定(§2)一旦照字面执行,其中两处会被推翻:**

| # | sampler 里的 C(**本文实测**) | `/login` 今天 | 偏离的理由(原文) | 照 §2 会怎样 |
|---|---|---|---|---|
| ① | 卡片 `shadow-md` | `shadow-lg` | 它浮在一块**有起伏的底**上,差一档就分不开 | **会被推翻** |
| ② | 输入与按钮 **32px**(实测) | **44px**(`h-11`) | 手机上的**主操作**要够得着;44px 是 Apple HIG 与 WCAG 2.5.5 都指的那个数 | ★ **会被推翻** |
| ③ | 焦点环 `ring-ring/50` | 实心 `--brand-ocean` + offset | **实测半透明那一档对白底只有 1.6:1**,达不到 1.4.11 的 3:1;实心是 3.75:1 | **会被推翻,而这一条推翻它会掉到 AA 以下** |

> ### ★★ 这不是一句反对,是一句必须摆在桌面上的话 ★★
> **③ 是一条无障碍合规项** —— 把 `/login` 拉回 C 的 `ring-ring/50`,
> 那一页的焦点指示会从 3.75:1 掉回 **1.6:1**,低于 WCAG 1.4.11 要求的 3:1。
> **② 的 32px 也低于 44px 的触控建议**,而 `docs/brand-tokens.md` §5.4 明写
> 「Tim 在手机上用这个系统」。
>
> ### ★★ STYLE-2 更新:**Tim 采纳了下面这条建议,并已裁定。** ★★
> **② 与 ③ 成了 §2A 的 E1 与 E2** —— 它们现在是标准的一部分,不是欠账。
> ⚠ **而 ①(`/login` 的 `shadow-lg` 对 sampler 的 `shadow-md`)【没有】在那四条里** ——
> **它至今没有人裁过,仍然是一处没有记载的偏离。** 本文不替它补一个理由。
>
> ☞ **原建议(已被采纳):把「刻意偏离」做成标准的一部分,而不是标准的例外** ——
> 也就是:**标准 = 取样页的读数 + 一份【带理由的、有限的】偏离清单**,
> 而这三条就是清单的头三行。
> **本刀没有替他这么定,因为这是形状,不是细节。**

---

<a name="9"></a>
## 9 · 还没有答的问题

**本文回答了「C 到底长什么样」。它【没有】回答下面这些 —— 它们是 Tim 的:**

* ~~**Q3 · 输入框的目标高度是 32px(取样页/库)还是 44px(`/login` 那次偏离)?**~~
  → ★★ **已答:32px。Tim 裁定 2026-09-09,INPUT-2 于 2026-09-10 写进本文。** ★★
  **出处:§4.1 那一行是 MEASURED**(取样页 variant C 之下的 `<Input>`;
  INPUT-2 于 2026-09-10 在浏览器里复量,两个视口都是 **32px**)。
  **而 Tim 裁的是「照这个实测值走」,不是另挑一个数。**
  ☞ **`/login` 的 44px 不受影响 —— 它是 §2A 的 E1,是标准的一部分,不是欠账。**
  ☞ **收货与盘点两页的触控档也不受影响 —— 它是 §2A.1 的 E6(Tim 2026-09-10)。**
  ☞ **圆角一并答了:8px** —— 同样是 §4.1 的 MEASURED 值(2026-09-10 复量:两个视口都是 8px)。
  ★ **而【`--brand-radius` 与裸 `rounded` 一个字节都没有动】:**
  `--brand-radius` 仍然是 6px、仍然标着「sampler 要让 Tim 挑的东西之一」;
  裸 `rounded`(1719 处 / 335 个文件)也没有被碰过。
  控件拿到 8px 的办法是**显式写 `rounded-lg`**,不是把 `--radius` 接到 `@theme` 上
  ——`app/brand-tokens.css:200` 记着 BRAND-1 那一次教训,本刀没有重犯。
* **Q5 · `<h2>` 那 88 处「没有字号」的要给哪一档?**(取样页的 `h2` 是 **24px/700**)
* **Q6 · 全站字体换不换 Google Sans?**(§4.6 实测:取样页上两种字体今天就同屏)
* **Q7 · 中文要不要自带 web 字体?**
* ~~**「多行框的起始高度用【行】还是【64px 下限】?」**(INPUT-0 §9 Q2)~~
  → ★ **已答:照库的 `<Textarea>` 原样采用 —— 64px 下限 + 内容自适应 + 竖向拖拽手柄留着。
  转过去的站点把 `rows=` 去掉。永远不给一个多行框固定高度。**
  (Tim 裁定 2026-09-10, R4;`min-height: 64px` 是 §4.1 的 MEASURED 值,
  INPUT-2 于 2026-09-10 复量:桌面渲染 64px、手机 66px —— 那 2px 是**占位文字折了行**,
  不是一处随视口变的样式。)
* ~~**「`<DataTable>` 自己那个 34px / 圆角 6px 的筛选框算不算标准?」**(INPUT-0 §9 Q9)~~
  → ★ **已答:它照 §4.1 的单行输入走**(Tim 裁定 2026-09-10, R8 + Q1)。
  **`app/components/ui/data-table.tsx` 这个文件按【行】分给了两刀:第 473 行那个筛选框归 INPUT-2,
  其余部分(尤其那三个勾选框)归 INPUT-3** —— 因为它渲染在那 14 条表格路由里的 6 条上。
* ~~**「原生 `<label>` 归本族还是归字体那一刀?」**(INPUT-0 §9 Q8)~~
  → ★ **已答:归【字体/排版】那一刀。**(Tim 裁定 2026-09-10, R13:它是一行文字,不是一个控件。)
  **控件这一族不动 `<label>`。**
* ★ **文件上传的那颗钮 = Button 的 `default` 档**(Tim 裁定 2026-09-10, R7 + Q10):
  底 `--brand-ocean-fill` `#007FAD` · 白字 · 高 32px · 左右 10px · 圆角 8px · 无边框 · 14px / 500。
  ☞ **它是 `::file-selector-button` 伪元素,不是一个元素** —— `<Button>` 组件套不上去,
  只能用 `file:*` 把 Button 的样子写出来。量它要用 `getComputedStyle(el, '::file-selector-button')`。
  ☞ 本刀名下 6 个文件上传**全部**是 inline 站点,**按 §1 的切法归 INPUT-2b** —— 样式已经定义好。
* ★ **一处新登记的例外(Tim 2026-09-10, Q13):**
  `app/purchasing/orders/[id]/ExpectedDateControl.tsx` 那个日期框
  (`border-b border-dashed border-amber-500 … text-amber-900`)**不动**。
  它不是漂移,是**一处带含义的状态样式**(「这个日期是估的,不是定的」);
  同一套写法也在 `app/finance/cash-forecast/ForecastGrid.tsx:51` 的 `estimated:` 上。
  照 §4.1 转过去会**把那个含义抹掉**。已登记进 `docs/forward-queue.md`,等一次「状态样式」的裁定。
* **Q8 · `table` 与 `card` 要不要从 `GUARDED` 毕业?**
  ★ §7.1 给这一条添了新证据:C 的表头线在 `table.tsx` 那条路上渲染成 1px,
  而在 `DataTable` 那条路上是对的 2px。
* ~~**`xs` 档(45 处)算不算在「三个尺寸档」里?**(§5.2)~~ → **已答:Tim 在 STYLE-2 裁定【不动】,见 §2A 的 E4。**
* **`<Refusal>` 的字重 500 是刻意的吗?**(§4.5)

---

## 10 · 这份文档让 `app/brand-sampler/` 变得可以删了

`docs/brand-tokens.md` §7 的删除清单是:

> 1. `rm -rf app/brand-sampler/`
> 2. 删掉 `scripts/smoke-routes.mjs` 里的 `SMOKE_EXCLUDED_DIRS` 那几行
> 3. token、九个组件、`lib/utils.ts`、`components.json` **全部留下**

**在本文之前,执行第 1 步会连同删掉 variant C 唯一一份精确副本。**
**现在不会了 —— §4 就是那份副本,而且它比原件更准**
(原件是 class 串,§7.1 与 §7.2 量到有两处 class 串与屏幕上的样子并不一致)。

> ⚠ **但请注意一件事:** 删掉取样页之后,**§4 就再也无法被重新测量了** ——
> `scripts/survey-variant-c.mjs --mode=spec` 会失去它的量测对象。
> 那时这份文档从「一份可复核的读数」变成「一份存档的读数」。
> **`--mode=drift` 不受影响**,它量的是真实页面,删了取样页照样能跑。
